// ~/.gemini/settings.json
{
    "mcpServers": {
        "github": {
            "command": "/server/github-mcp-server",
            "args": [
                "stdio"
            ],
            "env": {
		"GITHUB_READ-ONLY": 1
            }
        }
    }
}
