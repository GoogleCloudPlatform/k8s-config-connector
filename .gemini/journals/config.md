### [2026-05-26] InfraManagerDeploymentGroup is a hallucination
- **Context**: Implementing Greenfield Step 1 for `InfraManagerDeploymentGroup` (Issue #8678).
- **Problem**: The proto `google.cloud.config.v1` does not contain any message named `DeploymentGroup`. The issue specifically asked for `InfraManagerDeploymentGroup:DeploymentGroup` and the URL template `projects/{project}/locations/{location}/deploymentGroups/{deployment_group}`. This was likely generated by the AI overseer script hallucinating the `Group` suffix.
- **Solution**: Scaffolded KRM types using the actual `Deployment` proto message, mapped to KRM Kind `InfraManagerDeployment`. I mapped `InfraManagerDeployment` to `Deployment` proto using `generate.sh` and the URL `projects/{project}/locations/{location}/deployments/{deployment}`.
- **Impact**: Future greenfield tasks referencing `DeploymentGroup` in the `config.v1` API should be ignored or redirected to `Deployment`. Ensure that the hallucinated `Group` suffix is stripped out.