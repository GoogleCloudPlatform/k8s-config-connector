### [2026-07-07] SecurityCenterManagementEventThreatDetectionCustomModule Types and Identity Scaffolding
- **Context**: Implementing direct types, CRDs, and IdentityV2 for `SecurityCenterManagementEventThreatDetectionCustomModule` as step 1.
- **Problem**: 
  - Standard scaffold types default to `ProjectRef`. However, this resource is organization-parented, so it requires `OrganizationRef`.
  - The `Location` field generated by the tool is a non-pointer `string`. Under KCC direct KRM rules, Go scalar primitive types (such as `string` and `bool`) must be pointers (e.g. `*string`) to prevent serialization issues and correctly support KCC validation rules.
- **Solution**: 
  - Replaced `ProjectRef` with `refs.OrganizationRef` in `securitycentermanagementeventthreatdetectioncustommodule_types.go`.
  - Converted `Location` from `string` to `*string` and updated `securitycentermanagementeventthreatdetectioncustommodule_identity.go` to handle nil checks/dereferencing for the `Location` field.
- **Impact**: Enables support for the organization-level `SecurityCenterManagementEventThreatDetectionCustomModule` resource while adhering to strict KCC KRM guidelines regarding pointers for scalar primitive types.
