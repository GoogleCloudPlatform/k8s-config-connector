### BRIEF Change description

This PR upgrades the MockGCP implementation for `BillingBudgetsBudget` from `v1beta1` to `v1` proto, aligning it with the Direct controller and real GCP behavior.

Specifically:
- Upgraded proto service and package to `v1`.
- Added support for translating `v1beta1` requests (from DCL) to `v1`.
- Handled field name changes (`allUpdatesRule` -> `notificationsRule`).
- Implemented body unwrapping for `v1beta1` POST/PATCH requests to ensure compatibility with `v1` expectations.
- Verified that both Direct and DCL controllers pass against MockGCP.

#### record-gcp report
- `record-gcp` was attempted against real GCP (project `cnrm-barni-4`).
- It FAILED with `403 Forbidden: The caller does not have permission` when attempting to access the billing account.
- Error message: `error applying desired state: googleapi: Error 403: The caller does not have permission`.
- Fallback: Existing golden logs were preserved as they were recorded against real GCP in the past and already match the behavior (including a known re-creation bug in the Direct controller where it fails to capture the service-generated ID).

Fixes #12886

---
This PR was generated by the **overseer,overseer** agent (powered by the gemini-3.6-flash model).

```release-note
NONE
```

 
