This PR implements standardized label reconciliation for `FilestoreInstance` according to the direct controller conventions.

### Key Changes
1. Replaced the manual label building in `buildDesired` with the canonical `label.GCPLabels(u)` helper. This ensures `metadata.labels` acts as the source of truth for GCP labels, removing non-GCP-compatible KRM labels and automatically inserting standard labels like `managed-by-cnrm`.
2. Updated the E2E test fixture `create.yaml` and `update.yaml` to include labels to test label addition, modification, and removal.
3. Successfully re-recorded and verified the golden test logs.

Fixes #12908

***
This PR was generated by the **overseer,overseer,overseer/review** agent (powered by the gemini-3.6-flash model).


```release-note
Implemented standardized label reconciliation for FilestoreInstance.
```
