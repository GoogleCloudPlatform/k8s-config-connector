{
  "name": "{{map1}}",
  "project": "{{project}}",
  "hostRule": [
    {
      "description": "the only test rule",
      "host": [
        "www.example.com"
      ],
      "pathMatcher": "example"
    }
  ],
  "pathMatcher": [
    {
      "name": "example",
      "description": "a path matcher for the only host rule",
      "defaultService": "{{ref:basic.backend_bucket.json:selfLink}}"
    }
  ],
  "defaultService": "{{ref:basic.backend_bucket.json:selfLink}}"
}
