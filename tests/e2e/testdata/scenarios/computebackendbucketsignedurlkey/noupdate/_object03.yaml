apiVersion: compute.cnrm.cloud.google.com/v1alpha1
kind: ComputeBackendBucketSignedURLKey
metadata:
  annotations:
    cnrm.cloud.google.com/management-conflict-prevention-policy: none
    cnrm.cloud.google.com/project-id: ${projectId}
  finalizers:
  - cnrm.cloud.google.com/finalizer
  - cnrm.cloud.google.com/deletion-defender
  generation: 1
  name: computebbkey-noupdate-${uniqueId}
  namespace: ${projectId}
spec:
  backendBucketRef:
    external: computebbkey-noupdate-${uniqueId}
  keyValue:
    valueFrom:
      secretKeyRef:
        key: storage-signing-key
        name: signing-key-noupdate-${uniqueId}
  projectRef:
    external: ${projectId}
status:
  conditions:
  - lastTransitionTime: "1970-01-01T00:00:00Z"
    message: The resource is up to date
    reason: UpToDate
    status: "True"
    type: Ready
  externalRef: projects/${projectId}/global/backendBuckets/computebbkey-noupdate-${uniqueId}/signedUrlKeys/computebbkey-noupdate-${uniqueId}
  observedGeneration: 1
