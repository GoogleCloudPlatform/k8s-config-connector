apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubTopic
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: ${projectId}
  labels:
    label-one: value-one
    managed-by-cnrm: "true"
  name: pubsubtopic-${uniqueId}
spec:
  resourceID: pubsubtopic-${uniqueId}