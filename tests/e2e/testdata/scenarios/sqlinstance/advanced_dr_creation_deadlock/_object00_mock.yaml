apiVersion: sql.cnrm.cloud.google.com/v1beta1
kind: SQLInstance
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: ${projectId}
  finalizers:
  - cnrm.cloud.google.com/finalizer
  - cnrm.cloud.google.com/deletion-defender
  generation: 1
  name: sqlinstance-primary-${uniqueId}
  namespace: ${projectId}
spec:
  databaseVersion: MYSQL_8_0
  region: us-west1
  replicationCluster:
    failoverDrReplicaRef:
      name: sqlinstance-replica-${uniqueId}
  settings:
    backupConfiguration:
      backupRetentionSettings:
        retainedBackups: 15
        retentionUnit: COUNT
      binaryLogEnabled: true
      enabled: true
      startTime: "12:00"
      transactionLogRetentionDays: 14
    edition: ENTERPRISE_PLUS
    locationPreference:
      zone: us-west1-a
    tier: db-perf-optimized-N-2
status:
  conditions:
  - lastTransitionTime: "1970-01-01T00:00:00Z"
    message: reference SQLInstance ${projectId}/sqlinstance-replica-${uniqueId} is
      not found
    reason: DependencyNotFound
    status: "False"
    type: Ready
  observedGeneration: 1
