// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

package v1alpha1

import (
	"context"
	"fmt"
	"strings"

	refsv1beta1 "github.com/GoogleCloudPlatform/k8s-config-connector/apis/refs/v1beta1"
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/k8s"
	apierrors "k8s.io/apimachinery/pkg/api/errors"
	"k8s.io/apimachinery/pkg/apis/meta/v1/unstructured"
	"k8s.io/apimachinery/pkg/runtime/schema"
	"k8s.io/apimachinery/pkg/types"
	"sigs.k8s.io/controller-runtime/pkg/client"
)

// MemorystoreInstanceBackupRef defines the resource reference to MemorystoreInstanceBackup,
// which "External" field holds the GCP identifier for the KRM object.
type MemorystoreInstanceBackupRef struct {
	// A reference to an externally managed MemorystoreInstance resource.
	// Should be in the format "projects/{{projectID}}/locations/{{location}}/instances/{{instanceID}}".
	External string `json:"external,omitempty"`

	// The name of a MemorystoreInstance resource.
	Name string `json:"name,omitempty"`

	// The namespace of a MemorystoreInstance resource.
	Namespace string `json:"namespace,omitempty"`
}

func ResolveMemorystoreInstanceBackup(ctx context.Context, reader client.Reader, src client.Object, ref *MemorystoreInstanceBackupRef) (*MemorystoreInstanceBackupRef, error) {
	if ref == nil {
		return nil, nil
	}

	if ref.External != "" {
		if ref.Name != "" {
			return nil, fmt.Errorf("cannot specify both name and external on memorystoreinstance reference")
		}

		tokens := strings.Split(ref.External, "/")
		if len(tokens) == 8 && tokens[0] == "projects" && tokens[2] == "locations" && tokens[4] == "backupCollections" && tokens[6] == "backups" {
			return &MemorystoreInstanceBackupRef{
				External: fmt.Sprintf("projects/%s/locations/%s/backupCollections/%s/backups/%s", tokens[1], tokens[3], tokens[5], tokens[7]),
			}, nil
		}
		return nil, fmt.Errorf("format of memorystoreinstance external=%q was not known (use projects/<projectId>/locations/<location>/backupCollections/<backupCollectionName>/backups/<backupId>)", ref.External)
	}

	if ref.Name == "" {
		return nil, fmt.Errorf("must specify either name or external on memorystoreinstancebackup reference")
	}
	key := types.NamespacedName{
		Namespace: ref.Namespace,
		Name:      ref.Name,
	}
	if key.Namespace == "" {
		key.Namespace = src.GetNamespace()
	}

	backupObj := &unstructured.Unstructured{}
	backupObj.SetGroupVersionKind(schema.GroupVersionKind{
		Group:   "memorystore.cnrm.cloud.google.com",
		Version: "v1alpha1",
		Kind:    "MemorystoreInstanceBackup",
	})
	if err := reader.Get(ctx, key, backupObj); err != nil {
		if apierrors.IsNotFound(err) {
			return nil, fmt.Errorf("referenced MemorystoreInstanceBackup %v not found", key)
		}
		return nil, fmt.Errorf("error reading referenced MemorystoreInstanceBackup %v: %w", key, err)
	}

	// Get external from status.externalRef. This is the most trustworthy place.
	actualExternalRef, _, err := unstructured.NestedString(backupObj.Object, "status", "externalRef")
	if err != nil {
		return nil, fmt.Errorf("reading status.externalRef: %w", err)
	}
	if actualExternalRef == "" {
		return nil, k8s.NewReferenceNotReadyError(backupObj.GroupVersionKind(), key)
	}
	return &MemorystoreInstanceBackupRef{
		External: actualExternalRef,
	}, nil
}

// MemorystoreInstanceServiceAttachmentRef defines the resource reference to the GCP identifier
// for the ServiceAttachment managed by the MemorystoreInstance pointed by the MemorystoreInstanceRef.
type MemorystoreInstanceServiceAttachmentRef struct {
	// A reference to a ServiceAttachment managed by aMemorystoreInstance resource.
	ServiceAttachmentExternal string `json:"serviceAttachmentExternal,omitempty"`

	// The name of a MemorystoreInstance resource.
	MemorystoreInstanceRef *refsv1beta1.MemorystoreInstanceRef `json:"memorystoreInstanceRef,omitempty"`

	// The index of the PSC attachment details.
	PscAttachmentDetailsIndex *int `json:"pscAttachmentDetailsIndex,omitempty"`
}

// DeepCopyInto is an autogenerated deepcopy function, copying the receiver, writing into out. in must be non-nil.
func (in *MemorystoreInstanceServiceAttachmentRef) DeepCopyInto(out *MemorystoreInstanceServiceAttachmentRef) {
	*out = *in
	if in.MemorystoreInstanceRef != nil {
		in, out := &in.MemorystoreInstanceRef, &out.MemorystoreInstanceRef
		*out = new(refsv1beta1.MemorystoreInstanceRef)
		**out = **in
	}
	if in.PscAttachmentDetailsIndex != nil {
		in, out := &in.PscAttachmentDetailsIndex, &out.PscAttachmentDetailsIndex
		*out = new(int)
		**out = **in
	}
}

func (in *MemorystoreInstanceServiceAttachmentRef) DeepCopy() *MemorystoreInstanceServiceAttachmentRef {
	if in == nil {
		return nil
	}
	out := new(MemorystoreInstanceServiceAttachmentRef)
	in.DeepCopyInto(out)
	return out
}

func ResolveMemorystoreInstanceServiceAttachment(ctx context.Context, reader client.Reader, src client.Object, ref *MemorystoreInstanceServiceAttachmentRef) (*MemorystoreInstanceServiceAttachmentRef, error) {
	if ref == nil {
		return nil, nil
	}

	if ref.ServiceAttachmentExternal != "" {
		if ref.MemorystoreInstanceRef != nil {
			return nil, fmt.Errorf("cannot specify both name and external on memorystoreinstance reference")
		}

		tokens := strings.Split(ref.ServiceAttachmentExternal, "/")
		if len(tokens) == 6 && tokens[0] == "projects" && tokens[2] == "regions" && tokens[4] == "serviceAttachments" {
			return &MemorystoreInstanceServiceAttachmentRef{
				ServiceAttachmentExternal: fmt.Sprintf("projects/%s/regions/%s/serviceAttachments/%s", tokens[1], tokens[3], tokens[5]),
			}, nil
		}
		return nil, fmt.Errorf("format of memorystoreinstance external=%q was not known (use projects/<projectId>/regions/<region>/serviceAttachments/<serviceAttachmentId>)", ref.ServiceAttachmentExternal)
	}

	if ref.MemorystoreInstanceRef == nil {
		return nil, fmt.Errorf("must specify either memorystoreInstanceRef or serviceAttachmentExternal")
	}

	key := types.NamespacedName{
		Namespace: ref.MemorystoreInstanceRef.Namespace,
		Name:      ref.MemorystoreInstanceRef.Name,
	}
	if key.Namespace == "" {
		key.Namespace = src.GetNamespace()
	}
	if key.Name == "" {
		key.Name = src.GetName()
	}
	if key.Name == "" {
		return nil, fmt.Errorf("must specify either memorystoreInstanceRef.Name or serviceAttachmentExternal")
	}

	u := &unstructured.Unstructured{}
	u.SetGroupVersionKind(schema.GroupVersionKind{
		Group:   "memorystore.cnrm.cloud.google.com",
		Version: "v1beta1",
		Kind:    "MemorystoreInstance",
	})
	if err := reader.Get(ctx, key, u); err != nil {
		if apierrors.IsNotFound(err) {
			return nil, fmt.Errorf("referenced MemorystoreInstance %v not found", key)
		}
		return nil, fmt.Errorf("error reading referenced MemorystoreInstance %v: %w", key, err)
	}

	// Read status.observedState.pscAttachmentDetails[MemorystoreInstanceServiceAttachmentIndex]
	// to retrieve the service attachment external.
	pscAttachmentDetails, found, err := unstructured.NestedSlice(u.Object, "status", "observedState", "pscAttachmentDetails")
	if err != nil {
		return nil, fmt.Errorf("getting status.observedState.pscAttachmentDetails[]: %w", err)
	}
	if !found {
		return nil, k8s.NewReferenceNotFoundError(u.GroupVersionKind(), key)
	}
	if len(pscAttachmentDetails) <= int(*ref.PscAttachmentDetailsIndex) {
		return nil, fmt.Errorf("pscAttachmentDetailsindex is out of range")
	}

	pscAttachmentDetail, ok := pscAttachmentDetails[*ref.PscAttachmentDetailsIndex].(map[string]interface{})
	if !ok {
		return nil, fmt.Errorf("failed getting status.observedState.pscAttachmentDetails[%d]", *ref.PscAttachmentDetailsIndex)
	}
	serviceAttachmentExternal, ok := pscAttachmentDetail["serviceAttachment"].(string)
	if !ok {
		return nil, fmt.Errorf("failed getting status.observedState.pscAttachmentDetails[%d].serviceAttachment", *ref.PscAttachmentDetailsIndex)
	}

	return &MemorystoreInstanceServiceAttachmentRef{
		ServiceAttachmentExternal: serviceAttachmentExternal,
	}, nil
}
