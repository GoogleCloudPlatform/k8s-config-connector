apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: dialogflowsecuritysettings.dialogflow.cnrm.cloud.google.com
spec:
  group: dialogflow.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DialogflowSecuritySettings
    listKind: DialogflowSecuritySettingsList
    plural: dialogflowsecuritysettings
    shortNames:
    - gcpdialogflowsecuritysetting
    - gcpdialogflowsecuritysettings
    singular: dialogflowsecuritysettings
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DialogflowSecuritySettings is the Schema for the DialogflowSecuritySettings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DialogflowSecuritySettingsSpec defines the desired state
              of DialogflowSecuritySettings
            properties:
              audioExportSettings:
                description: Controls audio export settings for post-conversation
                  analytics.
                properties:
                  audioExportPattern:
                    description: Filename pattern for exported audio.
                    type: string
                  audioFormat:
                    description: |-
                      File format for exported audio file. Currently only in telephony
                       recordings.
                    type: string
                  enableAudioRedaction:
                    description: |-
                      Enable audio redaction if it is true.
                       Note that this only redacts end-user audio data;
                       Synthesised audio from the virtual agent is not redacted.
                    type: boolean
                  gcsBucket:
                    description: |-
                      Cloud Storage bucket to export audio record to.
                       Setting this field would grant the Storage Object Creator role to
                       the Dialogflow Service Agent.
                       API caller that tries to modify this field should have the permission of
                       storage.buckets.setIamPolicy.
                    type: string
                  storeTtsAudio:
                    description: |-
                      Whether to store TTS audio. By default, TTS audio from the virtual agent
                       is not exported.
                    type: boolean
                type: object
              deidentifyTemplate:
                description: |-
                  [DLP](https://cloud.google.com/dlp/docs) deidentify template name. Use this
                  template to define de-identification configuration for the content.
                type: string
              displayName:
                description: |-
                  Required. The human-readable name of the security settings, unique within
                  the location.
                type: string
              insightsExportSettings:
                description: Controls conversation exporting settings to Insights
                  after conversation is completed.
                properties:
                  enableInsightsExport:
                    description: |-
                      If enabled, we will automatically exports
                       conversations to Insights and Insights runs its analyzers.
                    type: boolean
                type: object
              inspectTemplate:
                description: |-
                  [DLP](https://cloud.google.com/dlp/docs) inspect template name. Use this
                  template to define inspect base settings.
                type: string
              location:
                description: Immutable. The location of this resource.
                type: string
                x-kubernetes-validations:
                - message: Location field is immutable
                  rule: self == oldSelf
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              purgeDataTypes:
                description: List of types of data to remove when retention settings
                  triggers purge.
                items:
                  type: string
                type: array
              redactionScope:
                description: |-
                  Defines the data for which Dialogflow applies redaction. Dialogflow does
                  not redact data that it does not have access to – for example, Cloud
                  logging.
                type: string
              redactionStrategy:
                description: Strategy that defines how we do redaction.
                type: string
              resourceID:
                description: The DialogflowSecuritySettings name. If not given, the
                  metadata.name will be used.
                type: string
              retentionStrategy:
                description: Specifies the retention behavior defined by SecuritySettings.RetentionStrategy.
                type: string
              retentionWindowDays:
                description: Retains the data for the specified number of days.
                format: int32
                type: integer
            required:
            - displayName
            - location
            - projectRef
            type: object
          status:
            description: DialogflowSecuritySettingsStatus defines the config connector
              machine state of DialogflowSecuritySettings
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DialogflowSecuritySettings
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
