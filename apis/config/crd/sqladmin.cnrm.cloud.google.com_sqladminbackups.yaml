apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: sqladminbackups.sqladmin.cnrm.cloud.google.com
spec:
  group: sqladmin.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: SQLAdminBackup
    listKind: SQLAdminBackupList
    plural: sqladminbackups
    shortNames:
    - gcpsqladminbackup
    - gcpsqladminbackups
    singular: sqladminbackup
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SQLAdminBackup is the Schema for the SQLAdminBackup API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SQLAdminBackupSpec defines the desired state of SQLAdminBackup
            properties:
              backupKind:
                description: Specifies the kind of backup, PHYSICAL or DEFAULT_SNAPSHOT.
                type: string
              description:
                description: The description of this backup, only applicable to on-demand
                  backups.
                type: string
              diskEncryptionConfiguration:
                description: Encryption configuration specific to a backup.
                properties:
                  kmsKeyRef:
                    description: KMS key used to encrypt the backup.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey.
                          Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
                        type: string
                      name:
                        description: The `name` of a `KMSCryptoKey` resource.
                        type: string
                      namespace:
                        description: The `namespace` of a `KMSCryptoKey` resource.
                        type: string
                    type: object
                type: object
              instanceRef:
                description: The SQLInstance that this backup belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The SQLInstance selfLink, when not managed by Config
                      Connector.
                    type: string
                  name:
                    description: The `name` field of a `SQLInstance` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `SQLInstance` resource.
                    type: string
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The SQLAdminBackup name (ID). If not given, the metadata.name
                  will be used.
                type: string
            required:
            - instanceRef
            - location
            - projectRef
            type: object
          status:
            description: SQLAdminBackupStatus defines the config connector machine
              state of SQLAdminBackup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the SQLAdminBackup resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  diskEncryptionStatus:
                    description: Encryption status specific to a backup.
                    properties:
                      kind:
                        description: This is always `sql#diskEncryptionStatus`.
                        type: string
                      kmsKeyVersionName:
                        description: KMS key version used to encrypt the Cloud SQL
                          instance resource
                        type: string
                    type: object
                  endTime:
                    description: The time the backup operation completed in UTC timezone
                      in RFC 3339 format.
                    type: string
                  enqueuedTime:
                    description: The time the run was enqueued in UTC timezone in
                      RFC 3339 format.
                    type: string
                  error:
                    description: Information about why the backup operation failed.
                      This is only present if the run has the FAILED status.
                    properties:
                      code:
                        description: Identifies the specific error that occurred.
                        type: string
                      kind:
                        description: This is always `sql#operationError`.
                        type: string
                      message:
                        description: Additional information about the error encountered.
                        type: string
                    type: object
                  id:
                    description: The identifier for this backup run. Unique only for
                      a specific Cloud SQL instance.
                    format: int64
                    type: integer
                  startTime:
                    description: The time the backup operation actually started in
                      UTC timezone in RFC 3339 format.
                    type: string
                  status:
                    description: The status of this run.
                    type: string
                  timeZone:
                    description: Backup time zone to prevent restores to an instance
                      with a different time zone. Now relevant only for SQL Server.
                    type: string
                  type:
                    description: The type of this run; can be either "AUTOMATED" or
                      "ON_DEMAND" or "FINAL".
                    type: string
                  windowStartTime:
                    description: The start time of the backup window during which
                      this the backup was attempted in RFC 3339 format.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
