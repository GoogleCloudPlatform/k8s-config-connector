apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: cloudbuildconnections.cloudbuild.cnrm.cloud.google.com
spec:
  group: cloudbuild.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: CloudBuildConnection
    listKind: CloudBuildConnectionList
    plural: cloudbuildconnections
    shortNames:
    - gcpcloudbuildconnection
    - gcpcloudbuildconnections
    singular: cloudbuildconnection
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudBuildConnection is the Schema for the CloudBuildConnection
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CloudBuildConnectionSpec defines the desired state of CloudBuildConnection
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Allows clients to store small amounts of arbitrary data.
                type: object
              bitbucketCloudConfig:
                description: Configuration for connections to Bitbucket Cloud.
                properties:
                  authorizerCredential:
                    description: Required. An access token with the `webhook`, `repository`,
                      `repository:admin` and `pullrequest` scope access. It can be
                      either a workspace, project or repository access token. It's
                      recommended to use a system account to generate these credentials.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  readAuthorizerCredential:
                    description: Required. An access token with the `repository` access.
                      It can be either a workspace, project or repository access token.
                      It's recommended to use a system account to generate the credentials.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  webhookSecretSecretVersionRef:
                    description: Required. SecretManager resource containing the webhook
                      secret used to verify webhook events, formatted as `projects/*/secrets/*/versions/*`.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                  workspace:
                    description: Required. The Bitbucket Cloud Workspace ID to be
                      connected to Google Cloud Platform.
                    type: string
                required:
                - authorizerCredential
                - readAuthorizerCredential
                - webhookSecretSecretVersionRef
                - workspace
                type: object
              bitbucketDataCenterConfig:
                description: Configuration for connections to Bitbucket Data Center.
                properties:
                  authorizerCredential:
                    description: Required. A http access token with the `REPO_ADMIN`
                      scope access.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  hostURI:
                    description: Required. The URI of the Bitbucket Data Center instance
                      or cluster this connection is for.
                    type: string
                  readAuthorizerCredential:
                    description: Required. A http access token with the `REPO_READ`
                      access.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  serviceDirectoryConfig:
                    description: Optional. Configuration for using Service Directory
                      to privately connect to a Bitbucket Data Center. This should
                      only be set if the Bitbucket Data Center is hosted on-premises
                      and not reachable by public internet. If this field is left
                      empty, calls to the Bitbucket Data Center will be made over
                      the public internet.
                    properties:
                      serviceRef:
                        description: Required. The Service Directory service name.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed DataprocJob
                              resource. Should be in the format "/projects/*/locations/*/namespaces/*/services/*".
                            type: string
                          name:
                            description: The name of a DataprocJob resource.
                            type: string
                          namespace:
                            description: The namespace of a DataprocJob resource.
                            type: string
                        type: object
                    type: object
                  sslCA:
                    description: Optional. SSL certificate to use for requests to
                      the Bitbucket Data Center.
                    type: string
                  webhookSecretSecretVersionRef:
                    description: Required. Immutable. SecretManager resource containing
                      the webhook secret used to verify webhook events, formatted
                      as `projects/*/secrets/*/versions/*`.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                required:
                - authorizerCredential
                - hostURI
                - readAuthorizerCredential
                - webhookSecretSecretVersionRef
                type: object
              disabled:
                description: If disabled is set to true, functionality is disabled
                  for this connection. Repository based API methods and webhooks processing
                  for repositories in this connection will be disabled.
                type: boolean
              githubConfig:
                description: Configuration for connections to github.com.
                properties:
                  appInstallationID:
                    description: GitHub App installation id.
                    format: int64
                    type: integer
                  authorizerCredential:
                    description: OAuth credential of the account that authorized the
                      Cloud Build GitHub App. It is recommended to use a robot account
                      instead of a human user account. The OAuth token must be tied
                      to the Cloud Build GitHub App.
                    properties:
                      oauthTokenSecretVersionRef:
                        description: 'A SecretManager resource containing the OAuth
                          token that authorizes the Cloud Build connection. Format:
                          `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    type: object
                type: object
              githubEnterpriseConfig:
                description: Configuration for connections to an instance of GitHub
                  Enterprise.
                properties:
                  apiKey:
                    description: Required. API Key used for authentication of webhook
                      events.
                    type: string
                  appID:
                    description: Id of the GitHub App created from the manifest.
                    format: int64
                    type: integer
                  appInstallationID:
                    description: ID of the installation of the GitHub App.
                    format: int64
                    type: integer
                  appSlug:
                    description: The URL-friendly name of the GitHub App.
                    type: string
                  hostURI:
                    description: Required. The URI of the GitHub Enterprise host this
                      connection is for.
                    type: string
                  privateKeySecretVersionRef:
                    description: SecretManager resource containing the private key
                      of the GitHub App, formatted as `projects/*/secrets/*/versions/*`.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                  serviceDirectoryConfig:
                    description: Configuration for using Service Directory to privately
                      connect to a GitHub Enterprise server. This should only be set
                      if the GitHub Enterprise server is hosted on-premises and not
                      reachable by public internet. If this field is left empty, calls
                      to the GitHub Enterprise server will be made over the public
                      internet.
                    properties:
                      serviceRef:
                        description: Required. The Service Directory service name.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed DataprocJob
                              resource. Should be in the format "/projects/*/locations/*/namespaces/*/services/*".
                            type: string
                          name:
                            description: The name of a DataprocJob resource.
                            type: string
                          namespace:
                            description: The namespace of a DataprocJob resource.
                            type: string
                        type: object
                    type: object
                  sslCA:
                    description: SSL certificate to use for requests to GitHub Enterprise.
                    type: string
                  webhookSecretSecretVersionRef:
                    description: SecretManager resource containing the webhook secret
                      of the GitHub App, formatted as `projects/*/secrets/*/versions/*`.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                required:
                - apiKey
                - hostURI
                type: object
              gitlabConfig:
                description: Configuration for connections to gitlab.com or an instance
                  of GitLab Enterprise.
                properties:
                  authorizerCredential:
                    description: Required. A GitLab personal access token with the
                      `api` scope access.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  hostURI:
                    description: The URI of the GitLab Enterprise host this connection
                      is for. If not specified, the default value is https://gitlab.com.
                    type: string
                  readAuthorizerCredential:
                    description: Required. A GitLab personal access token with the
                      minimum `read_api` scope access.
                    properties:
                      userTokenSecretVersionRef:
                        description: 'Required. A SecretManager resource containing
                          the user token that authorizes the Cloud Build connection.
                          Format: `projects/*/secrets/*/versions/*`.'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                            type: string
                          name:
                            description: The `name` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                          namespace:
                            description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                              resource.
                            type: string
                        type: object
                    required:
                    - userTokenSecretVersionRef
                    type: object
                  serviceDirectoryConfig:
                    description: Configuration for using Service Directory to privately
                      connect to a GitLab Enterprise server. This should only be set
                      if the GitLab Enterprise server is hosted on-premises and not
                      reachable by public internet. If this field is left empty, calls
                      to the GitLab Enterprise server will be made over the public
                      internet.
                    properties:
                      serviceRef:
                        description: Required. The Service Directory service name.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed DataprocJob
                              resource. Should be in the format "/projects/*/locations/*/namespaces/*/services/*".
                            type: string
                          name:
                            description: The name of a DataprocJob resource.
                            type: string
                          namespace:
                            description: The namespace of a DataprocJob resource.
                            type: string
                        type: object
                    type: object
                  sslCA:
                    description: SSL certificate to use for requests to GitLab Enterprise.
                    type: string
                  webhookSecretSecretVersionRef:
                    description: Required. Immutable. SecretManager resource containing
                      the webhook secret of a GitLab Enterprise project, formatted
                      as `projects/*/secrets/*/versions/*`.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                required:
                - authorizerCredential
                - readAuthorizerCredential
                - webhookSecretSecretVersionRef
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The CloudBuildConnection name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - location
            - projectRef
            type: object
          status:
            description: CloudBuildConnectionStatus defines the config connector machine
              state of CloudBuildConnection
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the CloudBuildConnection resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  bitbucketCloudConfig:
                    description: Configuration for connections to Bitbucket Cloud.
                    properties:
                      authorizerCredential:
                        description: Required. An access token with the `webhook`,
                          `repository`, `repository:admin` and `pullrequest` scope
                          access. It can be either a workspace, project or repository
                          access token. It's recommended to use a system account to
                          generate these credentials.
                        properties:
                          username:
                            description: Output only. The username associated to this
                              token.
                            type: string
                        type: object
                      readAuthorizerCredential:
                        description: Required. An access token with the `repository`
                          access. It can be either a workspace, project or repository
                          access token. It's recommended to use a system account to
                          generate the credentials.
                        properties:
                          username:
                            description: Output only. The username associated to this
                              token.
                            type: string
                        type: object
                    type: object
                  bitbucketDataCenterConfig:
                    description: Configuration for connections to Bitbucket Data Center.
                    properties:
                      serverVersion:
                        description: Output only. Version of the Bitbucket Data Center
                          running on the `host_uri`.
                        type: string
                    type: object
                  createTime:
                    description: Output only. Server assigned timestamp for when the
                      connection was created.
                    type: string
                  etag:
                    description: This checksum is computed by the server based on
                      the value of other fields, and may be sent on update and delete
                      requests to ensure the client has an up-to-date value before
                      proceeding.
                    type: string
                  githubConfig:
                    description: Configuration for connections to github.com.
                    properties:
                      authorizerCredential:
                        description: OAuth credential of the account that authorized
                          the Cloud Build GitHub App. It is recommended to use a robot
                          account instead of a human user account. The OAuth token
                          must be tied to the Cloud Build GitHub App.
                        properties:
                          username:
                            description: Output only. The username associated to this
                              token.
                            type: string
                        type: object
                    type: object
                  githubEnterpriseConfig:
                    description: Configuration for connections to an instance of GitHub
                      Enterprise.
                    properties:
                      serverVersion:
                        description: Output only. GitHub Enterprise version installed
                          at the host_uri.
                        type: string
                    type: object
                  gitlabConfig:
                    description: Configuration for connections to gitlab.com or an
                      instance of GitLab Enterprise.
                    properties:
                      readAuthorizerCredential:
                        description: Required. A GitLab personal access token with
                          the minimum `read_api` scope access.
                        properties:
                          username:
                            description: Output only. The username associated to this
                              token.
                            type: string
                        type: object
                      serverVersion:
                        description: Output only. Version of the GitLab Enterprise
                          server running on the `host_uri`.
                        type: string
                    type: object
                  installationState:
                    description: Output only. Installation state of the Connection.
                    properties:
                      actionURI:
                        description: Output only. Link to follow for next action.
                          Empty string if the installation is already complete.
                        type: string
                      message:
                        description: Output only. Message of what the user should
                          do next to continue the installation. Empty string if the
                          installation is already complete.
                        type: string
                      stage:
                        description: Output only. Current step of the installation
                          process.
                        type: string
                    type: object
                  reconciling:
                    description: Output only. Set to true when the connection is being
                      set up or updated in the background.
                    type: boolean
                  updateTime:
                    description: Output only. Server assigned timestamp for when the
                      connection was updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
