apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: apigeeapiproducts.apigee.cnrm.cloud.google.com
spec:
  group: apigee.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: ApigeeAPIProduct
    listKind: ApigeeAPIProductList
    plural: apigeeapiproducts
    shortNames:
    - gcpapigeeapiproduct
    - gcpapigeeapiproducts
    singular: apigeeapiproduct
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ApigeeAPIProduct is the Schema for the ApigeeAPIProduct API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ApigeeAPIProductSpec defines the desired state of ApigeeAPIProduct
            properties:
              apiResources:
                description: Comma-separated list of API resources to be bundled in
                  the API product.
                items:
                  type: string
                type: array
              approvalType:
                description: Flag that specifies how API keys are approved to access
                  the APIs defined by the API product.
                type: string
              attributes:
                description: Array of attributes that may be used to extend the default
                  API product profile with customer-specific metadata.
                items:
                  properties:
                    name:
                      description: API key of the attribute.
                      type: string
                    value:
                      description: Value of the attribute.
                      type: string
                  type: object
                type: array
              description:
                description: Description of the API product.
                type: string
              displayName:
                description: Name displayed in the UI or developer portal to developers
                  registering for API access.
                type: string
              environments:
                description: Comma-separated list of environment names to which the
                  API product is bound.
                items:
                  type: string
                type: array
              graphQLOperationGroup:
                description: Configuration used to group Apigee proxies or remote
                  services with graphQL operation name, graphQL operation type and
                  quotas.
                properties:
                  operationConfigType:
                    description: Flag that specifies whether the configuration is
                      for Apigee API proxy or a remote service. Valid values include
                      `proxy` or `remoteservice`. Defaults to `proxy`. Set to `proxy`
                      when Apigee API proxies are associated with the API product.
                      Set to `remoteservice` when non-Apigee proxies like Istio-Envoy
                      are associated with the API product.
                    type: string
                  operationConfigs:
                    description: Required. List of operation configurations for either
                      Apigee API proxies or other remote services that are associated
                      with this API product.
                    items:
                      properties:
                        apiSource:
                          description: Required. Name of the API proxy endpoint or
                            remote service with which the GraphQL operation and quota
                            are associated.
                          type: string
                        attributes:
                          description: Custom attributes associated with the operation.
                          items:
                            properties:
                              name:
                                description: API key of the attribute.
                                type: string
                              value:
                                description: Value of the attribute.
                                type: string
                            type: object
                          type: array
                        operations:
                          description: 'Required. List of GraphQL name/operation type
                            pairs for the proxy or remote service to which quota will
                            be applied. If only operation types are specified, the
                            quota will be applied to all GraphQL requests irrespective
                            of the GraphQL name. **Note**: Currently, you can specify
                            only a single GraphQLOperation. Specifying more than one
                            will cause the operation to fail.'
                          items:
                            properties:
                              operation:
                                description: GraphQL operation name. The name and
                                  operation type will be used to apply quotas. If
                                  no name is specified, the quota will be applied
                                  to all GraphQL operations irrespective of their
                                  operation names in the payload.
                                type: string
                              operationTypes:
                                description: 'Required. GraphQL operation types. Valid
                                  values include `query` or `mutation`. **Note**:
                                  Apigee does not currently support `subscription`
                                  types.'
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        quota:
                          description: Quota parameters to be enforced for the resources,
                            methods, and API source combination. If none are specified,
                            quota enforcement will not be done.
                          properties:
                            interval:
                              description: Required. Time interval over which the
                                number of request messages is calculated.
                              type: string
                            limit:
                              description: Required. Upper limit allowed for the time
                                interval and time unit specified. Requests exceeding
                                this limit will be rejected.
                              type: string
                            timeUnit:
                              description: Time unit defined for the `interval`. Valid
                                values include `minute`, `hour`, `day`, or `month`.
                                If `limit` and `interval` are valid, the default value
                                is `hour`; otherwise, the default is null.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              grpcOperationGroup:
                description: Optional. Configuration used to group Apigee proxies
                  with gRPC services and method names.
                properties:
                  operationConfigs:
                    description: Required. List of operation configurations for either
                      Apigee API proxies that are associated with this API product.
                    items:
                      properties:
                        apiSource:
                          description: Required. Name of the API proxy with which
                            the gRPC operation and quota are associated.
                          type: string
                        attributes:
                          description: Custom attributes associated with the operation.
                          items:
                            properties:
                              name:
                                description: API key of the attribute.
                                type: string
                              value:
                                description: Value of the attribute.
                                type: string
                            type: object
                          type: array
                        methods:
                          description: 'List of unqualified gRPC method names for
                            the proxy to which quota will be applied. If this field
                            is empty, the Quota will apply to all operations on the
                            gRPC service defined on the proxy. Example: Given a proxy
                            that is configured to serve com.petstore.PetService, the
                            methods com.petstore.PetService.ListPets and com.petstore.PetService.GetPet
                            would be specified here as simply ["ListPets", "GetPet"].'
                          items:
                            type: string
                          type: array
                        quota:
                          description: Quota parameters to be enforced for the methods
                            and API source combination. If none are specified, quota
                            enforcement will not be done.
                          properties:
                            interval:
                              description: Required. Time interval over which the
                                number of request messages is calculated.
                              type: string
                            limit:
                              description: Required. Upper limit allowed for the time
                                interval and time unit specified. Requests exceeding
                                this limit will be rejected.
                              type: string
                            timeUnit:
                              description: Time unit defined for the `interval`. Valid
                                values include `minute`, `hour`, `day`, or `month`.
                                If `limit` and `interval` are valid, the default value
                                is `hour`; otherwise, the default is null.
                              type: string
                          type: object
                        service:
                          description: Required. gRPC Service name associated to be
                            associated with the API proxy, on which quota rules can
                            be applied upon.
                          type: string
                      type: object
                    type: array
                type: object
              llmOperationGroup:
                description: Optional. Configuration used to group Apigee proxies
                  with resources, method types, LLM model and quotas.
                properties:
                  operationConfigs:
                    description: Required. List of LLM operation configurations for
                      either Apigee API proxies that are associated with this API
                      product.
                    items:
                      properties:
                        apiSource:
                          description: Required. Name of the API proxy or remote service
                            with which the resources, methods, and quota are associated.
                          type: string
                        attributes:
                          description: Optional. Custom attributes associated with
                            the operation.
                          items:
                            properties:
                              name:
                                description: API key of the attribute.
                                type: string
                              value:
                                description: Value of the attribute.
                                type: string
                            type: object
                          type: array
                        llmOperations:
                          description: 'Required. List of resource/method/model for
                            the API proxy to which quota will applied. **Note**: Currently,
                            you can specify only a single resource/method/model mapping.
                            The call will fail if more than one resource/method/model
                            mappings are provided.'
                          items:
                            properties:
                              methods:
                                description: 'Optional. methods refers to the REST
                                  verbs as in https://httpwg.org/specs/rfc9110.html
                                  For example: GET, POST, PUT, DELETE, etc. They need
                                  to be in uppercase. When none specified, all verb
                                  types are allowed.'
                                items:
                                  type: string
                                type: array
                              model:
                                description: Required. LLM model name associated with
                                  the API proxy
                                type: string
                              resource:
                                description: Required. REST resource path associated
                                  with the API proxy or remote service.
                                type: string
                            type: object
                          type: array
                        llmTokenQuota:
                          description: Required. LLM token Quota parameters to be
                            enforced for the resources, methods, and API source &
                            LLM model combination. If none are specified, quota enforcement
                            will not be done.
                          properties:
                            interval:
                              description: Required. Time interval over which the
                                number of request messages is calculated.
                              type: string
                            limit:
                              description: Required. Upper limit of LLM tokens allowed
                                for the time interval and time unit specified. Requests
                                exceeding this limit will be rejected.
                              type: string
                            timeUnit:
                              description: Optional. Time unit defined for the `interval`.
                                Valid values include `minute`, `hour`, `day`, or `month`.
                                If `limit` and `interval` are valid, the default value
                                is `hour`; otherwise, the default is null.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              llmQuota:
                description: Optional. Number of LLM tokens permitted per app by this
                  API product for the specified `llm_quota_interval` and `llm_quota_time_unit`.
                type: string
              llmQuotaInterval:
                description: Optional. Time interval over which the number of tokens
                  from LLM responses is calculated.
                type: string
              llmQuotaTimeUnit:
                description: Optional. Time unit defined for the `llm_quota_interval`.
                  Valid values include `minute`, `hour`, `day`, or `month`.
                type: string
              operationGroup:
                description: Configuration used to group Apigee proxies or remote
                  services with resources, method types, and quotas.
                properties:
                  operationConfigType:
                    description: Flag that specifes whether the configuration is for
                      Apigee API proxy or a remote service. Valid values include `proxy`
                      or `remoteservice`. Defaults to `proxy`. Set to `proxy` when
                      Apigee API proxies are associated with the API product. Set
                      to `remoteservice` when non-Apigee proxies like Istio-Envoy
                      are associated with the API product.
                    type: string
                  operationConfigs:
                    description: Required. List of operation configurations for either
                      Apigee API proxies or other remote services that are associated
                      with this API product.
                    items:
                      properties:
                        apiSource:
                          description: Required. Name of the API proxy or remote service
                            with which the resources, methods, and quota are associated.
                          type: string
                        attributes:
                          description: Custom attributes associated with the operation.
                          items:
                            properties:
                              name:
                                description: API key of the attribute.
                                type: string
                              value:
                                description: Value of the attribute.
                                type: string
                            type: object
                          type: array
                        operations:
                          description: 'List of resource/method pairs for the API
                            proxy or remote service to which quota will applied. **Note**:
                            Currently, you can specify only a single resource/method
                            pair. The call will fail if more than one resource/method
                            pair is provided.'
                          items:
                            properties:
                              methods:
                                description: methods refers to the REST verbs as in
                                  https://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html.
                                  When none specified, all verb types are allowed.
                                items:
                                  type: string
                                type: array
                              resource:
                                description: Required. REST resource path associated
                                  with the API proxy or remote service.
                                type: string
                            type: object
                          type: array
                        quota:
                          description: Quota parameters to be enforced for the resources,
                            methods, and API source combination. If none are specified,
                            quota enforcement will not be done.
                          properties:
                            interval:
                              description: Required. Time interval over which the
                                number of request messages is calculated.
                              type: string
                            limit:
                              description: Required. Upper limit allowed for the time
                                interval and time unit specified. Requests exceeding
                                this limit will be rejected.
                              type: string
                            timeUnit:
                              description: Time unit defined for the `interval`. Valid
                                values include `minute`, `hour`, `day`, or `month`.
                                If `limit` and `interval` are valid, the default value
                                is `hour`; otherwise, the default is null.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              organizationRef:
                description: Reference to the organization.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed ApigeeOrganization
                      resource. Should be in the format "organizations/{{organizationID}}".
                    type: string
                  name:
                    description: The name of an ApigeeOrganization resource.
                    type: string
                  namespace:
                    description: The namespace of an ApigeeOrganization resource.
                    type: string
                type: object
              proxies:
                description: Comma-separated list of API proxy names to which this
                  API product is bound.
                items:
                  type: string
                type: array
              quota:
                description: Number of request messages permitted per app by this
                  API product for the specified `quotaInterval` and `quotaTimeUnit`.
                type: string
              quotaCounterScope:
                description: Scope of the quota decides how the quota counter gets
                  applied and evaluate for quota violation.
                type: string
              quotaInterval:
                description: Time interval over which the number of request messages
                  is calculated.
                type: string
              quotaTimeUnit:
                description: Time unit defined for the `quotaInterval`.
                type: string
              resourceID:
                description: The ApigeeAPIProduct name. If not given, the metadata.name
                  will be used.
                type: string
              scopes:
                description: Comma-separated list of OAuth scopes that are validated
                  at runtime.
                items:
                  type: string
                type: array
            required:
            - organizationRef
            type: object
          status:
            description: ApigeeAPIProductStatus defines the config connector machine
              state of ApigeeAPIProduct
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the ApigeeAPIProduct resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createdAt:
                    description: Response only. Creation time of this environment
                      as milliseconds since epoch.
                    format: int64
                    type: integer
                  lastModifiedAt:
                    description: Response only. Modified time of this environment
                      as milliseconds since epoch.
                    format: int64
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
