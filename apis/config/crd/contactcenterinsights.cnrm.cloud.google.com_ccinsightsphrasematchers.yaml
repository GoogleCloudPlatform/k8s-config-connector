apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: ccinsightsphrasematchers.contactcenterinsights.cnrm.cloud.google.com
spec:
  group: contactcenterinsights.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: CCInsightsPhraseMatcher
    listKind: CCInsightsPhraseMatcherList
    plural: ccinsightsphrasematchers
    shortNames:
    - gcpccinsightsphrasematcher
    - gcpccinsightsphrasematchers
    singular: ccinsightsphrasematcher
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CCInsightsPhraseMatcher is the Schema for the CCInsightsPhraseMatcher
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CCInsightsPhraseMatcherSpec defines the desired state of
              CCInsightsPhraseMatcher
            properties:
              active:
                description: Applies the phrase matcher only when it is active.
                type: boolean
              displayName:
                description: The human-readable name of the phrase matcher.
                type: string
              location:
                description: Immutable.
                type: string
                x-kubernetes-validations:
                - message: Location field is immutable
                  rule: self == oldSelf
              phraseMatchRuleGroups:
                description: A list of phase match rule groups that are included in
                  this matcher.
                items:
                  properties:
                    phraseMatchRules:
                      description: A list of phrase match rules that are included
                        in this group.
                      items:
                        properties:
                          config:
                            description: |-
                              Provides additional information about the rule that specifies how to apply
                              the rule.
                            properties:
                              exactMatchConfig:
                                description: The configuration for the exact match
                                  rule.
                                properties:
                                  caseSensitive:
                                    description: Whether to consider case sensitivity
                                      when performing an exact match.
                                    type: boolean
                                type: object
                            type: object
                          negated:
                            description: |-
                              Specifies whether the phrase must be missing from the transcript segment or
                              present in the transcript segment.
                            type: boolean
                          query:
                            description: Required. The phrase to be matched.
                            type: string
                        required:
                        - query
                        type: object
                      type: array
                    type:
                      description: Required. The type of this phrase match rule group.
                      enum:
                      - PHRASE_MATCH_RULE_GROUP_TYPE_UNSPECIFIED
                      - ALL_OF
                      - ANY_OF
                      type: string
                  required:
                  - type
                  type: object
                type: array
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The CCInsightsPhraseMatcher name. If not given, the metadata.name
                  will be used.
                type: string
              roleMatch:
                description: |-
                  The role whose utterances the phrase matcher should be matched
                  against. If the role is ROLE_UNSPECIFIED it will be matched against any
                  utterances in the transcript.
                enum:
                - ROLE_UNSPECIFIED
                - HUMAN_AGENT
                - AUTOMATED_AGENT
                - END_USER
                - ANY_AGENT
                type: string
              type:
                description: Required. The type of this phrase matcher.
                enum:
                - PHRASE_MATCHER_TYPE_UNSPECIFIED
                - ALL_OF
                - ANY_OF
                type: string
              versionTag:
                description: |-
                  The customized version tag to use for the phrase matcher. If not specified,
                  it will default to `revision_id`.
                type: string
            required:
            - location
            - projectRef
            - type
            type: object
          status:
            description: CCInsightsPhraseMatcherStatus defines the config connector
              machine state of CCInsightsPhraseMatcher
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the CCInsightsPhraseMatcher resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  activationUpdateTime:
                    description: |-
                      Output only. The most recent time at which the activation status was
                      updated.
                    type: string
                  revisionCreateTime:
                    description: |-
                      Output only. The timestamp of when the revision was created. It is also the
                      create time when a new matcher is added.
                    type: string
                  revisionID:
                    description: |-
                      Output only. Immutable. The revision ID of the phrase matcher.
                      A new revision is committed whenever the matcher is changed, except when it
                      is activated or deactivated. A server generated random ID will be used.
                      Example: locations/global/phraseMatchers/my-first-matcher@1234567
                    type: string
                  updateTime:
                    description: Output only. The most recent time at which the phrase
                      matcher was updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
