apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: testingdevicesessions.testing.cnrm.cloud.google.com
spec:
  group: testing.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: TestingDeviceSession
    listKind: TestingDeviceSessionList
    plural: testingdevicesessions
    shortNames:
    - gcptestingdevicesession
    - gcptestingdevicesessions
    singular: testingdevicesession
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TestingDeviceSession is the Schema for the TestingDeviceSession
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TestingDeviceSessionSpec defines the desired state of TestingDeviceSession
            properties:
              androidDevice:
                description: Required. The requested device
                properties:
                  androidModelID:
                    description: |-
                      Required. The id of the Android device to be used.
                       Use the TestEnvironmentDiscoveryService to get supported options.
                    type: string
                  androidVersionID:
                    description: |-
                      Required. The id of the Android OS version to be used.
                       Use the TestEnvironmentDiscoveryService to get supported options.
                    type: string
                  locale:
                    description: |-
                      Required. The locale the test device used for testing.
                       Use the TestEnvironmentDiscoveryService to get supported options.
                    type: string
                  orientation:
                    description: |-
                      Required. How the device is oriented during the test.
                       Use the TestEnvironmentDiscoveryService to get supported options.
                    type: string
                required:
                - androidModelID
                - androidVersionID
                - locale
                - orientation
                type: object
              expireTime:
                description: |-
                  Optional. If the device is still in use at this time, any connections
                   will be ended and the SessionState will transition from ACTIVE to
                   FINISHED.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The TestingDeviceSession name. If not given, the metadata.name
                  will be used.
                type: string
              ttl:
                description: |-
                  Optional. The amount of time that a device will be initially allocated
                   for. This can eventually be extended with the UpdateDeviceSession RPC.
                   Default: 15 minutes.
                type: string
            required:
            - androidDevice
            - projectRef
            type: object
          status:
            description: TestingDeviceSessionStatus defines the config connector machine
              state of TestingDeviceSession
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the TestingDeviceSession resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  activeStartTime:
                    description: Output only. The timestamp that the session first
                      became ACTIVE.
                    type: string
                  createTime:
                    description: Output only. The time that the Session was created.
                    type: string
                  displayName:
                    description: Output only. The title of the DeviceSession to be
                      presented in the UI.
                    type: string
                  inactivityTimeout:
                    description: |-
                      Output only. The interval of time that this device must be interacted with
                       before it transitions from ACTIVE to TIMEOUT_INACTIVITY.
                    type: string
                  state:
                    description: Output only. Current state of the DeviceSession.
                    type: string
                  stateHistories:
                    description: |-
                      Output only. The historical state transitions of the session_state message
                       including the current session state.
                    items:
                      properties:
                        eventTime:
                          description: |-
                            Output only. The time that the session_state first encountered that
                             state.
                          type: string
                        sessionState:
                          description: Output only. The session_state tracked by this
                            event
                          type: string
                        stateMessage:
                          description: Output only. A human-readable message to explain
                            the state.
                          type: string
                      type: object
                    type: array
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
