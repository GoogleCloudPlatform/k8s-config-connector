apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: networksecuritypartnerssegateways.networksecurity.cnrm.cloud.google.com
spec:
  group: networksecurity.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: NetworkSecurityPartnerSSEGateway
    listKind: NetworkSecurityPartnerSSEGatewayList
    plural: networksecuritypartnerssegateways
    shortNames:
    - gcpnetworksecuritypartnerssegateway
    - gcpnetworksecuritypartnerssegateways
    singular: networksecuritypartnerssegateway
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkSecurityPartnerSSEGateway is the Schema for the NetworkSecurityPartnerSSEGateway
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSecurityPartnerSSEGatewaySpec defines the desired
              state of NetworkSecurityPartnerSSEGateway
            properties:
              labels:
                additionalProperties:
                  type: string
                description: Optional. Labels as key value pairs
                type: object
              location:
                description: The location of this resource.
                type: string
              partnerSubnetRange:
                description: Optional. Subnet range of the partner-owned subnet.
                type: string
              partnerVPCSubnetRange:
                description: |-
                  Optional. Subnet range of the partner_vpc
                   This field is deprecated. Use partner_subnet_range instead.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The NetworkSecurityPartnerSSEGateway name. If not given,
                  the metadata.name will be used.
                type: string
              sseGatewayReferenceID:
                description: Required. ID of the SSEGatewayReference that pairs with
                  this PartnerSSEGateway
                type: string
              sseSubnetRange:
                description: |-
                  Optional. Subnet range where SSE GW instances are deployed.
                   Default value is set to "100.88.255.0/24".
                   The CIDR suffix should be less than or equal to 25.
                type: string
              symantecOptions:
                description: Optional. Required iff Partner is Symantec.
                properties:
                  symantecSiteTargetHost:
                    description: |-
                      Optional. Target for the NCGs to send traffic to on the Symantec side.
                       Only supports IP address today.
                    type: string
                type: object
              vni:
                description: |-
                  Optional. Virtual Network Identifier to use in NCG.
                   Today the only partner that depends on it is Symantec.
                format: int32
                type: integer
            required:
            - location
            - projectRef
            - sseGatewayReferenceID
            type: object
          status:
            description: NetworkSecurityPartnerSSEGatewayStatus defines the config
              connector machine state of NetworkSecurityPartnerSSEGateway
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the NetworkSecurityPartnerSSEGateway
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  capacityBps:
                    description: |-
                      Output only. Copied from the associated NCC resource in Symantec NCCGW
                       flows. Used by Symantec API.
                    format: int64
                    type: integer
                  country:
                    description: |-
                      Output only. ISO-3166 alpha 2 country code used for localization.
                       Filled from the customer SSEGateway, and only for PartnerSSEGateways
                       associated with Symantec today.
                    type: string
                  createTime:
                    description: Output only. Create time stamp
                    type: string
                  partnerSSEEnvironment:
                    description: |-
                      Output only. Full URI of the partner environment this PartnerSSEGateway is
                       connected to. Filled from the customer SSEGateway, and only for
                       PartnerSSEGateways associated with Symantec today.
                    type: string
                  partnerSSERealm:
                    description: Output only. name of PartnerSSERealm owning the PartnerSSEGateway
                    type: string
                  proberSubnetRanges:
                    description: |-
                      Output only. Subnet ranges for Google-issued probe packets.
                       It's populated only for Prisma Access partners.
                    items:
                      type: string
                    type: array
                  sseBGPAsn:
                    description: Output only. ASN of SSE BGP
                    format: int32
                    type: integer
                  sseBGPIPs:
                    description: Output only. IP of SSE BGP
                    items:
                      type: string
                    type: array
                  sseNetwork:
                    description: |-
                      Output only. The ID of the network in sse_project containing
                       sse_subnet_range. This is also known as the partnerFacingNetwork. Only
                       filled for PartnerSSEGateways associated with Symantec today.
                    type: string
                  sseProject:
                    description: |-
                      Output only. The project owning partner_facing_network. Only filled for
                       PartnerSSEGateways associated with Symantec today.
                    type: string
                  sseTargetIP:
                    description: |-
                      Output only. Target IP that belongs to sse_subnet_range where partner
                       should send the traffic to reach the customer networks.
                    type: string
                  sseVPCSubnetRange:
                    description: |-
                      Output only. Subnet range of the subnet where partner traffic is routed.
                       This field is deprecated. Use sse_subnet_range instead.
                    type: string
                  sseVPCTargetIP:
                    description: |-
                      Output only. This is the IP where the partner traffic should be routed to.
                       This field is deprecated. Use sse_target_ip instead.
                    type: string
                  state:
                    description: Output only. State of the gateway.
                    type: string
                  symantecOptions:
                    description: Optional. Required iff Partner is Symantec.
                    properties:
                      symantecLocationUuid:
                        description: Output only. UUID of the Symantec Location created
                          on the customer's behalf.
                        type: string
                      symantecSite:
                        description: Output only. Symantec data center identifier
                          that this SSEGW will connect to. Filled from the customer
                          SSEGateway, and only for PartnerSSEGateways associated with
                          Symantec today.
                        type: string
                    type: object
                  timezone:
                    description: |-
                      Output only. tzinfo identifier used for localization.
                       Filled from the customer SSEGateway, and only for PartnerSSEGateways
                       associated with Symantec today.
                    type: string
                  updateTime:
                    description: Output only. Update time stamp
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
