script:
- prompt: "You need to create a KCC SecretManagerSecret resource named 'my-secret' that sources its data from a Kubernetes Secret named 'my-k8s-secret' and key 'secret-data'. Use the available tools to generate the appropriate YAML manifest and then write it to the file at $(git rev-parse --show-toplevel)/.build/tasks/secretmanager-1-from-k8s/secret.yaml. Hint: Use the available MCP tools to find the resource schema and generate the YAML. If the mcp tool is not available, use your knowledge to generate the YAML manifest. "
verifier: "verify.sh"
cleanup: "cleanup.sh"
difficulty: "medium"
