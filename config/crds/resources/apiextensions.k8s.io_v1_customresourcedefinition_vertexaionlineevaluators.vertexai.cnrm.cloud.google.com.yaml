# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaionlineevaluators.vertexai.cnrm.cloud.google.com
spec:
  group: vertexai.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIOnlineEvaluator
    listKind: VertexAIOnlineEvaluatorList
    plural: vertexaionlineevaluators
    shortNames:
    - gcpvertexaionlineevaluator
    - gcpvertexaionlineevaluators
    singular: vertexaionlineevaluator
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIOnlineEvaluator is the Schema for the VertexAIOnlineEvaluator
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIOnlineEvaluatorSpec defines the desired state of
              VertexAIOnlineEvaluator
            properties:
              agentResource:
                description: Required. Immutable. The name of the agent that the OnlineEvaluator
                  evaluates periodically.
                type: string
              cloudObservability:
                description: Data source for the OnlineEvaluator, based on GCP Observability
                  stack (Cloud Trace & Cloud Logging).
                properties:
                  logView:
                    description: Optional. Optional log view that will be used to
                      query logs. If empty, the `_Default` view will be used.
                    type: string
                  openTelemetry:
                    description: Data source follows OpenTelemetry convention.
                    properties:
                      semconvVersion:
                        description: Required. Defines which version OTel Semantic
                          Convention the data follows. Can be "1.39.0" or newer.
                        type: string
                    type: object
                  traceScope:
                    description: Scope online evaluation to single traces.
                    properties:
                      filter:
                        description: |-
                          Optional. A list of predicates to filter traces. Multiple predicates
                           are combined using AND.

                           The maximum number of predicates is 10.
                        items:
                          properties:
                            duration:
                              description: Filter on the duration of a trace.
                              properties:
                                comparisonOperator:
                                  description: Required. The comparison operator to
                                    apply.
                                  type: string
                                value:
                                  description: Required. The value to compare against.
                                  type: number
                              type: object
                            totalTokenUsage:
                              description: Filter on the total token usage within
                                a trace.
                              properties:
                                comparisonOperator:
                                  description: Required. The comparison operator to
                                    apply.
                                  type: string
                                value:
                                  description: Required. The value to compare against.
                                  type: number
                              type: object
                          type: object
                        type: array
                    type: object
                  traceView:
                    description: |-
                      Optional. Optional trace view that will be used to query traces.
                       If empty, the `_Default` view will be used.

                       NOTE: This field is not supported yet and will be ignored if set.
                    type: string
                type: object
              config:
                description: Required. Configuration for the OnlineEvaluator.
                properties:
                  maxEvaluatedSamplesPerRun:
                    description: Optional. The maximum number of evaluations to perform
                      per run. If set to 0, the number is unbounded.
                    format: int64
                    type: integer
                  randomSampling:
                    description: Random sampling method.
                    properties:
                      percentage:
                        description: Required. The percentage of traces to sample
                          for evaluation. Must be an integer between `1` and `100`.
                        format: int32
                        type: integer
                    type: object
                type: object
              displayName:
                description: Optional. Human-readable name for the `OnlineEvaluator`.
                type: string
              location:
                description: The location of this resource.
                type: string
              metricSources:
                description: Required. A list of metric sources to be used for evaluating
                  samples.
                items:
                  properties:
                    metric:
                      description: Inline metric config.
                      properties:
                        aggregationMetrics:
                          description: Optional. The aggregation metrics to use.
                          items:
                            type: string
                          type: array
                        bleuSpec:
                          description: Spec for bleu metric.
                          properties:
                            useEffectiveOrder:
                              description: Optional. Whether to use_effective_order
                                to compute bleu score.
                              type: boolean
                          type: object
                        computationBasedMetricSpec:
                          description: Spec for a computation based metric.
                          properties:
                            parameters:
                              description: 'Optional. A map of parameters for the
                                metric, e.g. {"rouge_type": "rougeL"}.'
                              x-kubernetes-preserve-unknown-fields: true
                            type:
                              description: Required. The type of the computation based
                                metric.
                              type: string
                          type: object
                        customCodeExecutionSpec:
                          description: Spec for Custom Code Execution metric.
                          properties:
                            evaluationFunction:
                              description: |-
                                Required. Python function.
                                 Expected user to define the following function, e.g.:
                                   def evaluate(instance: dict[str, Any]) -> float:
                                 Please include this function signature in the code snippet.
                                 Instance is the evaluation instance, any fields populated in the instance
                                 are available to the function as instance[field_name].

                                 Example:
                                  Example input:
                                  ```
                                  instance= EvaluationInstance(
                                      response=EvaluationInstance.InstanceData(text="The answer is 4."),
                                      reference=EvaluationInstance.InstanceData(text="4")
                                  )
                                  ```

                                  Example converted input:
                                  ```
                                  {
                                   'response': {'text': 'The answer is 4.'},
                                   'reference': {'text': '4'}
                                  }
                                  ```

                                  Example python function:
                                  ```
                                   def evaluate(instance: dict[str, Any]) -> float:
                                     if instance['response']['text'] == instance['reference']['text']:
                                       return 1.0
                                     return 0.0
                                  ```

                                 CustomCodeExecutionSpec is also supported in Batch Evaluation (EvalDataset
                                 RPC) and Tuning Evaluation. Each line in the input jsonl file will be
                                 converted to dict[str, Any] and passed to the evaluation function.
                              type: string
                          type: object
                        exactMatchSpec:
                          description: Spec for exact match metric.
                          type: object
                        llmBasedMetricSpec:
                          description: Spec for an LLM based metric.
                          properties:
                            additionalConfig:
                              description: Optional. Optional additional configuration
                                for the metric.
                              x-kubernetes-preserve-unknown-fields: true
                            judgeAutoraterConfig:
                              description: Optional. Optional configuration for the
                                judge LLM (Autorater).
                              properties:
                                autoraterModel:
                                  description: |-
                                    Optional. The fully qualified name of the publisher model or tuned
                                     autorater endpoint to use.

                                     Publisher model format:
                                     `projects/{project}/locations/{location}/publishers/*/models/*`

                                     Tuned model endpoint format:
                                     `projects/{project}/locations/{location}/endpoints/{endpoint}`
                                  type: string
                                flipEnabled:
                                  description: Optional. Default is true. Whether
                                    to flip the candidate and baseline responses.
                                    This is only applicable to the pairwise metric.
                                    If enabled, also provide PairwiseMetricSpec.candidate_response_field_name
                                    and PairwiseMetricSpec.baseline_response_field_name.
                                    When rendering PairwiseMetricSpec.metric_prompt_template,
                                    the candidate and baseline fields will be flipped
                                    for half of the samples to reduce bias.
                                  type: boolean
                                generationConfig:
                                  description: Optional. Configuration options for
                                    model generation and outputs.
                                  properties:
                                    audioTimestamp:
                                      description: Optional. If enabled, audio timestamp
                                        will be included in the request to the model.
                                      type: boolean
                                    candidateCount:
                                      description: Optional. Number of candidates
                                        to generate.
                                      format: int32
                                      type: integer
                                    frequencyPenalty:
                                      description: Optional. Frequency penalties.
                                      type: number
                                    logprobs:
                                      description: Optional. Logit probabilities.
                                      format: int32
                                      type: integer
                                    maxOutputTokens:
                                      description: Optional. The maximum number of
                                        output tokens to generate per message.
                                      format: int32
                                      type: integer
                                    mediaResolution:
                                      description: Optional. If specified, the media
                                        resolution specified will be used.
                                      type: string
                                    modelConfig:
                                      description: Optional. Config for model selection.
                                      properties:
                                        featureSelectionPreference:
                                          description: Required. Feature selection
                                            preference.
                                          type: string
                                      type: object
                                    presencePenalty:
                                      description: Optional. Positive penalties.
                                      type: number
                                    responseJsonSchema:
                                      description: |-
                                        Optional. Output schema of the generated response. This is an alternative
                                         to `response_schema` that accepts [JSON Schema](https://json-schema.org/).

                                         If set, `response_schema` must be omitted, but `response_mime_type` is
                                         required.

                                         While the full JSON Schema may be sent, not all features are supported.
                                         Specifically, only the following properties are supported:

                                         - `$id`
                                         - `$defs`
                                         - `$ref`
                                         - `$anchor`
                                         - `type`
                                         - `format`
                                         - `title`
                                         - `description`
                                         - `enum` (for strings and numbers)
                                         - `items`
                                         - `prefixItems`
                                         - `minItems`
                                         - `maxItems`
                                         - `minimum`
                                         - `maximum`
                                         - `anyOf`
                                         - `oneOf` (interpreted the same as `anyOf`)
                                         - `properties`
                                         - `additionalProperties`
                                         - `required`

                                         The non-standard `propertyOrdering` property may also be set.

                                         Cyclic references are unrolled to a limited degree and, as such, may only
                                         be used within non-required properties. (Nullable properties are not
                                         sufficient.) If `$ref` is set on a sub-schema, no other properties, except
                                         for than those starting as a `$`, may be set.
                                      x-kubernetes-preserve-unknown-fields: true
                                    responseLogprobs:
                                      description: Optional. If true, export the logprobs
                                        results in response.
                                      type: boolean
                                    responseMimeType:
                                      description: 'Optional. Output response mimetype
                                        of the generated candidate text. Supported
                                        mimetype: - `text/plain`: (default) Text output.
                                        - `application/json`: JSON response in the
                                        candidates. The model needs to be prompted
                                        to output the appropriate response type, otherwise
                                        the behavior is undefined. This is a preview
                                        feature.'
                                      type: string
                                    responseModalities:
                                      description: Optional. The modalities of the
                                        response.
                                      items:
                                        type: string
                                      type: array
                                    responseSchema:
                                      description: 'Optional. The `Schema` object
                                        allows the definition of input and output
                                        data types. These types can be objects, but
                                        also primitives and arrays. Represents a select
                                        subset of an [OpenAPI 3.0 schema object](https://spec.openapis.org/oas/v3.0.3#schema).
                                        If set, a compatible response_mime_type must
                                        also be set. Compatible mimetypes: `application/json`:
                                        Schema for JSON response.'
                                      x-kubernetes-preserve-unknown-fields: true
                                    routingConfig:
                                      description: Optional. Routing configuration.
                                      properties:
                                        autoMode:
                                          description: Automated routing.
                                          properties:
                                            modelRoutingPreference:
                                              description: The model routing preference.
                                              type: string
                                          type: object
                                        manualMode:
                                          description: Manual routing.
                                          properties:
                                            modelName:
                                              description: The model name to use.
                                                Only the public LLM models are accepted.
                                                e.g. 'gemini-1.5-pro-001'.
                                              type: string
                                          type: object
                                      type: object
                                    seed:
                                      description: Optional. Seed.
                                      format: int32
                                      type: integer
                                    speechConfig:
                                      description: Optional. The speech generation
                                        config.
                                      properties:
                                        voiceConfig:
                                          description: The configuration for the speaker
                                            to use.
                                          properties:
                                            prebuiltVoiceConfig:
                                              description: The configuration for the
                                                prebuilt voice to use.
                                              properties:
                                                voiceName:
                                                  description: The name of the preset
                                                    voice to use.
                                                  type: string
                                              type: object
                                          type: object
                                      type: object
                                    stopSequences:
                                      description: Optional. Stop sequences.
                                      items:
                                        type: string
                                      type: array
                                    temperature:
                                      description: Optional. Controls the randomness
                                        of predictions.
                                      type: number
                                    thinkingConfig:
                                      description: Optional. Config for thinking features.
                                        An error will be returned if this field is
                                        set for models that don't support thinking.
                                      properties:
                                        includeThoughts:
                                          description: Indicates whether to include
                                            thoughts in the response. If true, thoughts
                                            are returned only when available.
                                          type: boolean
                                        thinkingBudget:
                                          description: Optional. Indicates the thinking
                                            budget in tokens. This is only applied
                                            when enable_thinking is true.
                                          format: int32
                                          type: integer
                                      type: object
                                    topK:
                                      description: Optional. If specified, top-k sampling
                                        will be used.
                                      type: number
                                    topP:
                                      description: Optional. If specified, nucleus
                                        sampling will be used.
                                      type: number
                                  type: object
                                samplingCount:
                                  description: Optional. Number of samples for each
                                    instance in the dataset. If not specified, the
                                    default is 4. Minimum value is 1, maximum value
                                    is 32.
                                  format: int32
                                  type: integer
                              type: object
                            metricPromptTemplate:
                              description: Required. Template for the prompt sent
                                to the judge model.
                              type: string
                            predefinedRubricGenerationSpec:
                              description: Dynamically generate rubrics using a predefined
                                spec.
                              properties:
                                metricSpecName:
                                  description: Required. The name of a pre-defined
                                    metric, such as "instruction_following_v1" or
                                    "text_quality_v1".
                                  type: string
                                metricSpecParameters:
                                  description: Optional. The parameters needed to
                                    run the pre-defined metric.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            resultParserConfig:
                              description: Optional. The parser config for the metric
                                result.
                              properties:
                                customCodeParserConfig:
                                  description: Optional. Use custom code to parse
                                    the LLM response.
                                  properties:
                                    parsingFunction:
                                      description: |-
                                        Required. Python function for parsing results. The function should be
                                         defined within this string.

                                         The function takes a list of strings (LLM responses) and should return
                                         either a list of dictionaries (for rubrics) or a single dictionary
                                         (for a metric result).

                                         Example function signature:
                                         def parse(responses: list[str]) -> list[dict[str, Any]] | dict[str, Any]:

                                          When parsing rubrics, return a list of dictionaries, where each
                                          dictionary represents a Rubric.
                                          Example for rubrics:
                                          [
                                            {
                                              "content": {"property": {"description": "The response is
                                          factual."}},
                                              "type": "FACTUALITY",
                                              "importance": "HIGH"
                                            },
                                            {
                                              "content": {"property": {"description": "The response is
                                          fluent."}},
                                              "type": "FLUENCY",
                                              "importance": "MEDIUM"
                                            }
                                          ]

                                          When parsing critique results, return a dictionary representing a
                                          MetricResult.
                                          Example for a metric result:
                                          {
                                            "score": 0.8,
                                            "explanation": "The model followed most instructions.",
                                            "rubric_verdicts": [...]
                                          }

                                          ... code for result extraction and aggregation
                                      type: string
                                  type: object
                              type: object
                            rubricGenerationSpec:
                              description: Dynamically generate rubrics using this
                                specification.
                              properties:
                                modelConfig:
                                  description: Configuration for the model used in
                                    rubric generation. Configs including sampling
                                    count and base model can be specified here. Flipping
                                    is not supported for rubric generation.
                                  properties:
                                    autoraterModel:
                                      description: |-
                                        Optional. The fully qualified name of the publisher model or tuned
                                         autorater endpoint to use.

                                         Publisher model format:
                                         `projects/{project}/locations/{location}/publishers/*/models/*`

                                         Tuned model endpoint format:
                                         `projects/{project}/locations/{location}/endpoints/{endpoint}`
                                      type: string
                                    flipEnabled:
                                      description: Optional. Default is true. Whether
                                        to flip the candidate and baseline responses.
                                        This is only applicable to the pairwise metric.
                                        If enabled, also provide PairwiseMetricSpec.candidate_response_field_name
                                        and PairwiseMetricSpec.baseline_response_field_name.
                                        When rendering PairwiseMetricSpec.metric_prompt_template,
                                        the candidate and baseline fields will be
                                        flipped for half of the samples to reduce
                                        bias.
                                      type: boolean
                                    generationConfig:
                                      description: Optional. Configuration options
                                        for model generation and outputs.
                                      properties:
                                        audioTimestamp:
                                          description: Optional. If enabled, audio
                                            timestamp will be included in the request
                                            to the model.
                                          type: boolean
                                        candidateCount:
                                          description: Optional. Number of candidates
                                            to generate.
                                          format: int32
                                          type: integer
                                        frequencyPenalty:
                                          description: Optional. Frequency penalties.
                                          type: number
                                        logprobs:
                                          description: Optional. Logit probabilities.
                                          format: int32
                                          type: integer
                                        maxOutputTokens:
                                          description: Optional. The maximum number
                                            of output tokens to generate per message.
                                          format: int32
                                          type: integer
                                        mediaResolution:
                                          description: Optional. If specified, the
                                            media resolution specified will be used.
                                          type: string
                                        modelConfig:
                                          description: Optional. Config for model
                                            selection.
                                          properties:
                                            featureSelectionPreference:
                                              description: Required. Feature selection
                                                preference.
                                              type: string
                                          type: object
                                        presencePenalty:
                                          description: Optional. Positive penalties.
                                          type: number
                                        responseJsonSchema:
                                          description: |-
                                            Optional. Output schema of the generated response. This is an alternative
                                             to `response_schema` that accepts [JSON Schema](https://json-schema.org/).

                                             If set, `response_schema` must be omitted, but `response_mime_type` is
                                             required.

                                             While the full JSON Schema may be sent, not all features are supported.
                                             Specifically, only the following properties are supported:

                                             - `$id`
                                             - `$defs`
                                             - `$ref`
                                             - `$anchor`
                                             - `type`
                                             - `format`
                                             - `title`
                                             - `description`
                                             - `enum` (for strings and numbers)
                                             - `items`
                                             - `prefixItems`
                                             - `minItems`
                                             - `maxItems`
                                             - `minimum`
                                             - `maximum`
                                             - `anyOf`
                                             - `oneOf` (interpreted the same as `anyOf`)
                                             - `properties`
                                             - `additionalProperties`
                                             - `required`

                                             The non-standard `propertyOrdering` property may also be set.

                                             Cyclic references are unrolled to a limited degree and, as such, may only
                                             be used within non-required properties. (Nullable properties are not
                                             sufficient.) If `$ref` is set on a sub-schema, no other properties, except
                                             for than those starting as a `$`, may be set.
                                          x-kubernetes-preserve-unknown-fields: true
                                        responseLogprobs:
                                          description: Optional. If true, export the
                                            logprobs results in response.
                                          type: boolean
                                        responseMimeType:
                                          description: 'Optional. Output response
                                            mimetype of the generated candidate text.
                                            Supported mimetype: - `text/plain`: (default)
                                            Text output. - `application/json`: JSON
                                            response in the candidates. The model
                                            needs to be prompted to output the appropriate
                                            response type, otherwise the behavior
                                            is undefined. This is a preview feature.'
                                          type: string
                                        responseModalities:
                                          description: Optional. The modalities of
                                            the response.
                                          items:
                                            type: string
                                          type: array
                                        responseSchema:
                                          description: 'Optional. The `Schema` object
                                            allows the definition of input and output
                                            data types. These types can be objects,
                                            but also primitives and arrays. Represents
                                            a select subset of an [OpenAPI 3.0 schema
                                            object](https://spec.openapis.org/oas/v3.0.3#schema).
                                            If set, a compatible response_mime_type
                                            must also be set. Compatible mimetypes:
                                            `application/json`: Schema for JSON response.'
                                          x-kubernetes-preserve-unknown-fields: true
                                        routingConfig:
                                          description: Optional. Routing configuration.
                                          properties:
                                            autoMode:
                                              description: Automated routing.
                                              properties:
                                                modelRoutingPreference:
                                                  description: The model routing preference.
                                                  type: string
                                              type: object
                                            manualMode:
                                              description: Manual routing.
                                              properties:
                                                modelName:
                                                  description: The model name to use.
                                                    Only the public LLM models are
                                                    accepted. e.g. 'gemini-1.5-pro-001'.
                                                  type: string
                                              type: object
                                          type: object
                                        seed:
                                          description: Optional. Seed.
                                          format: int32
                                          type: integer
                                        speechConfig:
                                          description: Optional. The speech generation
                                            config.
                                          properties:
                                            voiceConfig:
                                              description: The configuration for the
                                                speaker to use.
                                              properties:
                                                prebuiltVoiceConfig:
                                                  description: The configuration for
                                                    the prebuilt voice to use.
                                                  properties:
                                                    voiceName:
                                                      description: The name of the
                                                        preset voice to use.
                                                      type: string
                                                  type: object
                                              type: object
                                          type: object
                                        stopSequences:
                                          description: Optional. Stop sequences.
                                          items:
                                            type: string
                                          type: array
                                        temperature:
                                          description: Optional. Controls the randomness
                                            of predictions.
                                          type: number
                                        thinkingConfig:
                                          description: Optional. Config for thinking
                                            features. An error will be returned if
                                            this field is set for models that don't
                                            support thinking.
                                          properties:
                                            includeThoughts:
                                              description: Indicates whether to include
                                                thoughts in the response. If true,
                                                thoughts are returned only when available.
                                              type: boolean
                                            thinkingBudget:
                                              description: Optional. Indicates the
                                                thinking budget in tokens. This is
                                                only applied when enable_thinking
                                                is true.
                                              format: int32
                                              type: integer
                                          type: object
                                        topK:
                                          description: Optional. If specified, top-k
                                            sampling will be used.
                                          type: number
                                        topP:
                                          description: Optional. If specified, nucleus
                                            sampling will be used.
                                          type: number
                                      type: object
                                    samplingCount:
                                      description: Optional. Number of samples for
                                        each instance in the dataset. If not specified,
                                        the default is 4. Minimum value is 1, maximum
                                        value is 32.
                                      format: int32
                                      type: integer
                                  type: object
                                promptTemplate:
                                  description: Template for the prompt used to generate
                                    rubrics. The details should be updated based on
                                    the most-recent recipe requirements.
                                  type: string
                                rubricContentType:
                                  description: The type of rubric content to be generated.
                                  type: string
                                rubricTypeOntology:
                                  description: Optional. An optional, pre-defined
                                    list of allowed types for generated rubrics. If
                                    this field is provided, it implies `include_rubric_type`
                                    should be true, and the generated rubric types
                                    should be chosen from this ontology.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            rubricGroupKey:
                              description: Use a pre-defined group of rubrics associated
                                with the input. Refers to a key in the rubric_groups
                                map of EvaluationInstance.
                              type: string
                            systemInstruction:
                              description: Optional. System instructions for the judge
                                model.
                              type: string
                          type: object
                        metadata:
                          description: Optional. Metadata about the metric, used for
                            visualization and organization.
                          properties:
                            otherMetadata:
                              description: Optional. Flexible metadata for user-defined
                                attributes.
                              x-kubernetes-preserve-unknown-fields: true
                            scoreRange:
                              description: Optional. The range of possible scores
                                for this metric, used for plotting.
                              properties:
                                description:
                                  description: Optional. The description of the score
                                    explaining the directionality etc.
                                  type: string
                                max:
                                  description: Required. The maximum value of the
                                    score range (inclusive).
                                  type: number
                                min:
                                  description: Required. The minimum value of the
                                    score range (inclusive).
                                  type: number
                                step:
                                  description: Optional. The distance between discrete
                                    steps in the range. If unset, the range is assumed
                                    to be continuous.
                                  type: number
                              type: object
                            title:
                              description: Optional. The user-friendly name for the
                                metric. If not set for a registered metric, it will
                                default to the metric's display name.
                              type: string
                          type: object
                        pairwiseMetricSpec:
                          description: Spec for pairwise metric.
                          properties:
                            baselineResponseFieldName:
                              description: Optional. The field name of the baseline
                                response.
                              type: string
                            candidateResponseFieldName:
                              description: Optional. The field name of the candidate
                                response.
                              type: string
                            customOutputFormatConfig:
                              description: Optional. CustomOutputFormatConfig allows
                                customization of metric output. When this config is
                                set, the default output is replaced with the raw output
                                string. If a custom format is chosen, the `pairwise_choice`
                                and `explanation` fields in the corresponding metric
                                result will be empty.
                              properties:
                                returnRawOutput:
                                  description: Optional. Whether to return raw output.
                                  type: boolean
                              type: object
                            metricPromptTemplate:
                              description: Required. Metric prompt template for pairwise
                                metric.
                              type: string
                            systemInstruction:
                              description: Optional. System instructions for pairwise
                                metric.
                              type: string
                          type: object
                        pointwiseMetricSpec:
                          description: Spec for pointwise metric.
                          properties:
                            customOutputFormatConfig:
                              description: 'Optional. CustomOutputFormatConfig allows
                                customization of metric output. By default, metrics
                                return a score and explanation. When this config is
                                set, the default output is replaced with either: -
                                The raw output string. - A parsed output based on
                                a user-defined schema. If a custom format is chosen,
                                the `score` and `explanation` fields in the corresponding
                                metric result will be empty.'
                              properties:
                                returnRawOutput:
                                  description: Optional. Whether to return raw output.
                                  type: boolean
                              type: object
                            metricPromptTemplate:
                              description: Required. Metric prompt template for pointwise
                                metric.
                              type: string
                            systemInstruction:
                              description: Optional. System instructions for pointwise
                                metric.
                              type: string
                          type: object
                        predefinedMetricSpec:
                          description: The spec for a pre-defined metric.
                          properties:
                            metricSpecName:
                              description: Required. The name of a pre-defined metric,
                                such as "instruction_following_v1" or "text_quality_v1".
                              type: string
                            metricSpecParameters:
                              description: Optional. The parameters needed to run
                                the pre-defined metric.
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        rougeSpec:
                          description: Spec for rouge metric.
                          properties:
                            rougeType:
                              description: Optional. Supported rouge types are rougen[1-9],
                                rougeL, and rougeLsum.
                              type: string
                            splitSummaries:
                              description: Optional. Whether to split summaries while
                                using rougeLsum.
                              type: boolean
                            useStemmer:
                              description: Optional. Whether to use stemmer to compute
                                rouge score.
                              type: boolean
                          type: object
                      type: object
                    metricResourceName:
                      description: Resource name for registered metric.
                      type: string
                  type: object
                type: array
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAIOnlineEvaluator name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - agentResource
            - config
            - location
            - metricSources
            - projectRef
            type: object
          status:
            description: VertexAIOnlineEvaluatorStatus defines the config connector
              machine state of VertexAIOnlineEvaluator
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIOnlineEvaluator resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Timestamp when the OnlineEvaluator was
                      created.
                    type: string
                  state:
                    description: Output only. The state of the OnlineEvaluator.
                    type: string
                  stateDetails:
                    description: Output only. Contains additional information about
                      the state of the OnlineEvaluator.
                    items:
                      type: object
                    type: array
                  updateTime:
                    description: Output only. Timestamp when the OnlineEvaluator was
                      last updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
