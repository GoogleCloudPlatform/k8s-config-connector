# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: networkconnectivityserviceconnectionmaps.networkconnectivity.cnrm.cloud.google.com
spec:
  group: networkconnectivity.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: NetworkConnectivityServiceConnectionMap
    listKind: NetworkConnectivityServiceConnectionMapList
    plural: networkconnectivityserviceconnectionmaps
    shortNames:
    - gcpnetworkconnectivityserviceconnectionmap
    - gcpnetworkconnectivityserviceconnectionmaps
    singular: networkconnectivityserviceconnectionmap
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkConnectivityServiceConnectionMap is the Schema for the
          NetworkConnectivityServiceConnectionMap API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkConnectivityServiceConnectionMapSpec defines the desired
              state of NetworkConnectivityServiceConnectionMap
            properties:
              consumerPSCConfigs:
                description: The PSC configurations on consumer side.
                items:
                  properties:
                    consumerInstanceProject:
                      description: 'Required. The project ID or project number of
                        the consumer project. This project is the one that the consumer
                        uses to interact with the producer instance. From the perspective
                        of a consumer who''s created a producer instance, this is
                        the project of the producer instance. Format: ''projects/''
                        Eg. ''projects/consumer-project'' or ''projects/1234'''
                      type: string
                    disableGlobalAccess:
                      description: This is used in PSC consumer ForwardingRule to
                        control whether the PSC endpoint can be accessed from another
                        region.
                      type: boolean
                    ipVersion:
                      description: The requested IP version for the PSC connection.
                      type: string
                    networkRef:
                      description: 'The resource path of the consumer network where
                        PSC connections are allowed to be created in. Note, this network
                        does not need be in the ConsumerPscConfig.project in the case
                        of SharedVPC. Example: projects/{projectNumOrId}/global/networks/{networkId}.'
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed ComputeNetwork
                            resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                          type: string
                        name:
                          description: The name of a ComputeNetwork resource.
                          type: string
                        namespace:
                          description: The namespace of a ComputeNetwork resource.
                          type: string
                      type: object
                    producerInstanceID:
                      description: Immutable. Deprecated. Use producer_instance_metadata
                        instead. An immutable identifier for the producer instance.
                      type: string
                    producerInstanceMetadata:
                      additionalProperties:
                        type: string
                      description: Immutable. An immutable map for the producer instance
                        metadata.
                      type: object
                    project:
                      description: The consumer project where PSC connections are
                        allowed to be created in.
                      type: string
                    serviceAttachmentIPAddressMap:
                      additionalProperties:
                        type: string
                      description: Optional. A map to store mapping between customer
                        vip and target service attachment. This field can be used
                        to specify a static IP address for a PSC connection.
                      type: object
                    state:
                      description: Output only. Overall state of PSC Connections management
                        for this consumer psc config.
                      type: string
                  type: object
                type: array
              description:
                description: A description of this resource.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: User-defined labels.
                type: object
              location:
                description: Immutable. The location of this resource.
                type: string
              producerPSCConfigs:
                description: The PSC configurations on producer side.
                items:
                  properties:
                    automatedDNSCreationSpec:
                      description: Optional. The specification for automatically creating
                        a DNS record for this PSC connection.
                      properties:
                        dnsSuffix:
                          description: 'Required. The DNS suffix to use for the DNS
                            record. Must end with a dot. This should be a valid DNS
                            domain name as per RFC 1035. Each label (between dots)
                            can contain letters, digits, and hyphens, and must not
                            start or end with a hyphen. Example: "my-service.example.com.",
                            "internal."'
                          type: string
                        hostname:
                          description: 'Required. The hostname (the first label of
                            the FQDN) to use for the DNS record. This should be a
                            valid DNS label as per RFC 1035. Generally, this means
                            the hostname can contain letters, digits, and hyphens,
                            and must not start or end with a hyphen. Example: "my-instance",
                            "db-1"'
                          type: string
                        ttl:
                          description: Optional. The Time To Live for the DNS record,
                            in seconds. If not provided, a default of 30 seconds will
                            be used.
                          type: string
                      type: object
                    serviceAttachmentRef:
                      description: 'The resource path of a service attachment. Example:
                        projects/{projectNumOrId}/regions/{region}/serviceAttachments/{resourceId}.'
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: The ComputeServiceAttachment selflink in the
                            form "projects/{{project}}/regions/{{region}}/serviceAttachments/{{name}}"
                            when not managed by Config Connector.
                          type: string
                        name:
                          description: The `name` field of a `ComputeServiceAttachment`
                            resource.
                          type: string
                        namespace:
                          description: The `namespace` field of a `ComputeServiceAttachment`
                            resource.
                          type: string
                      type: object
                  type: object
                type: array
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The NetworkConnectivityServiceConnectionMap name. If
                  not given, the metadata.name will be used.
                type: string
              serviceClass:
                description: The service class identifier this ServiceConnectionMap
                  is for. The user of ServiceConnectionMap create API needs to have
                  networkconnectivity.serviceClasses.use IAM permission for the service
                  class.
                type: string
              token:
                description: The token provided by the consumer. This token authenticates
                  that the consumer can create a connection within the specified project
                  and network.
                type: string
            required:
            - location
            - projectRef
            type: object
          status:
            description: NetworkConnectivityServiceConnectionMapStatus defines the
              config connector machine state of NetworkConnectivityServiceConnectionMap
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the NetworkConnectivityServiceConnectionMap
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  consumerPSCConnections:
                    description: Output only. PSC connection details on consumer side.
                    items:
                      properties:
                        dnsAutomationStatus:
                          description: Output only. The status of DNS automation for
                            this PSC connection.
                          properties:
                            error:
                              description: Output only. The error details if the state
                                is CREATE_FAILED or DELETE_FAILED.
                              properties:
                                code:
                                  description: The status code, which should be an
                                    enum value of google.rpc.Code.
                                  format: int32
                                  type: integer
                                message:
                                  description: A developer-facing error message, which
                                    should be in English. Any user-facing error message
                                    should be localized and sent in the google.rpc.Status.details
                                    field, or localized by the client.
                                  type: string
                              type: object
                            fqdn:
                              description: Output only. The fully qualified domain
                                name of the DNS record.
                              type: string
                            state:
                              description: Output only. The current state of DNS automation.
                              type: string
                          type: object
                        error:
                          description: The most recent error during operating this
                            connection.
                          properties:
                            code:
                              description: The status code, which should be an enum
                                value of google.rpc.Code.
                              format: int32
                              type: integer
                            message:
                              description: A developer-facing error message, which
                                should be in English. Any user-facing error message
                                should be localized and sent in the google.rpc.Status.details
                                field, or localized by the client.
                              type: string
                          type: object
                        errorInfo:
                          description: Output only. The error info for the latest
                            error during operating this connection.
                          properties:
                            domain:
                              description: 'The logical grouping to which the "reason"
                                belongs. The error domain is typically the registered
                                service name of the tool or product that generates
                                the error. Example: "pubsub.googleapis.com". If the
                                error is generated by some common infrastructure,
                                the error domain must be a globally unique value that
                                identifies the infrastructure. For Google API infrastructure,
                                the error domain is "googleapis.com".'
                              type: string
                            metadata:
                              additionalProperties:
                                type: string
                              description: 'Additional structured details about this
                                error. Keys must match a regular expression of `a-z+`
                                but should ideally be lowerCamelCase. Also, they must
                                be limited to 64 characters in length. When identifying
                                the current value of an exceeded limit, the units
                                should be contained in the key, not the value. For
                                example, rather than `{"instanceLimit": "100/request"}`,
                                should be returned as, `{"instanceLimitPerRequest":
                                "100"}`, if the client exceeds the number of instances
                                that can be created in a single (batch) request.'
                              type: object
                            reason:
                              description: The reason of the error. This is a constant
                                value that identifies the proximate cause of the error.
                                Error reasons are unique within a particular domain
                                of errors. This should be at most 63 characters and
                                match a regular expression of `A-Z+[A-Z0-9]`, which
                                represents UPPER_SNAKE_CASE.
                              type: string
                          type: object
                        errorType:
                          description: The error type indicates whether the error
                            is consumer facing, producer facing or system internal.
                          type: string
                        forwardingRule:
                          description: 'The URI of the consumer forwarding rule created.
                            Example: projects/{projectNumOrId}/regions/us-east1/networks/{resourceId}.'
                          type: string
                        gceOperation:
                          description: The last Compute Engine operation to setup
                            PSC connection.
                          type: string
                        ip:
                          description: The IP literal allocated on the consumer network
                            for the PSC forwarding rule that is created to connect
                            to the producer service attachment in this service connection
                            map.
                          type: string
                        ipVersion:
                          description: The requested IP version for the PSC connection.
                          type: string
                        network:
                          description: The consumer network whose PSC forwarding rule
                            is connected to the service attachments in this service
                            connection map. Note that the network could be on a different
                            project (shared VPC).
                          type: string
                        producerInstanceID:
                          description: Immutable. Deprecated. Use producer_instance_metadata
                            instead. An immutable identifier for the producer instance.
                          type: string
                        producerInstanceMetadata:
                          additionalProperties:
                            type: string
                          description: Immutable. An immutable map for the producer
                            instance metadata.
                          type: object
                        project:
                          description: The consumer project whose PSC forwarding rule
                            is connected to the service attachments in this service
                            connection map.
                          type: string
                        pscConnectionID:
                          description: The PSC connection id of the PSC forwarding
                            rule connected to the service attachments in this service
                            connection map.
                          type: string
                        selectedSubnetwork:
                          description: Output only. The URI of the selected subnetwork
                            selected to allocate IP address for this connection.
                          type: string
                        serviceAttachmentURI:
                          description: The URI of a service attachment which is the
                            target of the PSC connection.
                          type: string
                        state:
                          description: The state of the PSC connection.
                          type: string
                      type: object
                    type: array
                  createTime:
                    description: Output only. Time when the ServiceConnectionMap was
                      created.
                    type: string
                  etag:
                    description: Optional. The etag is computed by the server, and
                      may be sent on update and delete requests to ensure the client
                      has an up-to-date value before proceeding.
                    type: string
                  infrastructure:
                    description: Output only. The infrastructure used for connections
                      between consumers/producers.
                    type: string
                  serviceClassURI:
                    description: Output only. The service class uri this ServiceConnectionMap
                      is for.
                    type: string
                  updateTime:
                    description: Output only. Time when the ServiceConnectionMap was
                      updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
