# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: transcoderjobs.transcoder.cnrm.cloud.google.com
spec:
  group: transcoder.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: TranscoderJob
    listKind: TranscoderJobList
    plural: transcoderjobs
    shortNames:
    - gcptranscoderjob
    - gcptranscoderjobs
    singular: transcoderjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TranscoderJob is the Schema for the TranscoderJob API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TranscoderJobSpec defines the desired state of TranscoderJob
            properties:
              batchModePriority:
                description: The processing priority of a batch job. This field can
                  only be set for batch mode jobs. The default value is 0. This value
                  cannot be negative. Higher values correspond to higher priorities
                  for the job.
                format: int32
                type: integer
              config:
                description: The configuration for this job.
                properties:
                  adBreaks:
                    description: List of ad breaks. Specifies where to insert ad break
                      tags in the output manifests.
                    items:
                      properties:
                        startTimeOffset:
                          description: Start time in seconds for the ad break, relative
                            to the output file timeline. The default is `0s`.
                          type: string
                      type: object
                    type: array
                  editList:
                    description: List of edit atoms. Defines the ultimate timeline
                      of the resulting file or manifest.
                    items:
                      properties:
                        endTimeOffset:
                          description: End time in seconds for the atom, relative
                            to the input file timeline. When `end_time_offset` is
                            not specified, the `inputs` are used until the end of
                            the atom.
                          type: string
                        inputs:
                          description: List of [Input.key][google.cloud.video.transcoder.v1.Input.key]
                            values identifying files that should be used in this atom.
                            The listed `inputs` must have the same timeline.
                          items:
                            type: string
                          type: array
                        key:
                          description: A unique key for this atom. Must be specified
                            when using advanced mapping.
                          type: string
                        startTimeOffset:
                          description: Start time in seconds for the atom, relative
                            to the input file timeline. The default is `0s`.
                          type: string
                      type: object
                    type: array
                  elementaryStreams:
                    description: List of elementary streams.
                    items:
                      properties:
                        audioStream:
                          description: Encoding of an audio stream.
                          properties:
                            bitrateBps:
                              description: Required. Audio bitrate in bits per second.
                                Must be between 1 and 10,000,000.
                              format: int32
                              type: integer
                            channelCount:
                              description: Number of audio channels. Must be between
                                1 and 6. The default is 2.
                              format: int32
                              type: integer
                            channelLayout:
                              description: |-
                                A list of channel names specifying layout of the audio channels.
                                 This only affects the metadata embedded in the container headers, if
                                 supported by the specified format. The default is `["fl", "fr"]`.

                                 Supported channel names:

                                 - `fl` - Front left channel
                                 - `fr` - Front right channel
                                 - `sl` - Side left channel
                                 - `sr` - Side right channel
                                 - `fc` - Front center channel
                                 - `lfe` - Low frequency
                              items:
                                type: string
                              type: array
                            codec:
                              description: |-
                                The codec for this audio stream. The default is `aac`.

                                 Supported audio codecs:

                                 - `aac`
                                 - `aac-he`
                                 - `aac-he-v2`
                                 - `mp3`
                                 - `ac3`
                                 - `eac3`
                                 - `vorbis`
                              type: string
                            displayName:
                              description: The name for this particular audio stream
                                that will be added to the HLS/DASH manifest. Not supported
                                in MP4 files.
                              type: string
                            languageCode:
                              description: The BCP-47 language code, such as `en-US`
                                or `sr-Latn`. For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier.
                                Not supported in MP4 files.
                              type: string
                            mapping:
                              description: The mapping for the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list]
                                atoms with audio [EditAtom.inputs][google.cloud.video.transcoder.v1.EditAtom.inputs].
                              items:
                                properties:
                                  atomKey:
                                    description: Required. The [EditAtom.key][google.cloud.video.transcoder.v1.EditAtom.key]
                                      that references the atom with audio inputs in
                                      the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list].
                                    type: string
                                  gainDb:
                                    description: Audio volume control in dB. Negative
                                      values decrease volume, positive values increase.
                                      The default is 0.
                                    type: number
                                  inputChannel:
                                    description: Required. The zero-based index of
                                      the channel in the input audio stream.
                                    format: int32
                                    type: integer
                                  inputKey:
                                    description: Required. The [Input.key][google.cloud.video.transcoder.v1.Input.key]
                                      that identifies the input file.
                                    type: string
                                  inputTrack:
                                    description: Required. The zero-based index of
                                      the track in the input file.
                                    format: int32
                                    type: integer
                                  outputChannel:
                                    description: Required. The zero-based index of
                                      the channel in the output audio stream.
                                    format: int32
                                    type: integer
                                type: object
                              type: array
                            sampleRateHertz:
                              description: The audio sample rate in Hertz. The default
                                is 48000 Hertz.
                              format: int32
                              type: integer
                          type: object
                        key:
                          description: A unique key for this elementary stream.
                          type: string
                        textStream:
                          description: Encoding of a text stream. For example, closed
                            captions or subtitles.
                          properties:
                            codec:
                              description: |-
                                The codec for this text stream. The default is `webvtt`.

                                 Supported text codecs:

                                 - `srt`
                                 - `ttml`
                                 - `cea608`
                                 - `cea708`
                                 - `webvtt`
                              type: string
                            displayName:
                              description: The name for this particular text stream
                                that will be added to the HLS/DASH manifest. Not supported
                                in MP4 files.
                              type: string
                            languageCode:
                              description: The BCP-47 language code, such as `en-US`
                                or `sr-Latn`. For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier.
                                Not supported in MP4 files.
                              type: string
                            mapping:
                              description: The mapping for the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list]
                                atoms with text [EditAtom.inputs][google.cloud.video.transcoder.v1.EditAtom.inputs].
                              items:
                                properties:
                                  atomKey:
                                    description: Required. The [EditAtom.key][google.cloud.video.transcoder.v1.EditAtom.key]
                                      that references atom with text inputs in the
                                      [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list].
                                    type: string
                                  inputKey:
                                    description: Required. The [Input.key][google.cloud.video.transcoder.v1.Input.key]
                                      that identifies the input file.
                                    type: string
                                  inputTrack:
                                    description: Required. The zero-based index of
                                      the track in the input file.
                                    format: int32
                                    type: integer
                                type: object
                              type: array
                          type: object
                        videoStream:
                          description: Encoding of a video stream.
                          properties:
                            h264:
                              description: H264 codec settings.
                              properties:
                                allowOpenGop:
                                  description: Specifies whether an open Group of
                                    Pictures (GOP) structure should be allowed or
                                    not. The default is `false`.
                                  type: boolean
                                aqStrength:
                                  description: Specify the intensity of the adaptive
                                    quantizer (AQ). Must be between 0 and 1, where
                                    0 disables the quantizer and 1 maximizes the quantizer.
                                    A higher value equals a lower bitrate but smoother
                                    image. The default is 0.
                                  type: number
                                bFrameCount:
                                  description: The number of consecutive B-frames.
                                    Must be greater than or equal to zero. Must be
                                    less than [H264CodecSettings.gop_frame_count][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.gop_frame_count]
                                    if set. The default is 0.
                                  format: int32
                                  type: integer
                                bPyramid:
                                  description: Allow B-pyramid for reference frame
                                    selection. This may not be supported on all decoders.
                                    The default is `false`.
                                  type: boolean
                                bitrateBps:
                                  description: Required. The video bitrate in bits
                                    per second. The minimum value is 1,000. The maximum
                                    value is 800,000,000.
                                  format: int32
                                  type: integer
                                crfLevel:
                                  description: Target CRF level. Must be between 10
                                    and 36, where 10 is the highest quality and 36
                                    is the most efficient compression. The default
                                    is 21.
                                  format: int32
                                  type: integer
                                enableTwoPass:
                                  description: Use two-pass encoding strategy to achieve
                                    better video quality. [H264CodecSettings.rate_control_mode][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.rate_control_mode]
                                    must be `vbr`. The default is `false`.
                                  type: boolean
                                entropyCoder:
                                  description: |-
                                    The entropy coder to use. The default is `cabac`.

                                     Supported entropy coders:

                                     - `cavlc`
                                     - `cabac`
                                  type: string
                                frameRate:
                                  description: Required. The target video frame rate
                                    in frames per second (FPS). Must be less than
                                    or equal to 120.
                                  type: number
                                frameRateConversionStrategy:
                                  description: Optional. Frame rate conversion strategy
                                    for desired frame rate. The default is `DOWNSAMPLE`.
                                  type: string
                                gopDuration:
                                  description: Select the GOP size based on the specified
                                    duration. The default is `3s`. Note that `gopDuration`
                                    must be less than or equal to [`segmentDuration`](#SegmentSettings),
                                    and [`segmentDuration`](#SegmentSettings) must
                                    be divisible by `gopDuration`.
                                  type: string
                                gopFrameCount:
                                  description: Select the GOP size based on the specified
                                    frame count. Must be greater than zero.
                                  format: int32
                                  type: integer
                                heightPixels:
                                  description: |-
                                    The height of the video in pixels. Must be an even integer.
                                     When not specified, the height is adjusted to match the specified width
                                     and input aspect ratio. If both are omitted, the input height is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the height, in pixels, per the horizontal ASR. The API calculates
                                     the width per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                                hlg:
                                  description: Optional. HLG color format setting
                                    for H264.
                                  type: object
                                pixelFormat:
                                  description: |-
                                    Pixel format to use. The default is `yuv420p`.

                                     Supported pixel formats:

                                     - `yuv420p` pixel format
                                     - `yuv422p` pixel format
                                     - `yuv444p` pixel format
                                     - `yuv420p10` 10-bit HDR pixel format
                                     - `yuv422p10` 10-bit HDR pixel format
                                     - `yuv444p10` 10-bit HDR pixel format
                                     - `yuv420p12` 12-bit HDR pixel format
                                     - `yuv422p12` 12-bit HDR pixel format
                                     - `yuv444p12` 12-bit HDR pixel format
                                  type: string
                                preset:
                                  description: Enforces the specified codec preset.
                                    The default is `veryfast`. The available options
                                    are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Preset).
                                    Note that certain values for this field may cause
                                    the transcoder to override other fields you set
                                    in the `H264CodecSettings` message.
                                  type: string
                                profile:
                                  description: |-
                                    Enforces the specified codec profile. The following profiles are
                                     supported:

                                     *   `baseline`
                                     *   `main`
                                     *   `high` (default)

                                     The available options are
                                     [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Tune).
                                     Note that certain values for this field may cause the
                                     transcoder to override other fields you set in the `H264CodecSettings`
                                     message.
                                  type: string
                                rateControlMode:
                                  description: |-
                                    Specify the mode. The default is `vbr`.

                                     Supported rate control modes:

                                     - `vbr` - variable bitrate
                                     - `crf` - constant rate factor
                                  type: string
                                sdr:
                                  description: Optional. SDR color format setting
                                    for H264.
                                  type: object
                                tune:
                                  description: Enforces the specified codec tune.
                                    The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Tune).
                                    Note that certain values for this field may cause
                                    the transcoder to override other fields you set
                                    in the `H264CodecSettings` message.
                                  type: string
                                vbvFullnessBits:
                                  description: Initial fullness of the Video Buffering
                                    Verifier (VBV) buffer in bits. Must be greater
                                    than zero. The default is equal to 90% of [H264CodecSettings.vbv_size_bits][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.vbv_size_bits].
                                  format: int32
                                  type: integer
                                vbvSizeBits:
                                  description: Size of the Video Buffering Verifier
                                    (VBV) buffer in bits. Must be greater than zero.
                                    The default is equal to [H264CodecSettings.bitrate_bps][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.bitrate_bps].
                                  format: int32
                                  type: integer
                                widthPixels:
                                  description: |-
                                    The width of the video in pixels. Must be an even integer.
                                     When not specified, the width is adjusted to match the specified height
                                     and input aspect ratio. If both are omitted, the input width is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the width, in pixels, per the horizontal ASR. The API calculates
                                     the height per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                              type: object
                            h265:
                              description: H265 codec settings.
                              properties:
                                allowOpenGop:
                                  description: Specifies whether an open Group of
                                    Pictures (GOP) structure should be allowed or
                                    not. The default is `false`.
                                  type: boolean
                                aqStrength:
                                  description: Specify the intensity of the adaptive
                                    quantizer (AQ). Must be between 0 and 1, where
                                    0 disables the quantizer and 1 maximizes the quantizer.
                                    A higher value equals a lower bitrate but smoother
                                    image. The default is 0.
                                  type: number
                                bFrameCount:
                                  description: The number of consecutive B-frames.
                                    Must be greater than or equal to zero. Must be
                                    less than [H265CodecSettings.gop_frame_count][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.gop_frame_count]
                                    if set. The default is 0.
                                  format: int32
                                  type: integer
                                bPyramid:
                                  description: Allow B-pyramid for reference frame
                                    selection. This may not be supported on all decoders.
                                    The default is `false`.
                                  type: boolean
                                bitrateBps:
                                  description: Required. The video bitrate in bits
                                    per second. The minimum value is 1,000. The maximum
                                    value is 800,000,000.
                                  format: int32
                                  type: integer
                                crfLevel:
                                  description: Target CRF level. Must be between 10
                                    and 36, where 10 is the highest quality and 36
                                    is the most efficient compression. The default
                                    is 21.
                                  format: int32
                                  type: integer
                                enableTwoPass:
                                  description: Use two-pass encoding strategy to achieve
                                    better video quality. [H265CodecSettings.rate_control_mode][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.rate_control_mode]
                                    must be `vbr`. The default is `false`.
                                  type: boolean
                                frameRate:
                                  description: Required. The target video frame rate
                                    in frames per second (FPS). Must be less than
                                    or equal to 120.
                                  type: number
                                frameRateConversionStrategy:
                                  description: Optional. Frame rate conversion strategy
                                    for desired frame rate. The default is `DOWNSAMPLE`.
                                  type: string
                                gopDuration:
                                  description: Select the GOP size based on the specified
                                    duration. The default is `3s`. Note that `gopDuration`
                                    must be less than or equal to [`segmentDuration`](#SegmentSettings),
                                    and [`segmentDuration`](#SegmentSettings) must
                                    be divisible by `gopDuration`.
                                  type: string
                                gopFrameCount:
                                  description: Select the GOP size based on the specified
                                    frame count. Must be greater than zero.
                                  format: int32
                                  type: integer
                                hdr10:
                                  description: Optional. HDR10 color format setting
                                    for H265.
                                  type: object
                                heightPixels:
                                  description: |-
                                    The height of the video in pixels. Must be an even integer.
                                     When not specified, the height is adjusted to match the specified width
                                     and input aspect ratio. If both are omitted, the input height is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the height, in pixels, per the horizontal ASR. The API calculates
                                     the width per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                                hlg:
                                  description: Optional. HLG color format setting
                                    for H265.
                                  type: object
                                pixelFormat:
                                  description: |-
                                    Pixel format to use. The default is `yuv420p`.

                                     Supported pixel formats:

                                     - `yuv420p` pixel format
                                     - `yuv422p` pixel format
                                     - `yuv444p` pixel format
                                     - `yuv420p10` 10-bit HDR pixel format
                                     - `yuv422p10` 10-bit HDR pixel format
                                     - `yuv444p10` 10-bit HDR pixel format
                                     - `yuv420p12` 12-bit HDR pixel format
                                     - `yuv422p12` 12-bit HDR pixel format
                                     - `yuv444p12` 12-bit HDR pixel format
                                  type: string
                                preset:
                                  description: Enforces the specified codec preset.
                                    The default is `veryfast`. The available options
                                    are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.265).
                                    Note that certain values for this field may cause
                                    the transcoder to override other fields you set
                                    in the `H265CodecSettings` message.
                                  type: string
                                profile:
                                  description: |-
                                    Enforces the specified codec profile. The following profiles are
                                     supported:

                                     *   8-bit profiles
                                         *   `main` (default)
                                         *   `main-intra`
                                         *   `mainstillpicture`
                                     *   10-bit profiles
                                         *   `main10` (default)
                                         *   `main10-intra`
                                         *   `main422-10`
                                         *   `main422-10-intra`
                                         *   `main444-10`
                                         *   `main444-10-intra`
                                     *   12-bit profiles
                                         *   `main12` (default)
                                         *   `main12-intra`
                                         *   `main422-12`
                                         *   `main422-12-intra`
                                         *   `main444-12`
                                         *   `main444-12-intra`

                                     The available options are
                                     [FFmpeg-compatible](https://x265.readthedocs.io/).
                                     Note that certain values for this field may cause the
                                     transcoder to override other fields you set in the `H265CodecSettings`
                                     message.
                                  type: string
                                rateControlMode:
                                  description: |-
                                    Specify the mode. The default is `vbr`.

                                     Supported rate control modes:

                                     - `vbr` - variable bitrate
                                     - `crf` - constant rate factor
                                  type: string
                                sdr:
                                  description: Optional. SDR color format setting
                                    for H265.
                                  type: object
                                tune:
                                  description: Enforces the specified codec tune.
                                    The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.265).
                                    Note that certain values for this field may cause
                                    the transcoder to override other fields you set
                                    in the `H265CodecSettings` message.
                                  type: string
                                vbvFullnessBits:
                                  description: Initial fullness of the Video Buffering
                                    Verifier (VBV) buffer in bits. Must be greater
                                    than zero. The default is equal to 90% of [H265CodecSettings.vbv_size_bits][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.vbv_size_bits].
                                  format: int32
                                  type: integer
                                vbvSizeBits:
                                  description: Size of the Video Buffering Verifier
                                    (VBV) buffer in bits. Must be greater than zero.
                                    The default is equal to `VideoStream.bitrate_bps`.
                                  format: int32
                                  type: integer
                                widthPixels:
                                  description: |-
                                    The width of the video in pixels. Must be an even integer.
                                     When not specified, the width is adjusted to match the specified height
                                     and input aspect ratio. If both are omitted, the input width is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the width, in pixels, per the horizontal ASR. The API calculates
                                     the height per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                              type: object
                            vp9:
                              description: VP9 codec settings.
                              properties:
                                bitrateBps:
                                  description: Required. The video bitrate in bits
                                    per second. The minimum value is 1,000. The maximum
                                    value is 480,000,000.
                                  format: int32
                                  type: integer
                                crfLevel:
                                  description: |-
                                    Target CRF level. Must be between 10 and 36, where 10 is the highest
                                     quality and 36 is the most efficient compression. The default is 21.

                                     **Note:** This field is not supported.
                                  format: int32
                                  type: integer
                                frameRate:
                                  description: Required. The target video frame rate
                                    in frames per second (FPS). Must be less than
                                    or equal to 120.
                                  type: number
                                frameRateConversionStrategy:
                                  description: Optional. Frame rate conversion strategy
                                    for desired frame rate. The default is `DOWNSAMPLE`.
                                  type: string
                                gopDuration:
                                  description: Select the GOP size based on the specified
                                    duration. The default is `3s`. Note that `gopDuration`
                                    must be less than or equal to [`segmentDuration`](#SegmentSettings),
                                    and [`segmentDuration`](#SegmentSettings) must
                                    be divisible by `gopDuration`.
                                  type: string
                                gopFrameCount:
                                  description: Select the GOP size based on the specified
                                    frame count. Must be greater than zero.
                                  format: int32
                                  type: integer
                                heightPixels:
                                  description: |-
                                    The height of the video in pixels. Must be an even integer.
                                     When not specified, the height is adjusted to match the specified width
                                     and input aspect ratio. If both are omitted, the input height is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the height, in pixels, per the horizontal ASR. The API calculates
                                     the width per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                                hlg:
                                  description: Optional. HLG color format setting
                                    for VP9.
                                  type: object
                                pixelFormat:
                                  description: |-
                                    Pixel format to use. The default is `yuv420p`.

                                     Supported pixel formats:

                                     - `yuv420p` pixel format
                                     - `yuv422p` pixel format
                                     - `yuv444p` pixel format
                                     - `yuv420p10` 10-bit HDR pixel format
                                     - `yuv422p10` 10-bit HDR pixel format
                                     - `yuv444p10` 10-bit HDR pixel format
                                     - `yuv420p12` 12-bit HDR pixel format
                                     - `yuv422p12` 12-bit HDR pixel format
                                     - `yuv444p12` 12-bit HDR pixel format
                                  type: string
                                profile:
                                  description: |-
                                    Enforces the specified codec profile. The following profiles are
                                     supported:

                                     *   `profile0` (default)
                                     *   `profile1`
                                     *   `profile2`
                                     *   `profile3`

                                     The available options are
                                     [WebM-compatible](https://www.webmproject.org/vp9/profiles/).
                                     Note that certain values for this field may cause the
                                     transcoder to override other fields you set in the `Vp9CodecSettings`
                                     message.
                                  type: string
                                rateControlMode:
                                  description: |-
                                    Specify the mode. The default is `vbr`.

                                     Supported rate control modes:

                                     - `vbr` - variable bitrate
                                  type: string
                                sdr:
                                  description: Optional. SDR color format setting
                                    for VP9.
                                  type: object
                                widthPixels:
                                  description: |-
                                    The width of the video in pixels. Must be an even integer.
                                     When not specified, the width is adjusted to match the specified height
                                     and input aspect ratio. If both are omitted, the input width is used.

                                     For portrait videos that contain horizontal ASR and rotation metadata,
                                     provide the width, in pixels, per the horizontal ASR. The API calculates
                                     the height per the horizontal ASR. The API detects any rotation metadata
                                     and swaps the requested height and width for the output.
                                  format: int32
                                  type: integer
                              type: object
                          type: object
                      type: object
                    type: array
                  encryptions:
                    description: List of encryption configurations for the content.
                      Each configuration has an ID. Specify this ID in the [MuxStream.encryption_id][google.cloud.video.transcoder.v1.MuxStream.encryption_id]
                      field to indicate the configuration to use for that `MuxStream`
                      output.
                    items:
                      properties:
                        aes128:
                          description: Configuration for AES-128 encryption.
                          type: object
                        drmSystems:
                          description: Required. DRM system(s) to use; at least one
                            must be specified. If a DRM system is omitted, it is considered
                            disabled.
                          properties:
                            clearkey:
                              description: Clearkey configuration.
                              type: object
                            fairplay:
                              description: Fairplay configuration.
                              type: object
                            playready:
                              description: Playready configuration.
                              type: object
                            widevine:
                              description: Widevine configuration.
                              type: object
                          type: object
                        id:
                          description: Required. Identifier for this set of encryption
                            options.
                          type: string
                        mpegCenc:
                          description: Configuration for MPEG Common Encryption (MPEG-CENC).
                          properties:
                            scheme:
                              description: |-
                                Required. Specify the encryption scheme.

                                 Supported encryption schemes:

                                 - `cenc`
                                 - `cbcs`
                              type: string
                          type: object
                        sampleAes:
                          description: Configuration for SAMPLE-AES encryption.
                          type: object
                        secretManagerKeySource:
                          description: Keys are stored in Google Secret Manager.
                          properties:
                            secretVersion:
                              description: |-
                                Required. The name of the Secret Version containing the encryption key in
                                 the following format:
                                 `projects/{project}/secrets/{secret_id}/versions/{version_number}`

                                 Note that only numbered versions are supported. Aliases like "latest" are
                                 not supported.
                              type: string
                          type: object
                      type: object
                    type: array
                  inputs:
                    description: List of input assets stored in Cloud Storage.
                    items:
                      properties:
                        attributes:
                          description: Optional. Input Attributes.
                          properties:
                            trackDefinitions:
                              description: Optional. A list of track definitions for
                                the input asset.
                              items:
                                properties:
                                  detectLanguages:
                                    description: Optional. Whether to automatically
                                      detect the languages present in the track. If
                                      true, the system will attempt to identify all
                                      the languages present in the track and populate
                                      the languages field.
                                    type: boolean
                                  inputTrack:
                                    description: The input track.
                                    format: int32
                                    type: integer
                                  languages:
                                    description: Optional. A list of languages spoken
                                      in the input asset, represented by a BCP 47
                                      language code, such as "en-US" or "sr-Latn".
                                      For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                          type: object
                        key:
                          description: A unique key for this input. Must be specified
                            when using advanced mapping and edit lists.
                          type: string
                        preprocessingConfig:
                          description: Preprocessing configurations.
                          properties:
                            audio:
                              description: Audio preprocessing configuration.
                              properties:
                                highBoost:
                                  description: |-
                                    Enable boosting high frequency components. The default is `false`.

                                     **Note:** This field is not supported.
                                  type: boolean
                                lowBoost:
                                  description: |-
                                    Enable boosting low frequency components. The default is `false`.

                                     **Note:** This field is not supported.
                                  type: boolean
                                lufs:
                                  description: |-
                                    Specify audio loudness normalization in loudness units relative to full
                                     scale (LUFS). Enter a value between -24 and 0 (the default), where:

                                     *   -24 is the Advanced Television Systems Committee (ATSC A/85) standard
                                     *   -23 is the EU R128 broadcast standard
                                     *   -19 is the prior standard for online mono audio
                                     *   -18 is the ReplayGain standard
                                     *   -16 is the prior standard for stereo audio
                                     *   -14 is the new online audio standard recommended by Spotify, as well
                                         as Amazon Echo
                                     *   0 disables normalization
                                  type: number
                              type: object
                            color:
                              description: Color preprocessing configuration.
                              properties:
                                brightness:
                                  description: Control brightness of the video. Enter
                                    a value between -1 and 1, where -1 is minimum
                                    brightness and 1 is maximum brightness. 0 is no
                                    change. The default is 0.
                                  type: number
                                contrast:
                                  description: Control black and white contrast of
                                    the video. Enter a value between -1 and 1, where
                                    -1 is minimum contrast and 1 is maximum contrast.
                                    0 is no change. The default is 0.
                                  type: number
                                saturation:
                                  description: Control color saturation of the video.
                                    Enter a value between -1 and 1, where -1 is fully
                                    desaturated and 1 is maximum saturation. 0 is
                                    no change. The default is 0.
                                  type: number
                              type: object
                            crop:
                              description: Specify the video cropping configuration.
                              properties:
                                bottomPixels:
                                  description: The number of pixels to crop from the
                                    bottom. The default is 0.
                                  format: int32
                                  type: integer
                                leftPixels:
                                  description: The number of pixels to crop from the
                                    left. The default is 0.
                                  format: int32
                                  type: integer
                                rightPixels:
                                  description: The number of pixels to crop from the
                                    right. The default is 0.
                                  format: int32
                                  type: integer
                                topPixels:
                                  description: The number of pixels to crop from the
                                    top. The default is 0.
                                  format: int32
                                  type: integer
                              type: object
                            deblock:
                              description: Deblock preprocessing configuration.
                              properties:
                                enabled:
                                  description: Enable deblocker. The default is `false`.
                                  type: boolean
                                strength:
                                  description: Set strength of the deblocker. Enter
                                    a value between 0 and 1. The higher the value,
                                    the stronger the block removal. 0 is no deblocking.
                                    The default is 0.
                                  type: number
                              type: object
                            deinterlace:
                              description: Specify the video deinterlace configuration.
                              properties:
                                bwdif:
                                  description: Specifies the Bob Weaver Deinterlacing
                                    Filter Configuration.
                                  properties:
                                    deinterlaceAllFrames:
                                      description: Deinterlace all frames rather than
                                        just the frames identified as interlaced.
                                        The default is `false`.
                                      type: boolean
                                    mode:
                                      description: |-
                                        Specifies the deinterlacing mode to adopt.
                                         The default is `send_frame`.
                                         Supported values:

                                         - `send_frame`: Output one frame for each frame
                                         - `send_field`: Output one frame for each field
                                      type: string
                                    parity:
                                      description: |-
                                        The picture field parity assumed for the input interlaced video.
                                         The default is `auto`.
                                         Supported values:

                                         - `tff`: Assume the top field is first
                                         - `bff`: Assume the bottom field is first
                                         - `auto`: Enable automatic detection of field parity
                                      type: string
                                  type: object
                                yadif:
                                  description: Specifies the Yet Another Deinterlacing
                                    Filter Configuration.
                                  properties:
                                    deinterlaceAllFrames:
                                      description: Deinterlace all frames rather than
                                        just the frames identified as interlaced.
                                        The default is `false`.
                                      type: boolean
                                    disableSpatialInterlacing:
                                      description: Disable spacial interlacing. The
                                        default is `false`.
                                      type: boolean
                                    mode:
                                      description: |-
                                        Specifies the deinterlacing mode to adopt.
                                         The default is `send_frame`.
                                         Supported values:

                                         - `send_frame`: Output one frame for each frame
                                         - `send_field`: Output one frame for each field
                                      type: string
                                    parity:
                                      description: |-
                                        The picture field parity assumed for the input interlaced video.
                                         The default is `auto`.
                                         Supported values:

                                         - `tff`: Assume the top field is first
                                         - `bff`: Assume the bottom field is first
                                         - `auto`: Enable automatic detection of field parity
                                      type: string
                                  type: object
                              type: object
                            denoise:
                              description: Denoise preprocessing configuration.
                              properties:
                                strength:
                                  description: Set strength of the denoise. Enter
                                    a value between 0 and 1. The higher the value,
                                    the smoother the image. 0 is no denoising. The
                                    default is 0.
                                  type: number
                                tune:
                                  description: |-
                                    Set the denoiser mode. The default is `standard`.

                                     Supported denoiser modes:

                                     - `standard`
                                     - `grain`
                                  type: string
                              type: object
                            pad:
                              description: Specify the video pad filter configuration.
                              properties:
                                bottomPixels:
                                  description: The number of pixels to add to the
                                    bottom. The default is 0.
                                  format: int32
                                  type: integer
                                leftPixels:
                                  description: The number of pixels to add to the
                                    left. The default is 0.
                                  format: int32
                                  type: integer
                                rightPixels:
                                  description: The number of pixels to add to the
                                    right. The default is 0.
                                  format: int32
                                  type: integer
                                topPixels:
                                  description: The number of pixels to add to the
                                    top. The default is 0.
                                  format: int32
                                  type: integer
                              type: object
                          type: object
                        uri:
                          description: URI of the media. Input files must be at least
                            5 seconds in duration and stored in Cloud Storage (for
                            example, `gs://bucket/inputs/file.mp4`). If empty, the
                            value is populated from [Job.input_uri][google.cloud.video.transcoder.v1.Job.input_uri].
                            See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats).
                          type: string
                      type: object
                    type: array
                  manifests:
                    description: List of output manifests.
                    items:
                      properties:
                        dash:
                          description: '`DASH` manifest configuration.'
                          properties:
                            segmentReferenceScheme:
                              description: The segment reference scheme for a `DASH`
                                manifest. The default is `SEGMENT_LIST`.
                              type: string
                          type: object
                        fileName:
                          description: The name of the generated file. The default
                            is `manifest` with the extension suffix corresponding
                            to the [Manifest.type][google.cloud.video.transcoder.v1.Manifest.type].
                          type: string
                        muxStreams:
                          description: |-
                            Required. List of user supplied
                             [MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] values that
                             should appear in this manifest.

                             When [Manifest.type][google.cloud.video.transcoder.v1.Manifest.type] is
                             `HLS`, a media manifest with name
                             [MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] and `.m3u8`
                             extension is generated for each element in this list.
                          items:
                            type: string
                          type: array
                        type:
                          description: Required. Type of the manifest.
                          type: string
                      type: object
                    type: array
                  muxStreams:
                    description: List of multiplexing settings for output streams.
                    items:
                      properties:
                        container:
                          description: |-
                            The container format. The default is `mp4`

                             Supported streaming formats:

                             - `ts`
                             - `fmp4`- the corresponding file extension is `.m4s`

                             Supported standalone file formats:

                             - `mp4`
                             - `mp3`
                             - `ogg`
                             - `vtt`

                             See also:
                             [Supported input and output
                             formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats)
                          type: string
                        elementaryStreams:
                          description: List of [ElementaryStream.key][google.cloud.video.transcoder.v1.ElementaryStream.key]
                            values multiplexed in this stream.
                          items:
                            type: string
                          type: array
                        encryptionID:
                          description: Identifier of the encryption configuration
                            to use. If omitted, output will be unencrypted.
                          type: string
                        fileName:
                          description: |-
                            The name of the generated file. The default is
                             [MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] with the
                             extension suffix corresponding to the
                             [MuxStream.container][google.cloud.video.transcoder.v1.MuxStream.container].

                             Individual segments also have an incremental 10-digit zero-padded suffix
                             starting from 0 before the extension, such as `mux_stream0000000123.ts`.
                          type: string
                        fmp4:
                          description: Optional. `fmp4` container configuration.
                          properties:
                            codecTag:
                              description: |-
                                Optional. Specify the codec tag string that will be used in the media
                                 bitstream. When not specified, the codec appropriate value is used.

                                 Supported H265 codec tags:

                                 - `hvc1` (default)
                                 - `hev1`
                              type: string
                          type: object
                        key:
                          description: A unique key for this multiplexed stream.
                          type: string
                        segmentSettings:
                          description: Segment settings for `ts`, `fmp4` and `vtt`.
                          properties:
                            individualSegments:
                              description: Required. Create an individual segment
                                file. The default is `false`.
                              type: boolean
                            segmentDuration:
                              description: Duration of the segments in seconds. The
                                default is `6.0s`. Note that `segmentDuration` must
                                be greater than or equal to [`gopDuration`](#videostream),
                                and `segmentDuration` must be divisible by [`gopDuration`](#videostream).
                              type: string
                          type: object
                      type: object
                    type: array
                  output:
                    description: Output configuration.
                    properties:
                      uri:
                        description: URI for the output file(s). For example, `gs://my-bucket/outputs/`.
                          Must be a directory and not a top-level bucket. If empty,
                          the value is populated from [Job.output_uri][google.cloud.video.transcoder.v1.Job.output_uri].
                          See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats).
                        type: string
                    type: object
                  overlays:
                    description: List of overlays on the output video, in descending
                      Z-order.
                    items:
                      properties:
                        animations:
                          description: List of animations. The list should be chronological,
                            without any time overlap.
                          items:
                            properties:
                              animationEnd:
                                description: End previous animation.
                                properties:
                                  startTimeOffset:
                                    description: 'The time to end overlay object,
                                      in seconds. Default: 0'
                                    type: string
                                type: object
                              animationFade:
                                description: Display overlay object with fade animation.
                                properties:
                                  endTimeOffset:
                                    description: 'The time to end the fade animation,
                                      in seconds. Default: `start_time_offset` + 1s'
                                    type: string
                                  fadeType:
                                    description: 'Required. Type of fade animation:
                                      `FADE_IN` or `FADE_OUT`.'
                                    type: string
                                  startTimeOffset:
                                    description: 'The time to start the fade animation,
                                      in seconds. Default: 0'
                                    type: string
                                  xy:
                                    description: 'Normalized coordinates based on
                                      output video resolution. Valid values: `0.0`–`1.0`.
                                      `xy` is the upper-left coordinate of the overlay
                                      object. For example, use the x and y coordinates
                                      {0,0} to position the top-left corner of the
                                      overlay animation in the top-left corner of
                                      the output video.'
                                    properties:
                                      x:
                                        description: Normalized x coordinate.
                                        type: number
                                      "y":
                                        description: Normalized y coordinate.
                                        type: number
                                    type: object
                                type: object
                              animationStatic:
                                description: Display static overlay object.
                                properties:
                                  startTimeOffset:
                                    description: 'The time to start displaying the
                                      overlay object, in seconds. Default: 0'
                                    type: string
                                  xy:
                                    description: 'Normalized coordinates based on
                                      output video resolution. Valid values: `0.0`–`1.0`.
                                      `xy` is the upper-left coordinate of the overlay
                                      object. For example, use the x and y coordinates
                                      {0,0} to position the top-left corner of the
                                      overlay animation in the top-left corner of
                                      the output video.'
                                    properties:
                                      x:
                                        description: Normalized x coordinate.
                                        type: number
                                      "y":
                                        description: Normalized y coordinate.
                                        type: number
                                    type: object
                                type: object
                            type: object
                          type: array
                        image:
                          description: Image overlay.
                          properties:
                            alpha:
                              description: Target image opacity. Valid values are
                                from  `1.0` (solid, default) to `0.0` (transparent),
                                exclusive. Set this to a value greater than `0.0`.
                              type: number
                            resolution:
                              description: 'Normalized image resolution, based on
                                output video resolution. Valid values: `0.0`–`1.0`.
                                To respect the original image aspect ratio, set either
                                `x` or `y` to `0.0`. To use the original image resolution,
                                set both `x` and `y` to `0.0`.'
                              properties:
                                x:
                                  description: Normalized x coordinate.
                                  type: number
                                "y":
                                  description: Normalized y coordinate.
                                  type: number
                              type: object
                            uri:
                              description: Required. URI of the image in Cloud Storage.
                                For example, `gs://bucket/inputs/image.png`. Only
                                PNG and JPEG images are supported.
                              type: string
                          type: object
                      type: object
                    type: array
                  pubsubDestination:
                    description: Destination on Pub/Sub.
                    properties:
                      topicRef:
                        description: TopicRef is a reference to a PubSubTopic.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed PubSubTopic
                              resource. Should be in the format "projects/{{projectID}}/topics/{{topicID}}".
                            type: string
                          name:
                            description: The name of a PubSubTopic resource.
                            type: string
                          namespace:
                            description: The namespace of a PubSubTopic resource.
                            type: string
                        type: object
                    type: object
                  spriteSheets:
                    description: List of output sprite sheets. Spritesheets require
                      at least one VideoStream in the Jobconfig.
                    items:
                      properties:
                        columnCount:
                          description: The maximum number of sprites per row in a
                            sprite sheet. The default is 0, which indicates no maximum
                            limit.
                          format: int32
                          type: integer
                        endTimeOffset:
                          description: End time in seconds, relative to the output
                            file timeline. When `end_time_offset` is not specified,
                            the sprites are generated until the end of the output
                            file.
                          type: string
                        filePrefix:
                          description: |-
                            Required. File name prefix for the generated sprite sheets.

                             Each sprite sheet has an incremental 10-digit zero-padded suffix starting
                             from 0 before the extension, such as `sprite_sheet0000000123.jpeg`.
                          type: string
                        format:
                          description: |-
                            Format type. The default is `jpeg`.

                             Supported formats:

                             - `jpeg`
                          type: string
                        interval:
                          description: Starting from `0s`, create sprites at regular
                            intervals. Specify the interval value in seconds.
                          type: string
                        quality:
                          description: The quality of the generated sprite sheet.
                            Enter a value between 1 and 100, where 1 is the lowest
                            quality and 100 is the highest quality. The default is
                            100. A high quality value corresponds to a low image data
                            compression ratio.
                          format: int32
                          type: integer
                        rowCount:
                          description: The maximum number of rows per sprite sheet.
                            When the sprite sheet is full, a new sprite sheet is created.
                            The default is 0, which indicates no maximum limit.
                          format: int32
                          type: integer
                        spriteHeightPixels:
                          description: |-
                            Required. The height of sprite in pixels. Must be an even integer. To
                             preserve the source aspect ratio, set the
                             [SpriteSheet.sprite_height_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_height_pixels]
                             field or the
                             [SpriteSheet.sprite_width_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_width_pixels]
                             field, but not both (the API will automatically calculate the missing
                             field).

                             For portrait videos that contain horizontal ASR and rotation metadata,
                             provide the height, in pixels, per the horizontal ASR. The API calculates
                             the width per the horizontal ASR. The API detects any rotation metadata
                             and swaps the requested height and width for the output.
                          format: int32
                          type: integer
                        spriteWidthPixels:
                          description: |-
                            Required. The width of sprite in pixels. Must be an even integer. To
                             preserve the source aspect ratio, set the
                             [SpriteSheet.sprite_width_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_width_pixels]
                             field or the
                             [SpriteSheet.sprite_height_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_height_pixels]
                             field, but not both (the API will automatically calculate the missing
                             field).

                             For portrait videos that contain horizontal ASR and rotation metadata,
                             provide the width, in pixels, per the horizontal ASR. The API calculates
                             the height per the horizontal ASR. The API detects any rotation metadata
                             and swaps the requested height and width for the output.
                          format: int32
                          type: integer
                        startTimeOffset:
                          description: Start time in seconds, relative to the output
                            file timeline. Determines the first sprite to pick. The
                            default is `0s`.
                          type: string
                        totalCount:
                          description: Total number of sprites. Create the specified
                            number of sprites distributed evenly across the timeline
                            of the output media. The default is 100.
                          format: int32
                          type: integer
                      type: object
                    type: array
                type: object
              fillContentGaps:
                description: Optional. Insert silence and duplicate frames when timestamp
                  gaps are detected in a given stream.
                type: boolean
              inputURI:
                description: Input only. Specify the `input_uri` to populate empty
                  `uri` fields in each element of `Job.config.inputs` or `JobTemplate.config.inputs`
                  when using template. URI of the media. Input files must be at least
                  5 seconds in duration and stored in Cloud Storage (for example,
                  `gs://bucket/inputs/file.mp4`). See [Supported input and output
                  formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats).
                type: string
              location:
                description: The location of this resource.
                type: string
              mode:
                description: The processing mode of the job. The default is `PROCESSING_MODE_INTERACTIVE`.
                type: string
              optimization:
                description: Optional. The optimization strategy of the job. The default
                  is `AUTODETECT`.
                type: string
              outputURI:
                description: Input only. Specify the `output_uri` to populate an empty
                  `Job.config.output.uri` or `JobTemplate.config.output.uri` when
                  using template. URI for the output file(s). For example, `gs://my-bucket/outputs/`.
                  See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats).
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The TranscoderJob name. If not given, the metadata.name
                  will be used.
                type: string
              templateID:
                description: |-
                  Input only. Specify the `template_id` to use for populating `Job.config`.
                  The default is `preset/web-hd`, which is the only supported preset.

                  User defined JobTemplate: `{job_template_id}`
                type: string
              ttlAfterCompletionDays:
                description: Job time to live value in days, which will be effective
                  after job completion. Job should be deleted automatically after
                  the given TTL. Enter a value between 1 and 90. The default is 30.
                format: int32
                type: integer
            required:
            - location
            - projectRef
            type: object
          status:
            description: TranscoderJobStatus defines the config connector machine
              state of TranscoderJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the TranscoderJob resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  config:
                    description: The configuration for this job.
                    properties:
                      inputs:
                        description: List of input assets stored in Cloud Storage.
                        items:
                          properties:
                            attributes:
                              description: Optional. Input Attributes.
                              properties:
                                trackDefinitions:
                                  description: Optional. A list of track definitions
                                    for the input asset.
                                  items:
                                    properties:
                                      detectedLanguages:
                                        description: Output only. A list of languages
                                          detected in the input asset, represented
                                          by a BCP 47 language code, such as "en-US"
                                          or "sr-Latn". For more information, see
                                          https://www.unicode.org/reports/tr35/#Unicode_locale_identifier.
                                          This field is only populated if the detect_languages
                                          field is set to true.
                                        items:
                                          type: string
                                        type: array
                                    type: object
                                  type: array
                              type: object
                          type: object
                        type: array
                    type: object
                  createTime:
                    description: Output only. The time the job was created.
                    type: string
                  endTime:
                    description: Output only. The time the transcoding finished.
                    type: string
                  error:
                    description: Output only. An error object that describes the reason
                      for the failure. This property is always present when [ProcessingState][google.cloud.video.transcoder.v1.Job.ProcessingState]
                      is `FAILED`.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  startTime:
                    description: Output only. The time the transcoding started.
                    type: string
                  state:
                    description: Output only. The current state of the job.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
