# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: lustreinstances.lustre.cnrm.cloud.google.com
spec:
  group: lustre.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: LustreInstance
    listKind: LustreInstanceList
    plural: lustreinstances
    shortNames:
    - gcplustreinstance
    - gcplustreinstances
    singular: lustreinstance
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LustreInstance is the Schema for the LustreInstance API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LustreInstanceSpec defines the desired state of LustreInstance
            properties:
              accessRulesOptions:
                description: Optional. IP-based access rules and squash configuration
                  for the instance.
                properties:
                  accessRules:
                    description: Optional. The access rules for the instance.
                    items:
                      description: AccessRule defines a single policy group with IP-based
                        access rules for the Managed Lustre instance.
                      properties:
                        ipAddressRanges:
                          description: Required. The IP address ranges to which to
                            apply this access rule. Accepts non-overlapping CIDR ranges
                            (e.g., `192.168.1.0/24`) and IP addresses (e.g., `192.168.1.0`).
                          items:
                            type: string
                          type: array
                        name:
                          description: Required. The name of the access rule policy
                            group. Must be 16 characters or less and include only
                            alphanumeric characters or '_'.
                          maxLength: 16
                          pattern: ^[a-zA-Z0-9_]+$
                          type: string
                        squashMode:
                          description: 'Required. Squash mode for the access rule.
                            Possible values are: - `NO_SQUASH`: Squash is disabled.
                            Root users matching the ip_ranges are not squashed.'
                          enum:
                          - NO_SQUASH
                          type: string
                      type: object
                    type: array
                  defaultSquashGid:
                    description: Optional. The user squash GID for the default access
                      rule. This user squash GID applies to all root users connecting
                      from clients that are not matched by any of the access rules.
                      If not set, the default is 0 (no GID squash).
                    format: int32
                    type: integer
                  defaultSquashMode:
                    description: 'Required. The squash mode for the default access
                      rule. Possible values are: - `NO_SQUASH`: Root squash is disabled
                      for this instance. - `ROOT_SQUASH`: Root user squash is enabled.
                      Root users not matching any of the access_rules are squashed
                      to defaultSquashUid and defaultSquashGid.'
                    enum:
                    - NO_SQUASH
                    - ROOT_SQUASH
                    type: string
                  defaultSquashUid:
                    description: Optional. The user squash UID for the default access
                      rule. This user squash UID applies to all root users connecting
                      from clients that are not matched by any of the access rules.
                      If not set, the default is 0 (no UID squash).
                    format: int32
                    type: integer
                type: object
              capacityGib:
                description: Required. The storage capacity of the instance in gibibytes
                  (GiB). Allowed values are multiples of 36000, up to 6120000.
                format: int64
                type: integer
              description:
                description: Optional. A user-readable description of the instance.
                type: string
              filesystem:
                description: Required. Immutable. The filesystem name for this instance.
                  This name is used by client-side tools, including when mounting
                  the instance. Must be eight characters or less and can only contain
                  letters and numbers.
                type: string
              location:
                description: The location of this resource.
                type: string
              networkRef:
                description: Required. Immutable. The VPC network to which the instance
                  is connected.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed ComputeNetwork
                      resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                    type: string
                  name:
                    description: The name of a ComputeNetwork resource.
                    type: string
                  namespace:
                    description: The namespace of a ComputeNetwork resource.
                    type: string
                type: object
              perUnitStorageThroughput:
                description: Required. The throughput of the instance in MB/s/TiB.
                  Valid values are 125, 250, 500, 1000.
                format: int64
                type: integer
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The LustreInstance name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - location
            - projectRef
            type: object
          status:
            description: LustreInstanceStatus defines the config connector machine
              state of LustreInstance
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the LustreInstance resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  accessRulesOptions:
                    description: Output only. IP-based access rules and squash configuration
                      for the instance as observed in GCP.
                    properties:
                      accessRules:
                        description: Optional. The access rules for the instance.
                        items:
                          description: AccessRule defines a single policy group with
                            IP-based access rules for the Managed Lustre instance.
                          properties:
                            ipAddressRanges:
                              description: Required. The IP address ranges to which
                                to apply this access rule. Accepts non-overlapping
                                CIDR ranges (e.g., `192.168.1.0/24`) and IP addresses
                                (e.g., `192.168.1.0`).
                              items:
                                type: string
                              type: array
                            name:
                              description: Required. The name of the access rule policy
                                group. Must be 16 characters or less and include only
                                alphanumeric characters or '_'.
                              maxLength: 16
                              pattern: ^[a-zA-Z0-9_]+$
                              type: string
                            squashMode:
                              description: 'Required. Squash mode for the access rule.
                                Possible values are: - `NO_SQUASH`: Squash is disabled.
                                Root users matching the ip_ranges are not squashed.'
                              enum:
                              - NO_SQUASH
                              type: string
                          type: object
                        type: array
                      defaultSquashGid:
                        description: Optional. The user squash GID for the default
                          access rule. This user squash GID applies to all root users
                          connecting from clients that are not matched by any of the
                          access rules. If not set, the default is 0 (no GID squash).
                        format: int32
                        type: integer
                      defaultSquashMode:
                        description: 'Required. The squash mode for the default access
                          rule. Possible values are: - `NO_SQUASH`: Root squash is
                          disabled for this instance. - `ROOT_SQUASH`: Root user squash
                          is enabled. Root users not matching any of the access_rules
                          are squashed to defaultSquashUid and defaultSquashGid.'
                        enum:
                        - NO_SQUASH
                        - ROOT_SQUASH
                        type: string
                      defaultSquashUid:
                        description: Optional. The user squash UID for the default
                          access rule. This user squash UID applies to all root users
                          connecting from clients that are not matched by any of the
                          access rules. If not set, the default is 0 (no UID squash).
                        format: int32
                        type: integer
                    type: object
                  createTime:
                    description: Output only. Timestamp when the instance was created.
                    type: string
                  mountPoint:
                    description: Output only. Mount point of the instance in the format
                      `IP_ADDRESS@tcp:/FILESYSTEM`.
                    type: string
                  state:
                    description: Output only. The state of the instance.
                    type: string
                  updateTime:
                    description: Output only. Timestamp when the instance was last
                      updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
