# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: blockchainnodeengineblockchainnodes.blockchainnodeengine.cnrm.cloud.google.com
spec:
  group: blockchainnodeengine.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: BlockchainNodeEngineBlockchainNode
    listKind: BlockchainNodeEngineBlockchainNodeList
    plural: blockchainnodeengineblockchainnodes
    shortNames:
    - gcpblockchainnodeengineblockchainnode
    - gcpblockchainnodeengineblockchainnodes
    singular: blockchainnodeengineblockchainnode
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlockchainNodeEngineBlockchainNode is the Schema for the BlockchainNodeEngineBlockchainNode
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BlockchainNodeEngineBlockchainNodeSpec defines the desired
              state of BlockchainNodeEngineBlockchainNode
            properties:
              blockchainType:
                description: Immutable. The blockchain type of the node.
                enum:
                - ETHEREUM
                type: string
              ethereumDetails:
                description: Ethereum-specific blockchain node details.
                properties:
                  apiEnableAdmin:
                    description: Immutable. Enables JSON-RPC access to functions in
                      the `admin` namespace. Defaults to `false`.
                    type: boolean
                  apiEnableDebug:
                    description: Immutable. Enables JSON-RPC access to functions in
                      the `debug` namespace. Defaults to `false`.
                    type: boolean
                  consensusClient:
                    description: Immutable. The consensus client.
                    type: string
                  executionClient:
                    description: Immutable. The execution client
                    type: string
                  gethDetails:
                    description: Details for the Geth execution client.
                    properties:
                      garbageCollectionMode:
                        description: Immutable. Blockchain garbage collection mode.
                        type: string
                    type: object
                  network:
                    description: Immutable. The Ethereum environment being accessed.
                    type: string
                  nodeType:
                    description: Immutable. The type of Ethereum node.
                    type: string
                  validatorConfig:
                    description: Configuration for validator-related parameters on
                      the beacon client, and for any GCP-managed validator client.
                    properties:
                      beaconFeeRecipient:
                        description: |-
                          An Ethereum address which the beacon client will send fee rewards to if
                           no recipient is configured in the validator client.

                           See https://lighthouse-book.sigmaprime.io/suggested-fee-recipient.html
                           or https://docs.prylabs.network/docs/execution-node/fee-recipient for
                           examples of how this is used.

                           Note that while this is often described as "suggested", as we run the
                           execution node we can trust the execution node, and therefore this is
                           considered enforced.
                        type: string
                      managedValidatorClient:
                        description: Immutable. When true, deploys a GCP-managed validator
                          client alongside the beacon client.
                        type: boolean
                      mevRelayUrls:
                        description: URLs for MEV-relay services to use for block
                          building. When set, a GCP-managed MEV-boost service is configured
                          on the beacon client.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: User-provided key-value pairs.
                type: object
              location:
                description: Immutable. The location of this resource.
                type: string
              privateServiceConnectEnabled:
                description: 'Optional. When true, the node is only accessible via
                  Private Service Connect; no public endpoints are exposed. Otherwise,
                  the node is only accessible via public endpoints. Warning: These
                  nodes are deprecated, please use public endpoints instead.'
                type: boolean
              projectRef:
                description: Immutable. The Project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: Immutable. Optional. The BlockchainNode name. If not
                  given, the metadata.name will be used.
                type: string
            required:
            - location
            - projectRef
            type: object
          status:
            description: BlockchainNodeEngineBlockchainNodeStatus defines the config
              connector machine state of BlockchainNodeEngineBlockchainNode
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the BlockchainNodeEngineBlockchainNode
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  connectionInfo:
                    description: Output only. The connection information used to interact
                      with a blockchain node.
                    type: object
                  createTime:
                    description: Output only. The timestamp at which the blockchain
                      node was first created.
                    type: string
                  ethereumDetails:
                    description: Ethereum-specific blockchain node details.
                    properties:
                      additionalEndpoints:
                        description: Output only. Ethereum-specific endpoint information.
                        type: object
                    type: object
                  name:
                    description: Output only. The fully qualified name of the blockchain
                      node. e.g. `projects/my-project/locations/us-central1/blockchainNodes/my-node`.
                    type: string
                  state:
                    description: Output only. A status representing the state of the
                      node.
                    type: string
                  updateTime:
                    description: Output only. The timestamp at which the blockchain
                      node was last updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
