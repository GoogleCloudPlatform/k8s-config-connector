# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaicustomjobs.vertexai.cnrm.cloud.google.com
spec:
  group: vertexai.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAICustomJob
    listKind: VertexAICustomJobList
    plural: vertexaicustomjobs
    shortNames:
    - gcpvertexaicustomjob
    - gcpvertexaicustomjobs
    singular: vertexaicustomjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAICustomJob is the Schema for the VertexAICustomJob API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAICustomJobSpec defines the desired state of VertexAICustomJob
            properties:
              displayName:
                description: Required. The display name of the CustomJob. The name
                  can be up to 128 characters long and can consist of any UTF-8 characters.
                type: string
              encryptionSpec:
                description: Optional. Customer-managed encryption key options for
                  a CustomJob.
                properties:
                  kmsKeyRef:
                    description: Required. The Cloud KMS resource identifier of the
                      customer managed encryption key used to protect a resource.
                      The key needs to be in the same region as where the compute
                      resource is created.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/keyRings/{{keyring}}/cryptoKeys/{{cryptokey}}".
                        type: string
                      name:
                        description: The name of a KMSCryptoKey resource.
                        type: string
                      namespace:
                        description: The namespace of a KMSCryptoKey resource.
                        type: string
                    type: object
                required:
                - kmsKeyRef
                type: object
              jobSpec:
                description: Required. Job spec.
                properties:
                  baseOutputDirectory:
                    description: |-
                      The Cloud Storage location to store the output of this CustomJob or
                       PipelineJob in.

                       In most cases, this location is because Vertex AI associates a
                       Google Cloud Storage bucket with every pipeline run in order to store
                       artifacts. However, some pipeline components may override this value.

                       If other output destination is set, self-link will be returned,
                       and base_output_directory value will be ignored.

                       API users can configure AIP_BASE_OUTPUT_DIRECTORY to obtain this value.
                       Additionally, Vertex AI will map the environment variables:

                         * AIP_MODEL_DIR = `<base_output_directory>/<trial_id>/model/`
                         * AIP_CHECKPOINT_DIR = `<base_output_directory>/<trial_id>/checkpoints/`
                         * AIP_TENSORBOARD_LOG_DIR = `<base_output_directory>/<trial_id>/logs/`
                    properties:
                      outputURIPrefix:
                        description: Required. Google Cloud Storage URI to output
                          directory. If the uri doesn't end with '/', a '/' will be
                          automatically appended. The directory is created if it doesn't
                          exist.
                        type: string
                    type: object
                  enableDashboardAccess:
                    description: |-
                      Optional. Whether you want Vertex AI to enable access to the customized
                       dashboard in training chief container.

                       If set to `true`, you can access the dashboard at the URIs given
                       by [CustomJob.web_access_uris][google.cloud.aiplatform.v1beta1.CustomJob.web_access_uris] or
                       [Trial.web_access_uris][google.cloud.aiplatform.v1beta1.Trial.web_access_uris] (within
                       [HyperparameterTuningJob.trials][google.cloud.aiplatform.v1beta1.HyperparameterTuningJob.trials]).
                    type: boolean
                  enableWebAccess:
                    description: |-
                      Optional. Whether you want Vertex AI to enable [interactive shell
                       access](https://cloud.google.com/vertex-ai/docs/training/monitor-debug-interactive-shell)
                       for custom training.

                       If set to `true`, you can access interactive shells at the URIs given
                       by [CustomJob.web_access_uris][google.cloud.aiplatform.v1beta1.CustomJob.web_access_uris] or
                       [Trial.web_access_uris][google.cloud.aiplatform.v1beta1.Trial.web_access_uris] (within
                       [HyperparameterTuningJob.trials][google.cloud.aiplatform.v1beta1.HyperparameterTuningJob.trials]).
                    type: boolean
                  experimentRef:
                    description: 'Optional. The Experiment associated with this job.
                      Format: `projects/{project}/locations/{location}/metadataStores/{metadataStores}/contexts/{experiment-name}`'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed VertexAIExperiment
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/metadataStores/{{metadataStore}}/contexts/{{experimentID}}".
                        type: string
                      name:
                        description: The name of a VertexAIExperiment resource.
                        type: string
                      namespace:
                        description: The namespace of a VertexAIExperiment resource.
                        type: string
                    type: object
                  experimentRunRef:
                    description: 'Optional. The Experiment Run associated with this
                      job. Format: `projects/{project}/locations/{location}/metadataStores/{metadataStores}/contexts/{experiment-name}-{experiment-run-name}`'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed VertexAIExperimentRun
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/metadataStores/{{metadataStore}}/contexts/{{experimentRunID}}".
                        type: string
                      name:
                        description: The name of a VertexAIExperimentRun resource.
                        type: string
                      namespace:
                        description: The namespace of a VertexAIExperimentRun resource.
                        type: string
                    type: object
                  modelRefs:
                    description: |-
                      Optional. The name of the Model resources for which to generate a mapping
                       to the trial name formats. Model names should be of the form:
                       `projects/{project}/locations/{location}/models/{model}`

                       To specify this field, you must have already [configured Model Registry
                       integration for Vertex
                       AI](https://cloud.google.com/vertex-ai/docs/model-registry/model-registry-integration).

                       At most 50 model names can be specified. For each specified model, its
                       version alias is created for the custom job. The alias name format is
                       `customjob-{job_id}`. The model version alias is deleted when the custom
                       job is deleted. There will be at most one version alias for each model
                       returned. The "default" version alias is created for the first version of
                       the model, and can be moved to other versions later on. There will be
                       exactly one default version.
                    items:
                      description: AIPlatformModelRef is a reference to an AIPlatformModel.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed AIPlatformModel
                            resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/models/{{modelID}}".
                          type: string
                        name:
                          description: The name of an AIPlatformModel resource.
                          type: string
                        namespace:
                          description: The namespace of an AIPlatformModel resource.
                          type: string
                      type: object
                    type: array
                  networkRef:
                    description: |-
                      Optional. The full name of the Compute Engine
                       [network](/compute/docs/networks-and-firewalls#networks) to which the Job
                       should be peered. For example, `projects/12345/global/networks/myVPC`.
                       [Format](/compute/docs/reference/rest/v1/networks/insert)
                       is of the form `projects/{project}/global/networks/{network}`.
                       Where {project} is a project number, as in `12345`, and {network} is a
                       network name.

                       To specify this field, you must have already [configured VPC Network
                       Peering for Vertex
                       AI](https://cloud.google.com/vertex-ai/docs/general/vpc-peering).

                       If this field is left unspecified, the job is not peered with any network.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed ComputeNetwork
                          resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                        type: string
                      name:
                        description: The name of a ComputeNetwork resource.
                        type: string
                      namespace:
                        description: The namespace of a ComputeNetwork resource.
                        type: string
                    type: object
                  persistentResourceID:
                    description: |-
                      Optional. The ID of the PersistentResource in the same Project and Location
                       which to run

                       If this is specified, the job will be run on existing machines held by the
                       PersistentResource instead of on-demand short-live machines.
                       The network and CMEK configs on the job should be consistent with those on
                       the PersistentResource, otherwise, the job will be rejected.
                    type: string
                  protectedArtifactLocationID:
                    description: 'The ID of the location to store protected artifacts.
                      e.g. us-central1. Populate only when the location is different
                      than CustomJob location. List of supported locations: https://cloud.google.com/vertex-ai/docs/general/locations'
                    type: string
                  pscInterfaceConfig:
                    description: Optional. Configuration for PSC-I for CustomJob.
                    properties:
                      dnsPeeringConfigs:
                        description: Optional. DNS peering configurations. When specified,
                          Vertex AI will attempt to configure DNS peering zones in
                          the tenant project VPC to resolve the specified domains
                          using the target network's Cloud DNS. The user must grant
                          the dns.peer role to the Vertex AI Service Agent on the
                          target project.
                        items:
                          properties:
                            domain:
                              description: Required. The DNS name suffix of the zone
                                being peered to, e.g., "my-internal-domain.corp.".
                                Must end with a dot.
                              type: string
                            targetNetwork:
                              description: Required. The VPC network name in the target_project
                                where the DNS zone specified by 'domain' is visible.
                              type: string
                            targetProject:
                              description: Required. The project ID hosting the Cloud
                                DNS managed zone that contains the 'domain'. The Vertex
                                AI Service Agent requires the dns.peer role on this
                                project.
                              type: string
                          type: object
                        type: array
                      networkAttachment:
                        description: Optional. The name of the Compute Engine [network
                          attachment](https://cloud.google.com/vpc/docs/about-network-attachments)
                          to attach to the resource within the region and user project.
                          To specify this field, you must have already [created a
                          network attachment] (https://cloud.google.com/vpc/docs/create-manage-network-attachments#create-network-attachments).
                          This field is only used for resources using PSC-I.
                        type: string
                    type: object
                  reservedIPRanges:
                    description: |-
                      Optional. A list of names for the reserved ip ranges under the VPC network
                       that can be used for this job.

                       If set, we will deploy the job within the provided ip ranges. Otherwise,
                       the job will be deployed to any ip ranges under the provided VPC
                       network.

                       Example: ['vertex-ai-ip-range'].
                    items:
                      type: string
                    type: array
                  scheduling:
                    description: Scheduling options for a CustomJob.
                    properties:
                      disableRetries:
                        description: Optional. Indicates if the job should retry for
                          internal errors after the job starts running. If true, overrides
                          `Scheduling.restart_job_on_worker_restart` to false.
                        type: boolean
                      maxWaitDuration:
                        description: Optional. This is the maximum duration that a
                          job will wait for the requested resources to be provisioned
                          if the scheduling strategy is set to [Strategy.DWS_FLEX_START].
                          If set to 0, the job will wait indefinitely. The default
                          is 24 hours.
                        type: string
                      restartJobOnWorkerRestart:
                        description: Restarts the entire CustomJob if a worker gets
                          restarted. This feature can be used by distributed training
                          jobs that are not resilient to workers leaving and joining
                          a job.
                        type: boolean
                      strategy:
                        description: Optional. This determines which type of scheduling
                          strategy to use.
                        type: string
                      timeout:
                        description: The maximum job running time. The default is
                          7 days.
                        type: string
                    type: object
                  serviceAccountRef:
                    description: Specifies the service account for workload run-as
                      account. Users submitting jobs must have act-as permission on
                      this run-as account. If unspecified, the [Vertex AI Custom Code
                      Service Agent](https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents)
                      for the CustomJob's project is used.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The `email` field of an `IAMServiceAccount` resource.
                        type: string
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                        type: string
                      namespace:
                        description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                        type: string
                    type: object
                  tensorboardRef:
                    description: 'Optional. The name of a Vertex AI [Tensorboard][google.cloud.aiplatform.v1beta1.Tensorboard]
                      resource to which this CustomJob will upload Tensorboard logs.
                      Format: `projects/{project}/locations/{location}/tensorboards/{tensorboard}`'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed VertexAITensorboard
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/tensorboards/{{tensorboardID}}".
                        type: string
                      name:
                        description: The name of a VertexAITensorboard resource.
                        type: string
                      namespace:
                        description: The namespace of a VertexAITensorboard resource.
                        type: string
                    type: object
                  workerPoolSpecs:
                    description: Required. The spec of the worker pools including
                      machine type and Docker image. All worker pools except the first
                      one are optional and can be skipped by providing an empty value.
                    items:
                      properties:
                        containerSpec:
                          description: The custom container task.
                          properties:
                            args:
                              description: The arguments to be passed when starting
                                the container.
                              items:
                                type: string
                              type: array
                            command:
                              description: The command to be invoked when the container
                                is started. It overrides the entrypoint instruction
                                in Dockerfile when provided.
                              items:
                                type: string
                              type: array
                            env:
                              description: Environment variables to be passed to the
                                container. Maximum limit is 100.
                              items:
                                properties:
                                  name:
                                    description: Required. Name of the environment
                                      variable. Must be a valid C identifier.
                                    type: string
                                  value:
                                    description: 'Required. Variables that reference
                                      a $(VAR_NAME) are expanded using the previous
                                      defined environment variables in the container
                                      and any service environment variables. If a
                                      variable cannot be resolved, the reference in
                                      the input string will be unchanged. The $(VAR_NAME)
                                      syntax can be escaped with a double $$, ie:
                                      $$(VAR_NAME). Escaped references will never
                                      be expanded, regardless of whether the variable
                                      exists or not.'
                                    type: string
                                type: object
                              type: array
                            imageURI:
                              description: Required. The URI of a container image
                                in the Container Registry that is to be run on each
                                worker replica.
                              type: string
                          type: object
                        diskSpec:
                          description: Disk spec.
                          properties:
                            bootDiskSizeGB:
                              description: Size in GB of the boot disk (default is
                                100GB).
                              format: int32
                              type: integer
                            bootDiskType:
                              description: 'Type of the boot disk. For non-A3U machines,
                                the default value is "pd-ssd", for A3U machines, the
                                default value is "hyperdisk-balanced". Valid values:
                                "pd-ssd" (Persistent Disk Solid State Drive), "pd-standard"
                                (Persistent Disk Hard Disk Drive) or "hyperdisk-balanced".'
                              type: string
                          type: object
                        machineSpec:
                          description: Optional. Immutable. The specification of a
                            single machine.
                          properties:
                            acceleratorCount:
                              description: The number of accelerators to attach to
                                the machine.
                              format: int32
                              type: integer
                            acceleratorType:
                              description: Immutable. The type of accelerator(s) that
                                may be attached to the machine as per [accelerator_count][google.cloud.aiplatform.v1beta1.MachineSpec.accelerator_count].
                              type: string
                            gpuPartitionSize:
                              description: |-
                                Optional. Immutable. The Nvidia GPU partition size.

                                 When specified, the requested accelerators will be partitioned into
                                 smaller GPU partitions. For example, if the request is for 8 units of
                                 NVIDIA A100 GPUs, and gpu_partition_size="1g.10gb", the service will
                                 create 8 * 7 = 56 partitioned MIG instances.

                                 The partition size must be a value supported by the requested accelerator.
                                 Refer to
                                 [Nvidia GPU
                                 Partitioning](https://cloud.google.com/kubernetes-engine/docs/how-to/gpus-multi#multi-instance_gpu_partitions)
                                 for the available partition sizes.

                                 If set, the accelerator_count should be set to 1.
                              type: string
                            machineType:
                              description: |-
                                Immutable. The type of the machine.

                                 See the [list of machine types supported for
                                 prediction](https://cloud.google.com/vertex-ai/docs/predictions/configure-compute#machine-types)

                                 See the [list of machine types supported for custom
                                 training](https://cloud.google.com/vertex-ai/docs/training/configure-compute#machine-types).

                                 For [DeployedModel][google.cloud.aiplatform.v1beta1.DeployedModel] this
                                 field is optional, and the default value is `n1-standard-2`. For
                                 [BatchPredictionJob][google.cloud.aiplatform.v1beta1.BatchPredictionJob] or
                                 as part of [WorkerPoolSpec][google.cloud.aiplatform.v1beta1.WorkerPoolSpec]
                                 this field is required.
                              type: string
                            multihostGpuNodeCount:
                              description: Optional. Immutable. The number of nodes
                                per replica for multihost GPU deployments.
                              format: int32
                              type: integer
                            reservationAffinity:
                              description: Optional. Immutable. Configuration controlling
                                how this resource pool consumes reservation.
                              properties:
                                key:
                                  description: Optional. Corresponds to the label
                                    key of a reservation resource. To target a SPECIFIC_RESERVATION
                                    by name, use `compute.googleapis.com/reservation-name`
                                    as the key and specify the name of your reservation
                                    as its value.
                                  type: string
                                reservationAffinityType:
                                  description: Required. Specifies the reservation
                                    affinity type.
                                  type: string
                                values:
                                  description: Optional. Corresponds to the label
                                    values of a reservation resource. This must be
                                    the full resource name of the reservation.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            tpuTopology:
                              description: 'Immutable. The topology of the TPUs. Corresponds
                                to the TPU topologies available from GKE. (Example:
                                tpu_topology: "2x2x1").'
                              type: string
                          type: object
                        nfsMounts:
                          description: Optional. List of NFS mount spec.
                          items:
                            properties:
                              mountPoint:
                                description: Required. Destination mount path. The
                                  NFS will be mounted for the user under /mnt/nfs/<mount_point>
                                type: string
                              path:
                                description: Required. Source path exported from NFS
                                  server. Has to start with '/', and combined with
                                  the ip address, it indicates the source mount path
                                  in the form of `server:path`
                                type: string
                              server:
                                description: Required. IP address of the NFS server.
                                type: string
                            type: object
                          type: array
                        pythonPackageSpec:
                          description: The Python packaged task.
                          properties:
                            args:
                              description: Command line arguments to be passed to
                                the Python task.
                              items:
                                type: string
                              type: array
                            executorImageURI:
                              description: Required. The URI of a container image
                                in Artifact Registry that will run the provided Python
                                package. Vertex AI provides a wide range of custom
                                pre-built sub-containers to run custom training workloads.
                                If set, Google Cloud Storage paths inside the pythonPackageSpec
                                will be mapped to local paths within the partner container.
                              type: string
                            packageURIs:
                              description: Required. The Google Cloud Storage URIs
                                of the Python package files which are the training
                                program and its dependent packages.
                              items:
                                type: string
                              type: array
                            pythonModule:
                              description: Required. The Python module name to run
                                after installing the packages.
                              type: string
                          type: object
                        replicaCount:
                          description: Optional. The number of worker replicas to
                            use for this worker pool.
                          format: int64
                          type: integer
                      type: object
                    type: array
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Optional. The labels with user-defined metadata to organize
                  CustomJobs.
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAICustomJob name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - displayName
            - jobSpec
            - location
            - projectRef
            type: object
          status:
            description: VertexAICustomJobStatus defines the config connector machine
              state of VertexAICustomJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAICustomJob resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time when the CustomJob was created.
                    type: string
                  endTime:
                    description: 'Output only. Time when the CustomJob entered any
                      of the following states: `JOB_STATE_SUCCEEDED`, `JOB_STATE_FAILED`,
                      `JOB_STATE_CANCELLED`.'
                    type: string
                  error:
                    description: Output only. Only populated when job's state is `JOB_STATE_FAILED`
                      or `JOB_STATE_CANCELLED`.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  name:
                    description: Output only. Resource name of a CustomJob.
                    type: string
                  satisfiesPzi:
                    description: Output only. Reserved for future use.
                    type: boolean
                  satisfiesPzs:
                    description: Output only. Reserved for future use.
                    type: boolean
                  startTime:
                    description: Output only. Time when the CustomJob for the first
                      time entered the `JOB_STATE_RUNNING` state.
                    type: string
                  state:
                    description: Output only. The detailed state of the job.
                    type: string
                  updateTime:
                    description: Output only. Time when the CustomJob was most recently
                      updated.
                    type: string
                  webAccessURIs:
                    additionalProperties:
                      type: string
                    description: Output only. URIs for accessing interactive shells.
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
