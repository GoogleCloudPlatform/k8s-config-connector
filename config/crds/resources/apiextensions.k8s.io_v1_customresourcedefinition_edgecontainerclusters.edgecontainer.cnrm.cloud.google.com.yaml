# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: stable
    cnrm.cloud.google.com/system: "true"
    cnrm.cloud.google.com/tf2crd: "true"
  name: edgecontainerclusters.edgecontainer.cnrm.cloud.google.com
spec:
  group: edgecontainer.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: EdgeContainerCluster
    listKind: EdgeContainerClusterList
    plural: edgecontainerclusters
    shortNames:
    - gcpedgecontainercluster
    - gcpedgecontainerclusters
    singular: edgecontainercluster
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: EdgeContainerCluster is the Schema for the EdgeContainerCluster
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EdgeContainerClusterSpec defines the desired state of EdgeContainerCluster
            properties:
              authorization:
                description: Immutable. RBAC policy that will be applied and managed
                  by GEC.
                properties:
                  adminUsers:
                    description: User that will be granted the cluster-admin role
                      on the cluster, providing full access to the cluster.
                    properties:
                      usernameRef:
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: The `email` field of an `IAMServiceAccount`
                              resource.
                            type: string
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                            type: string
                          namespace:
                            description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                            type: string
                        type: object
                    required:
                    - usernameRef
                    type: object
                required:
                - adminUsers
                type: object
              controlPlane:
                description: Optional. The configuration of the cluster control plane.
                properties:
                  local:
                    description: Immutable. Local control plane configuration.
                    properties:
                      machineFilter:
                        description: Only machines matching this filter will be allowed
                          to host control plane nodes.
                        type: string
                      nodeCount:
                        description: The number of nodes to serve as replicas of the
                          Control Plane. Only 1 and 3 are supported.
                        format: int32
                        type: integer
                      nodeLocation:
                        description: Immutable. Name of the Google Distributed Cloud
                          Edge zones where this node pool will be created.
                        type: string
                      sharedDeploymentPolicy:
                        description: Policy configuration about how user applications
                          are deployed.
                        type: string
                    type: object
                  remote:
                    description: Immutable. Remote control plane configuration.
                    properties:
                      nodeLocation:
                        description: Immutable. Name of the Google Distributed Cloud
                          Edge zones where this node pool will be created.
                        type: string
                    type: object
                type: object
              controlPlaneEncryption:
                description: Optional. Remote control plane disk encryption options.
                  This field is only used when enabling CMEK support.
                properties:
                  kmsKeyActiveVersion:
                    description: Output only. The Cloud KMS CryptoKeyVersion currently
                      in use for protecting control plane disks. Only applicable if
                      kms_key is set.
                    type: string
                  kmsKeyRef:
                    description: The Cloud KMS CryptoKey to use for protecting control
                      plane disks.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey.
                          Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
                        type: string
                      name:
                        description: The `name` of a `KMSCryptoKey` resource.
                        type: string
                      namespace:
                        description: The `namespace` of a `KMSCryptoKey` resource.
                        type: string
                    type: object
                  kmsKeyState:
                    description: Output only. Availability of the Cloud KMS CryptoKey.
                    type: string
                  kmsStatus:
                    description: Output only. Error status returned by Cloud KMS when
                      using this key.
                    items:
                      properties:
                        code:
                          description: The status code, which should be an enum value
                            of google.rpc.Code.
                          format: int32
                          type: integer
                        message:
                          description: A developer-facing error message, which should
                            be in English.
                          type: string
                      type: object
                    type: array
                type: object
              defaultMaxPodsPerNode:
                description: Optional. The default maximum number of pods per node
                  used if a maximum value is not specified explicitly for a node pool
                  in this cluster.
                format: int32
                type: integer
              externalLoadBalancerIpv4AddressPools:
                description: Optional. IPv4 address pools for cluster data plane external
                  load balancing.
                items:
                  type: string
                type: array
              fleet:
                description: Immutable. Fleet related configuration.
                properties:
                  membership:
                    description: The name of the managed Hub Membership resource associated
                      to this cluster.
                    type: string
                  projectRef:
                    description: Required. The number of the Fleet host project where
                      this cluster will be registered.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The `projectID` field of a project, when not
                          managed by Config Connector.
                        type: string
                      kind:
                        description: The kind of the Project resource; optional but
                          must be `Project` if provided.
                        type: string
                      name:
                        description: The `name` field of a `Project` resource.
                        type: string
                      namespace:
                        description: The `namespace` field of a `Project` resource.
                        type: string
                    type: object
                required:
                - projectRef
                type: object
              location:
                description: Immutable. The location of the resource.
                type: string
              maintenancePolicy:
                description: Cluster-wide maintenance policy configuration.
                properties:
                  window:
                    description: Specifies the maintenance window in which maintenance
                      may be performed.
                    properties:
                      recurringWindow:
                        description: Represents an arbitrary window of time that recurs.
                        properties:
                          recurrence:
                            description: An RRULE for how this window recurs.
                            type: string
                          window:
                            description: Represents an arbitrary window of time.
                            properties:
                              endTime:
                                description: The time that the window ends.
                                type: string
                              startTime:
                                description: The time that the window first starts.
                                type: string
                            type: object
                        type: object
                    required:
                    - recurringWindow
                    type: object
                required:
                - window
                type: object
              networking:
                description: Fleet related configuration.
                properties:
                  clusterIpv4CidrBlocks:
                    description: Immutable. All pods in the cluster are assigned an
                      RFC1918 IPv4 address from these blocks. Only a single block
                      is supported. This field cannot be changed after creation.
                    items:
                      type: string
                    type: array
                  clusterIpv6CidrBlocks:
                    description: Immutable. If specified, dual stack mode is enabled
                      and all pods in the cluster are assigned an IPv6 address from
                      these blocks alongside from an IPv4 address. Only a single block
                      is supported. This field cannot be changed after creation.
                    items:
                      type: string
                    type: array
                  networkType:
                    description: IP addressing type of this cluster.
                    type: string
                  servicesIpv4CidrBlocks:
                    description: Immutable. All services in the cluster are assigned
                      an RFC1918 IPv4 address from these blocks. Only a single block
                      is supported. This field cannot be changed after creation.
                    items:
                      type: string
                    type: array
                  servicesIpv6CidrBlocks:
                    description: Immutable. If specified, dual stack mode is enabled
                      and all services in the cluster are assigned an RFC1918 IPv4
                      address from these blocks. Only a single block is supported.
                      This field cannot be changed after creation.
                    items:
                      type: string
                    type: array
                required:
                - clusterIpv4CidrBlocks
                - servicesIpv4CidrBlocks
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              releaseChannel:
                description: Optional. The release channel a cluster is subscribed
                  to.
                type: string
              resourceID:
                description: Immutable. Optional. The name of the resource. Used for
                  creation and acquisition. When unset, the value of `metadata.name`
                  is used as the default.
                type: string
              systemAddonsConfig:
                description: Config that customers are allowed to define for GDCE
                  system add-ons.
                properties:
                  ingress:
                    description: Config for the Ingress add-on.
                    properties:
                      disabled:
                        description: Whether Ingress is disabled.
                        type: boolean
                      ipv4Vip:
                        description: Ingress VIP.
                        type: string
                    type: object
                type: object
              targetVersion:
                description: 'Optional. The target cluster version. For example: "1.5.0".'
                type: string
            required:
            - authorization
            - fleet
            - location
            - networking
            - projectRef
            type: object
          status:
            description: EdgeContainerClusterStatus defines the config connector machine
              state of EdgeContainerCluster
            properties:
              clusterCaCertificate:
                description: Output only. The PEM-encoded public certificate of the
                  cluster's CA.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              controlPlaneVersion:
                description: Output only. The control plane release version.
                type: string
              createTime:
                description: Output only. The time the cluster was created, in RFC3339
                  text format.
                type: string
              endpoint:
                description: Output only. The IP address of the Kubernetes API server.
                type: string
              externalRef:
                description: A unique specifier for the EdgeContainerCluster resource
                  in GCP.
                type: string
              maintenanceEvents:
                description: Output only. All the maintenance events scheduled for
                  the cluster.
                items:
                  properties:
                    createTime:
                      description: Output only. The time when the maintenance event
                        request was created.
                      type: string
                    endTime:
                      description: Output only. The time when the maintenance event
                        ended, either successfully or not.
                      type: string
                    operation:
                      description: Output only. The operation for running the maintenance
                        event.
                      type: string
                    schedule:
                      description: Output only. The schedule of the maintenance event.
                      type: string
                    startTime:
                      description: Output only. The time when the maintenance event
                        started.
                      type: string
                    state:
                      description: Output only. Indicates the maintenance event state.
                      type: string
                    targetVersion:
                      description: Output only. The target version of the cluster.
                      type: string
                    type:
                      description: Output only. Indicates the maintenance event type.
                      type: string
                    updateTime:
                      description: Output only. The time when the maintenance event
                        message was updated.
                      type: string
                    uuid:
                      description: Output only. UUID of the maintenance event.
                      type: string
                  type: object
                type: array
              nodeVersion:
                description: Output only. The lowest release version among all worker
                  nodes. This field can be empty if the cluster does not have any
                  worker nodes.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              port:
                description: Output only. The port number of the Kubernetes API server.
                format: int32
                type: integer
              status:
                description: Output only. Indicates the status of the cluster.
                type: string
              updateTime:
                description: Output only. The time the cluster was last updated, in
                  RFC3339 text format.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
