# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: dialogflowtools.dialogflow.cnrm.cloud.google.com
spec:
  group: dialogflow.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DialogflowTool
    listKind: DialogflowToolList
    plural: dialogflowtools
    shortNames:
    - gcpdialogflowtool
    - gcpdialogflowtools
    singular: dialogflowtool
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DialogflowTool is the Schema for the DialogflowTool API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DialogflowToolSpec defines the desired state of DialogflowTool
            properties:
              agentRef:
                description: The DialogflowCXAgent that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed DialogflowCXAgent
                      resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/agents/{{agentID}}".
                    type: string
                  name:
                    description: The name of a DialogflowCXAgent resource.
                    type: string
                  namespace:
                    description: The namespace of a DialogflowCXAgent resource.
                    type: string
                type: object
              connectorSpec:
                description: Integration connectors tool specification.
                properties:
                  actions:
                    description: Required. Actions for the tool to use.
                    items:
                      properties:
                        connectionActionID:
                          description: ID of a Connection action for the tool to use.
                          type: string
                        entityOperation:
                          description: Entity operation configuration for the tool
                            to use.
                          properties:
                            entityID:
                              description: Required. ID of the entity.
                              type: string
                            operation:
                              description: Required. Operation to perform on the entity.
                              type: string
                          type: object
                        inputFields:
                          description: Optional. Entity fields to use as inputs for
                            the operation. If no fields are specified, all fields
                            of the Entity will be used.
                          items:
                            type: string
                          type: array
                        outputFields:
                          description: Optional. Entity fields to return from the
                            operation. If no fields are specified, all fields of the
                            Entity will be returned.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  endUserAuthConfig:
                    description: 'Optional. Integration Connectors end-user authentication
                      configuration. If configured, the end-user authentication fields
                      will be passed in the Integration Connectors API request and
                      override the admin, default authentication configured for the
                      Connection. **Note**: The Connection must have authentication
                      override enabled in order to specify an EUC configuration here
                      - otherwise, the ConnectorTool creation will fail. See: https://cloud.google.com/application-integration/docs/configure-connectors-task#configure-authentication-override'
                    properties:
                      oauth2AuthCodeConfig:
                        description: Oauth 2.0 Authorization Code authentication.
                        properties:
                          oauthToken:
                            description: Required. Oauth token value or parameter
                              name to pass it through.
                            type: string
                        type: object
                      oauth2JwtBearerConfig:
                        description: JWT Profile Oauth 2.0 Authorization Grant authentication.
                        properties:
                          clientKey:
                            description: Required. Client key value or parameter name
                              to pass it through.
                            type: string
                          issuer:
                            description: Required. Issuer value or parameter name
                              to pass it through.
                            type: string
                          subject:
                            description: Required. Subject value or parameter name
                              to pass it through.
                            type: string
                        type: object
                    type: object
                  name:
                    description: 'Required. The full resource name of the referenced
                      Integration Connectors Connection. Format: `projects/*/locations/*/connections/*`'
                    type: string
                type: object
              dataStoreSpec:
                description: Data store search tool specification.
                properties:
                  dataStoreConnections:
                    description: Required. List of data stores to search.
                    items:
                      properties:
                        dataStore:
                          description: 'The full name of the referenced data store.
                            Formats: `projects/{project}/locations/{location}/collections/{collection}/dataStores/{data_store}`
                            `projects/{project}/locations/{location}/dataStores/{data_store}`'
                          type: string
                        dataStoreType:
                          description: The type of the connected data store.
                          type: string
                        documentProcessingMode:
                          description: The document processing mode for the data store
                            connection. Should only be set for PUBLIC_WEB and UNSTRUCTURED
                            data stores. If not set it is considered as DOCUMENTS,
                            as this is the legacy mode.
                          type: string
                      type: object
                    type: array
                  fallbackPrompt:
                    description: Required. Fallback prompt configurations to use.
                    type: object
                type: object
              description:
                description: Required. High level description of the Tool and its
                  usage.
                type: string
              displayName:
                description: Required. The human-readable name of the Tool, unique
                  within an agent.
                type: string
              extensionSpec:
                description: Vertex extension tool specification.
                properties:
                  name:
                    description: 'Required. The full name of the referenced vertex
                      extension. Formats: `projects/{project}/locations/{location}/extensions/{extension}`'
                    type: string
                type: object
              functionSpec:
                description: Client side executed function specification.
                properties:
                  inputSchema:
                    description: Optional. The JSON schema is encapsulated in a [google.protobuf.Struct][google.protobuf.Struct]
                      to describe the input of the function. This input is a JSON
                      object that contains the function's parameters as properties
                      of the object.
                    x-kubernetes-preserve-unknown-fields: true
                  outputSchema:
                    description: Optional. The JSON schema is encapsulated in a [google.protobuf.Struct][google.protobuf.Struct]
                      to describe the output of the function. This output is a JSON
                      object that contains the function's parameters as properties
                      of the object.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              openAPISpec:
                description: OpenAPI specification of the Tool.
                properties:
                  authentication:
                    description: Optional. Authentication information required by
                      the API.
                    properties:
                      apiKeyConfig:
                        description: Config for API key auth.
                        properties:
                          apiKey:
                            description: Optional. The API key. If the `secret_version_for_api_key`
                              field is set, this field will be ignored.
                            type: string
                          keyName:
                            description: Required. The parameter name or the header
                              name of the API key. E.g., If the API request is "https://example.com/act?X-Api-Key=<API
                              KEY>", "X-Api-Key" would be the parameter name.
                            type: string
                          requestLocation:
                            description: Required. Key location in the request.
                            type: string
                        type: object
                      bearerTokenConfig:
                        description: Config for bearer token auth.
                        properties:
                          token:
                            description: Optional. The text token appended to the
                              text `Bearer` to the request Authorization header. [Session
                              parameters reference](https://cloud.google.com/dialogflow/cx/docs/concept/parameter#session-ref)
                              can be used to pass the token dynamically, e.g. `$session.params.parameter-id`.
                            type: string
                        type: object
                      oauthConfig:
                        description: Config for OAuth.
                        properties:
                          clientID:
                            description: Required. The client ID from the OAuth provider.
                            type: string
                          clientSecret:
                            description: Optional. The client secret from the OAuth
                              provider. If the `secret_version_for_client_secret`
                              field is set, this field will be ignored.
                            type: string
                          oauthGrantType:
                            description: Required. OAuth grant types.
                            type: string
                          scopes:
                            description: Optional. The OAuth scopes to grant.
                            items:
                              type: string
                            type: array
                          tokenEndpoint:
                            description: Required. The token endpoint in the OAuth
                              provider to exchange for an access token.
                            type: string
                        type: object
                      serviceAgentAuthConfig:
                        description: Config for [Diglogflow service agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent)
                          auth.
                        properties:
                          serviceAgentAuth:
                            description: Optional. Indicate the auth token type generated
                              from the [Diglogflow service agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
                              The generated token is sent in the Authorization header.
                            type: string
                        type: object
                    type: object
                  serviceDirectoryConfig:
                    description: Optional. Service Directory configuration.
                    properties:
                      service:
                        description: 'Required. The name of [Service Directory](https://cloud.google.com/service-directory)
                          service. Format: `projects/<ProjectID>/locations/<LocationID>/namespaces/<NamespaceID>/services/<ServiceID>`.
                          `LocationID` of the service directory must be the same as
                          the location of the agent.'
                        type: string
                    type: object
                  textSchema:
                    description: Required. The OpenAPI schema specified as a text.
                    type: string
                  tlsConfig:
                    description: Optional. TLS configuration for the HTTPS verification.
                    properties:
                      caCerts:
                        description: Required. Specifies a list of allowed custom
                          CA certificates for HTTPS verification.
                        items:
                          properties:
                            cert:
                              description: |-
                                Required. The allowed custom CA certificates (in DER format) for
                                 HTTPS verification. This overrides the default SSL trust store. If this
                                 is empty or unspecified, Dialogflow will use Google's default trust
                                 store to verify certificates. N.B. Make sure the HTTPS server
                                 certificates are signed with "subject alt name". For instance a
                                 certificate can be self-signed using the following command:

                                 ```
                                    openssl x509 -req -days 200 -in example.com.csr \
                                      -signkey example.com.key \
                                      -out example.com.crt \
                                      -extfile <(printf "\nsubjectAltName='DNS:www.example.com'")
                                 ```
                              format: byte
                              type: string
                            displayName:
                              description: Required. The name of the allowed custom
                                CA certificates. This can be used to disambiguate
                                the custom CA certificates.
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              resourceID:
                description: The DialogflowTool name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - agentRef
            - description
            - displayName
            type: object
          status:
            description: DialogflowToolStatus defines the config connector machine
              state of DialogflowTool
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DialogflowTool resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  toolType:
                    description: Output only. The tool type.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
