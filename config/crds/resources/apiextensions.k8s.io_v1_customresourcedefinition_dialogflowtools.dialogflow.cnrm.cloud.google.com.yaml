# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: dialogflowtools.dialogflow.cnrm.cloud.google.com
spec:
  group: dialogflow.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DialogflowTool
    listKind: DialogflowToolList
    plural: dialogflowtools
    shortNames:
    - gcpdialogflowtool
    - gcpdialogflowtools
    singular: dialogflowtool
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DialogflowTool is the Schema for the DialogflowTool API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DialogflowToolSpec defines the desired state of DialogflowTool
            properties:
              connectorSpec:
                description: Integration connectors tool specification.
                properties:
                  actions:
                    description: Required. Actions for the tool to use.
                    items:
                      properties:
                        connectionActionID:
                          description: ID of a Connection action for the tool to use.
                          type: string
                        entityOperation:
                          description: Entity operation configuration for the tool
                            to use.
                          properties:
                            entityID:
                              description: Required. ID of the entity.
                              type: string
                            operation:
                              description: Required. Operation to perform on the entity.
                              type: string
                          type: object
                        inputFields:
                          description: Optional. Entity fields to use as inputs for
                            the operation. If no fields are specified, all fields
                            of the Entity will be used.
                          items:
                            type: string
                          type: array
                        outputFields:
                          description: Optional. Entity fields to return from the
                            operation. If no fields are specified, all fields of the
                            Entity will be returned.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: 'Required. The full resource name of the referenced
                      Integration Connectors Connection. Format: ''projects/*/locations/*/connections/*'''
                    type: string
                type: object
              description:
                description: Optional. High level description of the Tool and its
                  usage.
                type: string
              displayName:
                description: Required. The human-readable name of the Tool, unique
                  within an agent.
                type: string
              extensionSpec:
                description: Vertex extension tool specification.
                properties:
                  extensionRef:
                    description: Required. The full name of the referenced vertex
                      extension.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed VertexAIExtension
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/extensions/{{extensionID}}".
                        type: string
                      name:
                        description: The name of a VertexAIExtension resource.
                        type: string
                      namespace:
                        description: The namespace of a VertexAIExtension resource.
                        type: string
                    type: object
                type: object
              functionSpec:
                description: Client side executed function specification.
                properties:
                  inputSchema:
                    description: Optional. The JSON schema is encapsulated in a [google.protobuf.Struct][google.protobuf.Struct]
                      to describe the input of the function. This input is a JSON
                      object that contains the function's parameters as properties
                      of the object.
                    x-kubernetes-preserve-unknown-fields: true
                  methodType:
                    description: Optional. The method type of the function. If not
                      specified, the default value is GET.
                    type: string
                  outputSchema:
                    description: Optional. The JSON schema is encapsulated in a [google.protobuf.Struct][google.protobuf.Struct]
                      to describe the output of the function. This output is a JSON
                      object that contains the function's parameters as properties
                      of the object.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              location:
                description: The location of this resource.
                type: string
              openAPISpec:
                description: OpenAPI specification of the Tool.
                properties:
                  authentication:
                    description: Optional. Authentication information required by
                      the API.
                    properties:
                      apiKeyConfig:
                        description: Config for API key auth.
                        properties:
                          apiKey:
                            description: Optional. The API key. If the `secret_version_for_api_key`
                              field is set, this field will be ignored.
                            type: string
                          keyName:
                            description: Required. The parameter name or the header
                              name of the API key. E.g., If the API request is "https://example.com/act?X-Api-Key=<API
                              KEY>", "X-Api-Key" would be the parameter name.
                            type: string
                          requestLocation:
                            description: Required. Key location in the request.
                            type: string
                          secretVersionForAPIKeyRef:
                            description: 'Optional. The name of the SecretManager
                              secret version resource storing the API key. If this
                              field is set, the `api_key` field will be ignored. Format:
                              `projects/{project}/secrets/{secret}/versions/{version}`'
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                                type: string
                              name:
                                description: The `name` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                              namespace:
                                description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                            type: object
                        type: object
                      bearerTokenConfig:
                        description: Config for bearer token auth.
                        properties:
                          secretVersionForTokenRef:
                            description: 'Optional. The name of the SecretManager
                              secret version resource storing the Bearer token. If
                              this field is set, the `token` field will be ignored.
                              Format: `projects/{project}/secrets/{secret}/versions/{version}`'
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                                type: string
                              name:
                                description: The `name` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                              namespace:
                                description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                            type: object
                          token:
                            description: Optional. The text token appended to the
                              text `Bearer` to the request Authorization header. [Session
                              parameters reference](https://cloud.google.com/dialogflow/cx/docs/concept/parameter#session-ref)
                              can be used to pass the token dynamically, e.g. `$session.params.parameter-id`.
                            type: string
                        type: object
                      oauthConfig:
                        description: Config for OAuth.
                        properties:
                          clientID:
                            description: Required. The client ID from the OAuth provider.
                            type: string
                          clientSecret:
                            description: Optional. The client secret from the OAuth
                              provider. If the `secret_version_for_client_secret`
                              field is set, this field will be ignored.
                            type: string
                          oauthGrantType:
                            description: Required. OAuth grant types.
                            type: string
                          scopes:
                            description: Optional. The OAuth scopes to grant.
                            items:
                              type: string
                            type: array
                          secretVersionForClientSecretRef:
                            description: 'Optional. The name of the SecretManager
                              secret version resource storing the client secret. If
                              this field is set, the `client_secret` field will be
                              ignored. Format: `projects/{project}/secrets/{secret}/versions/{version}`'
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                                type: string
                              name:
                                description: The `name` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                              namespace:
                                description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                                  resource.
                                type: string
                            type: object
                          tokenEndpoint:
                            description: Required. The token endpoint in the OAuth
                              provider to exchange for an access token.
                            type: string
                        type: object
                      serviceAgentAuthConfig:
                        description: Config for [Diglogflow service agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent)
                          auth.
                        properties:
                          serviceAgentAuth:
                            description: Optional. Indicate the auth token type generated
                              from the [Diglogflow service agent](https://cloud.google.com/iam/docs/service-agents#dialogflow-service-agent).
                              The generated token is sent in the Authorization header.
                            type: string
                        type: object
                    type: object
                  serviceDirectoryConfig:
                    description: Optional. Service Directory configuration.
                    properties:
                      service:
                        description: 'Required. The name of [Service Directory](https://cloud.google.com/service-directory)
                          service. Format: `projects/<ProjectID>/locations/<LocationID>/namespaces/<NamespaceID>/services/<ServiceID>`.
                          `LocationID` of the service directory must be the same as
                          the location of the tool.'
                        type: string
                    type: object
                  textSchema:
                    description: Required. The OpenAPI schema specified as a text.
                    type: string
                  tlsConfig:
                    description: Optional. TLS configuration for the HTTPS verification.
                    properties:
                      caCerts:
                        description: Required. Specifies a list of allowed custom
                          CA certificates for HTTPS verification.
                        items:
                          properties:
                            cert:
                              description: Required. The allowed custom CA certificates
                                (in DER format) for HTTPS verification. This overrides
                                the default SSL trust store. If this is empty or unspecified,
                                Dialogflow will use Google's default trust store to
                                verify certificates. N.B. Make sure the HTTPS server
                                certificates are signed with "subject alt name". For
                                instance a certificate can be self-signed using the
                                following command, ``` openssl x509 -req -days 200
                                -in example.com.csr \ -signkey example.com.key \ -out
                                example.com.crt \ -extfile <(printf "\nsubjectAltName='DNS:www.example.com'")
                                ```
                              format: byte
                              type: string
                            displayName:
                              description: Required. The name of the allowed custom
                                CA certificates. This can be used to disambiguate
                                the custom CA certificates.
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The DialogflowTool name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - displayName
            - location
            - projectRef
            type: object
          status:
            description: DialogflowToolStatus defines the config connector machine
              state of DialogflowTool
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DialogflowTool resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
