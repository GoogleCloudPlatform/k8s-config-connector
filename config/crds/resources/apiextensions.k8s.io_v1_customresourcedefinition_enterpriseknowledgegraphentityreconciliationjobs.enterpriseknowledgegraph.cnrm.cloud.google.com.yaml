# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: enterpriseknowledgegraphentityreconciliationjobs.enterpriseknowledgegraph.cnrm.cloud.google.com
spec:
  group: enterpriseknowledgegraph.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: EnterpriseKnowledgeGraphEntityReconciliationJob
    listKind: EnterpriseKnowledgeGraphEntityReconciliationJobList
    plural: enterpriseknowledgegraphentityreconciliationjobs
    shortNames:
    - gcpenterpriseknowledgegraphentityreconciliationjob
    - gcpenterpriseknowledgegraphentityreconciliationjobs
    singular: enterpriseknowledgegraphentityreconciliationjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EnterpriseKnowledgeGraphEntityReconciliationJob is the Schema
          for the EnterpriseKnowledgeGraphEntityReconciliationJob API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EnterpriseKnowledgeGraphEntityReconciliationJobSpec defines
              the desired state of EnterpriseKnowledgeGraphEntityReconciliationJob
            properties:
              inputConfig:
                description: Required. Information about the input BigQuery tables.
                properties:
                  bigqueryInputConfigs:
                    description: Set of input BigQuery tables.
                    items:
                      properties:
                        bigqueryTable:
                          description: Required. Format is `projects/*/datasets/*/tables/*`.
                          type: string
                        gcsURI:
                          description: Required. Schema mapping file
                          type: string
                      type: object
                    type: array
                  entityType:
                    description: Entity type
                    type: string
                  previousResultBigqueryTable:
                    description: Optional. Provide the bigquery table containing the
                      previous results if cluster ID stability is desired. Format
                      is `projects/*/datasets/*/tables/*`.
                    type: string
                type: object
              location:
                description: The location of this resource.
                type: string
              outputConfig:
                description: Required. The desired output location.
                properties:
                  bigqueryDataset:
                    description: Format is “projects/*/datasets/*”.
                    type: string
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              reconConfig:
                description: Optional. Recon configs to adjust the clustering behavior.
                properties:
                  affinityClusteringConfig:
                    description: Configs for affinity clustering.
                    properties:
                      compressionRoundCount:
                        description: Number of iterations to perform. Default value
                          is 1.
                        format: int64
                        type: integer
                    type: object
                  connectedComponentsConfig:
                    description: Configs for connected components.
                    properties:
                      weightThreshold:
                        description: Threshold used for connected components. Default
                          value is 0.85.
                        type: number
                    type: object
                  modelConfig:
                    description: Model Configs
                    properties:
                      modelName:
                        description: Model name. Refer to external documentation for
                          valid names. If unspecified, it defaults to the one mentioned
                          in the documentation.
                        type: string
                      versionTag:
                        description: Model version tag. Refer to external documentation
                          for valid tags. If unspecified, it defaults to the one mentioned
                          in the documentation.
                        type: string
                    type: object
                  options:
                    description: Extra options that affect entity clustering behavior.
                    properties:
                      enableGeocodingSeparation:
                        description: |-
                          If true, separate clusters by their geographic region (from geocoding).
                           Uses the following entity features:

                           - schema.org/addressLocality
                           - schema.org/addressRegion
                           - schema.org/addressCountry
                           Warning: processing will no longer be regionalized!
                        type: boolean
                    type: object
                type: object
              resourceID:
                description: The EnterpriseKnowledgeGraphEntityReconciliationJob name.
                  If not given, the metadata.name will be used.
                type: string
            required:
            - inputConfig
            - location
            - outputConfig
            - projectRef
            type: object
          status:
            description: EnterpriseKnowledgeGraphEntityReconciliationJobStatus defines
              the config connector machine state of EnterpriseKnowledgeGraphEntityReconciliationJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the EnterpriseKnowledgeGraphEntityReconciliationJob
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time when the EntityReconciliationJob
                      was created.
                    type: string
                  endTime:
                    description: 'Output only. Time when the EntityReconciliationJob
                      entered any of the following states: `JOB_STATE_SUCCEEDED`,
                      `JOB_STATE_FAILED`, `JOB_STATE_CANCELLED`.'
                    type: string
                  error:
                    description: Output only. Only populated when the job's state
                      is JOB_STATE_FAILED or JOB_STATE_CANCELLED.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  state:
                    description: Output only. The detailed state of the job.
                    type: string
                  updateTime:
                    description: Output only. Time when the EntityReconciliationJob
                      was most recently updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
