# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: storagebatchoperationsjobs.storagebatchoperations.cnrm.cloud.google.com
spec:
  group: storagebatchoperations.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: StorageBatchOperationsJob
    listKind: StorageBatchOperationsJobList
    plural: storagebatchoperationsjobs
    shortNames:
    - gcpstoragebatchoperationsjob
    - gcpstoragebatchoperationsjobs
    singular: storagebatchoperationsjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageBatchOperationsJob is the Schema for the StorageBatchOperationsJob
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StorageBatchOperationsJobSpec defines the desired state of
              StorageBatchOperationsJob
            properties:
              bucketList:
                description: Specifies a list of buckets and their objects to be transformed.
                properties:
                  buckets:
                    description: Required. List of buckets and their objects to be
                      transformed. Currently, only one bucket configuration is supported.
                      If multiple buckets are specified, an error will be returned.
                    items:
                      properties:
                        bucketRef:
                          description: Required. Bucket name for the objects to be
                            transformed.
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed StorageBucket
                                resource. Should be in the format "projects/{{projectID}}/buckets/{{bucketID}}".
                              type: string
                            name:
                              description: The name of a StorageBucket resource.
                              type: string
                            namespace:
                              description: The namespace of a StorageBucket resource.
                              type: string
                          type: object
                        manifest:
                          description: Specifies objects in a manifest file.
                          properties:
                            manifestLocation:
                              description: Required. `manifest_location` must contain
                                the manifest source file that is a CSV file in a Google
                                Cloud Storage bucket. Each row in the file must include
                                the object details i.e. BucketId and Name. Generation
                                may optionally be specified. When it is not specified
                                the live object is acted upon. `manifest_location`
                                should either be 1) An absolute path to the object
                                in the format of `gs://bucket_name/path/file_name.csv`.
                                2) An absolute path with a single wildcard character
                                in the file name, for example `gs://bucket_name/path/file_name*.csv`.
                                If manifest location is specified with a wildcard,
                                objects in all manifest files matching the pattern
                                will be acted upon.
                              type: string
                          type: object
                        prefixList:
                          description: Specifies objects matching a prefix set.
                          properties:
                            includedObjectPrefixes:
                              description: Optional. Include prefixes of the objects
                                to be transformed. * Supports full object name * Supports
                                prefix of the object name * Wildcards are not supported
                                * Supports empty string for all objects in a bucket.
                              items:
                                type: string
                              type: array
                          type: object
                      required:
                      - bucketRef
                      type: object
                    type: array
                required:
                - buckets
                type: object
              deleteObject:
                description: Delete objects.
                properties:
                  permanentObjectDeletionEnabled:
                    description: Required. Controls deletion behavior when versioning
                      is enabled for the object's bucket. If true both live and noncurrent
                      objects will be permanently deleted. Otherwise live objects
                      in versioned buckets will become noncurrent and objects that
                      were already noncurrent will be skipped. This setting doesn't
                      have any impact on the Soft Delete feature. All objects deleted
                      by this service can be be restored for the duration of the Soft
                      Delete retention duration if enabled. If enabled and the manifest
                      doesn't specify an object's generation, a GetObjectMetadata
                      call (a Class B operation) will be made to determine the live
                      object generation.
                    type: boolean
                type: object
              description:
                description: Optional. A description provided by the user for the
                  job. Its max length is 1024 bytes when Unicode-encoded.
                type: string
              location:
                description: The location of this resource.
                type: string
              loggingConfig:
                description: Optional. Logging configuration.
                properties:
                  logActionStates:
                    description: Required. States in which Action are logged.If empty,
                      no logs are generated.
                    items:
                      type: string
                    type: array
                  logActions:
                    description: Required. Specifies the actions to be logged.
                    items:
                      type: string
                    type: array
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              putMetadata:
                description: Updates object metadata. Allows updating fixed-key and
                  custom metadata and fixed-key metadata i.e. Cache-Control, Content-Disposition,
                  Content-Encoding, Content-Language, Content-Type, Custom-Time.
                properties:
                  cacheControl:
                    description: Optional. Updates objects Cache-Control fixed metadata.
                      Unset values will be ignored. Set empty values to clear the
                      metadata. Additionally, the value for Custom-Time cannot decrease.
                      Refer to documentation in https://cloud.google.com/storage/docs/metadata#caching_data.
                    type: string
                  contentDisposition:
                    description: Optional. Updates objects Content-Disposition fixed
                      metadata. Unset values will be ignored. Set empty values to
                      clear the metadata. Refer https://cloud.google.com/storage/docs/metadata#content-disposition
                      for additional documentation.
                    type: string
                  contentEncoding:
                    description: Optional. Updates objects Content-Encoding fixed
                      metadata. Unset values will be ignored. Set empty values to
                      clear the metadata. Refer to documentation in https://cloud.google.com/storage/docs/metadata#content-encoding.
                    type: string
                  contentLanguage:
                    description: Optional. Updates objects Content-Language fixed
                      metadata. Refer to ISO 639-1 language codes for typical values
                      of this metadata. Max length 100 characters. Unset values will
                      be ignored. Set empty values to clear the metadata. Refer to
                      documentation in https://cloud.google.com/storage/docs/metadata#content-language.
                    type: string
                  contentType:
                    description: Optional. Updates objects Content-Type fixed metadata.
                      Unset values will be ignored. Set empty values to clear the
                      metadata. Refer to documentation in https://cloud.google.com/storage/docs/metadata#content-type
                    type: string
                  customMetadata:
                    additionalProperties:
                      type: string
                    description: Optional. Updates objects custom metadata. Adds or
                      sets individual custom metadata key value pairs on objects.
                      Keys that are set with empty custom metadata values will have
                      its value cleared. Existing custom metadata not specified with
                      this flag is not changed. Refer to documentation in https://cloud.google.com/storage/docs/metadata#custom-metadata
                    type: object
                  customTime:
                    description: Optional. Updates objects Custom-Time fixed metadata.
                      Unset values will be ignored. Set empty values to clear the
                      metadata. Refer to documentation in https://cloud.google.com/storage/docs/metadata#custom-time.
                    type: string
                type: object
              putObjectHold:
                description: Changes object hold status.
                properties:
                  eventBasedHold:
                    description: Required. Updates object event based holds state.
                      When object event based hold is set, object cannot be deleted
                      or replaced. Resets object's time in the bucket for the purposes
                      of the retention period.
                    type: string
                  temporaryHold:
                    description: Required. Updates object temporary holds state. When
                      object temporary hold is set, object cannot be deleted or replaced.
                    type: string
                type: object
              resourceID:
                description: The StorageBatchOperationsJob name. If not given, the
                  metadata.name will be used.
                type: string
              rewriteObject:
                description: Rewrite the object and updates metadata like KMS key.
                properties:
                  kmsCryptoKeyRef:
                    description: 'Required. Resource name of the Cloud KMS key that
                      will be used to encrypt the object. The Cloud KMS key must be
                      located in same location as the object. Refer to https://cloud.google.com/storage/docs/encryption/using-customer-managed-keys#add-object-key
                      for additional documentation. Format: projects/{project}/locations/{location}/keyRings/{keyring}/cryptoKeys/{key}
                      For example: "projects/123456/locations/us-central1/keyRings/my-keyring/cryptoKeys/my-key".
                      The object will be rewritten and set with the specified KMS
                      key.'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey.
                          Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
                        type: string
                      name:
                        description: The `name` of a `KMSCryptoKey` resource.
                        type: string
                      namespace:
                        description: The `namespace` of a `KMSCryptoKey` resource.
                        type: string
                    type: object
                required:
                - kmsCryptoKeyRef
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: StorageBatchOperationsJobStatus defines the config connector
              machine state of StorageBatchOperationsJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the StorageBatchOperationsJob
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  completeTime:
                    description: Output only. The time that the job was completed.
                    type: string
                  counters:
                    description: Output only. Information about the progress of the
                      job.
                    properties:
                      failedObjectCount:
                        description: Output only. Number of objects failed.
                        format: int64
                        type: integer
                      succeededObjectCount:
                        description: Output only. Number of objects completed.
                        format: int64
                        type: integer
                      totalObjectCount:
                        description: Output only. Number of objects listed.
                        format: int64
                        type: integer
                    type: object
                  createTime:
                    description: Output only. The time that the job was created.
                    type: string
                  errorSummaries:
                    description: Output only. Summarizes errors encountered with sample
                      error log entries.
                    items:
                      properties:
                        errorCode:
                          description: Required. The canonical error code.
                          type: string
                        errorCount:
                          description: Required. Number of errors encountered per
                            `error_code`.
                          format: int64
                          type: integer
                        errorLogEntries:
                          description: Required. Sample error logs.
                          items:
                            properties:
                              errorDetails:
                                description: Optional. Output only. At most 5 error
                                  log entries are recorded for a given error code
                                  for a job.
                                items:
                                  type: string
                                type: array
                              objectURI:
                                description: Required. Output only. Object URL. e.g.
                                  gs://my_bucket/object.txt
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  scheduleTime:
                    description: Output only. The time that the job was scheduled.
                    type: string
                  state:
                    description: Output only. State of the job.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
