# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: backupdrbackups.backupdr.cnrm.cloud.google.com
spec:
  group: backupdr.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: BackupDRBackup
    listKind: BackupDRBackupList
    plural: backupdrbackups
    shortNames:
    - gcpbackupdrbackup
    - gcpbackupdrbackups
    singular: backupdrbackup
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupDRBackup is the Schema for the BackupDRBackup API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BackupDRBackupSpec defines the desired state of BackupDRBackup
            properties:
              backupApplianceLocks:
                description: Optional. The list of BackupLocks taken by the accessor
                  Backup Appliance.
                items:
                  properties:
                    backupApplianceLockInfo:
                      description: If the client is a backup and recovery appliance,
                        this contains metadata about why the lock exists.
                      properties:
                        backupApplianceID:
                          description: Required. The ID of the backup/recovery appliance
                            that created this lock.
                          format: int64
                          type: integer
                        backupApplianceName:
                          description: Required. The name of the backup/recovery appliance
                            that created this lock.
                          type: string
                        backupImage:
                          description: The image name that depends on this Backup.
                          type: string
                        jobName:
                          description: The job name on the backup/recovery appliance
                            that created this lock.
                          type: string
                        lockReason:
                          description: 'Required. The reason for the lock: e.g. MOUNT/RESTORE/BACKUP/etc.  The
                            value of this string is only meaningful to the client
                            and it is not interpreted by the BackupVault service.'
                          type: string
                        slaID:
                          description: The SLA on the backup/recovery appliance that
                            owns the lock.
                          format: int64
                          type: integer
                      type: object
                    lockUntilTime:
                      description: Required. The time after which this lock is not
                        considered valid and will no longer protect the Backup from
                        deletion.
                      type: string
                  type: object
                type: array
              backupVault:
                description: The BackupVault of this resource.
                type: string
              dataSource:
                description: The DataSource of this resource.
                type: string
              enforcedRetentionEndTime:
                description: Optional. The backup can not be deleted before this time.
                type: string
              expireTime:
                description: Optional. When this backup is automatically expired.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Optional. Resource labels to represent user provided
                  metadata. No labels currently defined.
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The BackupDRBackup name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - backupVault
            - dataSource
            - location
            - projectRef
            type: object
          status:
            description: BackupDRBackupStatus defines the config connector machine
              state of BackupDRBackup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the BackupDRBackup resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  backupApplianceLocks:
                    description: Optional. The list of BackupLocks taken by the accessor
                      Backup Appliance.
                    items:
                      properties:
                        serviceLockInfo:
                          description: Output only. Contains metadata about the lock
                            exist for Google Cloud native backups.
                          properties:
                            operation:
                              description: Output only. The name of the operation
                                that created this lock. The lock will automatically
                                be released when the operation completes.
                              type: string
                          type: object
                      type: object
                    type: array
                  backupType:
                    description: Output only. Type of the backup, unspecified, scheduled
                      or ondemand.
                    type: string
                  consistencyTime:
                    description: Output only. The point in time when this backup was
                      captured from the source.
                    type: string
                  createTime:
                    description: Output only. The time when the instance was created.
                    type: string
                  description:
                    description: Output only. The description of the Backup instance
                      (2048 characters or less).
                    type: string
                  etag:
                    description: Optional. Server specified ETag to prevent updates
                      from overwriting each other.
                    type: string
                  gcpBackupPlanInfo:
                    description: Output only. Configuration for a Google Cloud resource.
                    properties:
                      backupPlan:
                        description: 'Resource name of backup plan by which workload
                          is protected at the time of the backup. Format: projects/{project}/locations/{location}/backupPlans/{backupPlanId}'
                        type: string
                      backupPlanRevisionID:
                        description: The user friendly id of the backup plan revision
                          which triggered this backup in case of scheduled backup
                          or used for on demand backup.
                        type: string
                      backupPlanRevisionName:
                        description: 'Resource name of the backup plan revision which
                          triggered this backup in case of scheduled backup or used
                          for on demand backup. Format: projects/{project}/locations/{location}/backupPlans/{backupPlanId}/revisions/{revisionId}'
                        type: string
                      backupPlanRuleID:
                        description: The rule id of the backup plan which triggered
                          this backup in case of scheduled backup or used for
                        type: string
                    type: object
                  resourceSizeBytes:
                    description: Output only. source resource size in bytes at the
                      time of the backup.
                    format: int64
                    type: integer
                  satisfiesPzi:
                    description: Optional. Output only. Reserved for future use.
                    type: boolean
                  satisfiesPzs:
                    description: Optional. Output only. Reserved for future use.
                    type: boolean
                  serviceLocks:
                    description: Output only. The list of BackupLocks taken by the
                      service to prevent the deletion of the backup.
                    items:
                      properties:
                        serviceLockInfo:
                          description: Output only. Contains metadata about the lock
                            exist for Google Cloud native backups.
                          properties:
                            operation:
                              description: Output only. The name of the operation
                                that created this lock. The lock will automatically
                                be released when the operation completes.
                              type: string
                          type: object
                      type: object
                    type: array
                  state:
                    description: Output only. The Backup resource instance state.
                    type: string
                  updateTime:
                    description: Output only. The time when the instance was updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
