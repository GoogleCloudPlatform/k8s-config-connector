# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaistudies.aiplatform.cnrm.cloud.google.com
spec:
  group: aiplatform.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIStudy
    listKind: VertexAIStudyList
    plural: vertexaistudies
    shortNames:
    - gcpvertexaistudy
    - gcpvertexaistudies
    singular: vertexaistudy
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIStudy is the Schema for the VertexAIStudy API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIStudySpec defines the desired state of VertexAIStudy
            properties:
              displayName:
                description: Required. Describes the Study, default value is empty
                  string.
                type: string
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAIStudy name. If not given, the metadata.name
                  will be used.
                type: string
              studySpec:
                description: Required. Configuration of the Study.
                properties:
                  algorithm:
                    description: The search algorithm specified for the Study.
                    type: string
                  convexAutomatedStoppingSpec:
                    description: The automated early stopping spec using convex stopping
                      rule.
                    properties:
                      learningRateParameterName:
                        description: The hyper-parameter name used in the tuning job
                          that stands for learning rate. Leave it blank if learning
                          rate is not in a parameter in tuning. The learning_rate
                          is used to estimate the objective value of the ongoing trial.
                        type: string
                      maxStepCount:
                        description: Steps used in predicting the final objective
                          for early stopped trials. In general, it's set to be the
                          same as the defined steps in training / tuning. If not defined,
                          it will learn it from the completed trials. When use_steps
                          is false, this field is set to the maximum elapsed seconds.
                        format: int64
                        type: integer
                      minMeasurementCount:
                        description: The minimal number of measurements in a Trial.  Early-stopping
                          checks will not trigger if less than min_measurement_count+1
                          completed trials or pending trials with less than min_measurement_count
                          measurements. If not defined, the default value is 5.
                        format: int64
                        type: integer
                      minStepCount:
                        description: Minimum number of steps for a trial to complete.
                          Trials which do not have a measurement with step_count >
                          min_step_count won't be considered for early stopping. It's
                          ok to set it to 0, and a trial can be early stopped at any
                          stage. By default, min_step_count is set to be one-tenth
                          of the max_step_count. When use_elapsed_duration is true,
                          this field is set to the minimum elapsed seconds.
                        format: int64
                        type: integer
                      updateAllStoppedTrials:
                        description: ConvexAutomatedStoppingSpec by default only updates
                          the trials that needs to be early stopped using a newly
                          trained auto-regressive model. When this flag is set to
                          True, all stopped trials from the beginning are potentially
                          updated in terms of their `final_measurement`. Also, note
                          that the training logic of autoregressive models is different
                          in this case. Enabling this option has shown better results
                          and this may be the default option in the future.
                        type: boolean
                      useElapsedDuration:
                        description: This bool determines whether or not the rule
                          is applied based on elapsed_secs or steps. If use_elapsed_duration==false,
                          the early stopping decision is made according to the predicted
                          objective values according to the target steps. If use_elapsed_duration==true,
                          elapsed_secs is used instead of steps. Also, in this case,
                          the parameters max_num_steps and min_num_steps are overloaded
                          to contain max_elapsed_seconds and min_elapsed_seconds.
                        type: boolean
                    type: object
                  decayCurveStoppingSpec:
                    description: The automated early stopping spec using decay curve
                      rule.
                    properties:
                      useElapsedDuration:
                        description: True if [Measurement.elapsed_duration][google.cloud.aiplatform.v1.Measurement.elapsed_duration]
                          is used as the x-axis of each Trials Decay Curve. Otherwise,
                          [Measurement.step_count][google.cloud.aiplatform.v1.Measurement.step_count]
                          will be used as the x-axis.
                        type: boolean
                    type: object
                  measurementSelectionType:
                    description: Describe which measurement selection type will be
                      used
                    type: string
                  medianAutomatedStoppingSpec:
                    description: The automated early stopping spec using median rule.
                    properties:
                      useElapsedDuration:
                        description: True if median automated stopping rule applies
                          on [Measurement.elapsed_duration][google.cloud.aiplatform.v1.Measurement.elapsed_duration].
                          It means that elapsed_duration field of latest measurement
                          of current Trial is used to compute median objective value
                          for each completed Trials.
                        type: boolean
                    type: object
                  metrics:
                    description: Required. Metric specs for the Study.
                    items:
                      properties:
                        goal:
                          description: Required. The optimization goal of the metric.
                          type: string
                        metricID:
                          description: Required. The ID of the metric. Must not contain
                            whitespaces and must be unique amongst all MetricSpecs.
                          type: string
                        safetyConfig:
                          description: Used for safe search. In the case, the metric
                            will be a safety metric. You must provide a separate metric
                            for objective metric.
                          properties:
                            desiredMinSafeTrialsFraction:
                              description: Desired minimum fraction of safe trials
                                (over total number of trials) that should be targeted
                                by the algorithm at any time during the study (best
                                effort). This should be between 0.0 and 1.0 and a
                                value of 0.0 means that there is no minimum and an
                                algorithm proceeds without targeting any specific
                                fraction. A value of 1.0 means that the algorithm
                                attempts to only Suggest safe Trials.
                              type: number
                            safetyThreshold:
                              description: Safety threshold (boundary value between
                                safe and unsafe). NOTE that if you leave SafetyMetricConfig
                                unset, a default value of 0 will be used.
                              type: number
                          type: object
                      type: object
                    type: array
                  observationNoise:
                    description: The observation noise level of the study. Currently
                      only supported by the Vertex AI Vizier service. Not supported
                      by HyperparameterTuningJob or TrainingPipeline.
                    type: string
                  parameters:
                    description: Required. The set of parameters to tune.
                    items:
                      properties:
                        categoricalValueSpec:
                          description: The value spec for a 'CATEGORICAL' parameter.
                          properties:
                            defaultValue:
                              description: |-
                                A default value for a `CATEGORICAL` parameter that is assumed to be a
                                 relatively good starting point.  Unset value signals that there is no
                                 offered starting point.

                                 Currently only supported by the Vertex AI Vizier service. Not supported
                                 by HyperparameterTuningJob or TrainingPipeline.
                              type: string
                            values:
                              description: Required. The list of possible categories.
                              items:
                                type: string
                              type: array
                          type: object
                        conditionalParameterSpecs:
                          description: |-
                            A conditional parameter node is active if the parameter's value matches
                             the conditional node's parent_value_condition.

                             If two items in conditional_parameter_specs have the same name, they
                             must have disjoint parent_value_condition.
                          items:
                            properties:
                              parentCategoricalValues:
                                description: The spec for matching values from a parent
                                  parameter of `CATEGORICAL` type.
                                properties:
                                  values:
                                    description: Required. Matches values of the parent
                                      parameter of 'CATEGORICAL' type. All values
                                      must exist in `categorical_value_spec` of parent
                                      parameter.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              parentDiscreteValues:
                                description: The spec for matching values from a parent
                                  parameter of `DISCRETE` type.
                                properties:
                                  values:
                                    description: |-
                                      Required. Matches values of the parent parameter of 'DISCRETE' type.
                                       All values must exist in `discrete_value_spec` of parent parameter.

                                       The Epsilon of the value matching is 1e-10.
                                    items:
                                      type: number
                                    type: array
                                type: object
                              parentIntValues:
                                description: The spec for matching values from a parent
                                  parameter of `INTEGER` type.
                                properties:
                                  values:
                                    description: Required. Matches values of the parent
                                      parameter of 'INTEGER' type. All values must
                                      lie in `integer_value_spec` of parent parameter.
                                    items:
                                      format: int64
                                      type: integer
                                    type: array
                                type: object
                            type: object
                          type: array
                        discreteValueSpec:
                          description: The value spec for a 'DISCRETE' parameter.
                          properties:
                            defaultValue:
                              description: |-
                                A default value for a `DISCRETE` parameter that is assumed to be a
                                 relatively good starting point.  Unset value signals that there is no
                                 offered starting point.  It automatically rounds to the
                                 nearest feasible discrete point.

                                 Currently only supported by the Vertex AI Vizier service. Not supported
                                 by HyperparameterTuningJob or TrainingPipeline.
                              type: number
                            values:
                              description: Required. A list of possible values. The
                                list should be in increasing order and at least 1e-10
                                apart. For instance, this parameter might have possible
                                settings of 1.5, 2.5, and 4.0. This list should not
                                contain more than 1,000 values.
                              items:
                                type: number
                              type: array
                          type: object
                        doubleValueSpec:
                          description: The value spec for a 'DOUBLE' parameter.
                          properties:
                            defaultValue:
                              description: |-
                                A default value for a `DOUBLE` parameter that is assumed to be a
                                 relatively good starting point.  Unset value signals that there is no
                                 offered starting point.

                                 Currently only supported by the Vertex AI Vizier service. Not supported
                                 by HyperparameterTuningJob or TrainingPipeline.
                              type: number
                            maxValue:
                              description: Required. Inclusive maximum value of the
                                parameter.
                              type: number
                            minValue:
                              description: Required. Inclusive minimum value of the
                                parameter.
                              type: number
                          type: object
                        integerValueSpec:
                          description: The value spec for an 'INTEGER' parameter.
                          properties:
                            defaultValue:
                              description: |-
                                A default value for an `INTEGER` parameter that is assumed to be a
                                 relatively good starting point.  Unset value signals that there is no
                                 offered starting point.

                                 Currently only supported by the Vertex AI Vizier service. Not supported
                                 by HyperparameterTuningJob or TrainingPipeline.
                              format: int64
                              type: integer
                            maxValue:
                              description: Required. Inclusive maximum value of the
                                parameter.
                              format: int64
                              type: integer
                            minValue:
                              description: Required. Inclusive minimum value of the
                                parameter.
                              format: int64
                              type: integer
                          type: object
                        parameterID:
                          description: Required. The ID of the parameter. Must not
                            contain whitespaces and must be unique amongst all ParameterSpecs.
                          type: string
                        scaleType:
                          description: How the parameter should be scaled. Leave unset
                            for `CATEGORICAL` parameters.
                          type: string
                      type: object
                    type: array
                  studyStoppingConfig:
                    description: Conditions for automated stopping of a Study. Enable
                      automated stopping by configuring at least one condition.
                    properties:
                      maxDurationNoProgress:
                        description: |-
                          If the objective value has not improved for this much time, stop the
                           study.

                           WARNING: Effective only for single-objective studies.
                        type: string
                      maxNumTrials:
                        description: If there are more than this many trials, stop
                          the study.
                        properties:
                          value:
                            description: The int32 value.
                            format: int32
                            type: integer
                        type: object
                      maxNumTrialsNoProgress:
                        description: |-
                          If the objective value has not improved for this many consecutive
                           trials, stop the study.

                           WARNING: Effective only for single-objective studies.
                        properties:
                          value:
                            description: The int32 value.
                            format: int32
                            type: integer
                        type: object
                      maximumRuntimeConstraint:
                        description: If the specified time or duration has passed,
                          stop the study.
                        properties:
                          endTime:
                            description: Compares the wallclock time to this time.
                              Must use UTC timezone.
                            type: string
                          maxDuration:
                            description: Counts the wallclock time passed since the
                              creation of this Study.
                            type: string
                        type: object
                      minNumTrials:
                        description: If there are fewer than this many COMPLETED trials,
                          do not stop the study.
                        properties:
                          value:
                            description: The int32 value.
                            format: int32
                            type: integer
                        type: object
                      minimumRuntimeConstraint:
                        description: 'Each "stopping rule" in this proto specifies
                          an "if" condition. Before Vizier would generate a new suggestion,
                          it first checks each specified stopping rule, from top to
                          bottom in this list. Note that the first few rules (e.g.
                          minimum_runtime_constraint, min_num_trials) will prevent
                          other stopping rules from being evaluated until they are
                          met. For example, setting `min_num_trials=5` and `always_stop_after=
                          1 hour` means that the Study will ONLY stop after it has
                          5 COMPLETED trials, even if more than an hour has passed
                          since its creation. It follows the first applicable rule
                          (whose "if" condition is satisfied) to make a stopping decision.
                          If none of the specified rules are applicable, then Vizier
                          decides that the study should not stop. If Vizier decides
                          that the study should stop, the study enters STOPPING state
                          (or STOPPING_ASAP if should_stop_asap = true). IMPORTANT:
                          The automatic study state transition happens precisely as
                          described above; that is, deleting trials or updating StudyConfig
                          NEVER automatically moves the study state back to ACTIVE.
                          If you want to _resume_ a Study that was stopped, 1) change
                          the stopping conditions if necessary, 2) activate the study,
                          and then 3) ask for suggestions. If the specified time or
                          duration has not passed, do not stop the study.'
                        properties:
                          endTime:
                            description: Compares the wallclock time to this time.
                              Must use UTC timezone.
                            type: string
                          maxDuration:
                            description: Counts the wallclock time passed since the
                              creation of this Study.
                            type: string
                        type: object
                      shouldStopAsap:
                        description: |-
                          If true, a Study enters STOPPING_ASAP whenever it would normally enters
                           STOPPING state.

                           The bottom line is: set to true if you want to interrupt on-going
                           evaluations of Trials as soon as the study stopping condition is met.
                           (Please see Study.State documentation for the source of truth).
                        type: boolean
                    type: object
                type: object
            required:
            - displayName
            - location
            - projectRef
            - studySpec
            type: object
          status:
            description: VertexAIStudyStatus defines the config connector machine
              state of VertexAIStudy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIStudy resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time at which the study was created.
                    type: string
                  name:
                    description: 'Output only. The name of a study. The study''s globally
                      unique identifier. Format: `projects/{project}/locations/{location}/studies/{study}`'
                    type: string
                  state:
                    description: Output only. The detailed state of a Study.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
