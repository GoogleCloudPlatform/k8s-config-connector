# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaipersistentresources.aiplatform.cnrm.cloud.google.com
spec:
  group: aiplatform.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIPersistentResource
    listKind: VertexAIPersistentResourceList
    plural: vertexaipersistentresources
    shortNames:
    - gcpvertexaipersistentresource
    - gcpvertexaipersistentresources
    singular: vertexaipersistentresource
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIPersistentResource is the Schema for the VertexAIPersistentResource
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIPersistentResourceSpec defines the desired state
              of VertexAIPersistentResource
            properties:
              displayName:
                description: Optional. The display name of the PersistentResource.
                  The name can be up to 128 characters long and can consist of any
                  UTF-8 characters.
                type: string
              encryptionSpec:
                description: Optional. Customer-managed encryption key spec for a
                  PersistentResource. If set, this PersistentResource and all sub-resources
                  of this PersistentResource will be secured by this key.
                properties:
                  kmsKeyRef:
                    description: 'Required. The Cloud KMS resource identifier of the
                      customer managed encryption key used to protect a resource.
                      Has the form: `projects/my-project/locations/my-region/keyRings/my-kr/cryptoKeys/my-key`.
                      The key needs to be in the same region as where the compute
                      resource is created.'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/keyRings/{{keyring}}/cryptoKeys/{{cryptokey}}".
                        type: string
                      name:
                        description: The name of a KMSCryptoKey resource.
                        type: string
                      namespace:
                        description: The namespace of a KMSCryptoKey resource.
                        type: string
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Optional. The labels with user-defined metadata to organize
                   PersistentResource.

                   Label keys and values can be no longer than 64 characters
                   (Unicode codepoints), can only contain lowercase letters, numeric
                   characters, underscores and dashes. International characters are allowed.

                   See https://goo.gl/xmQnxf for more information and examples of labels.
                type: object
              location:
                description: The location of this resource.
                type: string
              networkRef:
                description: Optional. The full name of the Compute Engine network
                  to peer with Vertex AI to host the persistent resources.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed ComputeNetwork
                      resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                    type: string
                  name:
                    description: The name of a ComputeNetwork resource.
                    type: string
                  namespace:
                    description: The namespace of a ComputeNetwork resource.
                    type: string
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              pscInterfaceConfig:
                description: Optional. Configuration for PSC-I for PersistentResource.
                properties:
                  dnsPeeringConfigs:
                    description: Optional. DNS peering configurations.
                    items:
                      properties:
                        domain:
                          description: Required. The DNS name suffix of the zone being
                            peered to, e.g., "my-internal-domain.corp.". Must end
                            with a dot.
                          type: string
                        targetNetworkRef:
                          description: Required. The VPC network name in the target_project
                            where the DNS zone specified by 'domain' is visible.
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed ComputeNetwork
                                resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                              type: string
                            name:
                              description: The name of a ComputeNetwork resource.
                              type: string
                            namespace:
                              description: The namespace of a ComputeNetwork resource.
                              type: string
                          type: object
                        targetProject:
                          description: Required. The project ID hosting the Cloud
                            DNS managed zone that contains the 'domain'.
                          type: string
                      type: object
                    type: array
                  networkAttachmentRef:
                    description: Optional. The name of the Compute Engine network
                      attachment to attach to the resource within the region and user
                      project.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed ComputeNetworkAttachment
                          resource. Should be in the format "projects/{{projectID}}/regions/{{region}}/networkAttachments/{{networkattachmentID}}".
                        type: string
                      name:
                        description: The name of a ComputeNetworkAttachment resource.
                        type: string
                      namespace:
                        description: The namespace of a ComputeNetworkAttachment resource.
                        type: string
                    type: object
                type: object
              reservedIPRanges:
                description: |-
                  Optional. A list of names for the reserved IP ranges under the VPC network
                   that can be used for this persistent resource.

                   If set, we will deploy the persistent resource within the provided IP
                   ranges. Otherwise, the persistent resource is deployed to any IP
                   ranges under the provided VPC network.

                   Example: ['vertex-ai-ip-range'].
                items:
                  type: string
                type: array
              resourceID:
                description: The VertexAIPersistentResource name. If not given, the
                  metadata.name will be used.
                type: string
              resourcePools:
                description: Required. The spec of the pools of different resources.
                items:
                  properties:
                    autoscalingSpec:
                      description: Optional. Optional spec to configure GKE or Ray-on-Vertex
                        autoscaling
                      properties:
                        maxReplicaCount:
                          description: Optional. max replicas in the node pool, must
                            be ≥ replica_count and > min_replica_count or will throw
                            error
                          format: int64
                          type: integer
                        minReplicaCount:
                          description: Optional. min replicas in the node pool, must
                            be ≤ replica_count and < max_replica_count or will throw
                            error. For autoscaling enabled Ray-on-Vertex, we allow
                            min_replica_count of a resource_pool to be 0 to match
                            the OSS Ray behavior(https://docs.ray.io/en/latest/cluster/vms/user-guides/configuring-autoscaling.html#cluster-config-parameters).
                            As for Persistent Resource, the min_replica_count must
                            be > 0, we added a corresponding validation inside CreatePersistentResourceRequestValidator.java.
                          format: int64
                          type: integer
                      type: object
                    diskSpec:
                      description: Optional. Disk spec for the machine in this node
                        pool.
                      properties:
                        bootDiskSizeGB:
                          description: Size in GB of the boot disk (default is 100GB).
                          format: int32
                          type: integer
                        bootDiskType:
                          description: 'Type of the boot disk (default is "pd-ssd").
                            Valid values: "pd-ssd" (Persistent Disk Solid State Drive)
                            or "pd-standard" (Persistent Disk Hard Disk Drive).'
                          type: string
                      type: object
                    id:
                      description: Immutable. The unique ID in a PersistentResource
                        for referring to this resource pool. User can specify it if
                        necessary. Otherwise, it's generated automatically.
                      type: string
                    machineSpec:
                      description: Required. Immutable. The specification of a single
                        machine.
                      properties:
                        acceleratorCount:
                          description: The number of accelerators to attach to the
                            machine.
                          format: int32
                          type: integer
                        acceleratorType:
                          description: Immutable. The type of accelerator(s) that
                            may be attached to the machine as per [accelerator_count][google.cloud.aiplatform.v1.MachineSpec.accelerator_count].
                          type: string
                        machineType:
                          description: |-
                            Immutable. The type of the machine.

                             See the [list of machine types supported for
                             prediction](https://cloud.google.com/vertex-ai/docs/predictions/configure-compute#machine-types)

                             See the [list of machine types supported for custom
                             training](https://cloud.google.com/vertex-ai/docs/training/configure-compute#machine-types).

                             For [DeployedModel][google.cloud.aiplatform.v1.DeployedModel] this field is
                             optional, and the default value is `n1-standard-2`. For
                             [BatchPredictionJob][google.cloud.aiplatform.v1.BatchPredictionJob] or as
                             part of [WorkerPoolSpec][google.cloud.aiplatform.v1.WorkerPoolSpec] this
                             field is required.
                          type: string
                        reservationAffinity:
                          description: Optional. Immutable. Configuration controlling
                            how this resource pool consumes reservation.
                          properties:
                            key:
                              description: Optional. Corresponds to the label key
                                of a reservation resource. To target a SPECIFIC_RESERVATION
                                by name, use `compute.googleapis.com/reservation-name`
                                as the key and specify the name of your reservation
                                as its value.
                              type: string
                            reservationAffinityType:
                              description: Required. Specifies the reservation affinity
                                type.
                              type: string
                            values:
                              description: Optional. Corresponds to the label values
                                of a reservation resource. This must be the full resource
                                name of the reservation.
                              items:
                                type: string
                              type: array
                          type: object
                        tpuTopology:
                          description: 'Immutable. The topology of the TPUs. Corresponds
                            to the TPU topologies available from GKE. (Example: tpu_topology:
                            "2x2x1").'
                          type: string
                      type: object
                    replicaCount:
                      description: Optional. The total number of machines to use for
                        this resource pool.
                      format: int64
                      type: integer
                  type: object
                type: array
              resourceRuntimeSpec:
                description: Optional. Persistent Resource runtime spec. For example,
                  used for Ray cluster configuration.
                properties:
                  raySpec:
                    description: Optional. Ray cluster configuration. Required when
                      creating a dedicated RayCluster on the PersistentResource.
                    properties:
                      headNodeResourcePoolID:
                        description: Optional. This will be used to indicate which
                          resource pool will serve as the Ray head node(the first
                          node within that pool). Will use the machine from the first
                          workerpool as the head node by default if this field isn't
                          set.
                        type: string
                      imageURI:
                        description: Optional. Default image for user to choose a
                          preferred ML framework (for example, TensorFlow or Pytorch)
                          by choosing from [Vertex prebuilt images](https://cloud.google.com/vertex-ai/docs/training/pre-built-containers).
                          Either this or the resource_pool_images is required. Use
                          this field if you need all the resource pools to have the
                          same Ray image. Otherwise, use the {@code resource_pool_images}
                          field.
                        type: string
                      rayLogsSpec:
                        description: Optional. OSS Ray logging configurations.
                        properties:
                          disabled:
                            description: Optional. Flag to disable the export of Ray
                              OSS logs to Cloud Logging.
                            type: boolean
                        type: object
                      rayMetricSpec:
                        description: Optional. Ray metrics configurations.
                        properties:
                          disabled:
                            description: Optional. Flag to disable the Ray metrics
                              collection.
                            type: boolean
                        type: object
                      resourcePoolImages:
                        additionalProperties:
                          type: string
                        description: 'Optional. Required if image_uri isn''t set.
                          A map of resource_pool_id to prebuild Ray image if user
                          need to use different images for different head/worker pools.
                          This map needs to cover all the resource pool ids. Example:
                          { "ray_head_node_pool": "head image" "ray_worker_node_pool1":
                          "worker image" "ray_worker_node_pool2": "another worker
                          image" }'
                        type: object
                    type: object
                  serviceAccountSpec:
                    description: Optional. Configure the use of workload identity
                      on the PersistentResource
                    properties:
                      enableCustomServiceAccount:
                        description: Required. If true, custom user-managed service
                          account is enforced to run any workloads (for example, Vertex
                          Jobs) on the resource. Otherwise, uses the [Vertex AI Custom
                          Code Service Agent](https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents).
                        type: boolean
                      serviceAccountRef:
                        description: |-
                          Optional. Required when all below conditions are met
                            * `enable_custom_service_account` is true;
                            * any runtime is specified via `ResourceRuntimeSpec` on creation time,
                              for example, Ray

                           The users must have `iam.serviceAccounts.actAs` permission on this service
                           account and then the specified runtime containers will run as it.

                           Do not set this field if you want to submit jobs using custom service
                           account to this PersistentResource after creation, but only specify the
                           `service_account` inside the job.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: The `email` field of an `IAMServiceAccount`
                              resource.
                            type: string
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                            type: string
                          namespace:
                            description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                            type: string
                        type: object
                    type: object
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: VertexAIPersistentResourceStatus defines the config connector
              machine state of VertexAIPersistentResource
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIPersistentResource
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time when the PersistentResource was
                      created.
                    type: string
                  error:
                    description: Output only. Only populated when state is `ERROR`.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  resourcePools:
                    description: Output only. Resource pools inside the PersistentResource.
                    items:
                      properties:
                        usedReplicaCount:
                          description: Output only. The number of machines currently
                            in use by training jobs for this resource pool. Will replace
                            idle_replica_count.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  resourceRuntime:
                    description: Output only. Runtime information of the PersistentResource.
                    properties:
                      accessURIs:
                        additionalProperties:
                          type: string
                        description: 'Output only. URIs for user to connect to the
                          Cluster. Example: { "RAY_HEAD_NODE_INTERNAL_IP": "head-node-IP:10001"
                          "RAY_DASHBOARD_URI": "ray-dashboard-address:8888" }'
                        type: object
                    type: object
                  startTime:
                    description: Output only. Time when the PersistentResource for
                      the first time entered the `RUNNING` state.
                    type: string
                  state:
                    description: Output only. Only populated when state is `FREE`.
                    type: string
                  updateTime:
                    description: Output only. Time when the PersistentResource was
                      most recently updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
