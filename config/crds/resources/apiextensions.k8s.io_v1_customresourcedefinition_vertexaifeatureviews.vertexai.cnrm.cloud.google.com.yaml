# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaifeatureviews.vertexai.cnrm.cloud.google.com
spec:
  group: vertexai.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIFeatureView
    listKind: VertexAIFeatureViewList
    plural: vertexaifeatureviews
    shortNames:
    - gcpvertexaifeatureview
    - gcpvertexaifeatureviews
    singular: vertexaifeatureview
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIFeatureView is the Schema for the VertexAIFeatureView
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIFeatureViewSpec defines the desired state of VertexAIFeatureView
            properties:
              bigQuerySource:
                description: Optional. Configures how data is supposed to be extracted
                  from a BigQuery source to be loaded onto the FeatureOnlineStore.
                properties:
                  entityIDColumns:
                    description: Required. Columns to construct entity_id / row keys.
                    items:
                      type: string
                    type: array
                  uri:
                    description: Required. The BigQuery view URI that will be materialized
                      on each sync trigger based on FeatureView.SyncConfig.
                    type: string
                type: object
              featureOnlineStoreRef:
                description: The parent FeatureOnlineStore of this resource.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed VertexAIFeatureOnlineStore
                      resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/featureOnlineStores/{{featureOnlineStoreID}}".
                    type: string
                  name:
                    description: The name of a VertexAIFeatureOnlineStore resource.
                    type: string
                  namespace:
                    description: The namespace of a VertexAIFeatureOnlineStore resource.
                    type: string
                type: object
              featureRegistrySource:
                description: Optional. Configures the features from a Feature Registry
                  source that need to be loaded onto the FeatureOnlineStore.
                properties:
                  featureGroups:
                    description: Required. List of features that need to be synced
                      to Online Store.
                    items:
                      description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.FeatureView.FeatureRegistrySource.FeatureGroup
                      properties:
                        featureGroupID:
                          description: Required. Identifier of the feature group.
                          type: string
                        featureIDs:
                          description: Required. Identifiers of features under the
                            feature group.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  projectNumber:
                    description: Optional. The project number of the parent project
                      of the Feature Groups.
                    format: int64
                    type: integer
                type: object
              indexConfig:
                description: Optional. Configuration for index preparation for vector
                  search. It contains the required configurations to create an index
                  from source data, so that approximate nearest neighbor (a.k.a. an
                  ANN) algorithms search can be performed during online serving.
                properties:
                  bruteForceConfig:
                    description: Optional. Configuration options for using brute force
                      search, which simply implements the standard linear search in
                      the database for each query. It is primarily meant for benchmarking
                      and to generate the ground truth for approximate search.
                    properties:
                      x_dummyField:
                        description: Optional. Dummy field to pass Kubernetes empty
                          object validation.
                        type: boolean
                    type: object
                  crowdingColumn:
                    description: Optional. Column of crowding. This column contains
                      crowding attribute which is a constraint on a neighbor list
                      produced by [FeatureOnlineStoreService.SearchNearestEntities][google.cloud.aiplatform.v1beta1.FeatureOnlineStoreService.SearchNearestEntities]
                      to diversify search results. If [NearestNeighborQuery.per_crowding_attribute_neighbor_count][google.cloud.aiplatform.v1beta1.NearestNeighborQuery.per_crowding_attribute_neighbor_count]
                      is set to K in [SearchNearestEntitiesRequest][google.cloud.aiplatform.v1beta1.SearchNearestEntitiesRequest],
                      it's guaranteed that no more than K entities of the same crowding
                      attribute are returned in the response.
                    type: string
                  distanceMeasureType:
                    description: Optional. The distance measure used in nearest neighbor
                      search.
                    type: string
                  embeddingColumn:
                    description: Optional. Column of embedding. This column contains
                      the source data to create index for vector search. embedding_column
                      must be set when using vector search.
                    type: string
                  embeddingDimension:
                    description: Optional. The number of dimensions of the input embedding.
                    format: int32
                    type: integer
                  filterColumns:
                    description: Optional. Columns of features that're used to filter
                      vector search results.
                    items:
                      type: string
                    type: array
                  treeAhConfig:
                    description: Optional. Configuration options for the tree-AH algorithm
                      (Shallow tree https://arxiv.org/abs/1908.10396
                    properties:
                      leafNodeEmbeddingCount:
                        description: Optional. Number of embeddings on each leaf node.
                          The default value is 1000 if not set.
                        format: int64
                        type: integer
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Optional. The labels with user-defined metadata to organize your
                   FeatureViews.

                   Label keys and values can be no longer than 64 characters
                   (Unicode codepoints), can only contain lowercase letters, numeric
                   characters, underscores and dashes. International characters are allowed.

                   See https://goo.gl/xmQnxf for more information on and examples of labels.
                   No more than 64 user labels can be associated with one
                   FeatureOnlineStore(System labels are excluded)." System reserved label keys
                   are prefixed with "aiplatform.googleapis.com/" and are immutable.
                type: object
              location:
                description: The location of this resource.
                type: string
              optimizedConfig:
                description: Optional. Configuration for FeatureView created under
                  Optimized FeatureOnlineStore.
                properties:
                  automaticResources:
                    description: Optional. A description of resources that the FeatureView
                      uses, which to large degree are decided by Vertex AI, and optionally
                      allows only a modest additional configuration. If min_replica_count
                      is not set, the default value is 2. If max_replica_count is
                      not set, the default value is 6. The max allowed replica count
                      is 1000.
                    properties:
                      maxReplicaCount:
                        description: Immutable. The maximum number of replicas that
                          may be deployed on when the traffic against it increases.
                          If the requested value is too large, the deployment will
                          error, but if deployment succeeds then the ability to scale
                          to that many replicas is guaranteed (barring service outages).
                          If traffic increases beyond what its replicas at maximum
                          may handle, a portion of the traffic will be dropped. If
                          this value is not provided, a no upper bound for scaling
                          under heavy traffic will be assume, though Vertex AI may
                          be unable to scale beyond certain replica number.
                        format: int32
                        type: integer
                      minReplicaCount:
                        description: Immutable. The minimum number of replicas that
                          will be always deployed on. If traffic against it increases,
                          it may dynamically be deployed onto more replicas up to
                          [max_replica_count][google.cloud.aiplatform.v1beta1.AutomaticResources.max_replica_count],
                          and as traffic decreases, some of these extra replicas may
                          be freed. If the requested value is too large, the deployment
                          will error.
                        format: int32
                        type: integer
                    type: object
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAIFeatureView name. If not given, the metadata.name
                  will be used.
                type: string
              serviceAgentType:
                description: Optional. Service agent type used during data sync. By
                  default, the Vertex AI Service Agent is used. When using an IAM
                  Policy to isolate this FeatureView within a project, a separate
                  service account should be provisioned by setting this field to `SERVICE_AGENT_TYPE_FEATURE_VIEW`.
                  This will generate a separate service account to access the BigQuery
                  source table.
                type: string
              syncConfig:
                description: Configures when data is to be synced/updated for this
                  FeatureView. At the end of the sync the latest featureValues for
                  each entityId of this FeatureView are made ready for online serving.
                properties:
                  cron:
                    description: 'Cron schedule (https://en.wikipedia.org/wiki/Cron)
                      to launch scheduled runs. To explicitly set a timezone to the
                      cron tab, apply a prefix in the cron tab: "CRON_TZ=${IANA_TIME_ZONE}"
                      or "TZ=${IANA_TIME_ZONE}". The ${IANA_TIME_ZONE} may only be
                      a valid string from IANA time zone database. For example, "CRON_TZ=America/New_York
                      1 * * * *", or "TZ=America/New_York 1 * * * *".'
                    type: string
                type: object
              vectorSearchConfig:
                description: 'Optional. Deprecated: please use [FeatureView.index_config][google.cloud.aiplatform.v1beta1.FeatureView.index_config]
                  instead.'
                properties:
                  bruteForceConfig:
                    description: Optional. Configuration options for using brute force
                      search, which simply implements the standard linear search in
                      the database for each query. It is primarily meant for benchmarking
                      and to generate the ground truth for approximate search.
                    properties:
                      x_dummyField:
                        description: Optional. Dummy field to pass Kubernetes empty
                          object validation.
                        type: boolean
                    type: object
                  crowdingColumn:
                    description: Optional. Column of crowding. This column contains
                      crowding attribute which is a constraint on a neighbor list
                      produced by [FeatureOnlineStoreService.SearchNearestEntities][google.cloud.aiplatform.v1beta1.FeatureOnlineStoreService.SearchNearestEntities]
                      to diversify search results. If [NearestNeighborQuery.per_crowding_attribute_neighbor_count][google.cloud.aiplatform.v1beta1.NearestNeighborQuery.per_crowding_attribute_neighbor_count]
                      is set to K in [SearchNearestEntitiesRequest][google.cloud.aiplatform.v1beta1.SearchNearestEntitiesRequest],
                      it's guaranteed that no more than K entities of the same crowding
                      attribute are returned in the response.
                    type: string
                  distanceMeasureType:
                    description: Optional. The distance measure used in nearest neighbor
                      search.
                    type: string
                  embeddingColumn:
                    description: Optional. Column of embedding. This column contains
                      the source data to create index for vector search. embedding_column
                      must be set when using vector search.
                    type: string
                  embeddingDimension:
                    description: Optional. The number of dimensions of the input embedding.
                    format: int32
                    type: integer
                  filterColumns:
                    description: Optional. Columns of features that're used to filter
                      vector search results.
                    items:
                      type: string
                    type: array
                  treeAhConfig:
                    description: Optional. Configuration options for the tree-AH algorithm
                      (Shallow tree https://arxiv.org/abs/1908.10396
                    properties:
                      leafNodeEmbeddingCount:
                        description: Optional. Number of embeddings on each leaf node.
                          The default value is 1000 if not set.
                        format: int64
                        type: integer
                    type: object
                type: object
              vertexRagSource:
                description: Optional. The Vertex RAG Source that the FeatureView
                  is linked to.
                properties:
                  ragCorpusID:
                    description: Optional. The RAG corpus id corresponding to this
                      FeatureView.
                    format: int64
                    type: integer
                  uri:
                    description: 'Required. The BigQuery view/table URI that will
                      be materialized on each manual sync trigger. The table/view
                      is expected to have the following columns and types at least:
                      - `corpus_id` (STRING, NULLABLE/REQUIRED) - `file_id` (STRING,
                      NULLABLE/REQUIRED) - `chunk_id` (STRING, NULLABLE/REQUIRED)
                      - `chunk_data_type` (STRING, NULLABLE/REQUIRED) - `chunk_data`
                      (STRING, NULLABLE/REQUIRED) - `embeddings` (FLOAT, REPEATED)
                      - `file_original_uri` (STRING, NULLABLE/REQUIRED)'
                    type: string
                type: object
            required:
            - featureOnlineStoreRef
            - location
            - projectRef
            type: object
          status:
            description: VertexAIFeatureViewStatus defines the config connector machine
              state of VertexAIFeatureView
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIFeatureView resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  bigtableMetadata:
                    description: Metadata containing information about the Cloud Bigtable.
                    properties:
                      readAppProfile:
                        description: The Bigtable App Profile to use for reading from
                          Bigtable.
                        type: string
                    type: object
                  createTime:
                    description: Output only. Timestamp when this FeatureView was
                      created.
                    type: string
                  satisfiesPzi:
                    description: Output only. Reserved for future use.
                    type: boolean
                  satisfiesPzs:
                    description: Output only. Reserved for future use.
                    type: boolean
                  serviceAccountEmail:
                    description: Output only. A Service Account unique to this FeatureView.
                      The role bigquery.dataViewer should be granted to this service
                      account to allow Vertex AI Feature Store to sync data to the
                      online store.
                    type: string
                  updateTime:
                    description: Output only. Timestamp when this FeatureView was
                      last updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
