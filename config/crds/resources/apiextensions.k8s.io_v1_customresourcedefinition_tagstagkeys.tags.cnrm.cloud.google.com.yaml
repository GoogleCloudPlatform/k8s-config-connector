apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
    cnrm.cloud.google.com/tf2crd: "true"
  name: tagstagkeys.tags.cnrm.cloud.google.com
spec:
  group: tags.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: TagsTagKey
    listKind: TagsTagKeyList
    plural: tagstagkeys
    shortNames:
    - gcptagstagkey
    - gcptagstagkeys
    singular: tagstagkey
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TagsTagKey is the Schema for the TagsTagKey API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TagsTagKeySpec defines the desired state of TagsTagKey
            properties:
              description:
                description: Optional. A textual description of the resource.
                type: string
              parent:
                description: 'The resource name of the parent organization or project.
                  It will be in one of the following forms: - "projects/{project_number}"
                  - "organizations/{org_id}"'
                type: string
              purpose:
                description: 'Optional. A purpose denotes that this Tag is part of
                  a grouping of KCC-managed TagKeys, to which the following restrictions
                  apply: -  Only KCC can create, update, or delete a TagKey with a
                  purpose. - Only one KCC-managed TagKey may exist for a given project,
                  with a given purpose and ancestor.'
                type: string
              purposeData:
                additionalProperties:
                  type: string
                description: Optional. Purpose data corresponds to the policy system
                  that the tag is for. For example, a value of "data-governance-eng@acmeco.com"
                  suggests that the tag is a data governance tag managed by a central
                  team. This field may only be set by setIamPolicy.
                type: object
              resourceID:
                description: The TagsTagKey name. If not given, the metadata.name
                  will be used.
                type: string
              shortName:
                description: |-
                  Required. Immutable. The user friendly name for a TagKey. The short name
                  should be unique for TagKeys within the same tag namespace.

                  The short name must be 1-63 characters, beginning and ending with
                  an alphanumeric character ([a-z0-9A-Z]) with dashes (-), underscores (_),
                  dots (.), and alphanumerics between.
                type: string
            required:
            - parent
            - shortName
            type: object
          status:
            description: TagsTagKeyStatus defines the config connector machine state
              of TagsTagKey
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              createTime:
                description: Output only. Creation time.
                type: string
              name:
                description: 'Output only. Unique id attribute for a TagKey resource_id:
                  e.g. "tagKeys/123456789012"'
                type: string
              namespacedName:
                description: Output only. Immutable. Namespaced name of the TagKey.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              updateTime:
                description: Output only. Update time.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
