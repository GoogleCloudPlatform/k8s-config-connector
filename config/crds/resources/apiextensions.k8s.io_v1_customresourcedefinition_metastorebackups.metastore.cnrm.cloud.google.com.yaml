apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: metastorebackups.metastore.cnrm.cloud.google.com
spec:
  group: metastore.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: MetastoreBackup
    listKind: MetastoreBackupList
    plural: metastorebackups
    shortNames:
    - gcpmetastorebackup
    - gcpmetastorebackups
    singular: metastorebackup
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetastoreBackup is the Schema for the MetastoreBackup API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                description: The description of the backup.
                type: string
              resourceID:
                description: The MetastoreBackup name. If not given, the metadata.name
                  will be used.
                type: string
              serviceRef:
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed MetastoreService
                      resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/services/{{serviceID}}".
                    type: string
                  name:
                    description: The name of a MetastoreService resource.
                    type: string
                  namespace:
                    description: The namespace of a MetastoreService resource.
                    type: string
                type: object
            required:
            - serviceRef
            type: object
          status:
            description: MetastoreBackupStatus defines the config connector machine
              state of MetastoreBackup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the MetastoreBackup resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The time when the backup was started.
                    type: string
                  endTime:
                    description: Output only. The time when the backup finished creating.
                    type: string
                  restoringServices:
                    description: Output only. Services that are restoring from the
                      backup.
                    items:
                      type: string
                    type: array
                  serviceRevision:
                    description: Output only. The revision of the service at the time
                      of backup.
                    properties:
                      databaseType:
                        description: Immutable. The database type that the Metastore
                          service stores its data.
                        type: string
                      encryptionConfig:
                        description: Immutable. Information used to configure the
                          Dataproc Metastore service to encrypt customer data at rest.
                          Cannot be updated.
                        properties:
                          kmsKey:
                            description: |-
                              The fully qualified customer provided Cloud KMS key name to use for
                               customer data encryption, in the following form:

                               `projects/{project_number}/locations/{location_id}/keyRings/{key_ring_id}/cryptoKeys/{crypto_key_id}`.
                            type: string
                        type: object
                      hiveMetastoreConfig:
                        description: Configuration information specific to running
                          Hive metastore software as the metastore service.
                        properties:
                          configOverrides:
                            additionalProperties:
                              type: string
                            description: A mapping of Hive metastore configuration
                              key-value pairs to apply to the Hive metastore (configured
                              in `hive-site.xml`). The mappings override system defaults
                              (some keys cannot be overridden). These overrides are
                              also applied to auxiliary versions and can be further
                              customized in the auxiliary version's `AuxiliaryVersionConfig`.
                            type: object
                          endpointProtocol:
                            description: The protocol to use for the metastore service
                              endpoint. If unspecified, defaults to `THRIFT`.
                            type: string
                          kerberosConfig:
                            description: Information used to configure the Hive metastore
                              service as a service principal in a Kerberos realm.
                              To disable Kerberos, use the `UpdateService` method
                              and specify this field's path (`hive_metastore_config.kerberos_config`)
                              in the request's `update_mask` while omitting this field
                              from the request's `service`.
                            properties:
                              keytab:
                                description: A Kerberos keytab file that can be used
                                  to authenticate a service principal with a Kerberos
                                  Key Distribution Center (KDC).
                                properties:
                                  cloudSecret:
                                    description: |-
                                      The relative resource name of a Secret Manager secret version, in the
                                       following form:

                                       `projects/{project_number}/secrets/{secret_id}/versions/{version_id}`.
                                    type: string
                                type: object
                              krb5ConfigGCSURI:
                                description: A Cloud Storage URI that specifies the
                                  path to a krb5.conf file. It is of the form `gs://{bucket_name}/path/to/krb5.conf`,
                                  although the file does not need to be named krb5.conf
                                  explicitly.
                                type: string
                              principal:
                                description: A Kerberos principal that exists in the
                                  both the keytab the KDC to authenticate as. A typical
                                  principal is of the form `primary/instance@REALM`,
                                  but there is no exact format.
                                type: string
                            type: object
                          version:
                            description: Immutable. The Hive metastore schema version.
                            type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: User-defined labels for the metastore service.
                        type: object
                      maintenanceWindow:
                        description: The one hour maintenance window of the metastore
                          service. This specifies when the service can be restarted
                          for maintenance purposes in UTC time. Maintenance window
                          is not needed for services with the SPANNER database type.
                        properties:
                          dayOfWeek:
                            description: The day of week, when the window starts.
                            type: string
                          hourOfDay:
                            description: The hour of day (0-23) when the window starts.
                            properties:
                              value:
                                description: The int32 value.
                                format: int32
                                type: integer
                            type: object
                        type: object
                      name:
                        description: |-
                          Immutable. The relative resource name of the metastore service, in the
                           following format:

                           `projects/{project_number}/locations/{location_id}/services/{service_id}`.
                        type: string
                      network:
                        description: |-
                          Immutable. The relative resource name of the VPC network on which the
                           instance can be accessed. It is specified in the following form:

                           `projects/{project_number}/global/networks/{network_id}`.
                        type: string
                      networkConfig:
                        description: The configuration specifying the network settings
                          for the Dataproc Metastore service.
                        properties:
                          consumers:
                            description: Immutable. The consumer-side network configuration
                              for the Dataproc Metastore instance.
                            items:
                              properties:
                                subnetwork:
                                  description: |-
                                    Immutable. The subnetwork of the customer project from which an IP
                                     address is reserved and used as the Dataproc Metastore service's
                                     endpoint. It is accessible to hosts in the subnet and to all
                                     hosts in a subnet in the same region and same network. There must
                                     be at least one IP address available in the subnet's primary range. The
                                     subnet is specified in the following form:

                                     `projects/{project_number}/regions/{region_id}/subnetworks/{subnetwork_id}`
                                  type: string
                              type: object
                            type: array
                        type: object
                      port:
                        description: 'The TCP port at which the metastore service
                          is reached. Default: 9083.'
                        format: int32
                        type: integer
                      releaseChannel:
                        description: Immutable. The release channel of the service.
                          If unspecified, defaults to `STABLE`.
                        type: string
                      scalingConfig:
                        description: Scaling configuration of the metastore service.
                        properties:
                          instanceSize:
                            description: An enum of readable instance sizes, with
                              each instance size mapping to a float value (e.g. InstanceSize.EXTRA_SMALL
                              = scaling_factor(0.1))
                            type: string
                          scalingFactor:
                            description: Scaling factor, increments of 0.1 for values
                              less than 1.0, and increments of 1.0 for values greater
                              than 1.0.
                            type: number
                        type: object
                      telemetryConfig:
                        description: The configuration specifying telemetry settings
                          for the Dataproc Metastore service. If unspecified defaults
                          to `JSON`.
                        properties:
                          logFormat:
                            description: The output format of the Dataproc Metastore
                              service's logs.
                            type: string
                        type: object
                      tier:
                        description: The tier of the service.
                        type: string
                    type: object
                  state:
                    description: Output only. The current state of the backup.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
