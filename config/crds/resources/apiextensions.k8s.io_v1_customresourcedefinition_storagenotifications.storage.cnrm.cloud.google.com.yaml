apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: storagenotifications.storage.cnrm.cloud.google.com
spec:
  group: storage.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: StorageNotification
    listKind: StorageNotificationList
    plural: storagenotifications
    singular: storagenotification
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: StorageNotification is the Schema for the StorageNotification
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StorageNotificationSpec defines the desired state of StorageNotification
            properties:
              bucketRef:
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The StorageBucket selfLink, when not managed by Config
                      Connector.
                    type: string
                  name:
                    description: The `name` field of a `StorageBucket` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `StorageBucket` resource.
                    type: string
                type: object
              customAttributes:
                additionalProperties:
                  type: string
                description: An optional list of additional attributes to attach to
                  each Cloud PubSub message published for this notification subscription.
                type: object
              eventTypes:
                description: If present, only send notifications about listed event
                  types. If empty, sent notifications for all event types.
                items:
                  type: string
                type: array
              objectNamePrefix:
                description: If present, only apply this notification configuration
                  to object names that begin with this prefix.
                type: string
              payloadFormat:
                description: The desired content of the Payload.
                type: string
              resourceID:
                description: The StorageNotification name. If not given, the metadata.name
                  will be used.
                type: string
              topicRef:
                description: 'The Cloud PubSub topic to which this subscription publishes.
                  Formatted as: ''//pubsub.googleapis.com/projects/{project-identifier}/topics/{my-topic}'''
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
            type: object
          status:
            description: StorageNotificationStatus defines the config connector machine
              state of StorageNotification
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the StorageNotification resource
                  in GCP.
                type: string
              notificationId:
                description: 'Deprecated: use the `.status.externalRef` to identify
                  the resource. The ID of the created notification.'
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  etag:
                    description: HTTP 1.1 [https://tools.ietf.org/html/rfc7232#section-2.3][Entity
                      tag] for this subscription notification.
                    type: string
                  id:
                    description: The ID of the notification.
                    type: string
                  topic:
                    description: 'The Cloud PubSub topic to which this subscription
                      publishes. Formatted as: ''//pubsub.googleapis.com/projects/{project-identifier}/topics/{my-topic}'''
                    type: string
                type: object
              selfLink:
                description: 'Deprecated: use the `.status.externalRef` to identify
                  the resource. The URI of the created resource.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
