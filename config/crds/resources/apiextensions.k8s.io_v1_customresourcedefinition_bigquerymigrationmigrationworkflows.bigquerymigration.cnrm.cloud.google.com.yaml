# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: bigquerymigrationmigrationworkflows.bigquerymigration.cnrm.cloud.google.com
spec:
  group: bigquerymigration.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: BigQueryMigrationMigrationWorkflow
    listKind: BigQueryMigrationMigrationWorkflowList
    plural: bigquerymigrationmigrationworkflows
    shortNames:
    - gcpbigquerymigrationmigrationworkflow
    - gcpbigquerymigrationmigrationworkflows
    singular: bigquerymigrationmigrationworkflow
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BigQueryMigrationMigrationWorkflow is the Schema for the BigQueryMigrationMigrationWorkflow
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BigQueryMigrationMigrationWorkflowSpec defines the desired
              state of BigQueryMigrationMigrationWorkflow
            properties:
              displayName:
                description: The display name of the workflow. This can be set to
                  give a workflow a descriptive name. There is no guarantee or enforcement
                  of uniqueness.
                type: string
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The BigQueryMigrationMigrationWorkflow name. If not given,
                  the metadata.name will be used.
                type: string
              tasks:
                additionalProperties:
                  properties:
                    assessmentTaskDetails:
                      description: Task configuration for Assessment.
                      properties:
                        dataSource:
                          description: 'Required. The data source or data warehouse
                            type (eg: TERADATA/REDSHIFT) from which the input data
                            is extracted.'
                          type: string
                        inputPath:
                          description: Required. The Cloud Storage path for assessment
                            input files.
                          type: string
                        outputDataset:
                          description: Required. The BigQuery dataset for output.
                          type: string
                        querylogsPath:
                          description: Optional. An optional Cloud Storage path to
                            write the query logs (which is then used as an input path
                            on the translation task)
                          type: string
                      type: object
                    createTime:
                      description: Time when the task was created.
                      type: string
                    details:
                      description: DEPRECATED! Use one of the task_details below.
                        The details of the task. The type URL must be one of the supported
                        task details messages and correspond to the Task's type.
                      properties:
                        typeURL:
                          description: |-
                            A URL/resource name that uniquely identifies the type of the serialized
                             protocol buffer message. This string must contain at least
                             one "/" character. The last segment of the URL's path must represent
                             the fully qualified name of the type (as in
                             `path/google.protobuf.Duration`). The name should be in a canonical form
                             (e.g., leading "." is not accepted).

                             In practice, teams usually precompile into the binary all types that they
                             expect it to use in the context of Any. However, for URLs which use the
                             scheme `http`, `https`, or no scheme, one can optionally set up a type
                             server that maps type URLs to message definitions as follows:

                             * If no scheme is provided, `https` is assumed.
                             * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                               value in binary format, or produce an error.
                             * Applications are allowed to cache lookup results based on the
                               URL, or have them precompiled into a binary to avoid any
                               lookup. Therefore, binary compatibility needs to be preserved
                               on changes to types. (Use versioned type names to manage
                               breaking changes.)

                             Note: this functionality is not currently available in the official
                             protobuf release, and it is not used for type URLs beginning with
                             type.googleapis.com.

                             Schemes other than `http`, `https` (or the empty scheme) might be
                             used with implementation specific semantics.
                          type: string
                        value:
                          description: Must be a valid serialized protocol buffer
                            of the above specified type.
                          format: byte
                          type: string
                      type: object
                    lastUpdateTime:
                      description: Time when the task was last updated.
                      type: string
                    translationTaskDetails:
                      description: Task configuration for Batch/Offline SQL Translation.
                      properties:
                        bteqOptions:
                          description: The BTEQ specific settings for the translation
                            task.
                          properties:
                            defaultPathURI:
                              description: The Cloud Storage location to be used as
                                the default path for files that are not otherwise
                                specified in the file replacement map.
                              type: string
                            fileReplacementMap:
                              additionalProperties:
                                type: string
                              description: Maps the local paths that are used in BTEQ
                                scripts (the keys) to the paths in Cloud Storage that
                                should be used in their stead in the translation (the
                                value).
                              type: object
                            projectDataset:
                              description: Specifies the project and dataset in BigQuery
                                that will be used for external table creation during
                                the translation.
                              properties:
                                datasetID:
                                  description: A unique ID for this dataset, without
                                    the project name. The ID must contain only letters
                                    (a-z, A-Z), numbers (0-9), or underscores (_).
                                    The maximum length is 1,024 characters.
                                  type: string
                                projectID:
                                  description: The ID of the project containing this
                                    dataset.
                                  type: string
                              type: object
                          type: object
                        fileEncoding:
                          description: The file encoding type.
                          type: string
                        filePaths:
                          description: Cloud Storage files to be processed for translation.
                          items:
                            properties:
                              inputPath:
                                description: The Cloud Storage path for a file to
                                  translation in a subtask.
                                type: string
                              outputPath:
                                description: The Cloud Storage path to write back
                                  the corresponding input file to.
                                type: string
                            type: object
                          type: array
                        filter:
                          description: The filter applied to translation details.
                          properties:
                            inputFileExclusionPrefixes:
                              description: The list of prefixes used to exclude processing
                                for input files.
                              items:
                                type: string
                              type: array
                          type: object
                        identifierSettings:
                          description: The settings for SQL identifiers.
                          properties:
                            identifierRewriteMode:
                              description: Specifies the rewrite mode for SQL identifiers.
                              type: string
                            outputIdentifierCase:
                              description: The setting to control output queries'
                                identifier case.
                              type: string
                          type: object
                        inputPath:
                          description: The Cloud Storage path for translation input
                            files.
                          type: string
                        outputPath:
                          description: The Cloud Storage path for translation output
                            files.
                          type: string
                        schemaPath:
                          description: The Cloud Storage path to DDL files as table
                            schema to assist semantic translation.
                          type: string
                        teradataOptions:
                          description: The Teradata SQL specific settings for the
                            translation task.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        translationExceptionTable:
                          description: Specifies the exact name of the bigquery table
                            ("dataset.table") to be used for surfacing raw translation
                            errors. If the table does not exist, we will create it.
                            If it already exists and the schema is the same, we will
                            re-use. If the table exists and the schema is different,
                            we will throw an error.
                          type: string
                      type: object
                    type:
                      description: The type of the task. This must be a supported
                        task type.
                      type: string
                  type: object
                description: The tasks in a workflow in a named map. The name (i.e.
                  key) has no meaning and is merely a convenient way to address a
                  specific task in a workflow.
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: BigQueryMigrationMigrationWorkflowStatus defines the config
              connector machine state of BigQueryMigrationMigrationWorkflow
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the BigQueryMigrationMigrationWorkflow
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Time when the workflow was created.
                    type: string
                  lastUpdateTime:
                    description: Time when the workflow was last updated.
                    type: string
                  name:
                    description: |-
                      Output only. Immutable. The unique identifier for the migration workflow. The ID is
                       server-generated.

                       Example: `projects/123/locations/us/workflows/345`
                    type: string
                  state:
                    description: Output only. That status of the workflow.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
