# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: dataplexdatascans.dataplex.cnrm.cloud.google.com
spec:
  group: dataplex.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DataplexDataScan
    listKind: DataplexDataScanList
    plural: dataplexdatascans
    shortNames:
    - gcpdataplexdatascan
    - gcpdataplexdatascans
    singular: dataplexdatascan
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataplexDataScan is the Schema for the DataplexDataScan API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DataplexDataScanSpec defines the desired state of DataplexDataScan
            properties:
              data:
                description: Required. The data source for DataScan.
                properties:
                  entityRef:
                    description: 'Immutable. The Dataplex entity that represents the
                      data source (e.g. BigQuery table) for DataScan, of the form:
                      `projects/{project_number}/locations/{location_id}/lakes/{lake_id}/zones/{zone_id}/entities/{entity_id}`.'
                    properties:
                      external:
                        description: A reference to an externally managed Dataplex
                          Entity. Should be of the format `projects/{projectID}/locations/{location}/lakes/{lakeID}/zones/{zoneID}/entities/{entityID}`.
                        type: string
                    type: object
                  resource:
                    description: 'Immutable. The service-qualified full resource name
                      of the cloud resource for a DataScan job to scan against. The
                      field could be: BigQuery table of type "TABLE" for DataProfileScan/DataQualityScan
                      Format: //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID'
                    type: string
                type: object
              dataDiscoverySpec:
                description: Settings for a data discovery scan.
                properties:
                  bigqueryPublishingConfig:
                    description: Optional. Configuration for metadata publishing.
                    properties:
                      connectionRef:
                        description: Optional. The BigQuery connection used to create
                          BigLake tables. Must be in the form `projects/{project_id}/locations/{location_id}/connections/{connection_id}`
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed BigQuery
                              Connection. Should be of the format `projects/{projectID}/locations/{location}/connections/{connectionID}`.
                            type: string
                          name:
                            description: The `name` of a `BigQueryConnectionConnection`
                              resource.
                            type: string
                          namespace:
                            description: The `namespace` of a `BigQueryConnectionConnection`
                              resource.
                            type: string
                        type: object
                      tableType:
                        description: Optional. Determines whether to  publish discovered
                          tables as BigLake external tables or non-BigLake external
                          tables.
                        type: string
                    type: object
                  storageConfig:
                    description: Cloud Storage related configurations.
                    properties:
                      csvOptions:
                        description: Optional. Configuration for CSV data.
                        properties:
                          delimiter:
                            description: Optional. The delimiter that is used to separate
                              values. The default is `,` (comma).
                            type: string
                          encoding:
                            description: Optional. The character encoding of the data.
                              The default is UTF-8.
                            type: string
                          headerRows:
                            description: Optional. The number of rows to interpret
                              as header rows that should be skipped when reading data
                              rows.
                            format: int32
                            type: integer
                          quote:
                            description: Optional. The character used to quote column
                              values. Accepts `"` (double quotation mark) or `'` (single
                              quotation mark). If unspecified, defaults to `"` (double
                              quotation mark).
                            type: string
                          typeInferenceDisabled:
                            description: Optional. Whether to disable the inference
                              of data types for CSV data. If true, all columns are
                              registered as strings.
                            type: boolean
                        type: object
                      excludePatterns:
                        description: Optional. Defines the data to exclude during
                          discovery. Provide a list of patterns that identify the
                          data to exclude. For Cloud Storage bucket assets, these
                          patterns are interpreted as glob patterns used to match
                          object names. For BigQuery dataset assets, these patterns
                          are interpreted as patterns to match table names.
                        items:
                          type: string
                        type: array
                      includePatterns:
                        description: Optional. Defines the data to include during
                          discovery when only a subset of the data should be considered.
                          Provide a list of patterns that identify the data to include.
                          For Cloud Storage bucket assets, these patterns are interpreted
                          as glob patterns used to match object names. For BigQuery
                          dataset assets, these patterns are interpreted as patterns
                          to match table names.
                        items:
                          type: string
                        type: array
                      jsonOptions:
                        description: Optional. Configuration for JSON data.
                        properties:
                          encoding:
                            description: Optional. The character encoding of the data.
                              The default is UTF-8.
                            type: string
                          typeInferenceDisabled:
                            description: Optional. Whether to disable the inference
                              of data types for JSON data. If true, all columns are
                              registered as their primitive types (strings, number,
                              or boolean).
                            type: boolean
                        type: object
                    type: object
                type: object
              dataProfileSpec:
                description: Settings for a data profile scan.
                properties:
                  excludeFields:
                    description: |-
                      Optional. The fields to exclude from data profile.

                       If specified, the fields will be excluded from data profile, regardless of
                       `include_fields` value.
                    properties:
                      fieldNames:
                        description: |-
                          Optional. Expected input is a list of fully qualified names of fields as
                           in the schema.

                           Only top-level field names for nested fields are supported.
                           For instance, if 'x' is of nested field type, listing 'x' is supported
                           but 'x.y.z' is not supported. Here 'y' and 'y.z' are nested fields of
                           'x'.
                        items:
                          type: string
                        type: array
                    type: object
                  includeFields:
                    description: |-
                      Optional. The fields to include in data profile.

                       If not specified, all fields at the time of profile scan job execution are
                       included, except for ones listed in `exclude_fields`.
                    properties:
                      fieldNames:
                        description: |-
                          Optional. Expected input is a list of fully qualified names of fields as
                           in the schema.

                           Only top-level field names for nested fields are supported.
                           For instance, if 'x' is of nested field type, listing 'x' is supported
                           but 'x.y.z' is not supported. Here 'y' and 'y.z' are nested fields of
                           'x'.
                        items:
                          type: string
                        type: array
                    type: object
                  postScanActions:
                    description: Optional. Actions to take upon job completion..
                    properties:
                      bigqueryExport:
                        description: Optional. If set, results will be exported to
                          the provided BigQuery table.
                        properties:
                          resultsTableRef:
                            description: 'Optional. The BigQuery table to export DataProfileScan
                              results to. Format: //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID
                              or projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID'
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: A reference to an externally managed
                                  BigQuery Table. Should be of the format `//bigquery.googleapis.com/projects/{projectID}/datasets/{datasetID}/tables/{tableID}`
                                  or `projects/{projectID}/datasets/{datasetID}/tables/{tableID}`.
                                type: string
                              name:
                                description: The `name` of a `BigQueryTable` resource.
                                type: string
                              namespace:
                                description: The `namespace` of a `BigQueryTable`
                                  resource.
                                type: string
                            type: object
                        type: object
                    type: object
                  rowFilter:
                    description: |-
                      Optional. A filter applied to all rows in a single DataScan job.
                       The filter needs to be a valid SQL expression for a [WHERE clause in
                       GoogleSQL
                       syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#where_clause).

                       Example: col1 >= 0 AND col2 < 10
                    type: string
                  samplingPercent:
                    description: |-
                      Optional. The percentage of the records to be selected from the dataset for
                       DataScan.

                       * Value can range between 0.0 and 100.0 with up to 3 significant decimal
                       digits.
                       * Sampling is not applied if `sampling_percent` is not specified, 0 or
                       100.
                    type: number
                type: object
              dataQualitySpec:
                description: Settings for a data quality scan.
                properties:
                  postScanActions:
                    description: Optional. Actions to take upon job completion.
                    properties:
                      bigqueryExport:
                        description: Optional. If set, results will be exported to
                          the provided BigQuery table.
                        properties:
                          resultsTableRef:
                            description: 'Optional. The BigQuery table to export DataQualityScan
                              results to. Format: //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID
                              or projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID'
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: A reference to an externally managed
                                  BigQuery Table. Should be of the format `//bigquery.googleapis.com/projects/{projectID}/datasets/{datasetID}/tables/{tableID}`
                                  or `projects/{projectID}/datasets/{datasetID}/tables/{tableID}`.
                                type: string
                              name:
                                description: The `name` of a `BigQueryTable` resource.
                                type: string
                              namespace:
                                description: The `namespace` of a `BigQueryTable`
                                  resource.
                                type: string
                            type: object
                        type: object
                      notificationReport:
                        description: Optional. If set, results will be sent to the
                          provided notification receipts upon triggers.
                        properties:
                          jobEndTrigger:
                            description: Optional. If set, report will be sent when
                              a scan job ends.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          jobFailureTrigger:
                            description: Optional. If set, report will be sent when
                              a scan job fails.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          recipients:
                            description: Required. The recipients who will receive
                              the notification report.
                            properties:
                              emails:
                                description: Optional. The email recipients who will
                                  receive the DataQualityScan results report.
                                items:
                                  type: string
                                type: array
                            type: object
                          scoreThresholdTrigger:
                            description: Optional. If set, report will be sent when
                              score threshold is met.
                            properties:
                              scoreThreshold:
                                description: Optional. The score range is in [0,100].
                                type: number
                            type: object
                        type: object
                    type: object
                  rowFilter:
                    description: |-
                      Optional. A filter applied to all rows in a single DataScan job.
                       The filter needs to be a valid SQL expression for a [WHERE clause in
                       GoogleSQL
                       syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#where_clause).

                       Example: col1 >= 0 AND col2 < 10
                    type: string
                  rules:
                    description: Required. The list of rules to evaluate against a
                      data source. At least one rule is required.
                    items:
                      properties:
                        column:
                          description: Optional. The unnested column which this rule
                            is evaluated against.
                          type: string
                        description:
                          description: |-
                            Optional. Description of the rule.

                             * The maximum length is 1,024 characters.
                          type: string
                        dimension:
                          description: Required. The dimension a rule belongs to.
                            Results are also aggregated at the dimension level. Supported
                            dimensions are **["COMPLETENESS", "ACCURACY", "CONSISTENCY",
                            "VALIDITY", "UNIQUENESS", "FRESHNESS", "VOLUME"]**
                          type: string
                        ignoreNull:
                          description: |-
                            Optional. Rows with `null` values will automatically fail a rule, unless
                             `ignore_null` is `true`. In that case, such `null` rows are trivially
                             considered passing.

                             This field is only valid for the following type of rules:

                             * RangeExpectation
                             * RegexExpectation
                             * SetExpectation
                             * UniquenessExpectation
                          type: boolean
                        name:
                          description: |-
                            Optional. A mutable name for the rule.

                             * The name must contain only letters (a-z, A-Z), numbers (0-9), or
                             hyphens (-).
                             * The maximum length is 63 characters.
                             * Must start with a letter.
                             * Must end with a number or a letter.
                          type: string
                        nonNullExpectation:
                          description: Row-level rule which evaluates whether each
                            column value is null.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        rangeExpectation:
                          description: Row-level rule which evaluates whether each
                            column value lies between a specified range.
                          properties:
                            maxValue:
                              description: Optional. The maximum column value allowed
                                for a row to pass this validation. At least one of
                                `min_value` and `max_value` need to be provided.
                              type: string
                            minValue:
                              description: Optional. The minimum column value allowed
                                for a row to pass this validation. At least one of
                                `min_value` and `max_value` need to be provided.
                              type: string
                            strictMaxEnabled:
                              description: |-
                                Optional. Whether each value needs to be strictly lesser than ('<') the
                                 maximum, or if equality is allowed.

                                 Only relevant if a `max_value` has been defined. Default = false.
                              type: boolean
                            strictMinEnabled:
                              description: |-
                                Optional. Whether each value needs to be strictly greater than ('>') the
                                 minimum, or if equality is allowed.

                                 Only relevant if a `min_value` has been defined. Default = false.
                              type: boolean
                          type: object
                        regexExpectation:
                          description: Row-level rule which evaluates whether each
                            column value matches a specified regex.
                          properties:
                            regex:
                              description: Optional. A regular expression the column
                                value is expected to match.
                              type: string
                          type: object
                        rowConditionExpectation:
                          description: Row-level rule which evaluates whether each
                            row in a table passes the specified condition.
                          properties:
                            sqlExpression:
                              description: Optional. The SQL expression.
                              type: string
                          type: object
                        setExpectation:
                          description: Row-level rule which evaluates whether each
                            column value is contained by a specified set.
                          properties:
                            values:
                              description: Optional. Expected values for the column
                                value.
                              items:
                                type: string
                              type: array
                          type: object
                        sqlAssertion:
                          description: Aggregate rule which evaluates the number of
                            rows returned for the provided statement. If any rows
                            are returned, this rule fails.
                          properties:
                            sqlStatement:
                              description: Optional. The SQL statement.
                              type: string
                          type: object
                        statisticRangeExpectation:
                          description: Aggregate rule which evaluates whether the
                            column aggregate statistic lies between a specified range.
                          properties:
                            maxValue:
                              description: |-
                                Optional. The maximum column statistic value allowed for a row to pass
                                 this validation.

                                 At least one of `min_value` and `max_value` need to be provided.
                              type: string
                            minValue:
                              description: |-
                                Optional. The minimum column statistic value allowed for a row to pass
                                 this validation.

                                 At least one of `min_value` and `max_value` need to be provided.
                              type: string
                            statistic:
                              description: Optional. The aggregate metric to evaluate.
                              type: string
                            strictMaxEnabled:
                              description: |-
                                Optional. Whether column statistic needs to be strictly lesser than ('<')
                                 the maximum, or if equality is allowed.

                                 Only relevant if a `max_value` has been defined. Default = false.
                              type: boolean
                            strictMinEnabled:
                              description: |-
                                Optional. Whether column statistic needs to be strictly greater than
                                 ('>') the minimum, or if equality is allowed.

                                 Only relevant if a `min_value` has been defined. Default = false.
                              type: boolean
                          type: object
                        suspended:
                          description: Optional. Whether the Rule is active or suspended.
                            Default is false.
                          type: boolean
                        tableConditionExpectation:
                          description: Aggregate rule which evaluates whether the
                            provided expression is true for a table.
                          properties:
                            sqlExpression:
                              description: Optional. The SQL expression.
                              type: string
                          type: object
                        threshold:
                          description: |-
                            Optional. The minimum ratio of **passing_rows / total_rows** required to
                             pass this rule, with a range of [0.0, 1.0].

                             0 indicates default value (i.e. 1.0).

                             This field is only valid for row-level type rules.
                          type: number
                        uniquenessExpectation:
                          description: Row-level rule which evaluates whether each
                            column value is unique.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type: array
                  samplingPercent:
                    description: |-
                      Optional. The percentage of the records to be selected from the dataset for
                       DataScan.

                       * Value can range between 0.0 and 100.0 with up to 3 significant decimal
                       digits.
                       * Sampling is not applied if `sampling_percent` is not specified, 0 or
                       100.
                    type: number
                type: object
              description:
                description: |-
                  Optional. Description of the scan.

                  * Must be between 1-1024 characters.
                type: string
              displayName:
                description: |-
                  Optional. User friendly display name.

                  * Must be between 1-256 characters.
                type: string
              executionSpec:
                description: |-
                  Optional. DataScan execution settings.

                  If not specified, the fields in it will use their default values.
                properties:
                  field:
                    description: |-
                      Immutable. The unnested field (of type *Date* or *Timestamp*) that
                       contains values which monotonically increase over time.

                       If not specified, a data scan will run for all data in the table.
                    type: string
                  trigger:
                    description: |-
                      Optional. Spec related to how often and when a scan should be triggered.

                       If not specified, the default is `OnDemand`, which means the scan will
                       not run until the user calls `RunDataScan` API.
                    properties:
                      onDemand:
                        description: The scan runs once via `RunDataScan` API.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      schedule:
                        description: The scan is scheduled to run periodically.
                        properties:
                          cron:
                            description: |-
                              Required. [Cron](https://en.wikipedia.org/wiki/Cron) schedule for running
                               scans periodically.

                               To explicitly set a timezone in the cron tab, apply a prefix in the
                               cron tab: **"CRON_TZ=${IANA_TIME_ZONE}"** or **"TZ=${IANA_TIME_ZONE}"**.
                               The **${IANA_TIME_ZONE}** may only be a valid string from IANA time zone
                               database
                               ([wikipedia](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List)).
                               For example, `CRON_TZ=America/New_York 1 * * * *`, or
                               `TZ=America/New_York 1 * * * *`.

                               This field is required for Schedule scans.
                            type: string
                        type: object
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Optional. User-defined labels for the scan.
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The DataplexDataScan name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - data
            - location
            - projectRef
            type: object
          status:
            description: DataplexDataScanStatus defines the config connector machine
              state of DataplexDataScan
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DataplexDataScan resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The time when the scan was created.
                    type: string
                  dataDiscoveryResult:
                    description: Output only. The result of a data discovery scan.
                    properties:
                      bigqueryPublishing:
                        description: Output only. Configuration for metadata publishing.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      scanStatistics:
                        description: Output only. Describes result statistics of a
                          data scan discovery job.
                        properties:
                          dataProcessedBytes:
                            description: The data processed in bytes.
                            format: int64
                            type: integer
                          filesExcluded:
                            description: The number of files excluded.
                            format: int32
                            type: integer
                          filesetsCreated:
                            description: The number of filesets created.
                            format: int32
                            type: integer
                          filesetsDeleted:
                            description: The number of filesets deleted.
                            format: int32
                            type: integer
                          filesetsUpdated:
                            description: The number of filesets updated.
                            format: int32
                            type: integer
                          scannedFileCount:
                            description: The number of files scanned.
                            format: int32
                            type: integer
                          tablesCreated:
                            description: The number of tables created.
                            format: int32
                            type: integer
                          tablesDeleted:
                            description: The number of tables deleted.
                            format: int32
                            type: integer
                          tablesUpdated:
                            description: The number of tables updated.
                            format: int32
                            type: integer
                        type: object
                    type: object
                  dataProfileResult:
                    description: Output only. The result of a data profile scan.
                    properties:
                      postScanActionsResult:
                        description: Output only. The result of post scan actions.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      profile:
                        description: The profile information per field.
                        properties:
                          fields:
                            description: List of fields with structural and profile
                              information for each field.
                            items:
                              properties:
                                mode:
                                  description: |-
                                    The mode of the field. Possible values include:

                                     * REQUIRED, if it is a required field.
                                     * NULLABLE, if it is an optional field.
                                     * REPEATED, if it is a repeated field.
                                  type: string
                                name:
                                  description: The name of the field.
                                  type: string
                                profile:
                                  description: Profile information for the corresponding
                                    field.
                                  properties:
                                    distinctRatio:
                                      description: Ratio of rows with distinct values
                                        against total scanned rows. Not available
                                        for complex non-groupable field type, including
                                        RECORD, ARRAY, GEOGRAPHY, and JSON, as well
                                        as fields with REPEATABLE mode.
                                      type: number
                                    doubleProfile:
                                      description: Double type field information.
                                      properties:
                                        average:
                                          description: Average of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          type: number
                                        max:
                                          description: Maximum of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          type: number
                                        min:
                                          description: Minimum of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          type: number
                                        quartiles:
                                          description: 'A quartile divides the number
                                            of data points into four parts, or quarters,
                                            of more-or-less equal size. Three main
                                            quartiles used are: The first quartile
                                            (Q1) splits off the lowest 25% of data
                                            from the highest 75%. It is also known
                                            as the lower or 25th empirical quartile,
                                            as 25% of the data is below this point.
                                            The second quartile (Q2) is the median
                                            of a data set. So, 50% of the data lies
                                            below this point. The third quartile (Q3)
                                            splits off the highest 25% of data from
                                            the lowest 75%. It is known as the upper
                                            or 75th empirical quartile, as 75% of
                                            the data lies below this point. Here,
                                            the quartiles is provided as an ordered
                                            list of quartile values for the scanned
                                            data, occurring in order Q1, median, Q3.'
                                          items:
                                            type: number
                                          type: array
                                        standardDeviation:
                                          description: Standard deviation of non-null
                                            values in the scanned data. NaN, if the
                                            field has a NaN.
                                          type: number
                                      type: object
                                    integerProfile:
                                      description: Integer type field information.
                                      properties:
                                        average:
                                          description: Average of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          type: number
                                        max:
                                          description: Maximum of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          format: int64
                                          type: integer
                                        min:
                                          description: Minimum of non-null values
                                            in the scanned data. NaN, if the field
                                            has a NaN.
                                          format: int64
                                          type: integer
                                        quartiles:
                                          description: 'A quartile divides the number
                                            of data points into four parts, or quarters,
                                            of more-or-less equal size. Three main
                                            quartiles used are: The first quartile
                                            (Q1) splits off the lowest 25% of data
                                            from the highest 75%. It is also known
                                            as the lower or 25th empirical quartile,
                                            as 25% of the data is below this point.
                                            The second quartile (Q2) is the median
                                            of a data set. So, 50% of the data lies
                                            below this point. The third quartile (Q3)
                                            splits off the highest 25% of data from
                                            the lowest 75%. It is known as the upper
                                            or 75th empirical quartile, as 75% of
                                            the data lies below this point. Here,
                                            the quartiles is provided as an ordered
                                            list of approximate quartile values for
                                            the scanned data, occurring in order Q1,
                                            median, Q3.'
                                          items:
                                            format: int64
                                            type: integer
                                          type: array
                                        standardDeviation:
                                          description: Standard deviation of non-null
                                            values in the scanned data. NaN, if the
                                            field has a NaN.
                                          type: number
                                      type: object
                                    nullRatio:
                                      description: Ratio of rows with null value against
                                        total scanned rows.
                                      type: number
                                    stringProfile:
                                      description: String type field information.
                                      properties:
                                        averageLength:
                                          description: Average length of non-null
                                            values in the scanned data.
                                          type: number
                                        maxLength:
                                          description: Maximum length of non-null
                                            values in the scanned data.
                                          format: int64
                                          type: integer
                                        minLength:
                                          description: Minimum length of non-null
                                            values in the scanned data.
                                          format: int64
                                          type: integer
                                      type: object
                                    topNValues:
                                      description: The list of top N non-null values,
                                        frequency and ratio with which they occur
                                        in the scanned data. N is 10 or equal to the
                                        number of distinct values in the field, whichever
                                        is smaller. Not available for complex non-groupable
                                        field type, including RECORD, ARRAY, GEOGRAPHY,
                                        and JSON, as well as fields with REPEATABLE
                                        mode.
                                      items:
                                        properties:
                                          count:
                                            description: Count of the corresponding
                                              value in the scanned data.
                                            format: int64
                                            type: integer
                                          ratio:
                                            description: Ratio of the corresponding
                                              value in the field against the total
                                              number of rows in the scanned data.
                                            type: number
                                          value:
                                            description: String value of a top N non-null
                                              value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type:
                                  description: The data type retrieved from the schema
                                    of the data source. For instance, for a BigQuery
                                    native table, it is the [BigQuery Table Schema](https://cloud.google.com/bigquery/docs/reference/rest/v2/tables#tablefieldschema).
                                    For a Dataplex Entity, it is the [Entity Schema](https://cloud.google.com/dataplex/docs/reference/rpc/google.cloud.dataplex.v1#type_3).
                                  type: string
                              type: object
                            type: array
                        type: object
                      rowCount:
                        description: The count of rows scanned.
                        format: int64
                        type: integer
                      scannedData:
                        description: The data scanned for this result.
                        properties:
                          incrementalField:
                            description: The range denoted by values of an incremental
                              field
                            properties:
                              end:
                                description: Value that marks the end of the range.
                                type: string
                              field:
                                description: The field that contains values which
                                  monotonically increases over time (e.g. a timestamp
                                  column).
                                type: string
                              start:
                                description: Value that marks the start of the range.
                                type: string
                            type: object
                        type: object
                    type: object
                  dataQualityResult:
                    description: Output only. The result of a data quality scan.
                    properties:
                      columns:
                        description: |-
                          Output only. A list of results at the column level.

                           A column will have a corresponding `DataQualityColumnResult` if and only if
                           there is at least one rule with the 'column' field set to it.
                        items:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: array
                      dimensions:
                        description: |-
                          Output only. A list of results at the dimension level.

                           A dimension will have a corresponding `DataQualityDimensionResult` if and
                           only if there is at least one rule with the 'dimension' field set to it.
                        items:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: array
                      passed:
                        description: Output only. Overall data quality result -- `true`
                          if all rules passed.
                        type: boolean
                      postScanActionsResult:
                        description: Output only. The result of post scan actions.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      rowCount:
                        description: Output only. The count of rows processed.
                        format: int64
                        type: integer
                      rules:
                        description: Output only. A list of all the rules in a job,
                          and their results.
                        items:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: array
                      scannedData:
                        description: Output only. The data scanned for this result.
                        properties:
                          incrementalField:
                            description: The range denoted by values of an incremental
                              field
                            properties:
                              end:
                                description: Value that marks the end of the range.
                                type: string
                              field:
                                description: The field that contains values which
                                  monotonically increases over time (e.g. a timestamp
                                  column).
                                type: string
                              start:
                                description: Value that marks the start of the range.
                                type: string
                            type: object
                        type: object
                      score:
                        description: |-
                          Output only. The overall data quality score.

                           The score ranges between [0, 100] (up to two decimal points).
                        type: number
                    type: object
                  executionStatus:
                    description: Output only. Status of the data scan execution.
                    properties:
                      latestJobCreateTime:
                        description: Optional. The time when the DataScanJob execution
                          was created.
                        type: string
                      latestJobEndTime:
                        description: Optional. The time when the latest DataScanJob
                          ended.
                        type: string
                      latestJobStartTime:
                        description: Optional. The time when the latest DataScanJob
                          started.
                        type: string
                    type: object
                  state:
                    description: Output only. Current state of the DataScan.
                    type: string
                  type:
                    description: Output only. The type of DataScan.
                    type: string
                  uid:
                    description: Output only. System generated globally unique ID
                      for the scan. This ID will be different if the scan is deleted
                      and re-created with the same name.
                    type: string
                  updateTime:
                    description: Output only. The time when the scan was last updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
