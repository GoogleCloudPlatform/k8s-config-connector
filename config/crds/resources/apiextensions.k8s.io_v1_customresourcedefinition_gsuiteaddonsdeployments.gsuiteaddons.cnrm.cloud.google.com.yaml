# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: gsuiteaddonsdeployments.gsuiteaddons.cnrm.cloud.google.com
spec:
  group: gsuiteaddons.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: GSuiteAddonsDeployment
    listKind: GSuiteAddonsDeploymentList
    plural: gsuiteaddonsdeployments
    shortNames:
    - gcpgsuiteaddonsdeployment
    - gcpgsuiteaddonsdeployments
    singular: gsuiteaddonsdeployment
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GSuiteAddonsDeployment is the Schema for the GSuiteAddonsDeployment
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GSuiteAddonsDeploymentSpec defines the desired state of GSuiteAddonsDeployment
            properties:
              addOns:
                description: The Google Workspace add-on configuration.
                properties:
                  calendar:
                    description: Calendar add-on configuration.
                    properties:
                      conferenceSolution:
                        description: Defines conference solutions provided by this
                          add-on.
                        items:
                          properties:
                            id:
                              description: Required. IDs should be unique across ConferenceSolutions
                                within one add-on, but this is not strictly enforced.
                                It is up to the add-on developer to assign them uniquely,
                                otherwise the wrong ConferenceSolution may be used
                                when the add-on is triggered. While the developer
                                may change the display name of an add-on, the ID should
                                not be changed.
                              type: string
                            logoURL:
                              description: Required. The URL for the logo image of
                                the ConferenceSolution.
                              type: string
                            name:
                              description: Required. The display name of the ConferenceSolution.
                              type: string
                            onCreateFunction:
                              description: Required. The endpoint to call when ConferenceData
                                should be created.
                              type: string
                          type: object
                        type: array
                      createSettingsURLFunction:
                        description: An endpoint to execute that creates a URL to
                          the add-on's settings page.
                        type: string
                      currentEventAccess:
                        description: Define the level of data access when an event
                          addon is triggered.
                        type: string
                      eventOpenTrigger:
                        description: An endpoint to trigger when an event is opened
                          (viewed/edited).
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      eventUpdateTrigger:
                        description: An endpoint to trigger when the open event is
                          updated.
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      homepageTrigger:
                        description: |-
                          Defines an endpoint that will be executed contexts that don't
                           match a declared contextual trigger. Any cards generated by this function
                           will always be available to the user, but may be eclipsed by contextual
                           content when this add-on declares more targeted triggers.

                           If present, this overrides the configuration from
                           `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                    type: object
                  common:
                    description: Configuration that is common across all Google Workspace
                      add-ons.
                    properties:
                      addOnWidgetSet:
                        description: The widgets used in the add-on. If this field
                          is not specified, it indicates that default set is used.
                        properties:
                          usedWidgets:
                            description: The list of widgets used in an add-on.
                            items:
                              type: string
                            type: array
                        type: object
                      homepageTrigger:
                        description: Defines an endpoint that will be executed in
                          any context, in any host. Any cards generated by this function
                          will always be available to the user, but may be eclipsed
                          by contextual content when this add-on declares more targeted
                          triggers.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      layoutProperties:
                        description: Common layout properties for the add-on cards.
                        properties:
                          primaryColor:
                            description: The primary color of the add-on. It sets
                              the color of toolbar. If no primary color is set explicitly,
                              the default value provided by the framework is used.
                            type: string
                          secondaryColor:
                            description: The secondary color of the add-on. It sets
                              the color of buttons. If primary color is set but no
                              secondary color is set, the secondary color is the same
                              as the primary color. If neither primary color nor secondary
                              color is set, the default value provided by the framework
                              is used.
                            type: string
                        type: object
                      logoURL:
                        description: Required. The URL for the logo image shown in
                          the add-on toolbar.
                        type: string
                      name:
                        description: Required. The display name of the add-on.
                        type: string
                      openLinkURLPrefixes:
                        description: An OpenLink action can only use a URL with an
                          HTTPS, MAILTO or TEL scheme.  For HTTPS links, the URL must
                          also [match](/gmail/add-ons/concepts/manifests#whitelisting_urls)
                          one of the prefixes specified in this whitelist. If the
                          prefix omits the scheme, HTTPS is assumed.  Notice that
                          HTTP links are automatically rewritten to HTTPS links.
                        properties:
                          values:
                            description: Repeated field of dynamically typed values.
                            items:
                              properties:
                                boolValue:
                                  description: Represents a boolean value.
                                  type: boolean
                                listValue:
                                  description: Represents a repeated `Value`.
                                nullValue:
                                  description: Represents a null value.
                                  type: string
                                numberValue:
                                  description: Represents a double value.
                                  type: number
                                stringValue:
                                  description: Represents a string value.
                                  type: string
                                structValue:
                                  description: Represents a structured value.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            type: array
                        type: object
                      universalActions:
                        description: Defines a list of extension points in the universal
                          action menu which serves as a setting menu for the add-on.
                          The extension point can be link URL to open or an endpoint
                          to execute as a form submission.
                        items:
                          properties:
                            label:
                              description: Required. User-visible text describing
                                the action taken by activating this extension point,
                                for example, "Add a new contact".
                              type: string
                            openLink:
                              description: URL to be opened by the UniversalAction.
                              type: string
                            runFunction:
                              description: Endpoint to be run by the UniversalAction.
                              type: string
                          type: object
                        type: array
                      useLocaleFromApp:
                        description: Whether to pass locale information from host
                          app.
                        type: boolean
                    type: object
                  docs:
                    description: Docs add-on configuration.
                    properties:
                      homepageTrigger:
                        description: If present, this overrides the configuration
                          from `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      onFileScopeGrantedTrigger:
                        description: Endpoint to execute when file scope authorization
                          is granted for this document/user pair.
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                    type: object
                  drive:
                    description: Drive add-on configuration.
                    properties:
                      homepageTrigger:
                        description: If present, this overrides the configuration
                          from `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      onItemsSelectedTrigger:
                        description: Corresponds to behvior that should execute when
                          items are selected in relevant Drive view (e.g. the My Drive
                          Doclist).
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                    type: object
                  gmail:
                    description: Gmail add-on configuration.
                    properties:
                      authorizationCheckFunction:
                        description: The name of an endpoint that verifies that the
                          add-on has all the required third-party authorizations,
                          by probing the third-party APIs. If the probe fails, the
                          function should throw an exception to initiate the authorization
                          flow. This function is called before each invocation of
                          the add-on, in order to ensure a smooth user experience.
                        type: string
                      composeTrigger:
                        description: Defines the compose time trigger for a compose
                          time add-on. This is the trigger that causes an add-on to
                          take action when the user is composing an email. All compose
                          time addons are required to have the gmail.addons.current.action.compose
                          scope even though it might not edit the draft.
                        properties:
                          actions:
                            description: Defines the set of actions for compose time
                              add-on. These are actions that user can trigger on a
                              compose time addon.
                            items:
                              properties:
                                label:
                                  description: Required. User-visible text describing
                                    the action taken by activating this extension
                                    point. For example, "Insert invoice".
                                  type: string
                                logoURL:
                                  description: |-
                                    The URL for the logo image shown in the add-on toolbar.

                                     If not set, defaults to the add-on's primary logo URL.
                                  type: string
                                runFunction:
                                  description: Required. The endpoint to execute when
                                    this extension point is activated.
                                  type: string
                              type: object
                            type: array
                          draftAccess:
                            description: Define the level of data access when a compose
                              time addon is triggered.
                            type: string
                        type: object
                      contextualTriggers:
                        description: Defines the set of conditions that trigger the
                          add-on.
                        items:
                          properties:
                            onTriggerFunction:
                              description: Required. The name of the endpoint to call
                                when a message matches the trigger.
                              type: string
                            unconditional:
                              description: UnconditionalTriggers are executed when
                                any mail message is opened.
                              type: object
                          type: object
                        type: array
                      homepageTrigger:
                        description: |-
                          Defines an endpoint that will be executed in contexts that don't
                           match a declared contextual trigger. Any cards generated by this function
                           will always be available to the user, but may be eclipsed by contextual
                           content when this add-on declares more targeted triggers.

                           If present, this overrides the configuration from
                           `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      universalActions:
                        description: Defines set of [universal actions](/gmail/add-ons/how-tos/universal-actions)
                          for the add-on. The user triggers universal actions from
                          the add-on toolbar menu.
                        items:
                          properties:
                            openLink:
                              description: A link that is opened by Gmail when the
                                user triggers the action.
                              type: string
                            runFunction:
                              description: An endpoint that is called when the user
                                triggers the action. See the [universal actions guide](/gmail/add-ons/how-tos/universal-actions)
                                for details.
                              type: string
                            text:
                              description: Required. User-visible text describing
                                the action, for example, "Add a new contact."
                              type: string
                          type: object
                        type: array
                    type: object
                  httpOptions:
                    description: Options for sending requests to add-on HTTP endpoints
                    properties:
                      authorizationHeader:
                        description: Configuration for the token sent in the HTTP
                          Authorization header
                        type: string
                    type: object
                  sheets:
                    description: Sheets add-on configuration.
                    properties:
                      homepageTrigger:
                        description: If present, this overrides the configuration
                          from `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      onFileScopeGrantedTrigger:
                        description: Endpoint to execute when file scope authorization
                          is granted for this document/user pair.
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                    type: object
                  slides:
                    description: Slides add-on configuration.
                    properties:
                      homepageTrigger:
                        description: If present, this overrides the configuration
                          from `addOns.common.homepageTrigger`.
                        properties:
                          enabled:
                            description: |-
                              Optional. If set to `false`, disable the home-page view in this context.

                               Defaults to `true` if unset.

                               If an add-ons custom home-page view is disabled, an autogenerated overview
                               card will be provided for users instead.
                            type: boolean
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                      onFileScopeGrantedTrigger:
                        description: Endpoint to execute when file scope authorization
                          is granted for this document/user pair.
                        properties:
                          runFunction:
                            description: Required. The endpoint to execute when this
                              extension point is activated.
                            type: string
                        type: object
                    type: object
                type: object
              oauthScopes:
                description: The list of Google OAuth scopes for which to request
                  consent from the end user before executing an add-on endpoint.
                items:
                  type: string
                type: array
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The GSuiteAddonsDeployment name. If not given, the metadata.name
                  will be used.
                type: string
            required:
            - projectRef
            type: object
          status:
            description: GSuiteAddonsDeploymentStatus defines the config connector
              machine state of GSuiteAddonsDeployment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the GSuiteAddonsDeployment resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  etag:
                    description: This value is computed by the server based on the
                      version of the deployment in storage, and may be sent on update
                      and delete requests to ensure the client has an up-to-date value
                      before proceeding.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
