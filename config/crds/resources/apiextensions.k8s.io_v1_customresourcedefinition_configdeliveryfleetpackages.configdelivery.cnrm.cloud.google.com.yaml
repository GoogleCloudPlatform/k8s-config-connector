# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: configdeliveryfleetpackages.configdelivery.cnrm.cloud.google.com
spec:
  group: configdelivery.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: ConfigDeliveryFleetPackage
    listKind: ConfigDeliveryFleetPackageList
    plural: configdeliveryfleetpackages
    shortNames:
    - gcpconfigdeliveryfleetpackage
    - gcpconfigdeliveryfleetpackages
    singular: configdeliveryfleetpackage
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigDeliveryFleetPackage is the Schema for the ConfigDeliveryFleetPackage
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConfigDeliveryFleetPackageSpec defines the desired state
              of ConfigDeliveryFleetPackage
            properties:
              deletionPropagationPolicy:
                description: Optional. Information around how to handle kubernetes
                  resources at the target clusters when the `FleetPackage` is deleted.
                type: string
              location:
                description: The location that this resource belongs to.
                type: string
                x-kubernetes-validations:
                - message: Location is immutable.
                  rule: self == oldSelf
              projectRef:
                description: The Project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceBundleSelector:
                description: Required. Information specifying the source of kubernetes
                  configuration to deploy.
                properties:
                  cloudBuildRepository:
                    description: Information specifying `CloudBuildRepository`.
                    properties:
                      path:
                        description: Optional. path to the directory or file within
                          the repository that contains the kubernetes configuration.
                          If unspecified, path is assumed to the top level root directory
                          of the repository.
                        type: string
                      repositoryRef:
                        description: Required. Name of the cloud build repository.
                          Format is projects/{p}/locations/{l}/connections/{c}/repositories/{r}.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed CloudBuildRepository
                              resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/connections/{{connectionID}}/repositories/{{RepositoryID}}".
                            type: string
                          name:
                            description: The name of a CloudBuildRepository resource.
                            type: string
                          namespace:
                            description: The namespace of a CloudBuildRepository resource.
                            type: string
                        type: object
                      serviceAccountRef:
                        description: Required. Google service account to use in CloudBuild
                          triggers to fetch and store kubernetes configuration.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: The `email` field of an `IAMServiceAccount`
                              resource.
                            type: string
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                            type: string
                          namespace:
                            description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                            type: string
                        type: object
                      tag:
                        description: Required. git tag of the underlying git repository.
                          The git tag must be in the semantic version format `vX.Y.Z`.
                        type: string
                      variantsPattern:
                        description: 'Optional. variants_pattern is a glob pattern
                          that will be used to find variants in the repository. Examples:
                          `variants/*.yaml`, `us-*`'
                        type: string
                    type: object
                  resourceBundle:
                    description: Information specifying `ResourceBundle`.
                    properties:
                      resourceBundleRef:
                        description: Required. Name of the `ResourceBundle`. Format
                          is projects/{p}/locations/{l}/resourceBundles/{r}.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed ConfigDeliveryResourceBundle
                              resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/resourceBundles/{{resourceBundleID}}".
                            type: string
                          name:
                            description: The name of a ConfigDeliveryResourceBundle
                              resource.
                            type: string
                          namespace:
                            description: The namespace of a ConfigDeliveryResourceBundle
                              resource.
                            type: string
                        type: object
                      tag:
                        description: Required. Tag refers to a version of the release
                          in a `ResourceBundle`. This is a Git tag in the semantic
                          version format `vX.Y.Z`.
                        type: string
                    type: object
                type: object
              resourceID:
                description: The ConfigDeliveryFleetPackage name. If not given, the
                  metadata.name will be used.
                type: string
              rolloutStrategy:
                description: Optional. The strategy to use to deploy kubernetes configuration
                  to clusters.
                properties:
                  allAtOnce:
                    description: AllAtOnceStrategy causes all clusters to be updated
                      concurrently.
                    type: object
                  rolling:
                    description: RollingStrategy causes a specified number of clusters
                      to be updated concurrently until all clusters are updated.
                    properties:
                      maxConcurrent:
                        description: Optional. Maximum number of clusters to update
                          the resource bundle on concurrently.
                        format: int32
                        type: integer
                    type: object
                type: object
              state:
                description: Optional. The desired state of the fleet package.
                type: string
              target:
                description: Optional. Configuration to select target clusters to
                  deploy kubernetes configuration to.
                properties:
                  fleet:
                    description: The GKE fleet information.
                    properties:
                      projectRef:
                        description: Required. The host project for the GKE fleet.
                          Format is `projects/{project}`.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: The `projectID` field of a project, when
                              not managed by Config Connector.
                            type: string
                          kind:
                            description: The kind of the Project resource; optional
                              but must be `Project` if provided.
                            type: string
                          name:
                            description: The `name` field of a `Project` resource.
                            type: string
                          namespace:
                            description: The `namespace` field of a `Project` resource.
                            type: string
                        type: object
                      selector:
                        description: Optional. selector allows targeting a subset
                          of fleet members using their labels.
                        properties:
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: Optional. match_labels is a map of {key,value}
                              pairs. Each {key,value} pair must match an existing
                              label key and value exactly in order to satisfy the
                              match.
                            type: object
                        type: object
                    type: object
                type: object
              variantSelector:
                description: Required. Information specifying how to map a `ResourceBundle`
                  variant to a target cluster.
                properties:
                  variantNameTemplate:
                    description: |-
                      Required. variant_name_template is a template that can refer to
                       variables containing cluster membership metadata such as location,
                       name, and labels to generate the name of the variant for a target
                       cluster. The variable syntax is similar to the unix shell variables.

                       Available variables are `${membership.name}`, `${membership.location}`,
                       `${membership.project}` and `${membership.labels['label_name']}`.

                       If you want to deploy a specific variant, say "default" to all the
                       clusters, you can use "default" (string without any variables) as
                       the variant_name_template.
                    type: string
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: ConfigDeliveryFleetPackageStatus defines the config connector
              machine state of ConfigDeliveryFleetPackage
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the ConfigDeliveryFleetPackage
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time at which the `FleetPackage` was
                      created.
                    type: string
                  info:
                    description: Output only. Information containing the rollout status
                      of the `FleetPackage` across all the target clusters.
                    properties:
                      activeRollout:
                        description: Optional. The active rollout, if any. Format
                          is `projects/{project}/locations/{location}/fleetPackages/{fleet_package}/rollouts/{rollout}`.
                        type: string
                      errors:
                        description: Optional. Output only. Errors encountered during
                          configuration deployment (if any).
                        items:
                          properties:
                            errorMessage:
                              description: Optional. A description of the error.
                              type: string
                          type: object
                        type: array
                      lastCompletedRollout:
                        description: Optional. The last completed rollout, if any.
                          Format is `projects/{project}/locations/{location}/fleetPackages/{fleet_package}/rollouts/{rollout}`.
                        type: string
                      state:
                        description: Optional. Output only. The current state of the
                          `FleetPackage`.
                        type: string
                    type: object
                  updateTime:
                    description: Output only. Most recent time at which the `FleetPackage`
                      was updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
