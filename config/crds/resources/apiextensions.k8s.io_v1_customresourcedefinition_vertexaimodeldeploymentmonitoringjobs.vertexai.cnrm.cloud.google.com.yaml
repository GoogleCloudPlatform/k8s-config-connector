# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaimodeldeploymentmonitoringjobs.vertexai.cnrm.cloud.google.com
spec:
  group: vertexai.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIModelDeploymentMonitoringJob
    listKind: VertexAIModelDeploymentMonitoringJobList
    plural: vertexaimodeldeploymentmonitoringjobs
    shortNames:
    - gcpvertexaimodeldeploymentmonitoringjob
    - gcpvertexaimodeldeploymentmonitoringjobs
    singular: vertexaimodeldeploymentmonitoringjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIModelDeploymentMonitoringJob is the Schema for the VertexAIModelDeploymentMonitoringJob
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIModelDeploymentMonitoringJobSpec defines the desired
              state of VertexAIModelDeploymentMonitoringJob
            properties:
              analysisInstanceSchemaURI:
                description: |-
                  YAML schema file uri describing the format of a single instance that you
                  want Tensorflow Data Validation (TFDV) to analyze.

                  If this field is empty, all the feature data types are inferred from
                  [predict_instance_schema_uri][google.cloud.aiplatform.v1beta1.ModelDeploymentMonitoringJob.predict_instance_schema_uri],
                  meaning that TFDV will use the data in the exact format(data type) as
                  prediction request/response.
                  If there are any data type differences between predict instance and TFDV
                  instance, this field can be used to override the schema.
                  For models trained with Vertex AI, this field must be set as all the
                  fields in predict instance formatted as string.
                type: string
              displayName:
                description: Required. The user-defined name of the ModelDeploymentMonitoringJob.
                  The name can be up to 128 characters long and can consist of any
                  UTF-8 characters. Display name of a ModelDeploymentMonitoringJob.
                type: string
              enableMonitoringPipelineLogs:
                description: If true, the scheduled monitoring pipeline logs are sent
                  to Google Cloud Logging, including pipeline status and anomalies
                  detected. Please note the logs incur cost, which are subject to
                  [Cloud Logging pricing](https://cloud.google.com/logging#pricing).
                type: boolean
              encryptionSpec:
                description: Customer-managed encryption key spec for a ModelDeploymentMonitoringJob.
                  If set, this ModelDeploymentMonitoringJob and all sub-resources
                  of this ModelDeploymentMonitoringJob will be secured by this key.
                properties:
                  kmsKeyRef:
                    description: Required. The Cloud KMS resource identifier of the
                      customer managed encryption key used to protect a resource.
                      The key needs to be in the same region as where the compute
                      resource is created.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed KMSCryptoKey.
                          Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
                        type: string
                      name:
                        description: The `name` of a `KMSCryptoKey` resource.
                        type: string
                      namespace:
                        description: The `namespace` of a `KMSCryptoKey` resource.
                        type: string
                    type: object
                required:
                - kmsKeyRef
                type: object
              endpointRef:
                description: 'Required. Endpoint resource name. Format: `projects/{project}/locations/{location}/endpoints/{endpoint}`'
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed VertexAIEndpoint
                      resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/endpoints/{{endpoint}}"
                    type: string
                  name:
                    description: The name of a VertexAIEndpoint resource.
                    type: string
                  namespace:
                    description: The namespace of a VertexAIEndpoint resource.
                    type: string
                type: object
              labels:
                additionalProperties:
                  type: string
                description: |-
                  The labels with user-defined metadata to organize your
                  ModelDeploymentMonitoringJob.

                  Label keys and values can be no longer than 64 characters
                  (Unicode codepoints), can only contain lowercase letters, numeric
                  characters, underscores and dashes. International characters are allowed.

                  See https://goo.gl/xmQnxf for more information and examples of labels.
                type: object
              location:
                description: The location of this resource.
                type: string
              logTTL:
                description: 'The TTL of BigQuery tables in user projects which stores
                  logs. A day is the basic unit of the TTL and we take the ceil of
                  TTL/86400(a day). e.g. { second: 3600} indicates ttl = 1 day.'
                type: string
              loggingSamplingStrategy:
                description: Required. Sample Strategy for logging.
                properties:
                  randomSampleConfig:
                    description: Random sample config. Will support more sampling
                      strategies later.
                    properties:
                      sampleRate:
                        description: Sample rate (0, 1]
                        type: number
                    type: object
                type: object
              modelDeploymentMonitoringObjectiveConfigs:
                description: Required. The config for monitoring objectives. This
                  is a per DeployedModel config. Each DeployedModel needs to be configured
                  separately.
                items:
                  properties:
                    deployedModelID:
                      description: The DeployedModel ID of the objective config.
                      type: string
                    objectiveConfig:
                      description: The objective config of for the modelmonitoring
                        job of this deployed model.
                      properties:
                        explanationConfig:
                          description: The config for integrating with Vertex Explainable
                            AI.
                          properties:
                            enableFeatureAttributes:
                              description: If want to analyze the Vertex Explainable
                                AI feature attribute scores or not. If set to true,
                                Vertex AI will log the feature attributions from explain
                                response and do the skew/drift detection for them.
                              type: boolean
                            explanationBaseline:
                              description: Predictions generated by the BatchPredictionJob
                                using baseline dataset.
                              properties:
                                bigquery:
                                  description: BigQuery location for BatchExplain
                                    output.
                                  properties:
                                    outputURI:
                                      description: |-
                                        Required. BigQuery URI to a project or table, up to 2000 characters long.

                                         When only the project is specified, the Dataset and Table is created.
                                         When the full table reference is specified, the Dataset must exist and
                                         table must not exist.

                                         Accepted forms:

                                         *  BigQuery path. For example:
                                         `bq://projectId` or `bq://projectId.bqDatasetId` or
                                         `bq://projectId.bqDatasetId.bqTableId`.
                                      type: string
                                  type: object
                                gcs:
                                  description: Cloud Storage location for BatchExplain
                                    output.
                                  properties:
                                    outputURIPrefix:
                                      description: Required. Google Cloud Storage
                                        URI to output directory. If the uri doesn't
                                        end with '/', a '/' will be automatically
                                        appended. The directory is created if it doesn't
                                        exist.
                                      type: string
                                  type: object
                                predictionFormat:
                                  description: The storage format of the predictions
                                    generated BatchPrediction job.
                                  type: string
                              type: object
                          type: object
                        predictionDriftDetectionConfig:
                          description: The config for drift of prediction data.
                          properties:
                            defaultDriftThreshold:
                              description: Drift anomaly detection threshold used
                                by all features. When the per-feature thresholds are
                                not set, this field can be used to specify a threshold
                                for all features.
                              properties:
                                value:
                                  description: 'Specify a threshold value that can
                                    trigger the alert. If this threshold config is
                                    for feature distribution distance: 1. For categorical
                                    feature, the distribution distance is calculated
                                    by L-inifinity norm. 2. For numerical feature,
                                    the distribution distance is calculated by Jensen–Shannon
                                    divergence. Each feature must have a non-zero
                                    threshold if they need to be monitored. Otherwise
                                    no alert will be triggered for that feature.'
                                  type: number
                              type: object
                          type: object
                        trainingDataset:
                          description: Training dataset for models. This field has
                            to be set only if TrainingPredictionSkewDetectionConfig
                            is specified.
                          properties:
                            bigquerySource:
                              description: The BigQuery table of the unmanaged Dataset
                                used to train this Model.
                              properties:
                                inputURI:
                                  description: |-
                                    Required. BigQuery URI to a table, up to 2000 characters long.
                                     Accepted forms:

                                     *  BigQuery path. For example: `bq://projectId.bqDatasetId.bqTableId`.
                                  type: string
                              type: object
                            dataFormat:
                              description: |-
                                Data format of the dataset, only applicable if the input is from
                                 Google Cloud Storage.
                                 The possible formats are:

                                 "tf-record"
                                 The source file is a TFRecord file.

                                 "csv"
                                 The source file is a CSV file.
                                 "jsonl"
                                 The source file is a JSONL file.
                              type: string
                            dataset:
                              description: The resource name of the Dataset used to
                                train this Model.
                              type: string
                            gcsSource:
                              description: The Google Cloud Storage uri of the unmanaged
                                Dataset used to train this Model.
                              properties:
                                uris:
                                  description: Required. Google Cloud Storage URI(-s)
                                    to the input file(s). May contain wildcards. For
                                    more information on wildcards, see https://cloud.google.com/storage/docs/wildcards.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            loggingSamplingStrategy:
                              description: Strategy to sample data from Training Dataset.
                                If not set, we process the whole dataset.
                              properties:
                                randomSampleConfig:
                                  description: Random sample config. Will support
                                    more sampling strategies later.
                                  properties:
                                    sampleRate:
                                      description: Sample rate (0, 1]
                                      type: number
                                  type: object
                              type: object
                            targetField:
                              description: The target field name the model is to predict.
                                This field will be excluded when doing Predict and
                                (or) Explain for the training data.
                              type: string
                          type: object
                        trainingPredictionSkewDetectionConfig:
                          description: The config for skew between training data and
                            prediction data.
                          properties:
                            defaultSkewThreshold:
                              description: Skew anomaly detection threshold used by
                                all features. When the per-feature thresholds are
                                not set, this field can be used to specify a threshold
                                for all features.
                              properties:
                                value:
                                  description: 'Specify a threshold value that can
                                    trigger the alert. If this threshold config is
                                    for feature distribution distance: 1. For categorical
                                    feature, the distribution distance is calculated
                                    by L-inifinity norm. 2. For numerical feature,
                                    the distribution distance is calculated by Jensen–Shannon
                                    divergence. Each feature must have a non-zero
                                    threshold if they need to be monitored. Otherwise
                                    no alert will be triggered for that feature.'
                                  type: number
                              type: object
                          type: object
                      type: object
                  type: object
                type: array
              modelDeploymentMonitoringScheduleConfig:
                description: Required. Schedule config for running the monitoring
                  job.
                properties:
                  monitorInterval:
                    description: Required. The model monitoring job scheduling interval.
                      It will be rounded up to next full hour. This defines how often
                      the monitoring jobs are triggered.
                    type: string
                  monitorWindow:
                    description: The time window of the prediction data being included
                      in each prediction dataset. This window specifies how long the
                      data should be collected from historical model results for each
                      run. If not set, [ModelDeploymentMonitoringScheduleConfig.monitor_interval][google.cloud.aiplatform.v1beta1.ModelDeploymentMonitoringScheduleConfig.monitor_interval]
                      will be used. e.g. If currently the cutoff time is 2022-01-08
                      14:30:00 and the monitor_window is set to be 3600, then data
                      from 2022-01-08 13:30:00 to 2022-01-08 14:30:00 will be retrieved
                      and aggregated to calculate the monitoring statistics.
                    type: string
                type: object
              modelMonitoringAlertConfig:
                description: Alert config for model monitoring.
                properties:
                  emailAlertConfig:
                    description: Email alert config.
                    properties:
                      userEmails:
                        description: The email addresses to send the alert.
                        items:
                          type: string
                        type: array
                    type: object
                  enableLogging:
                    description: Dump the anomalies to Cloud Logging. The anomalies
                      will be put to json payload encoded from proto [google.cloud.aiplatform.logging.ModelMonitoringAnomaliesLogEntry][].
                      This can be further sinked to Pub/Sub or any other services
                      supported by Cloud Logging.
                    type: boolean
                  notificationChannels:
                    description: Resource names of the NotificationChannels to send
                      alert. Must be of the format `projects/<project_id_or_number>/notificationChannels/<channel_id>`
                    items:
                      type: string
                    type: array
                type: object
              predictInstanceSchemaURI:
                description: YAML schema file uri describing the format of a single
                  instance, which are given to format this Endpoint's prediction (and
                  explanation). If not set, we will generate predict schema from collected
                  predict requests.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAIModelDeploymentMonitoringJob name. If not
                  given, the metadata.name will be used.
                type: string
              samplePredictInstance:
                description: Sample Predict instance, same format as [PredictRequest.instances][google.cloud.aiplatform.v1beta1.PredictRequest.instances],
                  this can be set as a replacement of [ModelDeploymentMonitoringJob.predict_instance_schema_uri][google.cloud.aiplatform.v1beta1.ModelDeploymentMonitoringJob.predict_instance_schema_uri].
                  If not set, we will generate predict schema from collected predict
                  requests.
                x-kubernetes-preserve-unknown-fields: true
              statsAnomaliesBaseDirectory:
                description: Stats anomalies base folder path.
                properties:
                  outputURIPrefix:
                    description: Required. Google Cloud Storage URI to output directory.
                      If the uri doesn't end with '/', a '/' will be automatically
                      appended. The directory is created if it doesn't exist.
                    type: string
                type: object
            required:
            - displayName
            - endpointRef
            - location
            - loggingSamplingStrategy
            - modelDeploymentMonitoringObjectiveConfigs
            - modelDeploymentMonitoringScheduleConfig
            - projectRef
            type: object
          status:
            description: VertexAIModelDeploymentMonitoringJobStatus defines the config
              connector machine state of VertexAIModelDeploymentMonitoringJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIModelDeploymentMonitoringJob
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  bigqueryTables:
                    description: 'Output only. The created bigquery tables for the
                      job under customer project. Customer could do their own query
                      & analysis. There could be 4 log tables in maximum: 1. Training
                      data logging predict request/response 2. Serving data logging
                      predict request/response'
                    items:
                      properties:
                        bigqueryTablePath:
                          description: 'The created BigQuery table to store logs.
                            Customer could do their own query & analysis. Format:
                            `bq://<project_id>.model_deployment_monitoring_<endpoint_id>.<tolower(log_source)>_<tolower(log_type)>`'
                          type: string
                        logSource:
                          description: The source of log.
                          type: string
                        logType:
                          description: The type of log.
                          type: string
                      type: object
                    type: array
                  createTime:
                    description: Output only. Timestamp when this ModelDeploymentMonitoringJob
                      was created.
                    type: string
                  error:
                    description: Output only. Only populated when the job's state
                      is `JOB_STATE_FAILED` or `JOB_STATE_CANCELLED`.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  latestMonitoringPipelineMetadata:
                    description: Output only. Latest triggered monitoring pipeline
                      metadata.
                    properties:
                      runTime:
                        description: The time that most recent monitoring pipelines
                          that is related to this run.
                        type: string
                      status:
                        description: The status of the most recent monitoring pipeline.
                        properties:
                          code:
                            description: The status code, which should be an enum
                              value of [google.rpc.Code][google.rpc.Code].
                            format: int32
                            type: integer
                          message:
                            description: A developer-facing error message, which should
                              be in English. Any user-facing error message should
                              be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                              field, or localized by the client.
                            type: string
                        type: object
                    type: object
                  nextScheduleTime:
                    description: Output only. Timestamp when this monitoring pipeline
                      will be scheduled to run for the next round.
                    type: string
                  satisfiesPzi:
                    description: Output only. Reserved for future use.
                    type: boolean
                  satisfiesPzs:
                    description: Output only. Reserved for future use.
                    type: boolean
                  scheduleState:
                    description: Output only. Schedule state when the monitoring job
                      is in Running state.
                    type: string
                  state:
                    description: Output only. The detailed state of the monitoring
                      job. When the job is still creating, the state will be 'PENDING'.
                      Once the job is successfully created, the state will be 'RUNNING'.
                      Pause the job, the state will be 'PAUSED'. Resume the job, the
                      state will return to 'RUNNING'.
                    type: string
                  updateTime:
                    description: Output only. Timestamp when this ModelDeploymentMonitoringJob
                      was updated most recently.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
