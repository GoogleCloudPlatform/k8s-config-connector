# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: hypercomputeclusterclusters.hypercomputecluster.cnrm.cloud.google.com
spec:
  group: hypercomputecluster.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: HypercomputeClusterCluster
    listKind: HypercomputeClusterClusterList
    plural: hypercomputeclusterclusters
    shortNames:
    - gcphypercomputeclustercluster
    - gcphypercomputeclusterclusters
    singular: hypercomputeclustercluster
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HypercomputeClusterCluster is the Schema for the HypercomputeClusterCluster
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HypercomputeClusterClusterSpec defines the desired state
              of HypercomputeClusterCluster
            properties:
              computeResources:
                additionalProperties:
                  properties:
                    config:
                      description: Required. Immutable. Configuration for this compute
                        resource, which describes how it should be created at runtime.
                      properties:
                        newFlexStartInstances:
                          description: Optional. Immutable. If set, indicates that
                            this resource should use flex-start VMs.
                          properties:
                            machineType:
                              description: Required. Immutable. Name of the Compute
                                Engine machine type to use, e.g. `n2-standard-2`.
                              type: string
                            maxDuration:
                              description: Required. Immutable. Specifies the time
                                limit for created instances. Instances will be terminated
                                at the end of this duration.
                              type: string
                            zone:
                              description: Required. Immutable. Name of the zone in
                                which VM instances should run, e.g., `us-central1-a`.
                                Must be in the same region as the cluster, and must
                                match the zone of any other resources specified in
                                the cluster.
                              type: string
                          type: object
                        newOnDemandInstances:
                          description: Optional. Immutable. If set, indicates that
                            this resource should use on-demand VMs.
                          properties:
                            machineType:
                              description: Required. Immutable. Name of the Compute
                                Engine machine type to use, e.g. `n2-standard-2`.
                              type: string
                            zone:
                              description: Required. Immutable. Name of the zone in
                                which VM instances should run, e.g., `us-central1-a`.
                                Must be in the same region as the cluster, and must
                                match the zone of any other resources specified in
                                the cluster.
                              type: string
                          type: object
                        newReservedInstances:
                          description: Optional. Immutable. If set, indicates that
                            this resource should use reserved VMs.
                          properties:
                            reservation:
                              description: Optional. Immutable. Name of the reservation
                                from which VM instances should be created, in the
                                format `projects/{project}/zones/{zone}/reservations/{reservation}`.
                              type: string
                          type: object
                        newSpotInstances:
                          description: Optional. Immutable. If set, indicates that
                            this resource should use spot VMs.
                          properties:
                            machineType:
                              description: Required. Immutable. Name of the Compute
                                Engine machine type to use, e.g. `n2-standard-2`.
                              type: string
                            terminationAction:
                              description: Optional. Termination action for the instance.
                                If not specified, Compute Engine sets the termination
                                action to DELETE.
                              type: string
                            zone:
                              description: Required. Immutable. Name of the zone in
                                which VM instances should run, e.g., `us-central1-a`.
                                Must be in the same region as the cluster, and must
                                match the zone of any other resources specified in
                                the cluster.
                              type: string
                          type: object
                      type: object
                  type: object
                description: Optional. Compute resource configuration for the cluster.
                type: object
              description:
                description: Optional. User-provided description of the cluster.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Optional. Labels applied to the cluster. Labels can be
                  used to organize clusters and to filter them in queries.
                type: object
              location:
                description: The location that this resource belongs to.
                type: string
                x-kubernetes-validations:
                - message: Location is immutable.
                  rule: self == oldSelf
              networkResources:
                additionalProperties:
                  properties:
                    config:
                      description: Immutable. Configuration for this network resource,
                        which describes how it should be created or imported.
                      properties:
                        existingNetwork:
                          description: Optional. Immutable. If set, indicates that
                            an existing network should be imported.
                          properties:
                            network:
                              description: Required. Immutable. Name of the network
                                to import, in the format `projects/{project}/global/networks/{network}`.
                              type: string
                            subnetwork:
                              description: Required. Immutable. Particular subnetwork
                                to use, in the format `projects/{project}/regions/{region}/subnetworks/{subnetwork}`.
                              type: string
                          type: object
                        newNetwork:
                          description: Optional. Immutable. If set, indicates that
                            a new network should be created.
                          properties:
                            description:
                              description: Optional. Immutable. Description of the
                                network. Maximum of 2048 characters.
                              type: string
                            network:
                              description: Required. Immutable. Name of the network
                                to create, in the format `projects/{project}/global/networks/{network}`.
                              type: string
                          type: object
                      type: object
                    network:
                      description: Reference to a network in Google Compute Engine.
                      type: object
                  type: object
                description: Optional. Network resource configuration for the cluster.
                type: object
              orchestrator:
                description: Optional. Orchestrator that is responsible for scheduling
                  and running jobs on the cluster.
                properties:
                  slurm:
                    description: Optional. If set, indicates that the cluster should
                      use Slurm as the orchestrator.
                    properties:
                      defaultPartition:
                        description: Optional. Default partition to use for submitted
                          jobs that do not explicitly specify a partition.
                        type: string
                      epilogBashScripts:
                        description: Optional. Slurm epilog scripts, which will be
                          executed by compute nodes whenever a node finishes running
                          a job.
                        items:
                          type: string
                        type: array
                      loginNodes:
                        description: Required. Configuration for login nodes, which
                          allow users to access the cluster over SSH.
                        properties:
                          bootDisk:
                            description: Optional. Boot disk for the login node.
                            properties:
                              sizeGB:
                                description: Required. Immutable. Size of the disk
                                  in gigabytes. Must be at least 10GB.
                                format: int64
                                type: integer
                              type:
                                description: Required. Immutable. Persistent disk
                                  type.
                                type: string
                            type: object
                          count:
                            description: Required. Number of login node instances
                              to create.
                            format: int64
                            type: integer
                          enableOSLogin:
                            description: Optional. Whether OS Login should be enabled
                              on login node instances.
                            type: boolean
                          enablePublicIps:
                            description: Optional. Whether login node instances should
                              be assigned external IP addresses.
                            type: boolean
                          labels:
                            additionalProperties:
                              type: string
                            description: Optional. Labels that should be applied to
                              each login node instance.
                            type: object
                          machineType:
                            description: Required. Name of the Compute Engine machine
                              type to use for login nodes, e.g. `n2-standard-2`.
                            type: string
                          startupScript:
                            description: Optional. Startup script to be run on each
                              login node instance. Max 256KB.
                            type: string
                          storageConfigs:
                            description: Optional. How storage resources should be
                              mounted on each login node.
                            items:
                              properties:
                                id:
                                  description: Required. ID of the storage resource
                                    to mount, which must match a key in the cluster's
                                    storage_resources.
                                  type: string
                                localMount:
                                  description: Required. A directory inside the VM
                                    instance's file system where the storage resource
                                    should be mounted (e.g., `/mnt/share`).
                                  type: string
                              type: object
                            type: array
                          zone:
                            description: Required. Name of the zone in which login
                              nodes should run, e.g., `us-central1-a`. Must be in
                              the same region as the cluster, and must match the zone
                              of any other resources specified in the cluster.
                            type: string
                        type: object
                      nodeSets:
                        description: Optional. Compute resource configuration for
                          the Slurm nodesets in your cluster. If not specified, the
                          cluster won't create any nodes.
                        items:
                          properties:
                            computeID:
                              description: Optional. ID of the compute resource on
                                which this nodeset will run.
                              type: string
                            computeInstance:
                              description: Optional. If set, indicates that the nodeset
                                should be backed by Compute Engine instances.
                              properties:
                                bootDisk:
                                  description: Optional. Boot disk for the compute
                                    instance
                                  properties:
                                    sizeGB:
                                      description: Required. Immutable. Size of the
                                        disk in gigabytes. Must be at least 10GB.
                                      format: int64
                                      type: integer
                                    type:
                                      description: Required. Immutable. Persistent
                                        disk type.
                                      type: string
                                  type: object
                                labels:
                                  additionalProperties:
                                    type: string
                                  description: Optional. Labels that should be applied
                                    to each VM instance in the nodeset.
                                  type: object
                                startupScript:
                                  description: Optional. Startup script to be run
                                    on each VM instance in the nodeset. Max 256KB.
                                  type: string
                              type: object
                            id:
                              description: Required. Identifier for the nodeset. Must
                                conform to RFC-1034.
                              type: string
                            maxDynamicNodeCount:
                              description: Optional. Controls how many additional
                                nodes a cluster can bring online to handle workloads.
                              format: int64
                              type: integer
                            staticNodeCount:
                              description: Optional. Number of nodes to be statically
                                created for this nodeset.
                              format: int64
                              type: integer
                            storageConfigs:
                              description: Optional. How storage resources should
                                be mounted on each compute node.
                              items:
                                properties:
                                  id:
                                    description: Required. ID of the storage resource
                                      to mount, which must match a key in the cluster's
                                      storage_resources.
                                    type: string
                                  localMount:
                                    description: Required. A directory inside the
                                      VM instance's file system where the storage
                                      resource should be mounted (e.g., `/mnt/share`).
                                    type: string
                                type: object
                              type: array
                          type: object
                        type: array
                      partitions:
                        description: Optional. Configuration for the Slurm partitions
                          in your cluster.
                        items:
                          properties:
                            id:
                              description: Required. ID of the partition, which is
                                how users will identify it. Must conform to RFC-1034.
                              type: string
                            nodeSetIds:
                              description: Required. IDs of the nodesets that make
                                up this partition.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      prologBashScripts:
                        description: Optional. Slurm prolog scripts, which will be
                          executed by compute nodes before a node begins running a
                          new job.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              projectRef:
                description: The Project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The HypercomputeClusterCluster name. If not given, the
                  metadata.name will be used.
                type: string
              storageResources:
                additionalProperties:
                  properties:
                    bucket:
                      description: Reference to a Google Cloud Storage bucket. Populated
                        if and only if the storage resource was configured to use
                        Google Cloud Storage.
                      type: object
                    config:
                      description: Required. Immutable. Configuration for this storage
                        resource, which describes how it should be created or imported.
                      properties:
                        existingBucket:
                          description: Optional. Immutable. If set, indicates that
                            an existing Cloud Storage bucket should be imported.
                          properties:
                            bucket:
                              description: Required. Immutable. Name of the Cloud
                                Storage bucket to import.
                              type: string
                          type: object
                        existingFilestore:
                          description: Optional. Immutable. If set, indicates that
                            an existing Filestore instance should be imported.
                          properties:
                            filestore:
                              description: Required. Immutable. Name of the Filestore
                                instance to import, in the format `projects/{project}/locations/{location}/instances/{instance}`
                              type: string
                          type: object
                        existingLustre:
                          description: Optional. Immutable. If set, indicates that
                            an existing Managed Lustre instance should be imported.
                          properties:
                            lustre:
                              description: Required. Immutable. Name of the Managed
                                Lustre instance to import, in the format `projects/{project}/locations/{location}/instances/{instance}`
                              type: string
                          type: object
                        newBucket:
                          description: Optional. Immutable. If set, indicates that
                            a new Cloud Storage bucket should be created.
                          properties:
                            autoclass:
                              description: Optional. Immutable. If set, indicates
                                that the bucket should use Autoclass.
                              properties:
                                enabled:
                                  description: Required. Enables Auto-class feature.
                                  type: boolean
                                terminalStorageClass:
                                  description: Optional. Terminal storage class of
                                    the autoclass bucket
                                  type: string
                              type: object
                            bucket:
                              description: Required. Immutable. Name of the Cloud
                                Storage bucket to create.
                              type: string
                            hierarchicalNamespace:
                              description: Optional. Immutable. If set, indicates
                                that the bucket should use hierarchical namespaces.
                              properties:
                                enabled:
                                  description: Required. Enables hierarchical namespace
                                    setup for the bucket.
                                  type: boolean
                              type: object
                            storageClass:
                              description: Optional. Immutable. If set, uses the provided
                                storage class as the bucket's default storage class.
                              type: string
                          type: object
                        newFilestore:
                          description: Optional. Immutable. If set, indicates that
                            a new Filestore instance should be created.
                          properties:
                            description:
                              description: Optional. Immutable. Description of the
                                instance. Maximum of 2048 characters.
                              type: string
                            fileShares:
                              description: Required. Immutable. File system shares
                                on the instance. Exactly one file share must be specified.
                              items:
                                properties:
                                  capacityGB:
                                    description: Required. Size of the filestore in
                                      GB. Must be between 1024 and 102400.
                                    format: int64
                                    type: integer
                                  fileShare:
                                    description: Required. Filestore share location
                                    type: string
                                type: object
                              type: array
                            filestore:
                              description: Required. Immutable. Name of the Filestore
                                instance to create, in the format `projects/{project}/locations/{location}/instances/{instance}`
                              type: string
                            protocol:
                              description: Optional. Immutable. Access protocol to
                                use for all file shares in the instance. Defaults
                                to NFS V3 if not set.
                              type: string
                            tier:
                              description: Required. Immutable. Service tier to use
                                for the instance.
                              type: string
                          type: object
                        newLustre:
                          description: Optional. Immutable. If set, indicates that
                            a new Managed Lustre instance should be created.
                          properties:
                            capacityGB:
                              description: Required. Immutable. Storage capacity of
                                the instance in gibibytes (GiB). Allowed values are
                                between 18000 and 7632000.
                              format: int64
                              type: integer
                            description:
                              description: Optional. Immutable. Description of the
                                Managed Lustre instance. Maximum of 2048 characters.
                              type: string
                            filesystem:
                              description: Required. Immutable. Filesystem name for
                                this instance. Must be 8 characters or less and can
                                only contain letters and numbers.
                              type: string
                            lustre:
                              description: Required. Immutable. Name of the Managed
                                Lustre instance to create, in the format `projects/{project}/locations/{location}/instances/{instance}`
                              type: string
                          type: object
                      type: object
                    filestore:
                      description: Reference to a Filestore instance. Populated if
                        and only if the storage resource was configured to use Filestore.
                      type: object
                    lustre:
                      description: Reference to a Managed Lustre instance. Populated
                        if and only if the storage resource was configured to use
                        Managed Lustre.
                      type: object
                  type: object
                description: Optional. Storage resource configuration for the cluster.
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: HypercomputeClusterClusterStatus defines the config connector
              machine state of HypercomputeClusterCluster
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the HypercomputeClusterCluster
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time that the cluster was originally
                      created.
                    type: string
                  orchestrator:
                    description: Optional. Orchestrator that is responsible for scheduling
                      and running jobs on the cluster.
                    properties:
                      slurm:
                        description: Optional. If set, indicates that the cluster
                          should use Slurm as the orchestrator.
                        properties:
                          loginNodes:
                            description: Required. Configuration for login nodes,
                              which allow users to access the cluster over SSH.
                            properties:
                              instances:
                                description: Output only. Information about the login
                                  node instances that were created in Compute Engine.
                                items:
                                  properties:
                                    instance:
                                      description: Output only. Name of the VM instance,
                                        in the format `projects/{project}/zones/{zone}/instances/{instance}`.
                                      type: string
                                  type: object
                                type: array
                            type: object
                        type: object
                    type: object
                  reconciling:
                    description: Output only. Indicates whether changes to the cluster
                      are currently in flight. If this is `true`, then the current
                      state might not match the cluster's intended state.
                    type: boolean
                  updateTime:
                    description: Output only. Time that the cluster was most recently
                      updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
