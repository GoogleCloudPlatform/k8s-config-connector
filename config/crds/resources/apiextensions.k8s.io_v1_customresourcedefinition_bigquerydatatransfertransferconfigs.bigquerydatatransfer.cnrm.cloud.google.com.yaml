# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: bigquerydatatransfertransferconfigs.bigquerydatatransfer.cnrm.cloud.google.com
spec:
  group: bigquerydatatransfer.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: BigQueryDataTransferTransferConfig
    listKind: BigQueryDataTransferTransferConfigList
    plural: bigquerydatatransfertransferconfigs
    shortNames:
    - gcpbigquerydatatransfertransferconfig
    - gcpbigquerydatatransfertransferconfigs
    singular: bigquerydatatransfertransferconfig
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BigQueryDataTransferTransferConfig is the Schema for the BigQueryDataTransferTransferConfig
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BigQueryDataTransferTransferConfigSpec defines the desired
              state of BigQueryDataTransferTransferConfig
            properties:
              dataRefreshWindowDays:
                description: The number of days to look back to automatically refresh
                  the data. For example, if `data_refresh_window_days = 10`, then
                  every day BigQuery reingests data for [today-10, today-1], rather
                  than ingesting data for just [today-1]. Only valid if the data source
                  supports the feature. Set the value to 0 to use the default value.
                format: int32
                type: integer
              dataSourceID:
                description: 'Data source ID. This cannot be changed once data transfer
                  is created. The full list of available data source IDs can be returned
                  through an API call: https://cloud.google.com/bigquery-transfer/docs/reference/datatransfer/rest/v1/projects.locations.dataSources/list'
                type: string
              destinationDatasetID:
                description: The BigQuery target dataset id.
                type: string
              disabled:
                description: Is this config disabled. When set to true, no runs will
                  be scheduled for this transfer config.
                type: boolean
              displayName:
                description: User specified display name for the data transfer.
                type: string
              emailPreferences:
                description: Email notifications will be sent according to these preferences
                  to the email address of the user who owns this transfer config.
                properties:
                  enableFailureEmail:
                    description: If true, email notifications will be sent on transfer
                      run failures.
                    type: boolean
                type: object
              encryptionConfiguration:
                description: The encryption configuration part. Currently, it is only
                  used for the optional KMS key name. The BigQuery service account
                  of your project must be granted permissions to use the key. Read
                  methods will return the key name applied in effect. Write methods
                  will apply the key if it is present, or otherwise try to apply project
                  default keys if it is absent.
                properties:
                  kmsKeyName:
                    description: The name of the KMS key used for encrypting BigQuery
                      data.
                    type: string
                type: object
              location:
                description: The location of this resource.
                type: string
              notificationPubsubTopicRef:
                description: |-
                  Pub/Sub topic where notifications will be sent after transfer runs
                  associated with this transfer config finish.

                  The format for specifying a pubsub topic is:
                  `projects/{project_id}/topics/{topic_id}`
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed PubSubTopic
                      resource. Should be in the format "projects/{{projectID}}/topics/{{topicID}}".
                    type: string
                  name:
                    description: The name of a PubSubTopic resource.
                    type: string
                  namespace:
                    description: The namespace of a PubSubTopic resource.
                    type: string
                type: object
              params:
                description: 'Parameters specific to each data source. For more information
                  see the bq tab in the ''Setting up a data transfer'' section for
                  each data source. For example the parameters for Cloud Storage transfers
                  are listed here: https://cloud.google.com/bigquery-transfer/docs/cloud-storage-transfer#bq'
                x-kubernetes-preserve-unknown-fields: true
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The BigQueryDataTransferTransferConfig name. If not given,
                  the metadata.name will be used.
                type: string
              schedule:
                description: |-
                  Data transfer schedule.
                  If the data source does not support a custom schedule, this should be
                  empty. If it is empty, the default value for the data source will be used.
                  The specified times are in UTC.
                  Examples of valid format:
                  `1st,3rd monday of month 15:30`,
                  `every wed,fri of jan,jun 13:15`, and
                  `first sunday of quarter 00:00`.
                  See more explanation about the format here:
                  https://cloud.google.com/appengine/docs/flexible/python/scheduling-jobs-with-cron-yaml#the_schedule_format

                  NOTE: The minimum interval time between recurring transfers depends on the
                  data source; refer to the documentation for your data source.
                type: string
              scheduleOptions:
                description: Options customizing the data transfer schedule.
                properties:
                  disableAutoScheduling:
                    description: If true, automatic scheduling of data transfer runs
                      for this configuration will be disabled. The runs can be started
                      on ad-hoc basis using StartManualTransferRuns API. When automatic
                      scheduling is disabled, the TransferConfig.schedule field will
                      be ignored.
                    type: boolean
                  endTime:
                    description: Defines time to stop scheduling transfer runs. A
                      transfer run cannot be scheduled at or after the end time. The
                      end time can be changed at any moment. The time when a data
                      transfer can be triggered manually is not limited by this option.
                    type: string
                  startTime:
                    description: Specifies time to start scheduling transfer runs.
                      The first run will be scheduled at or after the start time according
                      to a recurrence pattern defined in the schedule string. The
                      start time can be changed at any moment. The time when a data
                      transfer can be triggered manually is not limited by this option.
                    type: string
                type: object
              scheduleOptionsV2:
                description: Options customizing different types of data transfer
                  schedule. This field replaces "schedule" and "schedule_options"
                  fields. ScheduleOptionsV2 cannot be used together with ScheduleOptions/Schedule.
                properties:
                  eventDrivenSchedule:
                    description: Event driven transfer schedule options. If set, the
                      transfer will be scheduled upon events arrial.
                    properties:
                      pubsubSubscriptionRef:
                        description: 'Pub/Sub subscription name used to receive events.
                          Only Google Cloud Storage data source support this option.
                          Format: projects/{project}/subscriptions/{subscription}'
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed PubSubSubscription
                              resource. Should be in the format "projects/{{projectID}}/subscriptions/{{subscriptionID}}".
                            type: string
                          name:
                            description: The name of a PubSubSubscription resource.
                            type: string
                          namespace:
                            description: The namespace of a PubSubSubscription resource.
                            type: string
                        type: object
                    type: object
                  manualSchedule:
                    description: Manual transfer schedule. If set, the transfer run
                      will not be auto-scheduled by the system, unless the client
                      invokes StartManualTransferRuns.  This is equivalent to disable_auto_scheduling
                      = true.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  timeBasedSchedule:
                    description: Time based transfer schedule options. This is the
                      default schedule option.
                    properties:
                      endTime:
                        description: Defines time to stop scheduling transfer runs.
                          A transfer run cannot be scheduled at or after the end time.
                          The end time can be changed at any moment.
                        type: string
                      schedule:
                        description: |-
                          Data transfer schedule.
                           If the data source does not support a custom schedule, this should be
                           empty. If it is empty, the default value for the data source will be used.
                           The specified times are in UTC.
                           Examples of valid format:
                           `1st,3rd monday of month 15:30`,
                           `every wed,fri of jan,jun 13:15`, and
                           `first sunday of quarter 00:00`.
                           See more explanation about the format here:
                           https://cloud.google.com/appengine/docs/flexible/python/scheduling-jobs-with-cron-yaml#the_schedule_format

                           NOTE: The minimum interval time between recurring transfers depends on the
                           data source; refer to the documentation for your data source.
                        type: string
                      startTime:
                        description: Specifies time to start scheduling transfer runs.
                          The first run will be scheduled at or after the start time
                          according to a recurrence pattern defined in the schedule
                          string. The start time can be changed at any moment.
                        type: string
                    type: object
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: BigQueryDataTransferTransferConfigStatus defines the config
              connector machine state of BigQueryDataTransferTransferConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the BigQueryDataTransferTransferConfig
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  datasetRegion:
                    description: Output only. Region in which BigQuery dataset is
                      located.
                    type: string
                  nextRunTime:
                    description: Output only. Next time when data transfer will run.
                    type: string
                  ownerInfo:
                    description: Output only. Information about the user whose credentials
                      are used to transfer data. Populated only for `transferConfigs.get`
                      requests. In case the user information is not available, this
                      field will not be populated.
                    properties:
                      email:
                        description: E-mail address of the user.
                        type: string
                    type: object
                  state:
                    description: Output only. State of the most recently updated transfer
                      run.
                    type: string
                  updateTime:
                    description: Output only. Data transfer modification time. Ignored
                      by server on input.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
