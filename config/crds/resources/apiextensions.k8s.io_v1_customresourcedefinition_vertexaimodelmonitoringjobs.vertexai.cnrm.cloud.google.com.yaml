# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: vertexaimodelmonitoringjobs.vertexai.cnrm.cloud.google.com
spec:
  group: vertexai.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: VertexAIModelMonitoringJob
    listKind: VertexAIModelMonitoringJobList
    plural: vertexaimodelmonitoringjobs
    shortNames:
    - gcpvertexaimodelmonitoringjob
    - gcpvertexaimodelmonitoringjobs
    singular: vertexaimodelmonitoringjob
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VertexAIModelMonitoringJob is the Schema for the VertexAIModelMonitoringJob
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VertexAIModelMonitoringJobSpec defines the desired state
              of VertexAIModelMonitoringJob
            properties:
              displayName:
                description: The display name of the ModelMonitoringJob. The name
                  can be up to 128 characters long and can consist of any UTF-8.
                type: string
              location:
                description: The location of this resource.
                type: string
              modelMonitorRef:
                description: The parent ModelMonitor of this resource.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed VertexAIModelMonitor
                      resource. Should be in the format "projects/{{project}}/locations/{{location}}/modelMonitors/{{modelMonitor}}"
                    type: string
                  name:
                    description: The name of a VertexAIModelMonitor resource.
                    type: string
                  namespace:
                    description: The namespace of a VertexAIModelMonitor resource.
                    type: string
                type: object
              modelMonitoringSpec:
                description: Monitoring monitoring job spec. It outlines the specifications
                  for monitoring objectives, notifications, and result exports. If
                  left blank, the default monitoring specifications from the top-level
                  resource 'ModelMonitor' will be applied. If provided, we will use
                  the specification defined here rather than the default one.
                properties:
                  notificationSpec:
                    description: The model monitoring notification spec.
                    properties:
                      emailConfig:
                        description: Email alert config.
                        properties:
                          userEmails:
                            description: The email addresses to send the alerts.
                            items:
                              type: string
                            type: array
                        type: object
                      enableCloudLogging:
                        description: Dump the anomalies to Cloud Logging. The anomalies
                          will be put to json payload encoded from proto [google.cloud.aiplatform.logging.ModelMonitoringAnomaliesLogEntry][].
                          This can be further sinked to Pub/Sub or any other services
                          supported by Cloud Logging.
                        type: boolean
                      notificationChannelConfigs:
                        description: Notification channel config.
                        items:
                          description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.ModelMonitoringNotificationSpec.NotificationChannelConfig
                          properties:
                            notificationChannel:
                              description: Resource names of the NotificationChannels.
                                Must be of the format `projects/<project_id_or_number>/notificationChannels/<channel_id>`
                              type: string
                          type: object
                        type: array
                    type: object
                  objectiveSpec:
                    description: The monitoring objective spec.
                    properties:
                      baselineDataset:
                        description: Baseline dataset. It could be the training dataset
                          or production serving dataset from a previous period.
                        properties:
                          batchPredictionOutput:
                            description: Vertex AI Batch prediction Job.
                            properties:
                              batchPredictionJob:
                                description: Vertex AI Batch prediction job resource
                                  name. The job must match the model version specified
                                  in [ModelMonitor].[model_monitoring_target].
                                type: string
                            type: object
                          columnizedDataset:
                            description: Columnized dataset.
                            properties:
                              bigquerySource:
                                description: BigQuery data source.
                                properties:
                                  query:
                                    description: Standard SQL to be used instead of
                                      the `table_uri`.
                                    type: string
                                  tableURI:
                                    description: |-
                                      BigQuery URI to a table, up to 2000 characters long. All the columns
                                       in the table will be selected. Accepted forms:

                                       *  BigQuery path. For example:
                                       `bq://projectId.bqDatasetId.bqTableId`.
                                    type: string
                                type: object
                              gcsSource:
                                description: Google Cloud Storage data source.
                                properties:
                                  format:
                                    description: Data format of the dataset.
                                    type: string
                                  gcsURI:
                                    description: Google Cloud Storage URI to the input
                                      file(s). May contain wildcards. For more information
                                      on wildcards, see https://cloud.google.com/storage/docs/wildcards.
                                    type: string
                                type: object
                              timestampField:
                                description: The timestamp field. Usually for serving
                                  data.
                                type: string
                              vertexDataset:
                                description: Resource name of the Vertex AI managed
                                  dataset.
                                type: string
                            type: object
                          timeInterval:
                            description: The time interval (pair of start_time and
                              end_time) for which results should be returned.
                            properties:
                              endTime:
                                description: |-
                                  Optional. Exclusive end of the interval.

                                   If specified, a Timestamp matching this interval will have to be before the
                                   end.
                                type: string
                              startTime:
                                description: |-
                                  Optional. Inclusive start of the interval.

                                   If specified, a Timestamp matching this interval will have to be the same
                                   or after the start.
                                type: string
                            type: object
                          timeOffset:
                            description: The time offset setting for which results
                              should be returned.
                            properties:
                              offset:
                                description: '[offset] is the time difference from
                                  the cut-off time. For scheduled jobs, the cut-off
                                  time is the scheduled time. For non-scheduled jobs,
                                  it''s the time when the job was created. Currently
                                  we support the following format: ''w|W'': Week,
                                  ''d|D'': Day, ''h|H'': Hour E.g. ''1h'' stands for
                                  1 hour, ''2d'' stands for 2 days.'
                                type: string
                              window:
                                description: '[window] refers to the scope of data
                                  selected for analysis. It allows you to specify
                                  the quantity of data you wish to examine. Currently
                                  we support the following format: ''w|W'': Week,
                                  ''d|D'': Day, ''h|H'': Hour E.g. ''1h'' stands for
                                  1 hour, ''2d'' stands for 2 days.'
                                type: string
                            type: object
                          vertexEndpointLogs:
                            description: Vertex AI Endpoint request & response logging.
                            properties:
                              endpoints:
                                description: List of endpoint resource names. The
                                  endpoints must enable the logging with the [Endpoint].[request_response_logging_config],
                                  and must contain the deployed model corresponding
                                  to the model version specified in [ModelMonitor].[model_monitoring_target].
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      explanationSpec:
                        description: The explanation spec. This spec is required when
                          the objectives spec includes feature attribution objectives.
                        properties:
                          metadata:
                            description: Optional. Metadata describing the Model's
                              input and output for explanation.
                            properties:
                              featureAttributionsSchemaURI:
                                description: 'Points to a YAML file stored on Google
                                  Cloud Storage describing the format of the [feature
                                  attributions][google.cloud.aiplatform.v1beta1.Attribution.feature_attributions].
                                  The schema is defined as an OpenAPI 3.0.2 [Schema
                                  Object](https://github.com/OAI/OpenAPI-Specification/blob/main/versions/3.0.2.md#schemaObject).
                                  AutoML tabular Models always have this field populated
                                  by Vertex AI. Note: The URI given on output may
                                  be different, including the URI scheme, than the
                                  one given on input. The output URI will point to
                                  a location where the user only has a read access.'
                                type: string
                              latentSpaceSource:
                                description: Name of the source to generate embeddings
                                  for example based explanations.
                                type: string
                            type: object
                          parameters:
                            description: Required. Parameters that configure explaining
                              of the Model's predictions.
                            properties:
                              examples:
                                description: Example-based explanations that returns
                                  the nearest neighbors from the provided dataset.
                                properties:
                                  exampleGCSSource:
                                    description: The Cloud Storage input instances.
                                    properties:
                                      dataFormat:
                                        description: The format in which instances
                                          are given, if not specified, assume it's
                                          JSONL format. Currently only JSONL format
                                          is supported.
                                        type: string
                                      gcsSource:
                                        description: The Cloud Storage location for
                                          the input instances.
                                        properties:
                                          uris:
                                            description: Required. Google Cloud Storage
                                              URI(-s) to the input file(s). May contain
                                              wildcards. For more information on wildcards,
                                              see https://cloud.google.com/storage/docs/wildcards.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                    type: object
                                  gcsSource:
                                    description: The Cloud Storage locations that
                                      contain the instances to be indexed for approximate
                                      nearest neighbor search.
                                    properties:
                                      uris:
                                        description: Required. Google Cloud Storage
                                          URI(-s) to the input file(s). May contain
                                          wildcards. For more information on wildcards,
                                          see https://cloud.google.com/storage/docs/wildcards.
                                        items:
                                          type: string
                                        type: array
                                    type: object
                                  nearestNeighborSearchConfig:
                                    description: The full configuration for the generated
                                      index, the semantics are the same as [metadata][google.cloud.aiplatform.v1beta1.Index.metadata]
                                      and should match [NearestNeighborSearchConfig](https://cloud.google.com/vertex-ai/docs/explainable-ai/configuring-explanations-example-based#nearest-neighbor-search-config).
                                    type: object
                                    x-kubernetes-preserve-unknown-fields: true
                                  neighborCount:
                                    description: The number of neighbors to return
                                      when querying for examples.
                                    format: int32
                                    type: integer
                                  presets:
                                    description: Simplified preset configuration,
                                      which automatically sets configuration values
                                      based on the desired query speed-precision trade-off
                                      and modality.
                                    properties:
                                      modality:
                                        description: The modality of the uploaded
                                          model, which automatically configures the
                                          distance measurement and feature normalization
                                          for the underlying example index and queries.
                                          If your model does not precisely fit one
                                          of these types, it is okay to choose the
                                          closest type.
                                        type: string
                                      query:
                                        description: Preset option controlling parameters
                                          for speed-precision trade-off when querying
                                          for examples. If omitted, defaults to `PRECISE`.
                                        type: string
                                    type: object
                                type: object
                              integratedGradientsAttribution:
                                description: 'An attribution method that computes
                                  Aumann-Shapley values taking advantage of the model''s
                                  fully differentiable structure. Refer to this paper
                                  for more details: https://arxiv.org/abs/1703.01365'
                                properties:
                                  blurBaselineConfig:
                                    description: |-
                                      Config for IG with blur baseline.

                                       When enabled, a linear path from the maximally blurred image to the input
                                       image is created. Using a blurred baseline instead of zero (black image) is
                                       motivated by the BlurIG approach explained here:
                                       https://arxiv.org/abs/2004.03383
                                    properties:
                                      maxBlurSigma:
                                        description: The standard deviation of the
                                          blur kernel for the blurred baseline. The
                                          same blurring parameter is used for both
                                          the height and the width dimension. If not
                                          set, the method defaults to the zero (i.e.
                                          black for images) baseline.
                                        type: number
                                    type: object
                                  smoothGradConfig:
                                    description: |-
                                      Config for SmoothGrad approximation of gradients.

                                       When enabled, the gradients are approximated by averaging the gradients
                                       from noisy samples in the vicinity of the inputs. Adding
                                       noise can help improve the computed gradients. Refer to this paper for more
                                       details: https://arxiv.org/pdf/1706.03825.pdf
                                    properties:
                                      featureNoiseSigma:
                                        description: This is similar to [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma],
                                          but provides additional flexibility. A separate
                                          noise sigma can be provided for each feature,
                                          which is useful if their distributions are
                                          different. No noise is added to features
                                          that are not set. If this field is unset,
                                          [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma]
                                          will be used for all features.
                                        properties:
                                          noiseSigma:
                                            description: Noise sigma per feature.
                                              No noise is added to features that are
                                              not set.
                                            items:
                                              description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.FeatureNoiseSigma.NoiseSigmaForFeature
                                              properties:
                                                name:
                                                  description: The name of the input
                                                    feature for which noise sigma
                                                    is provided. The features are
                                                    defined in [explanation metadata
                                                    inputs][google.cloud.aiplatform.v1beta1.ExplanationMetadata.inputs].
                                                  type: string
                                                sigma:
                                                  description: This represents the
                                                    standard deviation of the Gaussian
                                                    kernel that will be used to add
                                                    noise to the feature prior to
                                                    computing gradients. Similar to
                                                    [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma]
                                                    but represents the noise added
                                                    to the current feature. Defaults
                                                    to 0.1.
                                                  type: number
                                              type: object
                                            type: array
                                        type: object
                                      noiseSigma:
                                        description: |-
                                          This is a single float value and will be used to add noise to all the
                                           features. Use this field when all features are normalized to have the
                                           same distribution: scale to range [0, 1], [-1, 1] or z-scoring, where
                                           features are normalized to have 0-mean and 1-variance. Learn more about
                                           [normalization](https://developers.google.com/machine-learning/data-prep/transform/normalization).

                                           For best results the recommended value is about 10% - 20% of the standard
                                           deviation of the input feature. Refer to section 3.2 of the SmoothGrad
                                           paper: https://arxiv.org/pdf/1706.03825.pdf. Defaults to 0.1.

                                           If the distribution is different per feature, set
                                           [feature_noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.feature_noise_sigma]
                                           instead for each feature.
                                        type: number
                                      noisySampleCount:
                                        description: The number of gradient samples
                                          to use for approximation. The higher this
                                          number, the more accurate the gradient is,
                                          but the runtime complexity increases by
                                          this factor as well. Valid range of its
                                          value is [1, 50]. Defaults to 3.
                                        format: int32
                                        type: integer
                                    type: object
                                  stepCount:
                                    description: |-
                                      Required. The number of steps for approximating the path integral.
                                       A good value to start is 50 and gradually increase until the
                                       sum to diff property is within the desired error range.

                                       Valid range of its value is [1, 100], inclusively.
                                    format: int32
                                    type: integer
                                type: object
                              outputIndices:
                                description: |-
                                  If populated, only returns attributions that have
                                   [output_index][google.cloud.aiplatform.v1beta1.Attribution.output_index]
                                   contained in output_indices. It must be an ndarray of integers, with the
                                   same shape of the output it's explaining.

                                   If not populated, returns attributions for
                                   [top_k][google.cloud.aiplatform.v1beta1.ExplanationParameters.top_k]
                                   indices of outputs. If neither top_k nor output_indices is populated,
                                   returns the argmax index of the outputs.

                                   Only applicable to Models that predict multiple outputs (e,g, multi-class
                                   Models that predict multiple classes).
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              sampledShapleyAttribution:
                                description: 'An attribution method that approximates
                                  Shapley values for features that contribute to the
                                  label being predicted. A sampling strategy is used
                                  to approximate the value rather than considering
                                  all subsets of features. Refer to this paper for
                                  model details: https://arxiv.org/abs/1306.4265.'
                                properties:
                                  pathCount:
                                    description: |-
                                      Required. The number of feature permutations to consider when approximating
                                       the Shapley values.

                                       Valid range of its value is [1, 50], inclusively.
                                    format: int32
                                    type: integer
                                type: object
                              topK:
                                description: If populated, returns attributions for
                                  top K indices of outputs (defaults to 1). Only applies
                                  to Models that predicts more than one outputs (e,g,
                                  multi-class Models). When set to -1, returns explanations
                                  for all outputs.
                                format: int32
                                type: integer
                              xraiAttribution:
                                description: |-
                                  An attribution method that redistributes Integrated Gradients
                                   attribution to segmented regions, taking advantage of the model's fully
                                   differentiable structure. Refer to this paper for
                                   more details: https://arxiv.org/abs/1906.02825

                                   XRAI currently performs better on natural images, like a picture of a
                                   house or an animal. If the images are taken in artificial environments,
                                   like a lab or manufacturing line, or from diagnostic equipment, like
                                   x-rays or quality-control cameras, use Integrated Gradients instead.
                                properties:
                                  blurBaselineConfig:
                                    description: |-
                                      Config for XRAI with blur baseline.

                                       When enabled, a linear path from the maximally blurred image to the input
                                       image is created. Using a blurred baseline instead of zero (black image) is
                                       motivated by the BlurIG approach explained here:
                                       https://arxiv.org/abs/2004.03383
                                    properties:
                                      maxBlurSigma:
                                        description: The standard deviation of the
                                          blur kernel for the blurred baseline. The
                                          same blurring parameter is used for both
                                          the height and the width dimension. If not
                                          set, the method defaults to the zero (i.e.
                                          black for images) baseline.
                                        type: number
                                    type: object
                                  smoothGradConfig:
                                    description: |-
                                      Config for SmoothGrad approximation of gradients.

                                       When enabled, the gradients are approximated by averaging the gradients
                                       from noisy samples in the vicinity of the inputs. Adding
                                       noise can help improve the computed gradients. Refer to this paper for more
                                       details: https://arxiv.org/pdf/1706.03825.pdf
                                    properties:
                                      featureNoiseSigma:
                                        description: This is similar to [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma],
                                          but provides additional flexibility. A separate
                                          noise sigma can be provided for each feature,
                                          which is useful if their distributions are
                                          different. No noise is added to features
                                          that are not set. If this field is unset,
                                          [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma]
                                          will be used for all features.
                                        properties:
                                          noiseSigma:
                                            description: Noise sigma per feature.
                                              No noise is added to features that are
                                              not set.
                                            items:
                                              description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.FeatureNoiseSigma.NoiseSigmaForFeature
                                              properties:
                                                name:
                                                  description: The name of the input
                                                    feature for which noise sigma
                                                    is provided. The features are
                                                    defined in [explanation metadata
                                                    inputs][google.cloud.aiplatform.v1beta1.ExplanationMetadata.inputs].
                                                  type: string
                                                sigma:
                                                  description: This represents the
                                                    standard deviation of the Gaussian
                                                    kernel that will be used to add
                                                    noise to the feature prior to
                                                    computing gradients. Similar to
                                                    [noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.noise_sigma]
                                                    but represents the noise added
                                                    to the current feature. Defaults
                                                    to 0.1.
                                                  type: number
                                              type: object
                                            type: array
                                        type: object
                                      noiseSigma:
                                        description: |-
                                          This is a single float value and will be used to add noise to all the
                                           features. Use this field when all features are normalized to have the
                                           same distribution: scale to range [0, 1], [-1, 1] or z-scoring, where
                                           features are normalized to have 0-mean and 1-variance. Learn more about
                                           [normalization](https://developers.google.com/machine-learning/data-prep/transform/normalization).

                                           For best results the recommended value is about 10% - 20% of the standard
                                           deviation of the input feature. Refer to section 3.2 of the SmoothGrad
                                           paper: https://arxiv.org/pdf/1706.03825.pdf. Defaults to 0.1.

                                           If the distribution is different per feature, set
                                           [feature_noise_sigma][google.cloud.aiplatform.v1beta1.SmoothGradConfig.feature_noise_sigma]
                                           instead for each feature.
                                        type: number
                                      noisySampleCount:
                                        description: The number of gradient samples
                                          to use for approximation. The higher this
                                          number, the more accurate the gradient is,
                                          but the runtime complexity increases by
                                          this factor as well. Valid range of its
                                          value is [1, 50]. Defaults to 3.
                                        format: int32
                                        type: integer
                                    type: object
                                  stepCount:
                                    description: |-
                                      Required. The number of steps for approximating the path integral.
                                       A good value to start is 50 and gradually increase until the
                                       sum to diff property is met within the desired error range.

                                       Valid range of its value is [1, 100], inclusively.
                                    format: int32
                                    type: integer
                                type: object
                            type: object
                        type: object
                      tabularObjective:
                        description: Tabular monitoring objective.
                        properties:
                          featureAttributionSpec:
                            description: Feature attribution monitoring spec.
                            properties:
                              batchExplanationDedicatedResources:
                                description: The config of resources used by the Model
                                  Monitoring during the batch explanation for non-AutoML
                                  models. If not set, `n1-standard-2` machine type
                                  will be used by default.
                                properties:
                                  flexStart:
                                    description: 'Optional. Immutable. If set, use
                                      DWS resource to schedule the deployment workload.
                                      reference: (https://cloud.google.com/blog/products/compute/introducing-dynamic-workload-scheduler)'
                                    properties:
                                      maxRuntimeDuration:
                                        description: The max duration of the deployment
                                          is max_runtime_duration. The deployment
                                          will be terminated after the duration. The
                                          max_runtime_duration can be set up to 7
                                          days.
                                        type: string
                                    type: object
                                  machineSpec:
                                    description: Required. Immutable. The specification
                                      of a single machine.
                                    properties:
                                      acceleratorCount:
                                        description: The number of accelerators to
                                          attach to the machine.
                                        format: int32
                                        type: integer
                                      acceleratorType:
                                        description: Immutable. The type of accelerator(s)
                                          that may be attached to the machine as per
                                          [accelerator_count][google.cloud.aiplatform.v1beta1.MachineSpec.accelerator_count].
                                        type: string
                                      gpuPartitionSize:
                                        description: |-
                                          Optional. Immutable. The Nvidia GPU partition size.

                                           When specified, the requested accelerators will be partitioned into
                                           smaller GPU partitions. For example, if the request is for 8 units of
                                           NVIDIA A100 GPUs, and gpu_partition_size="1g.10gb", the service will
                                           create 8 * 7 = 56 partitioned MIG instances.

                                           The partition size must be a value supported by the requested accelerator.
                                           Refer to
                                           [Nvidia GPU
                                           Partitioning](https://cloud.google.com/kubernetes-engine/docs/how-to/gpus-multi#multi-instance_gpu_partitions)
                                           for the available partition sizes.

                                           If set, the accelerator_count should be set to 1.
                                        type: string
                                      machineType:
                                        description: |-
                                          Immutable. The type of the machine.

                                           See the [list of machine types supported for
                                           prediction](https://cloud.google.com/vertex-ai/docs/predictions/configure-compute#machine-types)

                                           See the [list of machine types supported for custom
                                           training](https://cloud.google.com/vertex-ai/docs/training/configure-compute#machine-types).

                                           For [DeployedModel][google.cloud.aiplatform.v1beta1.DeployedModel] this
                                           field is optional, and the default value is `n1-standard-2`. For
                                           [BatchPredictionJob][google.cloud.aiplatform.v1beta1.BatchPredictionJob] or
                                           as part of [WorkerPoolSpec][google.cloud.aiplatform.v1beta1.WorkerPoolSpec]
                                           this field is required.
                                        type: string
                                      minGpuDriverVersion:
                                        description: Optional. Immutable. The minimum
                                          GPU driver version that this machine requires.
                                          For example, "535.104.06". If not specified,
                                          the default GPU driver version will be used
                                          by the underlying infrastructure.
                                        type: string
                                      multihostGpuNodeCount:
                                        description: Optional. Immutable. The number
                                          of nodes per replica for multihost GPU deployments.
                                        format: int32
                                        type: integer
                                      reservationAffinity:
                                        description: Optional. Immutable. Configuration
                                          controlling how this resource pool consumes
                                          reservation.
                                        properties:
                                          key:
                                            description: Optional. Corresponds to
                                              the label key of a reservation resource.
                                              To target a SPECIFIC_RESERVATION by
                                              name, use `compute.googleapis.com/reservation-name`
                                              as the key and specify the name of your
                                              reservation as its value.
                                            type: string
                                          reservationAffinityType:
                                            description: Required. Specifies the reservation
                                              affinity type.
                                            type: string
                                          values:
                                            description: Optional. Corresponds to
                                              the label values of a reservation resource.
                                              This must be the full resource name
                                              of the reservation.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                      tpuTopology:
                                        description: 'Immutable. The topology of the
                                          TPUs. Corresponds to the TPU topologies
                                          available from GKE. (Example: tpu_topology:
                                          "2x2x1").'
                                        type: string
                                    type: object
                                  maxReplicaCount:
                                    description: Immutable. The maximum number of
                                      machine replicas the batch operation may be
                                      scaled to. The default value is 10.
                                    format: int32
                                    type: integer
                                  spot:
                                    description: Optional. If true, schedule the deployment
                                      workload on [spot VMs](https://cloud.google.com/kubernetes-engine/docs/concepts/spot-vms).
                                    type: boolean
                                  startingReplicaCount:
                                    description: Immutable. The number of machine
                                      replicas used at the start of the batch operation.
                                      If not set, Vertex AI decides starting number,
                                      not greater than [max_replica_count][google.cloud.aiplatform.v1beta1.BatchDedicatedResources.max_replica_count]
                                    format: int32
                                    type: integer
                                type: object
                              defaultAlertCondition:
                                description: Default alert condition for all the features.
                                properties:
                                  threshold:
                                    description: A condition that compares a stats
                                      value against a threshold. Alert will be triggered
                                      if value above the threshold.
                                    type: number
                                type: object
                              features:
                                description: Feature names interested in monitoring.
                                  These should be a subset of the input feature names
                                  specified in the monitoring schema. If the field
                                  is not specified all features outlied in the monitoring
                                  schema will be used.
                                items:
                                  type: string
                                type: array
                            type: object
                          featureDriftSpec:
                            description: Input feature distribution drift monitoring
                              spec.
                            properties:
                              categoricalMetricType:
                                description: 'Supported metrics type: * l_infinity
                                  * jensen_shannon_divergence'
                                type: string
                              defaultCategoricalAlertCondition:
                                description: Default alert condition for all the categorical
                                  features.
                                properties:
                                  threshold:
                                    description: A condition that compares a stats
                                      value against a threshold. Alert will be triggered
                                      if value above the threshold.
                                    type: number
                                type: object
                              defaultNumericAlertCondition:
                                description: Default alert condition for all the numeric
                                  features.
                                properties:
                                  threshold:
                                    description: A condition that compares a stats
                                      value against a threshold. Alert will be triggered
                                      if value above the threshold.
                                    type: number
                                type: object
                              features:
                                description: Feature names / Prediction output names
                                  interested in monitoring. These should be a subset
                                  of the input feature names or prediction output
                                  names specified in the monitoring schema. If the
                                  field is not specified all features / prediction
                                  outputs outlied in the monitoring schema will be
                                  used.
                                items:
                                  type: string
                                type: array
                              numericMetricType:
                                description: 'Supported metrics type: * jensen_shannon_divergence'
                                type: string
                            type: object
                          predictionOutputDriftSpec:
                            description: Prediction output distribution drift monitoring
                              spec.
                            properties:
                              categoricalMetricType:
                                description: 'Supported metrics type: * l_infinity
                                  * jensen_shannon_divergence'
                                type: string
                              defaultCategoricalAlertCondition:
                                description: Default alert condition for all the categorical
                                  features.
                                properties:
                                  threshold:
                                    description: A condition that compares a stats
                                      value against a threshold. Alert will be triggered
                                      if value above the threshold.
                                    type: number
                                type: object
                              defaultNumericAlertCondition:
                                description: Default alert condition for all the numeric
                                  features.
                                properties:
                                  threshold:
                                    description: A condition that compares a stats
                                      value against a threshold. Alert will be triggered
                                      if value above the threshold.
                                    type: number
                                type: object
                              features:
                                description: Feature names / Prediction output names
                                  interested in monitoring. These should be a subset
                                  of the input feature names or prediction output
                                  names specified in the monitoring schema. If the
                                  field is not specified all features / prediction
                                  outputs outlied in the monitoring schema will be
                                  used.
                                items:
                                  type: string
                                type: array
                              numericMetricType:
                                description: 'Supported metrics type: * jensen_shannon_divergence'
                                type: string
                            type: object
                        type: object
                      targetDataset:
                        description: Target dataset.
                        properties:
                          batchPredictionOutput:
                            description: Vertex AI Batch prediction Job.
                            properties:
                              batchPredictionJob:
                                description: Vertex AI Batch prediction job resource
                                  name. The job must match the model version specified
                                  in [ModelMonitor].[model_monitoring_target].
                                type: string
                            type: object
                          columnizedDataset:
                            description: Columnized dataset.
                            properties:
                              bigquerySource:
                                description: BigQuery data source.
                                properties:
                                  query:
                                    description: Standard SQL to be used instead of
                                      the `table_uri`.
                                    type: string
                                  tableURI:
                                    description: |-
                                      BigQuery URI to a table, up to 2000 characters long. All the columns
                                       in the table will be selected. Accepted forms:

                                       *  BigQuery path. For example:
                                       `bq://projectId.bqDatasetId.bqTableId`.
                                    type: string
                                type: object
                              gcsSource:
                                description: Google Cloud Storage data source.
                                properties:
                                  format:
                                    description: Data format of the dataset.
                                    type: string
                                  gcsURI:
                                    description: Google Cloud Storage URI to the input
                                      file(s). May contain wildcards. For more information
                                      on wildcards, see https://cloud.google.com/storage/docs/wildcards.
                                    type: string
                                type: object
                              timestampField:
                                description: The timestamp field. Usually for serving
                                  data.
                                type: string
                              vertexDataset:
                                description: Resource name of the Vertex AI managed
                                  dataset.
                                type: string
                            type: object
                          timeInterval:
                            description: The time interval (pair of start_time and
                              end_time) for which results should be returned.
                            properties:
                              endTime:
                                description: |-
                                  Optional. Exclusive end of the interval.

                                   If specified, a Timestamp matching this interval will have to be before the
                                   end.
                                type: string
                              startTime:
                                description: |-
                                  Optional. Inclusive start of the interval.

                                   If specified, a Timestamp matching this interval will have to be the same
                                   or after the start.
                                type: string
                            type: object
                          timeOffset:
                            description: The time offset setting for which results
                              should be returned.
                            properties:
                              offset:
                                description: '[offset] is the time difference from
                                  the cut-off time. For scheduled jobs, the cut-off
                                  time is the scheduled time. For non-scheduled jobs,
                                  it''s the time when the job was created. Currently
                                  we support the following format: ''w|W'': Week,
                                  ''d|D'': Day, ''h|H'': Hour E.g. ''1h'' stands for
                                  1 hour, ''2d'' stands for 2 days.'
                                type: string
                              window:
                                description: '[window] refers to the scope of data
                                  selected for analysis. It allows you to specify
                                  the quantity of data you wish to examine. Currently
                                  we support the following format: ''w|W'': Week,
                                  ''d|D'': Day, ''h|H'': Hour E.g. ''1h'' stands for
                                  1 hour, ''2d'' stands for 2 days.'
                                type: string
                            type: object
                          vertexEndpointLogs:
                            description: Vertex AI Endpoint request & response logging.
                            properties:
                              endpoints:
                                description: List of endpoint resource names. The
                                  endpoints must enable the logging with the [Endpoint].[request_response_logging_config],
                                  and must contain the deployed model corresponding
                                  to the model version specified in [ModelMonitor].[model_monitoring_target].
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                    type: object
                  outputSpec:
                    description: The Output destination spec for metrics, error logs,
                      etc.
                    properties:
                      gcsBaseDirectory:
                        description: Google Cloud Storage base folder path for metrics,
                          error logs, etc.
                        properties:
                          outputURIPrefix:
                            description: Required. Google Cloud Storage URI to output
                              directory. If the uri doesn't end with '/', a '/' will
                              be automatically appended. The directory is created
                              if it doesn't exist.
                            type: string
                        type: object
                    type: object
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The VertexAIModelMonitoringJob name. If not given, the
                  metadata.name will be used.
                type: string
            required:
            - location
            - modelMonitorRef
            - projectRef
            type: object
          status:
            description: VertexAIModelMonitoringJobStatus defines the config connector
              machine state of VertexAIModelMonitoringJob
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the VertexAIModelMonitoringJob
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Timestamp when this ModelMonitoringJob
                      was created.
                    type: string
                  jobExecutionDetail:
                    description: Output only. Execution results for all the monitoring
                      objectives.
                    properties:
                      baselineDatasets:
                        description: Processed baseline datasets.
                        items:
                          description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.ModelMonitoringJobExecutionDetail.ProcessedDataset
                          properties:
                            location:
                              description: Actual data location of the processed dataset.
                              type: string
                            timeRange:
                              description: Dataset time range information if any.
                              properties:
                                endTime:
                                  description: |-
                                    Optional. Exclusive end of the interval.

                                     If specified, a Timestamp matching this interval will have to be before the
                                     end.
                                  type: string
                                startTime:
                                  description: |-
                                    Optional. Inclusive start of the interval.

                                     If specified, a Timestamp matching this interval will have to be the same
                                     or after the start.
                                  type: string
                              type: object
                          type: object
                        type: array
                      error:
                        description: Additional job error status.
                        properties:
                          code:
                            description: The status code, which should be an enum
                              value of [google.rpc.Code][google.rpc.Code].
                            format: int32
                            type: integer
                          message:
                            description: A developer-facing error message, which should
                              be in English. Any user-facing error message should
                              be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                              field, or localized by the client.
                            type: string
                        type: object
                      targetDatasets:
                        description: Processed target datasets.
                        items:
                          description: skipped_kcc:proto=google.cloud.aiplatform.v1beta1.ModelMonitoringJobExecutionDetail.ProcessedDataset
                          properties:
                            location:
                              description: Actual data location of the processed dataset.
                              type: string
                            timeRange:
                              description: Dataset time range information if any.
                              properties:
                                endTime:
                                  description: |-
                                    Optional. Exclusive end of the interval.

                                     If specified, a Timestamp matching this interval will have to be before the
                                     end.
                                  type: string
                                startTime:
                                  description: |-
                                    Optional. Inclusive start of the interval.

                                     If specified, a Timestamp matching this interval will have to be the same
                                     or after the start.
                                  type: string
                              type: object
                          type: object
                        type: array
                    type: object
                  name:
                    description: 'Output only. Resource name of a ModelMonitoringJob.
                      Format: `projects/{project_id}/locations/{location_id}/modelMonitors/{model_monitor_id}/modelMonitoringJobs/{model_monitoring_job_id}`'
                    type: string
                  schedule:
                    description: Output only. Schedule resource name. It will only
                      appear when this job is triggered by a schedule.
                    type: string
                  scheduleTime:
                    description: Output only. Timestamp when this ModelMonitoringJob
                      was scheduled. It will only appear when this job is triggered
                      by a schedule.
                    type: string
                  state:
                    description: Output only. The state of the monitoring job. * When
                      the job is still creating, the state will be 'JOB_STATE_PENDING'.
                      * Once the job is successfully created, the state will be 'JOB_STATE_RUNNING'.
                      * Once the job is finished, the state will be one of 'JOB_STATE_FAILED',
                      'JOB_STATE_SUCCEEDED', 'JOB_STATE_PARTIALLY_SUCCEEDED'.
                    type: string
                  updateTime:
                    description: Output only. Timestamp when this ModelMonitoringJob
                      was updated most recently.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
