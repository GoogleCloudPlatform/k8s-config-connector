# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: dataplexmetadatafeeds.dataplex.cnrm.cloud.google.com
spec:
  group: dataplex.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DataplexMetadataFeed
    listKind: DataplexMetadataFeedList
    plural: dataplexmetadatafeeds
    shortNames:
    - gcpdataplexmetadatafeed
    - gcpdataplexmetadatafeeds
    singular: dataplexmetadatafeed
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataplexMetadataFeed is the Schema for the DataplexMetadataFeed
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DataplexMetadataFeedSpec defines the desired state of DataplexMetadataFeed
            properties:
              filters:
                description: Optional. The filters of the metadata feed. Only the
                  changes that match the filters are published.
                properties:
                  aspectTypeRefs:
                    description: 'Optional. The aspect types that you want to listen
                      to. Depending on how the aspect is attached to the entry, in
                      the format: `projects/{project_id_or_number}/locations/{location}/aspectTypes/{aspect_type_id}`.'
                    items:
                      description: AspectTypeRef is a reference to a DataplexAspectType.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed DataplexAspectType
                            resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/aspectTypes/{{aspecttypeID}}".
                          type: string
                        name:
                          description: The name of a DataplexAspectType resource.
                          type: string
                        namespace:
                          description: The namespace of a DataplexAspectType resource.
                          type: string
                      type: object
                    type: array
                  changeTypes:
                    description: Optional. The type of change that you want to listen
                      to. If not specified, all changes are published.
                    items:
                      enum:
                      - CREATE
                      - UPDATE
                      - DELETE
                      type: string
                    type: array
                  entryTypeRefs:
                    description: Optional. The entry types that you want to listen
                      to, specified as relative resource names in the format `projects/{project_id_or_number}/locations/{location}/entryTypes/{entry_type_id}`.
                      Only entries that belong to the specified entry types are published.
                    items:
                      description: EntryTypeRef is a reference to a DataplexEntryType.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed DataplexEntryType
                            resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/entryTypes/{{entrytypeID}}".
                          type: string
                        name:
                          description: The name of a DataplexEntryType resource.
                          type: string
                        namespace:
                          description: The namespace of a DataplexEntryType resource.
                          type: string
                      type: object
                    type: array
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Optional. User-defined labels.
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              pubsubTopic:
                description: 'Optional. The pubsub topic that you want the metadata
                  feed messages to publish to. Please grant Dataplex service account
                  the permission to publish messages to the topic. The service account
                  is: service-{PROJECT_NUMBER}@gcp-sa-dataplex.iam.gserviceaccount.com.'
                type: string
              resourceID:
                description: The DataplexMetadataFeed name. If not given, the metadata.name
                  will be used.
                type: string
              scope:
                description: Required. The scope of the metadata feed. Only the in
                  scope changes are published.
                properties:
                  entryGroupRefs:
                    description: 'Optional. The entry groups whose entries you want
                      to listen to. Must be in the format: `projects/{project_id_or_number}/locations/{location_id}/entryGroups/{entry_group_id}`.'
                    items:
                      description: EntryGroupRef is a reference to a DataplexEntryGroup.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed DataplexEntryGroup
                            resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/entryGroups/{{entrygroupID}}".
                          type: string
                        name:
                          description: The name of a DataplexEntryGroup resource.
                          type: string
                        namespace:
                          description: The namespace of a DataplexEntryGroup resource.
                          type: string
                      type: object
                    type: array
                  organizationLevel:
                    description: |-
                      Optional. Whether the metadata feed is at the organization-level.

                       - If `true`, all changes happened to the entries in the same
                       organization as the feed are published.
                       - If `false`, you must specify a list of projects or a list of entry
                       groups whose entries you want to listen to.

                       The default is `false`.
                    type: boolean
                  projectRefs:
                    description: 'Optional. The projects whose entries you want to
                      listen to. Must be in the same organization as the feed. Must
                      be in the format: `projects/{project_id_or_number}`.'
                    items:
                      description: The Project that this resource belongs to.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: The `projectID` field of a project, when not
                            managed by Config Connector.
                          type: string
                        kind:
                          description: The kind of the Project resource; optional
                            but must be `Project` if provided.
                          type: string
                        name:
                          description: The `name` field of a `Project` resource.
                          type: string
                        namespace:
                          description: The `namespace` field of a `Project` resource.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: DataplexMetadataFeedStatus defines the config connector machine
              state of DataplexMetadataFeed
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DataplexMetadataFeed resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The time when the feed was created.
                    type: string
                  uid:
                    description: Output only. A system-generated, globally unique
                      ID for the metadata job. If the metadata job is deleted and
                      then re-created with the same name, this ID is different.
                    type: string
                  updateTime:
                    description: Output only. The time when the feed was updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
