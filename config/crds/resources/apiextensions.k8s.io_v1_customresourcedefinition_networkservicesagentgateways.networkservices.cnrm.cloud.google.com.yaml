# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: networkservicesagentgateways.networkservices.cnrm.cloud.google.com
spec:
  group: networkservices.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: NetworkServicesAgentGateway
    listKind: NetworkServicesAgentGatewayList
    plural: networkservicesagentgateways
    shortNames:
    - gcpnetworkservicesagentgateway
    - gcpnetworkservicesagentgateways
    singular: networkservicesagentgateway
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkServicesAgentGateway is the Schema for the NetworkServicesAgentGateway
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkServicesAgentGatewaySpec defines the desired state
              of NetworkServicesAgentGateway
            properties:
              description:
                description: Optional. A free-text description of the resource. Max
                  length 1024 characters.
                type: string
              googleManaged:
                description: Optional. Proxy is orchestrated and managed by GoogleCloud
                  in a tenant project.
                properties:
                  governedAccessPath:
                    description: Optional. Operating Mode of Agent Gateway.
                    type: string
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Optional. Set of label tags associated with the AgentGateway
                  resource.
                type: object
              location:
                description: The location of this resource.
                type: string
              networkConfig:
                description: Optional. Network configuration for the AgentGateway.
                properties:
                  dnsPeeringConfig:
                    description: Optional. Optional DNS peering configuration for
                      connectivity to your private VPC network.
                    properties:
                      domains:
                        description: Required. Domain names for which DNS queries
                          should be forwarded to the target network.
                        items:
                          type: string
                        type: array
                      targetNetworkRef:
                        description: Required. Target network in 'target project'
                          to which DNS queries should be forwarded to. Must be in
                          format of `projects/{project}/global/networks/{network}`.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed ComputeNetwork
                              resource. Should be in the format "projects/{{projectID}}/global/networks/{{networkID}}".
                            type: string
                          name:
                            description: The name of a ComputeNetwork resource.
                            type: string
                          namespace:
                            description: The namespace of a ComputeNetwork resource.
                            type: string
                        type: object
                      targetProject:
                        description: Required. Target project ID to which DNS queries
                          should be forwarded to. This can be the same project that
                          contains the AgentGateway or a different project.
                        type: string
                    type: object
                  egress:
                    description: Optional. Optional PSC-Interface network attachment
                      for connectivity to your private VPCs network.
                    properties:
                      networkAttachmentRef:
                        description: Optional. The URI of the Network Attachment resource.
                        oneOf:
                        - not:
                            required:
                            - external
                          required:
                          - name
                        - not:
                            anyOf:
                            - required:
                              - name
                            - required:
                              - namespace
                          required:
                          - external
                        properties:
                          external:
                            description: A reference to an externally managed ComputeNetworkAttachment
                              resource. Should be in the format "projects/{{projectID}}/regions/{{region}}/networkAttachments/{{networkattachmentID}}".
                            type: string
                          name:
                            description: The name of a ComputeNetworkAttachment resource.
                            type: string
                          namespace:
                            description: The namespace of a ComputeNetworkAttachment
                              resource.
                            type: string
                        type: object
                      trustConfig:
                        description: Optional. TrustConfig defines the trust configuration
                          for egress.
                        properties:
                          pemCertificates:
                            description: Required. PEM encoded root certificates used
                              to validate the identity of the upstream servers/destinations
                              during egress connections.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              registries:
                description: 'Optional. A list of Agent registries containing the
                  agents, MCP servers and tools governed by the Agent Gateway. Note:
                  Currently limited to project-scoped registries Must be of format
                  `//agentregistry.googleapis.com/projects/{project}/locations/{location}/`'
                items:
                  type: string
                type: array
              resourceID:
                description: The NetworkServicesAgentGateway name. If not given, the
                  metadata.name will be used.
                type: string
              selfManaged:
                description: Optional. Attach to existing Application Load Balancers
                  or Secure Web Proxies.
                properties:
                  resourceURI:
                    description: Optional. A supported Google Cloud networking proxy
                      in the Project and Location
                    type: string
                  resourceURIs:
                    description: Optional. List of supported Google Cloud networking
                      proxies in the Project and Location. resource_uris is mutually
                      exclusive with resource_uri.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: NetworkServicesAgentGatewayStatus defines the config connector
              machine state of NetworkServicesAgentGateway
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the NetworkServicesAgentGateway
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  agentGatewayCard:
                    description: Output only. Field for populated AgentGateway card.
                    properties:
                      mtlsEndpoint:
                        description: Output only. mTLS Endpoint associated with this
                          AgentGateway
                        type: string
                      rootCertificates:
                        description: Output only. Root Certificates for Agents to
                          validate this AgentGateway
                        items:
                          type: string
                        type: array
                      serviceExtensionsServiceAccount:
                        description: Output only. Service Account used by Service
                          Extensions to operate.
                        type: string
                    type: object
                  createTime:
                    description: Output only. The timestamp when the resource was
                      created.
                    type: string
                  updateTime:
                    description: Output only. The timestamp when the resource was
                      updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
