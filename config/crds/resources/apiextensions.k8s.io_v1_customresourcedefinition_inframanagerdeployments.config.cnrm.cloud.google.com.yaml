# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: inframanagerdeployments.config.cnrm.cloud.google.com
spec:
  group: config.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: InfraManagerDeployment
    listKind: InfraManagerDeploymentList
    plural: inframanagerdeployments
    shortNames:
    - gcpinframanagerdeployment
    - gcpinframanagerdeployments
    singular: inframanagerdeployment
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfraManagerDeployment is the Schema for the InfraManagerDeployment
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InfraManagerDeploymentSpec defines the desired state of InfraManagerDeployment
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Optional. Arbitrary key-value metadata storage e.g. to
                  help client tools identify deployments during automation. See https://google.aip.dev/148#annotations
                  for details on format and size limitations.
                type: object
              artifactsGCSBucket:
                description: |-
                  Optional. User-defined location of Cloud Build logs and artifacts in Google
                   Cloud Storage. Format: `gs://{bucket}/{folder}`

                   A default bucket will be bootstrapped if the field is not set or empty.
                   Default bucket format: `gs://<project number>-<region>-blueprint-config`
                   Constraints:
                   - The bucket needs to be in the same project as the deployment
                   - The path cannot be within the path of `gcs_source`
                   - The field cannot be updated, including changing its presence
                type: string
              importExistingResources:
                description: |-
                  By default, Infra Manager will return a failure when
                   Terraform encounters a 409 code (resource conflict error) during actuation.
                   If this flag is set to true, Infra Manager will instead
                   attempt to automatically import the resource into the Terraform state (for
                   supported resource types) and continue actuation.

                   Not all resource types are supported, refer to documentation.
                type: boolean
              labels:
                additionalProperties:
                  type: string
                description: Optional. User-defined metadata for the deployment.
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              quotaValidation:
                description: Optional. Input to control quota checks for resources
                  in terraform configuration files. There are limited resources on
                  which quota validation applies.
                enum:
                - QUOTA_VALIDATION_UNSPECIFIED
                - ENABLED
                - ENFORCED
                type: string
              resourceID:
                description: The InfraManagerDeployment name. If not given, the metadata.name
                  will be used.
                type: string
              serviceAccountRef:
                description: 'Required. User-specified Service Account (SA) credentials
                  to be used when actuating resources. Format: `projects/{projectID}/serviceAccounts/{serviceAccount}`'
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `email` field of an `IAMServiceAccount` resource.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                type: object
              terraformBlueprint:
                description: A blueprint described using Terraform's HashiCorp Configuration
                  Language as a root module.
                properties:
                  gcsSource:
                    description: |-
                      URI of an object in Google Cloud Storage.
                       Format: `gs://{bucket}/{object}`

                       URI may also specify an object version for zipped objects.
                       Format: `gs://{bucket}/{object}#{version}`
                    type: string
                  gitSource:
                    description: URI of a public Git repo.
                    properties:
                      directory:
                        description: 'Optional. Subdirectory inside the repository.
                          Example: ''staging/my-package'''
                        type: string
                      ref:
                        description: Optional. Git reference (e.g. branch or tag).
                        type: string
                      repo:
                        description: 'Optional. Repository URL. Example: ''https://github.com/kubernetes/examples.git'''
                        type: string
                    type: object
                  inputValues:
                    additionalProperties:
                      properties:
                        inputValue:
                          description: Optional. Input variable value.
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    description: Optional. Input variable values for the Terraform
                      blueprint.
                    type: object
                type: object
              tfVersionConstraint:
                description: 'Optional. The user-specified Terraform version constraint.
                  Example: "=1.3.10".'
                type: string
              workerPoolRef:
                description: 'Optional. The user-specified Cloud Build worker pool
                  resource in which the Cloud Build job will execute. Format: `projects/{project}/locations/{location}/workerPools/{workerPoolId}`.
                  If this field is unspecified, the default Cloud Build worker pool
                  will be used.'
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: A reference to an externally managed CloudBuildWorkerPool
                      resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/workerPools/{{workerPoolID}}".
                    type: string
                  name:
                    description: The name of a CloudBuildWorkerPool resource.
                    type: string
                  namespace:
                    description: The namespace of a CloudBuildWorkerPool resource.
                    type: string
                type: object
            required:
            - location
            - projectRef
            - serviceAccountRef
            type: object
          status:
            description: InfraManagerDeploymentStatus defines the config connector
              machine state of InfraManagerDeployment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the InfraManagerDeployment resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. Time when the deployment was created.
                    type: string
                  deleteBuild:
                    description: Output only. Cloud Build instance UUID associated
                      with deleting this deployment.
                    type: string
                  deleteLogs:
                    description: 'Output only. Location of Cloud Build logs in Google
                      Cloud Storage, populated when deleting this deployment. Format:
                      `gs://{bucket}/{object}`.'
                    type: string
                  deleteResults:
                    description: Output only. Location of artifacts from a DeleteDeployment
                      operation.
                    properties:
                      artifacts:
                        description: 'Location of artifacts (e.g. logs) in Google
                          Cloud Storage. Format: `gs://{bucket}/{object}`'
                        type: string
                      content:
                        description: 'Location of a blueprint copy and other manifests
                          in Google Cloud Storage. Format: `gs://{bucket}/{object}`'
                        type: string
                      outputs:
                        additionalProperties:
                          properties:
                            sensitive:
                              description: Identifies whether Terraform has set this
                                output as a potential sensitive value.
                              type: boolean
                            value:
                              description: Value of output.
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        description: Map of output name to output info.
                        type: object
                    type: object
                  errorCode:
                    description: Output only. Error code describing errors that may
                      have occurred.
                    type: string
                  errorLogs:
                    description: 'Output only. Location of Terraform error logs in
                      Google Cloud Storage. Format: `gs://{bucket}/{object}`.'
                    type: string
                  latestRevision:
                    description: 'Output only. Revision name that was most recently
                      applied. Format: `projects/{project}/locations/{location}/deployments/{deployment}/
                      revisions/{revision}`'
                    type: string
                  lockState:
                    description: Output only. Current lock state of the deployment.
                    type: string
                  state:
                    description: Output only. Current state of the deployment.
                    type: string
                  stateDetail:
                    description: Output only. Additional information regarding the
                      current state.
                    type: string
                  tfErrors:
                    description: Output only. Errors encountered when deleting this
                      deployment. Errors are truncated to 10 entries, see `delete_results`
                      and `error_logs` for full details.
                    items:
                      properties:
                        error:
                          description: Output only. Original error response from underlying
                            Google API, if available.
                          properties:
                            code:
                              description: The status code, which should be an enum
                                value of [google.rpc.Code][google.rpc.Code].
                              format: int32
                              type: integer
                            message:
                              description: A developer-facing error message, which
                                should be in English. Any user-facing error message
                                should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                                field, or localized by the client.
                              type: string
                          type: object
                        errorDescription:
                          description: A human-readable error description.
                          type: string
                        httpResponseCode:
                          description: HTTP response code returned from Google Cloud
                            Platform APIs when Terraform fails to provision the resource.
                            If unset or 0, no HTTP response code was returned by Terraform.
                          format: int32
                          type: integer
                        resourceAddress:
                          description: Address of the resource associated with the
                            error, e.g. `google_compute_network.vpc_network`.
                          type: string
                      type: object
                    type: array
                  tfVersion:
                    description: Output only. The current Terraform version set on
                      the deployment. It is in the format of "Major.Minor.Patch",
                      for example, "1.3.10".
                    type: string
                  updateTime:
                    description: Output only. Time when the deployment was last modified.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
