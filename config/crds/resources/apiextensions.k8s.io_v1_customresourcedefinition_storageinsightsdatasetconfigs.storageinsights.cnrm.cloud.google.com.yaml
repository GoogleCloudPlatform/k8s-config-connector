# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: storageinsightsdatasetconfigs.storageinsights.cnrm.cloud.google.com
spec:
  group: storageinsights.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: StorageInsightsDatasetConfig
    listKind: StorageInsightsDatasetConfigList
    plural: storageinsightsdatasetconfigs
    shortNames:
    - gcpstorageinsightsdatasetconfig
    - gcpstorageinsightsdatasetconfigs
    singular: storageinsightsdatasetconfig
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageInsightsDatasetConfig is the Schema for the StorageInsightsDatasetConfig
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StorageInsightsDatasetConfigSpec defines the desired state
              of StorageInsightsDatasetConfig
            properties:
              cloudStorageObjectPath:
                description: |-
                  Input only. Cloud Storage object path containing a list of
                  project or folder numbers to include in the dataset;
                  it cannot contain a mix of project and folders.

                  The object must be a text file where each line has one of the following
                  entries:

                  - Project number, formatted as `projects/<project_number>`, for example,
                  `projects/1234567890`.
                  - Folder identifier, formatted as `folders/<folder_number>`, for example,
                  `folders/9876543210`.
                  Path must be in the format `gs://<bucket_name>/<object_name>`.
                type: string
              description:
                description: |-
                  Optional. A user-provided description for the dataset configuration.

                  Maximum length: 256 characters.
                type: string
              excludeCloudStorageBuckets:
                properties:
                  cloudStorageBuckets:
                    items:
                      properties:
                        bucketPrefixRegex:
                          description: A regex pattern for bucket names matching the
                            regex. Regex should follow the syntax specified in `google/re2`
                            on GitHub.
                          type: string
                        bucketRef:
                          description: Cloud Storage bucket reference.
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed StorageBucket
                                resource. Should be in the format "projects/{{projectID}}/buckets/{{bucketID}}".
                              type: string
                            name:
                              description: The name of a StorageBucket resource.
                              type: string
                            namespace:
                              description: The namespace of a StorageBucket resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              excludeCloudStorageLocations:
                properties:
                  locations:
                    items:
                      type: string
                    type: array
                type: object
              identity:
                description: Identity used by this `datasetConfig`.
                properties:
                  type:
                    description: Type of identity to use for the datasetConfig.
                    type: string
                type: object
              includeCloudStorageBuckets:
                properties:
                  cloudStorageBuckets:
                    items:
                      properties:
                        bucketPrefixRegex:
                          description: A regex pattern for bucket names matching the
                            regex. Regex should follow the syntax specified in `google/re2`
                            on GitHub.
                          type: string
                        bucketRef:
                          description: Cloud Storage bucket reference.
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed StorageBucket
                                resource. Should be in the format "projects/{{projectID}}/buckets/{{bucketID}}".
                              type: string
                            name:
                              description: The name of a StorageBucket resource.
                              type: string
                            namespace:
                              description: The namespace of a StorageBucket resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              includeCloudStorageLocations:
                properties:
                  locations:
                    items:
                      type: string
                    type: array
                type: object
              includeNewlyCreatedBuckets:
                description: If set to `true`, the request includes all the newly
                  created buckets in the dataset that meet the inclusion and exclusion
                  rules.
                type: boolean
              labels:
                additionalProperties:
                  type: string
                description: Labels as key value pairs
                type: object
              link:
                description: Details of the linked dataset.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              location:
                description: The location of this resource.
                type: string
              organizationNumber:
                description: Optional. Organization resource ID that the source projects
                  should belong to. Projects that do not belong to the provided organization
                  are not considered when creating the dataset.
                format: int64
                type: integer
              organizationScope:
                description: Defines the options for providing a source organization
                  for the dataset.
                type: boolean
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The StorageInsightsDatasetConfig name. If not given,
                  the metadata.name will be used.
                type: string
              retentionPeriodDays:
                description: Number of days of history that must be retained.
                format: int32
                type: integer
              skipVerificationAndIngest:
                description: Optional. If set to `false`, then all the permission
                  checks must be successful before the system can start ingesting
                  data. This field can only be updated before the system ingests data
                  for the first time. Any attempt to modify the field after data ingestion
                  starts results in an error.
                type: boolean
              sourceFolders:
                description: Defines the options for providing source folders for
                  the dataset.
                properties:
                  folderNumbers:
                    description: Optional. The list of folder numbers to include in
                      the dataset.
                    items:
                      format: int64
                      type: integer
                    type: array
                type: object
              sourceProjects:
                description: Defines the options for providing source projects for
                  the dataset.
                properties:
                  projectNumbers:
                    description: Optional. The list of project numbers to include
                      in the dataset.
                    items:
                      format: int64
                      type: integer
                    type: array
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: StorageInsightsDatasetConfigStatus defines the config connector
              machine state of StorageInsightsDatasetConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the StorageInsightsDatasetConfig
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The UTC time at which the dataset configuration
                      was created. This is auto-populated.
                    type: string
                  datasetConfigState:
                    description: Output only. State of the `datasetConfig`.
                    type: string
                  identity:
                    description: Identity used by this `datasetConfig`.
                    properties:
                      name:
                        description: Output only. Name of the identity.
                        type: string
                    type: object
                  link:
                    description: Details of the linked dataset.
                    properties:
                      dataset:
                        description: Output only. Dataset name for linked dataset.
                        type: string
                      linked:
                        description: Output only. State of the linked dataset.
                        type: boolean
                    type: object
                  status:
                    description: Output only. Status of the `datasetConfig`.
                    properties:
                      code:
                        description: The status code, which should be an enum value
                          of [google.rpc.Code][google.rpc.Code].
                        format: int32
                        type: integer
                      message:
                        description: A developer-facing error message, which should
                          be in English. Any user-facing error message should be localized
                          and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                          field, or localized by the client.
                        type: string
                    type: object
                  uid:
                    description: Output only. System generated unique identifier for
                      the resource.
                    type: string
                  updateTime:
                    description: Output only. The UTC time at which the dataset configuration
                      was last updated. This is auto-populated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
