# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: apihubdependencies.apihub.cnrm.cloud.google.com
spec:
  group: apihub.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: APIHubDependency
    listKind: APIHubDependencyList
    plural: apihubdependencies
    shortNames:
    - gcpapihubdependency
    - gcpapihubdependencies
    singular: apihubdependency
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: APIHubDependency is the Schema for the APIHubDependency API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: APIHubDependencySpec defines the desired state of APIHubDependency
            properties:
              attributes:
                description: Optional. The list of user defined attributes associated
                  with the dependency resource.
                items:
                  properties:
                    attributeRef:
                      description: The attribute reference.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: The `id` of a `APIHubAttribute` resource, when
                            not managed by Config Connector.
                          type: string
                        name:
                          description: The `name` field of a `APIHubAttribute` resource.
                          type: string
                        namespace:
                          description: The `namespace` field of a `APIHubAttribute`
                            resource.
                          type: string
                      type: object
                    values:
                      description: The attribute values associated with a resource.
                      properties:
                        enumValues:
                          description: The attribute values associated with a resource
                            in case attribute data type is enum.
                          properties:
                            values:
                              description: Required. The attribute values in case
                                attribute data type is enum.
                              items:
                                properties:
                                  description:
                                    description: Optional. The detailed description
                                      of the allowed value.
                                    type: string
                                  displayName:
                                    description: Required. The display name of the
                                      allowed value.
                                    type: string
                                  id:
                                    description: |-
                                      Required. The ID of the allowed value.
                                       * If provided, the same will be used. The service will throw an error if
                                       the specified id is already used by another allowed value in the same
                                       attribute resource.
                                       * If not provided, a system generated id derived from the display name
                                       will be used. In this case, the service will handle conflict resolution
                                       by adding a system generated suffix in case of duplicates.

                                       This value should be 4-63 characters, and valid characters
                                       are /[a-z][0-9]-/.
                                    type: string
                                  immutable:
                                    description: Optional. When set to true, the allowed
                                      value cannot be updated or deleted by the user.
                                      It can only be true for System defined attributes.
                                    type: boolean
                                type: object
                              type: array
                          type: object
                        jsonValues:
                          description: The attribute values associated with a resource
                            in case attribute data type is JSON.
                          properties:
                            values:
                              description: Required. The attribute values in case
                                attribute data type is string or JSON.
                              items:
                                type: string
                              type: array
                          type: object
                        stringValues:
                          description: The attribute values associated with a resource
                            in case attribute data type is string.
                          properties:
                            values:
                              description: Required. The attribute values in case
                                attribute data type is string or JSON.
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                  required:
                  - attributeRef
                  - values
                  type: object
                type: array
              consumer:
                description: Required. Immutable. The entity acting as the consumer
                  in the dependency.
                properties:
                  externalAPIRef:
                    description: |-
                      The resource name of an external API in the API Hub.

                       Format:
                       `projects/{project}/locations/{location}/externalApis/{external_api}`
                    properties:
                      external:
                        description: A reference to an externally managed API Hub
                          External API resource. Should be of the format `projects/{project}/locations/{location}/externalApis/{external_api}`.
                        type: string
                    type: object
                  operationRef:
                    description: |-
                      The resource name of an operation in the API Hub.

                       Format:
                       `projects/{project}/locations/{location}/apis/{api}/versions/{version}/operations/{operation}`
                    properties:
                      external:
                        description: A reference to an externally managed API Hub
                          Operation resource. Should be of the format `projects/{project}/locations/{location}/apis/{api}/versions/{version}/operations/{operation}`.
                        type: string
                    type: object
                type: object
              description:
                description: Optional. Human readable description corresponding of
                  the dependency.
                type: string
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The APIHubDependency name. If not given, the metadata.name
                  will be used.
                type: string
              supplier:
                description: Required. Immutable. The entity acting as the supplier
                  in the dependency.
                properties:
                  externalAPIRef:
                    description: |-
                      The resource name of an external API in the API Hub.

                       Format:
                       `projects/{project}/locations/{location}/externalApis/{external_api}`
                    properties:
                      external:
                        description: A reference to an externally managed API Hub
                          External API resource. Should be of the format `projects/{project}/locations/{location}/externalApis/{external_api}`.
                        type: string
                    type: object
                  operationRef:
                    description: |-
                      The resource name of an operation in the API Hub.

                       Format:
                       `projects/{project}/locations/{location}/apis/{api}/versions/{version}/operations/{operation}`
                    properties:
                      external:
                        description: A reference to an externally managed API Hub
                          Operation resource. Should be of the format `projects/{project}/locations/{location}/apis/{api}/versions/{version}/operations/{operation}`.
                        type: string
                    type: object
                type: object
            required:
            - consumer
            - location
            - projectRef
            - supplier
            type: object
          status:
            description: APIHubDependencyStatus defines the config connector machine
              state of APIHubDependency
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the APIHubDependency resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  consumer:
                    description: Required. Immutable. The entity acting as the consumer
                      in the dependency.
                    properties:
                      displayName:
                        description: Output only. Display name of the entity.
                        type: string
                    type: object
                  createTime:
                    description: Output only. The time at which the dependency was
                      created.
                    type: string
                  discoveryMode:
                    description: Output only. Discovery mode of the dependency.
                    type: string
                  errorDetail:
                    description: Output only. Error details of a dependency if the
                      system has detected it internally.
                    properties:
                      error:
                        description: Optional. Error in the dependency.
                        type: string
                      errorTime:
                        description: Optional. Timestamp at which the error was found.
                        type: string
                    type: object
                  state:
                    description: Output only. State of the dependency.
                    type: string
                  updateTime:
                    description: Output only. The time at which the dependency was
                      last updated.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
