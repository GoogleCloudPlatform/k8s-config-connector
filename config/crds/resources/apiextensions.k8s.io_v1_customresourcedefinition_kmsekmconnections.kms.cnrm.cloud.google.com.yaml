# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: kmsekmconnections.kms.cnrm.cloud.google.com
spec:
  group: kms.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: KMSEKMConnection
    listKind: KMSEKMConnectionList
    plural: kmsekmconnections
    shortNames:
    - gcpkmsekmconnection
    - gcpkmsekmconnections
    singular: kmsekmconnection
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'KMSEKMConnection is the Schema for the KMSEKMConnection API.
          Note: EkmConnection resources cannot be deleted via the Google Cloud API.
          To remove this resource from Config Connector management, users must use
          the ''cnrm.cloud.google.com/deletion-policy: abandon'' annotation.'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KMSEKMConnectionSpec defines the desired state of KMSEKMConnection
            properties:
              cryptoSpacePath:
                description: 'Optional. Identifies the EKM Crypto Space that this
                  EkmConnection maps to. Note: This field is required if KeyManagementMode
                  is CLOUD_KMS.'
                type: string
              keyManagementMode:
                description: Optional. Describes who can perform control plane operations
                  on the EKM. If unset, this defaults to MANUAL.
                type: string
              location:
                description: Immutable. The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The KMSEKMConnection name. If not given, the metadata.name
                  will be used.
                type: string
              serviceResolvers:
                description: Optional. A list of ServiceResolvers where the EKM can
                  be reached. There should be one ServiceResolver per EKM replica.
                  Currently, only a single ServiceResolver is supported.
                items:
                  properties:
                    endpointFilter:
                      description: Optional. The filter applied to the endpoints of
                        the resolved service. If no filter is specified, all endpoints
                        will be considered. An endpoint will be chosen arbitrarily
                        from the filtered list for each request.
                      type: string
                    hostname:
                      description: Required. The hostname of the EKM replica used
                        at TLS and HTTP layers.
                      type: string
                    serverCertificates:
                      description: Required. A list of leaf server certificates used
                        to authenticate HTTPS connections to the EKM replica. Currently,
                        a maximum of 10 Certificate is supported.
                      items:
                        properties:
                          rawDer:
                            description: Required. The raw certificate bytes in DER
                              format.
                            format: byte
                            type: string
                        type: object
                      type: array
                    serviceDirectoryServiceRef:
                      description: Required. The resource name of the Service Directory
                        service pointing to an EKM replica, in the format projects/*/locations/*/namespaces/*/services/*.
                      oneOf:
                      - not:
                          required:
                          - external
                        required:
                        - name
                      - not:
                          anyOf:
                          - required:
                            - name
                          - required:
                            - namespace
                        required:
                        - external
                      properties:
                        external:
                          description: A reference to an externally managed ServiceDirectoryService
                            resource. Should be in the format "projects/*/locations/*/namespaces/*/services/*".
                          type: string
                        name:
                          description: The name of a ServiceDirectoryService resource.
                          type: string
                        namespace:
                          description: The namespace of a ServiceDirectoryService
                            resource.
                          type: string
                      type: object
                  type: object
                type: array
            required:
            - location
            - projectRef
            type: object
          status:
            description: KMSEKMConnectionStatus defines the config connector machine
              state of KMSEKMConnection
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the KMSEKMConnection resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The time at which the EkmConnection
                      was created.
                    type: string
                  serviceResolvers:
                    description: Output only. The parsed server certificates from
                      the service resolvers.
                    items:
                      properties:
                        serverCertificates:
                          items:
                            properties:
                              issuer:
                                description: Output only. The issuer distinguished
                                  name in RFC 2253 format. Only present if parsed
                                  is true.
                                type: string
                              notAfterTime:
                                description: Output only. The certificate is not valid
                                  after this time. Only present if parsed is true.
                                type: string
                              notBeforeTime:
                                description: Output only. The certificate is not valid
                                  before this time. Only present if parsed is true.
                                type: string
                              parsed:
                                description: Output only. True if the certificate
                                  was parsed successfully.
                                type: boolean
                              serialNumber:
                                description: Output only. The certificate serial number
                                  as a hex string. Only present if parsed is true.
                                type: string
                              sha256Fingerprint:
                                description: Output only. The SHA-256 certificate
                                  fingerprint as a hex string. Only present if parsed
                                  is true.
                                type: string
                              subject:
                                description: Output only. The subject distinguished
                                  name in RFC 2253 format. Only present if parsed
                                  is true.
                                type: string
                              subjectAlternativeDNSNames:
                                description: Output only. The subject Alternative
                                  DNS names. Only present if parsed is true.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
