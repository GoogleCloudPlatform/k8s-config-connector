apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: computeregiontargettcpproxies.compute.cnrm.cloud.google.com
spec:
  group: compute.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: ComputeRegionTargetTCPProxy
    listKind: ComputeRegionTargetTCPProxyList
    plural: computeregiontargettcpproxies
    shortNames:
    - gcpcomputeregiontargettcpproxy
    - gcpcomputeregiontargettcpproxys
    singular: computeregiontargettcpproxy
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComputeRegionTargetTCPProxy is the Schema for the ComputeRegionTargetTCPProxy
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ComputeRegionTargetTCPProxySpec defines the desired state
              of ComputeRegionTargetTCPProxy
            properties:
              backendServiceRef:
                description: Immutable. An optional description of this resource.
                  Immutable. A reference to the ComputeBackendService resource.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The ComputeBackendService selflink in the form "projects/{{project}}/global/backendServices/{{name}}"
                      or "projects/{{project}}/regions/{{region}}/backendServices/{{name}}"
                      when not managed by Config Connector.
                    type: string
                  name:
                    description: The `name` field of a `ComputeBackendService` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `ComputeBackendService`
                      resource.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: BackendServiceRef is immutable
                  rule: self == oldSelf
              description:
                description: Immutable. An optional description of this resource.
                type: string
                x-kubernetes-validations:
                - message: Description is immutable
                  rule: self == oldSelf
              location:
                description: 'Location represents the geographical location of the
                  ComputeTargetTCPProxy. If it is not provided, the provider project
                  is used. Reference: GCP definition of regions/zones (https://cloud.google.com/compute/docs/regions-zones/)'
                type: string
                x-kubernetes-validations:
                - message: Location is immutable
                  rule: self == oldSelf
              proxyBind:
                description: Immutable. This field only applies when the forwarding
                  rule that references this target proxy has a loadBalancingScheme
                  set to INTERNAL_SELF_MANAGED.
                type: boolean
                x-kubernetes-validations:
                - message: ProxyBind is immutable
                  rule: self == oldSelf
              proxyHeader:
                description: 'Immutable. Specifies the type of proxy header to append
                  before sending data to the backend. Default value: "NONE" Possible
                  values: ["NONE", "PROXY_V1"].'
                type: string
                x-kubernetes-validations:
                - message: ProxyHeader is immutable
                  rule: self == oldSelf
              resourceID:
                description: Immutable. The ComputeRegionTargetTCPProxy name. If not
                  given, the metadata.name will be used.
                type: string
                x-kubernetes-validations:
                - message: ResourceID field is immutable
                  rule: self == oldSelf
            required:
            - backendServiceRef
            - location
            type: object
          status:
            description: ComputeRegionTargetTCPProxyStatus defines the config connector
              machine state of ComputeRegionTargetTCPProxy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              creationTimestamp:
                description: Creation timestamp in RFC3339 text format.
                type: string
              externalRef:
                description: A unique specifier for the ComputeRegionTargetTCPProxy
                  resource in GCP.
                type: string
              kind:
                description: Type of the resource. Always compute#targetTcpProxy for
                  target TCP proxies.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                type: object
              proxyId:
                description: The unique identifier for the resource.
                format: int64
                type: integer
              region:
                description: URL of the region where the regional TCP proxy resides.
                type: string
              selfLink:
                description: The SelfLink for the resource.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
