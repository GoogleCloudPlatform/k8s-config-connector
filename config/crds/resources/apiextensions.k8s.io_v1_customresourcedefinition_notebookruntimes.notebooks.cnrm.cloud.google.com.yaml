# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: notebookruntimes.notebooks.cnrm.cloud.google.com
spec:
  group: notebooks.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: NotebookRuntime
    listKind: NotebookRuntimeList
    plural: notebookruntimes
    shortNames:
    - gcpnotebookruntime
    - gcpnotebookruntimes
    singular: notebookruntime
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotebookRuntime is the Schema for the NotebookRuntime API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NotebookRuntimeSpec defines the desired state of NotebookRuntime
            properties:
              accessConfig:
                description: The config settings for accessing runtime.
                properties:
                  accessType:
                    description: The type of access mode this instance.
                    type: string
                  runtimeOwner:
                    description: 'The owner of this runtime after creation. Format:
                      `alias@example.com` Currently supports one owner only.'
                    type: string
                type: object
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The NotebookRuntime name. If not given, the metadata.name
                  will be used.
                type: string
              softwareConfig:
                description: The config settings for software inside the runtime.
                properties:
                  customGpuDriverPath:
                    description: Specify a custom Cloud Storage path where the GPU
                      driver is stored. If not specified, we'll automatically choose
                      from official GPU drivers.
                    type: string
                  disableTerminal:
                    description: 'Bool indicating whether JupyterLab terminal will
                      be available or not. Default: False'
                    type: boolean
                  enableHealthMonitoring:
                    description: 'Verifies core internal services are running. Default:
                      True'
                    type: boolean
                  idleShutdown:
                    description: 'Runtime will automatically shutdown after idle_shutdown_time.
                      Default: True'
                    type: boolean
                  idleShutdownTimeout:
                    description: 'Time in minutes to wait before shutting down runtime.
                      Default: 180 minutes'
                    format: int32
                    type: integer
                  installGpuDriver:
                    description: 'Install Nvidia Driver automatically. Default: True'
                    type: boolean
                  kernels:
                    description: Optional. Use a list of container images to use as
                      Kernels in the notebook instance.
                    items:
                      properties:
                        repository:
                          description: 'Required. The path to the container image
                            repository. For example: `gcr.io/{project_id}/{image_name}`'
                          type: string
                        tag:
                          description: The tag of the container image. If not specified,
                            this defaults to the latest tag.
                          type: string
                      type: object
                    type: array
                  notebookUpgradeSchedule:
                    description: Cron expression in UTC timezone, used to schedule
                      instance auto upgrade. Please follow the [cron format](https://en.wikipedia.org/wiki/Cron).
                    type: string
                  postStartupScript:
                    description: Path to a Bash script that automatically runs after
                      a notebook instance fully boots up. The path must be a URL or
                      Cloud Storage path (`gs://path-to-file/file-name`).
                    type: string
                  postStartupScriptBehavior:
                    description: Behavior for the post startup script.
                    type: string
                type: object
              virtualMachine:
                description: Use a Compute Engine VM image to start the managed notebook
                  instance.
                properties:
                  virtualMachineConfig:
                    description: Virtual Machine configuration settings.
                    properties:
                      acceleratorConfig:
                        description: Optional. The Compute Engine accelerator configuration
                          for this runtime.
                        properties:
                          coreCount:
                            description: Count of cores of this accelerator.
                            format: int64
                            type: integer
                          type:
                            description: Accelerator model.
                            type: string
                        type: object
                      bootImage:
                        description: Optional. Boot image metadata used for runtime
                          upgradeability.
                        type: object
                      containerImages:
                        description: Optional. Use a list of container images to use
                          as Kernels in the notebook instance.
                        items:
                          properties:
                            repository:
                              description: 'Required. The path to the container image
                                repository. For example: `gcr.io/{project_id}/{image_name}`'
                              type: string
                            tag:
                              description: The tag of the container image. If not
                                specified, this defaults to the latest tag.
                              type: string
                          type: object
                        type: array
                      dataDisk:
                        description: Required. Data disk option configuration settings.
                        properties:
                          initializeParams:
                            description: |-
                              Input only. Specifies the parameters for a new disk that will be created
                               alongside the new instance. Use initialization parameters to create boot
                               disks or local SSDs attached to the new instance.

                               This property is mutually exclusive with the source property; you can only
                               define one or the other, but not both.
                            properties:
                              description:
                                description: Optional. Provide this property when
                                  creating the disk.
                                type: string
                              diskName:
                                description: Optional. Specifies the disk name. If
                                  not specified, the default is to use the name of
                                  the instance. If the disk with the instance name
                                  exists already in the given zone/region, a new name
                                  will be automatically generated.
                                type: string
                              diskSizeGB:
                                description: Optional. Specifies the size of the disk
                                  in base-2 GB. If not specified, the disk will be
                                  the same size as the image (usually 10GB). If specified,
                                  the size must be equal to or larger than 10GB. Default
                                  100 GB.
                                format: int64
                                type: integer
                              diskType:
                                description: Input only. The type of the boot disk
                                  attached to this instance, defaults to standard
                                  persistent disk (`PD_STANDARD`).
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: Optional. Labels to apply to this disk.
                                  These can be later modified by the disks.setLabels
                                  method. This field is only applicable for persistent
                                  disks.
                                type: object
                            type: object
                          interface:
                            description: |-
                              Specifies the disk interface to use for attaching this disk, which is
                               either SCSI or NVME. The default is SCSI. Persistent disks must always use
                               SCSI and the request will fail if you attempt to attach a persistent disk
                               in any other format than SCSI. Local SSDs can use either NVME or SCSI. For
                               performance characteristics of SCSI over NVMe, see Local SSD performance.
                               Valid values:

                               * `NVME`
                               * `SCSI`
                            type: string
                          mode:
                            description: |-
                              The mode in which to attach this disk, either `READ_WRITE` or `READ_ONLY`.
                               If not specified, the default is to attach the disk in `READ_WRITE` mode.
                               Valid values:

                               * `READ_ONLY`
                               * `READ_WRITE`
                            type: string
                          source:
                            description: Specifies a valid partial or full URL to
                              an existing Persistent Disk resource.
                            type: string
                          type:
                            description: |-
                              Specifies the type of the disk, either `SCRATCH` or `PERSISTENT`. If not
                               specified, the default is `PERSISTENT`.
                               Valid values:

                               * `PERSISTENT`
                               * `SCRATCH`
                            type: string
                        type: object
                      encryptionConfig:
                        description: Optional. Encryption settings for virtual machine
                          data disk.
                        properties:
                          kmsKey:
                            description: 'The Cloud KMS resource identifier of the
                              customer-managed encryption key used to protect a resource,
                              such as a disks. It has the following format: `projects/{PROJECT_ID}/locations/{REGION}/keyRings/{KEY_RING_NAME}/cryptoKeys/{KEY_NAME}`'
                            type: string
                        type: object
                      internalIPOnly:
                        description: Optional. If true, runtime will only have internal
                          IP addresses. By default, runtimes are not restricted to
                          internal IP addresses, and will have ephemeral external
                          IP addresses assigned to each vm. This `internal_ip_only`
                          restriction can only be enabled for subnetwork enabled networks,
                          and all dependencies must be configured to be accessible
                          without external IP addresses.
                        type: boolean
                      labels:
                        additionalProperties:
                          type: string
                        description: Optional. The labels to associate with this runtime.
                          Label **keys** must contain 1 to 63 characters, and must
                          conform to [RFC 1035](https://www.ietf.org/rfc/rfc1035.txt).
                          Label **values** may be empty, but, if present, must contain
                          1 to 63 characters, and must conform to [RFC 1035](https://www.ietf.org/rfc/rfc1035.txt).
                          No more than 32 labels can be associated with a cluster.
                        type: object
                      machineType:
                        description: 'Required. The Compute Engine machine type used
                          for runtimes. Short name is valid. Examples: * `n1-standard-2`
                          * `e2-standard-8`'
                        type: string
                      metadata:
                        additionalProperties:
                          type: string
                        description: Optional. The Compute Engine metadata entries
                          to add to virtual machine. (see [Project and instance metadata](https://cloud.google.com/compute/docs/storing-retrieving-metadata#project_and_instance_metadata)).
                        type: object
                      network:
                        description: |-
                          Optional. The Compute Engine network to be used for machine
                           communications. Cannot be specified with subnetwork. If neither
                           `network` nor `subnet` is specified, the "default" network of
                           the project is used, if it exists.

                           A full URL or partial URI. Examples:

                           * `https://www.googleapis.com/compute/v1/projects/[project_id]/global/networks/default`
                           * `projects/[project_id]/global/networks/default`

                           Runtimes are managed resources inside Google Infrastructure.
                           Runtimes support the following network configurations:

                           * Google Managed Network (Network & subnet are empty)
                           * Consumer Project VPC (network & subnet are required). Requires
                           configuring Private Service Access.
                           * Shared VPC (network & subnet are required). Requires configuring Private
                           Service Access.
                        type: string
                      nicType:
                        description: Optional. The type of vNIC to be used on this
                          interface. This may be gVNIC or VirtioNet.
                        type: string
                      reservedIPRange:
                        description: |-
                          Optional. Reserved IP Range name is used for VPC Peering.
                           The subnetwork allocation will use the range *name* if it's assigned.

                           Example: managed-notebooks-range-c

                               PEERING_RANGE_NAME_3=managed-notebooks-range-c
                               gcloud compute addresses create $PEERING_RANGE_NAME_3 \
                                 --global \
                                 --prefix-length=24 \
                                 --description="Google Cloud Managed Notebooks Range 24 c" \
                                 --network=$NETWORK \
                                 --addresses=192.168.0.0 \
                                 --purpose=VPC_PEERING

                           Field value will be: `managed-notebooks-range-c`
                        type: string
                      shieldedInstanceConfig:
                        description: Optional. Shielded VM Instance configuration
                          settings.
                        properties:
                          enableIntegrityMonitoring:
                            description: |-
                              Defines whether the instance has integrity monitoring enabled.

                               Enables monitoring and attestation of the boot integrity of the instance.
                               The attestation is performed against the integrity policy baseline. This
                               baseline is initially derived from the implicitly trusted boot image when
                               the instance is created. Enabled by default.
                            type: boolean
                          enableSecureBoot:
                            description: |-
                              Defines whether the instance has Secure Boot enabled.

                               Secure Boot helps ensure that the system only runs authentic software by
                               verifying the digital signature of all boot components, and halting the
                               boot process if signature verification fails. Disabled by default.
                            type: boolean
                          enableVTPM:
                            description: Defines whether the instance has the vTPM
                              enabled. Enabled by default.
                            type: boolean
                        type: object
                      subnet:
                        description: |-
                          Optional. The Compute Engine subnetwork to be used for machine
                           communications. Cannot be specified with network.

                           A full URL or partial URI are valid. Examples:

                           * `https://www.googleapis.com/compute/v1/projects/[project_id]/regions/us-east1/subnetworks/sub0`
                           * `projects/[project_id]/regions/us-east1/subnetworks/sub0`
                        type: string
                      tags:
                        description: Optional. The Compute Engine tags to add to runtime
                          (see [Tagging instances](https://cloud.google.com/compute/docs/label-or-tag-resources#tags)).
                        items:
                          type: string
                        type: array
                    type: object
                type: object
            required:
            - location
            - projectRef
            type: object
          status:
            description: NotebookRuntimeStatus defines the config connector machine
              state of NotebookRuntime
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the NotebookRuntime resource in
                  GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  accessConfig:
                    description: The config settings for accessing runtime.
                    properties:
                      proxyURI:
                        description: Output only. The proxy endpoint that is used
                          to access the runtime.
                        type: string
                    type: object
                  createTime:
                    description: Output only. Runtime creation time.
                    type: string
                  healthState:
                    description: Output only. Runtime health_state.
                    type: string
                  metrics:
                    description: Output only. Contains Runtime daemon metrics such
                      as Service status and JupyterLab stats.
                    properties:
                      systemMetrics:
                        additionalProperties:
                          type: string
                        description: Output only. The system metrics.
                        type: object
                    type: object
                  name:
                    description: 'Output only. The resource name of the runtime. Format:
                      `projects/{project}/locations/{location}/runtimes/{runtimeId}`'
                    type: string
                  softwareConfig:
                    description: The config settings for software inside the runtime.
                    properties:
                      upgradeable:
                        description: Output only. Bool indicating whether an newer
                          image is available in an image family.
                        type: boolean
                      version:
                        description: Output only. version of boot image such as M100,
                          from release label of the image.
                        type: string
                    type: object
                  state:
                    description: Output only. Runtime state.
                    type: string
                  updateTime:
                    description: Output only. Runtime update time.
                    type: string
                  virtualMachine:
                    description: Use a Compute Engine VM image to start the managed
                      notebook instance.
                    properties:
                      instanceID:
                        description: Output only. The unique identifier of the Managed
                          Compute Engine instance.
                        type: string
                      instanceName:
                        description: Output only. The user-friendly name of the Managed
                          Compute Engine instance.
                        type: string
                      virtualMachineConfig:
                        description: Virtual Machine configuration settings.
                        properties:
                          dataDisk:
                            description: Required. Data disk option configuration
                              settings.
                            properties:
                              autoDelete:
                                description: Optional. Output only. Specifies whether
                                  the disk will be auto-deleted when the instance
                                  is deleted (but not when the disk is detached from
                                  the instance).
                                type: boolean
                              boot:
                                description: Optional. Output only. Indicates that
                                  this is a boot disk. The virtual machine will use
                                  the first partition of the disk for its root filesystem.
                                type: boolean
                              deviceName:
                                description: |-
                                  Optional. Output only. Specifies a unique device name
                                   of your choice that is reflected into the
                                   `/dev/disk/by-id/google-*` tree of a Linux operating system running within
                                   the instance. This name can be used to reference the device for mounting,
                                   resizing, and so on, from within the instance.

                                   If not specified, the server chooses a default device name to apply to this
                                   disk, in the form persistent-disk-x, where x is a number assigned by Google
                                   Compute Engine. This field is only applicable for persistent disks.
                                type: string
                              guestOSFeatures:
                                description: Output only. Indicates a list of features
                                  to enable on the guest operating system. Applicable
                                  only for bootable images. Read  Enabling guest operating
                                  system features to see a list of available options.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        The ID of a supported feature. Read [Enabling guest operating system
                                         features](https://cloud.google.com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features)
                                         to see a list of available options.

                                         Valid values:

                                         * `FEATURE_TYPE_UNSPECIFIED`
                                         * `MULTI_IP_SUBNET`
                                         * `SECURE_BOOT`
                                         * `UEFI_COMPATIBLE`
                                         * `VIRTIO_SCSI_MULTIQUEUE`
                                         * `WINDOWS`
                                      type: string
                                  type: object
                                type: array
                              index:
                                description: Output only. A zero-based index to this
                                  disk, where 0 is reserved for the boot disk. If
                                  you have many disks attached to an instance, each
                                  disk would have a unique index number.
                                format: int32
                                type: integer
                              kind:
                                description: Output only. Type of the resource. Always
                                  compute#attachedDisk for attached disks.
                                type: string
                              licenses:
                                description: Output only. Any valid publicly visible
                                  licenses.
                                items:
                                  type: string
                                type: array
                            type: object
                          guestAttributes:
                            additionalProperties:
                              type: string
                            description: Output only. The Compute Engine guest attributes.
                              (see [Project and instance guest attributes](https://cloud.google.com/compute/docs/storing-retrieving-metadata#guest_attributes)).
                            type: object
                          zone:
                            description: 'Output only. The zone where the virtual
                              machine is located. If using regional request, the notebooks
                              service will pick a location in the corresponding runtime
                              region. On a get request, zone will always be present.
                              Example: * `us-central1-b`'
                            type: string
                        type: object
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
