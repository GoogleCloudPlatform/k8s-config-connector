# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: alpha
    cnrm.cloud.google.com/system: "true"
  name: dlpdiscoveryconfigs.dlp.cnrm.cloud.google.com
spec:
  group: dlp.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: DLPDiscoveryConfig
    listKind: DLPDiscoveryConfigList
    plural: dlpdiscoveryconfigs
    shortNames:
    - gcpdlpdiscoveryconfig
    - gcpdlpdiscoveryconfigs
    singular: dlpdiscoveryconfig
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DLPDiscoveryConfig is the Schema for the DLPDiscoveryConfig API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DLPDiscoveryConfigSpec defines the desired state of DLPDiscoveryConfig
            properties:
              actions:
                description: Actions to execute at the completion of scanning.
                items:
                  properties:
                    exportData:
                      description: Export data profiles into a provided location.
                      properties:
                        profileTable:
                          description: |-
                            Store all profiles to BigQuery.

                             * The system will create a new dataset and table for you if none are
                               are provided. The dataset will be named
                               `sensitive_data_protection_discovery` and table will be named
                               `discovery_profiles`. This table will be placed in the same project as
                               the container project running the scan. After the first profile is
                               generated and the dataset and table are created, the discovery scan
                               configuration will be updated with the dataset and table names.
                             * See [Analyze data profiles stored in
                             BigQuery](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles).
                             * See [Sample queries for your BigQuery
                             table](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles#sample_sql_queries).
                             *  Data is inserted using [streaming
                                insert](https://cloud.google.com/blog/products/bigquery/life-of-a-bigquery-streaming-insert)
                                and so data may be in the buffer for a period of time after the
                                profile has finished.
                              * The Pub/Sub notification is sent before the streaming buffer is
                                guaranteed to be written, so data may not be instantly
                                visible to queries by the time your topic receives the Pub/Sub
                                notification.
                              * The best practice is to use the same table for an entire organization
                                so that you can take advantage of the [provided Data Studio
                                reports](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles#use_a_premade_report).
                                If you use VPC Service Controls to define security perimeters, then
                                you must use a separate table for each boundary.
                          properties:
                            datasetID:
                              description: Dataset ID of the table.
                              type: string
                            projectID:
                              description: The Google Cloud project ID of the project
                                containing the table. If omitted, project ID is inferred
                                from the API call.
                              type: string
                            tableID:
                              description: Name of the table.
                              type: string
                          type: object
                        sampleFindingsTable:
                          description: Store sample [data profile findings][google.privacy.dlp.v2.DataProfileFinding]
                            in an existing table or a new table in an existing dataset.
                            Each regeneration will result in new rows in BigQuery.
                            Data is inserted using [streaming insert](https://cloud.google.com/blog/products/bigquery/life-of-a-bigquery-streaming-insert)
                            and so data may be in the buffer for a period of time
                            after the profile has finished.
                          properties:
                            datasetID:
                              description: Dataset ID of the table.
                              type: string
                            projectID:
                              description: The Google Cloud project ID of the project
                                containing the table. If omitted, project ID is inferred
                                from the API call.
                              type: string
                            tableID:
                              description: Name of the table.
                              type: string
                          type: object
                      type: object
                    pubSubNotification:
                      description: Publish a message into the Pub/Sub topic.
                      properties:
                        detailOfMessage:
                          description: How much data to include in the Pub/Sub message.
                            If the user wishes to limit the size of the message, they
                            can use resource_name and fetch the profile fields they
                            wish to. Per table profile (not per column).
                          type: string
                        event:
                          description: The type of event that triggers a Pub/Sub.
                            At most one `PubSubNotification` per EventType is permitted.
                          type: string
                        pubsubCondition:
                          description: Conditions (e.g., data risk or sensitivity
                            level) for triggering a Pub/Sub.
                          properties:
                            expressions:
                              description: An expression.
                              properties:
                                conditions:
                                  description: Conditions to apply to the expression.
                                  items:
                                    properties:
                                      minimumRiskScore:
                                        description: The minimum data risk score that
                                          triggers the condition.
                                        type: string
                                      minimumSensitivityScore:
                                        description: The minimum sensitivity level
                                          that triggers the condition.
                                        type: string
                                    type: object
                                  type: array
                                logicalOperator:
                                  description: The operator to apply to the collection
                                    of conditions.
                                  type: string
                              type: object
                          type: object
                        topicRef:
                          description: Cloud Pub/Sub topic to send notifications to.
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed PubSubTopic
                                resource. Should be in the format "projects/{{projectID}}/topics/{{topicID}}".
                              type: string
                            name:
                              description: The name of a PubSubTopic resource.
                              type: string
                            namespace:
                              description: The namespace of a PubSubTopic resource.
                              type: string
                          type: object
                      type: object
                    publishToChronicle:
                      description: Publishes generated data profiles to Google Security
                        Operations. For more information, see [Use Sensitive Data
                        Protection data in context-aware analytics](https://cloud.google.com/chronicle/docs/detection/usecase-dlp-high-risk-user-download).
                      type: object
                    publishToDataplexCatalog:
                      description: Publishes a portion of each profile to Dataplex
                        Universal Catalog with the aspect type Sensitive Data Protection
                        Profile.
                      properties:
                        lowerDataRiskToLow:
                          description: Whether creating a Dataplex Universal Catalog
                            aspect for a profiled resource should lower the risk of
                            the profile for that resource. This also lowers the data
                            risk of resources at the lower levels of the resource
                            hierarchy. For example, reducing the data risk of a table
                            data profile also reduces the data risk of the constituent
                            column data profiles.
                          type: boolean
                      type: object
                    publishToScc:
                      description: Publishes findings to Security Command Center for
                        each data profile.
                      type: object
                    tagResources:
                      description: Tags the profiled resources with the specified
                        tag values.
                      properties:
                        lowerDataRiskToLow:
                          description: Whether applying a tag to a resource should
                            lower the risk of the profile for that resource. For example,
                            in conjunction with an [IAM deny policy](https://cloud.google.com/iam/docs/deny-overview),
                            you can deny all principals a permission if a tag value
                            is present, mitigating the risk of the resource. This
                            also lowers the data risk of resources at the lower levels
                            of the resource hierarchy. For example, reducing the data
                            risk of a table data profile also reduces the data risk
                            of the constituent column data profiles.
                          type: boolean
                        profileGenerationsToTag:
                          description: The profile generations for which the tag should
                            be attached to resources. If you attach a tag to only
                            new profiles, then if the sensitivity score of a profile
                            subsequently changes, its tag doesn't change. By default,
                            this field includes only new profiles. To include both
                            new and updated profiles for tagging, this field should
                            explicitly include both `PROFILE_GENERATION_NEW` and `PROFILE_GENERATION_UPDATE`.
                          items:
                            type: string
                          type: array
                        tagConditions:
                          description: The tags to associate with different conditions.
                          items:
                            properties:
                              sensitivityScore:
                                description: Conditions attaching the tag to a resource
                                  on its profile having this sensitivity score.
                                properties:
                                  score:
                                    description: The sensitivity score applied to
                                      the resource.
                                    type: string
                                type: object
                              tag:
                                description: The tag value to attach to resources.
                                properties:
                                  namespacedValue:
                                    description: The namespaced name for the tag value
                                      to attach to resources. Must be in the format
                                      `{parent_id}/{tag_key_short_name}/{short_name}`,
                                      for example, "123456/environment/prod" for an
                                      organization parent, or "my-project/environment/prod"
                                      for a project parent.
                                    type: string
                                type: object
                            type: object
                          type: array
                      type: object
                  type: object
                type: array
              displayName:
                description: Display name (max 100 chars)
                type: string
              inspectTemplates:
                description: Detection logic for profile generation.
                items:
                  type: string
                type: array
              location:
                description: The location of this resource.
                type: string
              orgConfig:
                description: Only set when the parent is an org.
                properties:
                  location:
                    description: 'The data to scan: folder, org, or project'
                    properties:
                      folderID:
                        description: The ID of the folder within an organization to
                          be scanned.
                        format: int64
                        type: integer
                      organizationID:
                        description: The ID of an organization to scan.
                        format: int64
                        type: integer
                    type: object
                  projectID:
                    description: The project that will run the scan. The DLP service
                      account that exists within this project must have access to
                      all resources that are profiled, and the DLP API must be enabled.
                    type: string
                type: object
              otherCloudStartingLocation:
                description: Must be set only when scanning other clouds.
                properties:
                  awsLocation:
                    description: The AWS starting location for discovery.
                    properties:
                      accountID:
                        description: 'The AWS account ID that this discovery config
                          applies to. Within an AWS organization, you can find the
                          AWS account ID inside an AWS account ARN. Example: arn:{partition}:organizations::{management_account_id}:account/{org_id}/{account_id}'
                        type: string
                      allAssetInventoryAssets:
                        description: All AWS assets stored in Asset Inventory that
                          didn't match other AWS discovery configs.
                        type: boolean
                    type: object
                type: object
              processingLocation:
                description: Optional. Processing location configuration. Vertex AI
                  dataset scanning will set processing_location.image_fallback_type
                  to MultiRegionProcessing by default.
                properties:
                  documentFallbackLocation:
                    description: Document processing falls back using this configuration.
                    properties:
                      globalProcessing:
                        description: Processing occurs in the global region.
                        type: object
                      multiRegionProcessing:
                        description: Processing occurs in a multi-region that contains
                          the current region if available.
                        type: object
                    type: object
                  imageFallbackLocation:
                    description: Image processing falls back using this configuration.
                    properties:
                      globalProcessing:
                        description: Processing occurs in the global region.
                        type: object
                      multiRegionProcessing:
                        description: Processing occurs in a multi-region that contains
                          the current region if available.
                        type: object
                    type: object
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The DLPDiscoveryConfig name. If not given, the metadata.name
                  will be used.
                type: string
              status:
                description: Required. A status for this configuration.
                type: string
              targets:
                description: Target to match against for determining what to scan
                  and how frequently.
                items:
                  properties:
                    bigQueryTarget:
                      description: BigQuery target for Discovery. The first target
                        to match a table will be the one applied.
                      properties:
                        cadence:
                          description: How often and when to update profiles. New
                            tables that match both the filter and conditions are scanned
                            as quickly as possible depending on system capacity.
                          properties:
                            inspectTemplateModifiedCadence:
                              description: Governs when to update data profiles when
                                the inspection rules defined by the `InspectTemplate`
                                change. If not set, changing the template will not
                                cause a data profile to update.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when the template is modified. Defaults
                                    to never.
                                  type: string
                              type: object
                            refreshFrequency:
                              description: Frequency at which profiles should be updated,
                                regardless of whether the underlying resource has
                                changed. Defaults to never.
                              type: string
                            schemaModifiedCadence:
                              description: Governs when to update data profiles when
                                a schema is modified.
                              properties:
                                frequency:
                                  description: How frequently profiles may be updated
                                    when schemas are modified. Defaults to monthly.
                                  type: string
                                types:
                                  description: The type of events to consider when
                                    deciding if the table's schema has been modified
                                    and should have the profile updated. Defaults
                                    to NEW_COLUMNS.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            tableModifiedCadence:
                              description: Governs when to update data profiles when
                                a table is modified.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when tables are modified. Defaults to
                                    never.
                                  type: string
                                types:
                                  description: The type of events to consider when
                                    deciding if the table has been modified and should
                                    have the profile updated. Defaults to MODIFIED_TIMESTAMP.
                                  items:
                                    type: string
                                  type: array
                              type: object
                          type: object
                        conditions:
                          description: In addition to matching the filter, these conditions
                            must be true before a profile is generated.
                          properties:
                            createdAfter:
                              description: BigQuery table must have been created after
                                this date. Used to avoid backfilling.
                              type: string
                            orConditions:
                              description: At least one of the conditions must be
                                true for a table to be scanned.
                              properties:
                                minAge:
                                  description: Minimum age a table must have before
                                    Cloud DLP can profile it. Value must be 1 hour
                                    or greater.
                                  type: string
                                minRowCount:
                                  description: Minimum number of rows that should
                                    be present before Cloud DLP profiles a table
                                  format: int32
                                  type: integer
                              type: object
                            typeCollection:
                              description: Restrict discovery to categories of table
                                types.
                              type: string
                            types:
                              description: Restrict discovery to specific table types.
                              properties:
                                types:
                                  description: A set of BigQuery table types.
                                  items:
                                    type: string
                                  type: array
                              type: object
                          type: object
                        disabled:
                          description: Tables that match this filter will not have
                            profiles created.
                          type: object
                        filter:
                          description: Required. The tables the discovery cadence
                            applies to. The first target with a matching filter will
                            be the one to apply to a table.
                          properties:
                            otherTables:
                              description: Catch-all. This should always be the last
                                filter in the list because anything above it will
                                apply first. Should only appear once in a configuration.
                                If none is specified, a default one will be added
                                automatically.
                              type: object
                            tableReference:
                              description: The table to scan. Discovery configurations
                                including this can only include one DiscoveryTarget
                                (the DiscoveryTarget with this TableReference).
                              properties:
                                datasetID:
                                  description: Dataset ID of the table.
                                  type: string
                                projectID:
                                  description: The Google Cloud project ID of the
                                    project containing the table. If omitted, the
                                    project ID is inferred from the parent project.
                                    This field is required if the parent resource
                                    is an organization.
                                  type: string
                                tableID:
                                  description: Name of the table.
                                  type: string
                              type: object
                            tables:
                              description: A specific set of tables for this filter
                                to apply to. A table collection must be specified
                                in only one filter per config. If a table id or dataset
                                is empty, Cloud DLP assumes all tables in that collection
                                must be profiled. Must specify a project ID.
                              properties:
                                includeRegexes:
                                  description: A collection of regular expressions
                                    to match a BigQuery table against.
                                  properties:
                                    patterns:
                                      description: A single BigQuery regular expression
                                        pattern to match against one or more tables,
                                        datasets, or projects that contain BigQuery
                                        tables.
                                      items:
                                        properties:
                                          datasetIDRegex:
                                            description: If unset, this property matches
                                              all datasets.
                                            type: string
                                          projectIDRegex:
                                            description: For organizations, if unset,
                                              will match all projects. Has no effect
                                              for data profile configurations created
                                              within a project.
                                            type: string
                                          tableIDRegex:
                                            description: If unset, this property matches
                                              all tables.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                              type: object
                          type: object
                      type: object
                    cloudSQLTarget:
                      description: Cloud SQL target for Discovery. The first target
                        to match a table will be the one applied.
                      properties:
                        conditions:
                          description: In addition to matching the filter, these conditions
                            must be true before a profile is generated.
                          properties:
                            databaseEngines:
                              description: Optional. Database engines that should
                                be profiled. Optional. Defaults to ALL_SUPPORTED_DATABASE_ENGINES
                                if unspecified.
                              items:
                                type: string
                              type: array
                            types:
                              description: Data profiles will only be generated for
                                the database resource types specified in this field.
                                If not specified, defaults to [DATABASE_RESOURCE_TYPE_ALL_SUPPORTED_TYPES].
                              items:
                                type: string
                              type: array
                          type: object
                        disabled:
                          description: Disable profiling for database resources that
                            match this filter.
                          type: object
                        filter:
                          description: Required. The tables the discovery cadence
                            applies to. The first target with a matching filter will
                            be the one to apply to a table.
                          properties:
                            collection:
                              description: A specific set of database resources for
                                this filter to apply to.
                              properties:
                                includeRegexes:
                                  description: A collection of regular expressions
                                    to match a database resource against.
                                  properties:
                                    patterns:
                                      description: A group of regular expression patterns
                                        to match against one or more database resources.
                                        Maximum of 100 entries. The sum of all regular
                                        expression's length can't exceed 10 KiB.
                                      items:
                                        properties:
                                          databaseRegex:
                                            description: Regex to test the database
                                              name against. If empty, all databases
                                              match.
                                            type: string
                                          databaseResourceNameRegex:
                                            description: Regex to test the database
                                              resource's name against. An example
                                              of a database resource name is a table's
                                              name. Other database resource names
                                              like view names could be included in
                                              the future. If empty, all database resources
                                              match.
                                            type: string
                                          instanceRegex:
                                            description: Regex to test the instance
                                              name against. If empty, all instances
                                              match.
                                            type: string
                                          projectIDRegex:
                                            description: For organizations, if unset,
                                              will match all projects. Has no effect
                                              for configurations created within a
                                              project.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                              type: object
                            databaseResourceReference:
                              description: The database resource to scan. Targets
                                including this can only include one target (the target
                                with this database resource reference).
                              properties:
                                database:
                                  description: Required. Name of a database within
                                    the instance.
                                  type: string
                                databaseResource:
                                  description: Required. Name of a database resource,
                                    for example, a table within the database.
                                  type: string
                                instance:
                                  description: 'Required. The instance where this
                                    resource is located. For example: Cloud SQL instance
                                    ID.'
                                  type: string
                                projectID:
                                  description: Required. If within a project-level
                                    config, then this must match the config's project
                                    ID.
                                  type: string
                              type: object
                            others:
                              description: Catch-all. This should always be the last
                                target in the list because anything above it will
                                apply first. Should only appear once in a configuration.
                                If none is specified, a default one will be added
                                automatically.
                              type: object
                          type: object
                        generationCadence:
                          description: How often and when to update profiles. New
                            tables that match both the filter and conditions are scanned
                            as quickly as possible depending on system capacity.
                          properties:
                            inspectTemplateModifiedCadence:
                              description: Governs when to update data profiles when
                                the inspection rules defined by the `InspectTemplate`
                                change. If not set, changing the template will not
                                cause a data profile to update.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when the template is modified. Defaults
                                    to never.
                                  type: string
                              type: object
                            refreshFrequency:
                              description: Data changes (non-schema changes) in Cloud
                                SQL tables can't trigger reprofiling. If you set this
                                field, profiles are refreshed at this frequency regardless
                                of whether the underlying tables have changed. Defaults
                                to never.
                              type: string
                            schemaModifiedCadence:
                              description: When to reprofile if the schema has changed.
                              properties:
                                frequency:
                                  description: Frequency to regenerate data profiles
                                    when the schema is modified. Defaults to monthly.
                                  type: string
                                types:
                                  description: The types of schema modifications to
                                    consider. Defaults to NEW_COLUMNS.
                                  items:
                                    type: string
                                  type: array
                              type: object
                          type: object
                      type: object
                    cloudStorageTarget:
                      description: Cloud Storage target for Discovery. The first target
                        to match a table will be the one applied.
                      properties:
                        conditions:
                          description: Optional. In addition to matching the filter,
                            these conditions must be true before a profile is generated.
                          properties:
                            cloudStorageConditions:
                              description: Optional. Cloud Storage conditions.
                              properties:
                                includedBucketAttributes:
                                  description: Required. Only objects with the specified
                                    attributes will be scanned. Defaults to [ALL_SUPPORTED_BUCKETS]
                                    if unset.
                                  items:
                                    type: string
                                  type: array
                                includedObjectAttributes:
                                  description: Required. Only objects with the specified
                                    attributes will be scanned. If an object has one
                                    of the specified attributes but is inside an excluded
                                    bucket, it will not be scanned. Defaults to [ALL_SUPPORTED_OBJECTS].
                                    A profile will be created even if no objects match
                                    the included_object_attributes.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            createdAfter:
                              description: Optional. File store must have been created
                                after this date. Used to avoid backfilling.
                              type: string
                            minAge:
                              description: Optional. Minimum age a file store must
                                have. If set, the value must be 1 hour or greater.
                              type: string
                          type: object
                        disabled:
                          description: Optional. Disable profiling for buckets that
                            match this filter.
                          type: object
                        filter:
                          description: Required. The buckets the generation_cadence
                            applies to. The first target with a matching filter will
                            be the one to apply to a bucket.
                          properties:
                            cloudStorageResourceReference:
                              description: Optional. The bucket to scan. Targets including
                                this can only include one target (the target with
                                this bucket). This enables profiling the contents
                                of a single bucket, while the other options allow
                                for easy profiling of many bucets within a project
                                or an organization.
                              properties:
                                bucketName:
                                  description: Required. The bucket to scan.
                                  type: string
                                projectID:
                                  description: Required. If within a project-level
                                    config, then this must match the config's project
                                    id.
                                  type: string
                              type: object
                            collection:
                              description: Optional. A specific set of buckets for
                                this filter to apply to.
                              properties:
                                includeRegexes:
                                  description: Optional. A collection of regular expressions
                                    to match a file store against.
                                  properties:
                                    patterns:
                                      description: Required. The group of regular
                                        expression patterns to match against one or
                                        more file stores. Maximum of 100 entries.
                                        The sum of all regular expression's length
                                        can't exceed 10 KiB.
                                      items:
                                        properties:
                                          cloudStorageRegex:
                                            description: Optional. Regex for Cloud
                                              Storage.
                                            properties:
                                              bucketNameRegex:
                                                description: 'Optional. Regex to test
                                                  the bucket name against. If empty,
                                                  all buckets match. Example: "marketing2021"
                                                  or "(marketing)\d{4}" will both
                                                  match the bucket gs://marketing2021'
                                                type: string
                                              projectIDRegex:
                                                description: Optional. For organizations,
                                                  if unset, will match all projects.
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                includeTags:
                                  description: |-
                                    Optional. To be included in the collection, a resource must meet all of the
                                     following requirements:

                                      - If tag filters are provided, match all provided tag filters.
                                      - If one or more patterns are specified, match at least one pattern.

                                     For a resource to match the tag filters, the resource must have all of the
                                     provided tags attached. Tags refer to Resource Manager tags bound to the
                                     resource or its ancestors. For more information, see [Manage
                                     schedules](https://cloud.google.com/sensitive-data-protection/docs/profile-project-cloud-storage#manage-schedules).
                                  properties:
                                    tagFilters:
                                      description: Required. A resource must match
                                        ALL of the specified tag filters to be included
                                        in the collection.
                                      items:
                                        properties:
                                          namespacedTagKey:
                                            description: The namespaced name for the
                                              tag key. Must be in the format `{parent_id}/{tag_key_short_name}`,
                                              for example, "123456/sensitive" for
                                              an organization parent, or "my-project/sensitive"
                                              for a project parent.
                                            type: string
                                          namespacedTagValue:
                                            description: The namespaced name for the
                                              tag value. Must be in the format `{parent_id}/{tag_key_short_name}/{short_name}`,
                                              for example, "123456/environment/prod"
                                              for an organization parent, or "my-project/environment/prod"
                                              for a project parent.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                              type: object
                            others:
                              description: Optional. Catch-all. This should always
                                be the last target in the list because anything above
                                it will apply first. Should only appear once in a
                                configuration. If none is specified, a default one
                                will be added automatically.
                              type: object
                          type: object
                        generationCadence:
                          description: Optional. How often and when to update profiles.
                            New buckets that match both the filter and conditions
                            are scanned as quickly as possible depending on system
                            capacity.
                          properties:
                            inspectTemplateModifiedCadence:
                              description: Optional. Governs when to update data profiles
                                when the inspection rules defined by the `InspectTemplate`
                                change. If not set, changing the template will not
                                cause a data profile to update.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when the template is modified. Defaults
                                    to never.
                                  type: string
                              type: object
                            refreshFrequency:
                              description: Optional. Data changes in Cloud Storage
                                can't trigger reprofiling. If you set this field,
                                profiles are refreshed at this frequency regardless
                                of whether the underlying buckets have changed. Defaults
                                to never.
                              type: string
                          type: object
                      type: object
                    otherCloudTarget:
                      description: Other clouds target for discovery. The first target
                        to match a resource will be the one applied.
                      properties:
                        conditions:
                          description: Optional. In addition to matching the filter,
                            these conditions must be true before a profile is generated.
                          properties:
                            amazonS3BucketConditions:
                              description: Amazon S3 bucket conditions.
                              properties:
                                bucketTypes:
                                  description: Optional. Bucket types that should
                                    be profiled. Optional. Defaults to TYPE_ALL_SUPPORTED
                                    if unspecified.
                                  items:
                                    type: string
                                  type: array
                                objectStorageClasses:
                                  description: Optional. Object classes that should
                                    be profiled. Optional. Defaults to ALL_SUPPORTED_CLASSES
                                    if unspecified.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            minAge:
                              description: Minimum age a resource must be before Cloud
                                DLP can profile it. Value must be 1 hour or greater.
                              type: string
                          type: object
                        dataSourceType:
                          description: 'Required. The type of data profiles generated
                            by this discovery target. Supported values are: * aws/s3/bucket'
                          properties:
                            dataSource:
                              description: |-
                                A string that identifies the type of resource being profiled.
                                 Current values:

                                 * google/bigquery/table
                                 * google/project
                                 * google/sql/table
                                 * google/gcs/bucket
                              type: string
                          type: object
                        disabled:
                          description: Disable profiling for resources that match
                            this filter.
                          type: object
                        filter:
                          description: Required. The resources that the discovery
                            cadence applies to. The first target with a matching filter
                            will be the one to apply to a resource.
                          properties:
                            collection:
                              description: A collection of resources for this filter
                                to apply to.
                              properties:
                                includeRegexes:
                                  description: A collection of regular expressions
                                    to match a resource against.
                                  properties:
                                    patterns:
                                      description: A group of regular expression patterns
                                        to match against one or more resources. Maximum
                                        of 100 entries. The sum of all regular expression's
                                        length can't exceed 10 KiB.
                                      items:
                                        properties:
                                          amazonS3BucketRegex:
                                            description: Regex for Amazon S3 buckets.
                                            properties:
                                              awsAccountRegex:
                                                description: The AWS account regex.
                                                properties:
                                                  accountIDRegex:
                                                    description: Optional. Regex to
                                                      test the AWS account ID against.
                                                      If empty, all accounts match.
                                                    type: string
                                                type: object
                                              bucketNameRegex:
                                                description: Optional. Regex to test
                                                  the bucket name against. If empty,
                                                  all buckets match.
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                              type: object
                            others:
                              description: Optional. Catch-all. This should always
                                be the last target in the list because anything above
                                it will apply first. Should only appear once in a
                                configuration. If none is specified, a default one
                                will be added automatically.
                              type: object
                            singleResource:
                              description: The resource to scan. Configs using this
                                filter can only have one target (the target with this
                                single resource reference).
                              properties:
                                amazonS3Bucket:
                                  description: Amazon S3 bucket.
                                  properties:
                                    awsAccount:
                                      description: The AWS account.
                                      properties:
                                        accountID:
                                          description: Required. AWS account ID.
                                          type: string
                                      type: object
                                    bucketName:
                                      description: Required. The bucket name.
                                      type: string
                                  type: object
                              type: object
                          type: object
                        generationCadence:
                          description: How often and when to update data profiles.
                            New resources that match both the filter and conditions
                            are scanned as quickly as possible depending on system
                            capacity.
                          properties:
                            inspectTemplateModifiedCadence:
                              description: Optional. Governs when to update data profiles
                                when the inspection rules defined by the `InspectTemplate`
                                change. If not set, changing the template will not
                                cause a data profile to update.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when the template is modified. Defaults
                                    to never.
                                  type: string
                              type: object
                            refreshFrequency:
                              description: Optional. Frequency to update profiles
                                regardless of whether the underlying resource has
                                changes. Defaults to never.
                              type: string
                          type: object
                      type: object
                    secretsTarget:
                      description: Discovery target that looks for credentials and
                        secrets stored in cloud resource metadata and reports them
                        as vulnerabilities to Security Command Center. Only one target
                        of this type is allowed.
                      type: object
                    vertexDatasetTarget:
                      description: Vertex AI dataset target for Discovery. The first
                        target to match a dataset will be the one applied. Note that
                        discovery for Vertex AI can incur Cloud Storage Class B operation
                        charges for storage.objects.get operations and retrieval fees.
                        For more information, see [Cloud Storage pricing](https://cloud.google.com/storage/pricing#price-tables).
                        Note that discovery for Vertex AI dataset will not be able
                        to scan images unless DiscoveryConfig.processing_location.image_fallback_location
                        has multi_region_processing or global_processing configured.
                      properties:
                        conditions:
                          description: In addition to matching the filter, these conditions
                            must be true before a profile is generated.
                          properties:
                            createdAfter:
                              description: Vertex AI dataset must have been created
                                after this date. Used to avoid backfilling.
                              type: string
                            minAge:
                              description: Minimum age a Vertex AI dataset must have.
                                If set, the value must be 1 hour or greater.
                              type: string
                          type: object
                        disabled:
                          description: Disable profiling for datasets that match this
                            filter.
                          type: object
                        filter:
                          description: Required. The datasets the discovery cadence
                            applies to. The first target with a matching filter will
                            be the one to apply to a dataset.
                          properties:
                            collection:
                              description: A specific set of Vertex AI datasets for
                                this filter to apply to.
                              properties:
                                vertexDatasetRegexes:
                                  description: The regex used to filter dataset resources.
                                  properties:
                                    patterns:
                                      description: Required. The group of regular
                                        expression patterns to match against one or
                                        more datasets. Maximum of 100 entries. The
                                        sum of the lengths of all regular expressions
                                        can't exceed 10 KiB.
                                      items:
                                        properties:
                                          projectIDRegex:
                                            description: For organizations, if unset,
                                              will match all projects. Has no effect
                                              for configurations created within a
                                              project.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                              type: object
                            others:
                              description: Catch-all. This should always be the last
                                target in the list because anything above it will
                                apply first. Should only appear once in a configuration.
                                If none is specified, a default one will be added
                                automatically.
                              type: object
                            vertexDatasetResourceReference:
                              description: The dataset resource to scan. Targets including
                                this can only include one target (the target with
                                this dataset resource reference).
                              properties:
                                datasetResourceName:
                                  description: |-
                                    Required. The name of the Vertex AI resource. If set within a project-level
                                     configuration, the specified resource must be within the project.
                                     Examples:

                                     * `projects/{project}/locations/{location}/datasets/{dataset}`
                                  type: string
                              type: object
                          type: object
                        generationCadence:
                          description: How often and when to update profiles. New
                            datasets that match both the filter and conditions are
                            scanned as quickly as possible depending on system capacity.
                          properties:
                            inspectTemplateModifiedCadence:
                              description: Governs when to update data profiles when
                                the inspection rules defined by the `InspectTemplate`
                                change. If not set, changing the template will not
                                cause a data profile to be updated.
                              properties:
                                frequency:
                                  description: How frequently data profiles can be
                                    updated when the template is modified. Defaults
                                    to never.
                                  type: string
                              type: object
                            refreshFrequency:
                              description: If you set this field, profiles are refreshed
                                at this frequency regardless of whether the underlying
                                datasets have changed. Defaults to never.
                              type: string
                          type: object
                      type: object
                  type: object
                type: array
            required:
            - location
            - projectRef
            - status
            type: object
          status:
            description: DLPDiscoveryConfigStatus defines the config connector machine
              state of DLPDiscoveryConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the DLPDiscoveryConfig resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The creation timestamp of a DiscoveryConfig.
                    type: string
                  errors:
                    description: Output only. A stream of errors encountered when
                      the config was activated. Repeated errors may result in the
                      config automatically being paused. Output only field. Will return
                      the last 100 errors. Whenever the config is modified this list
                      will be cleared.
                    items:
                      properties:
                        details:
                          description: Detailed error codes and messages.
                          properties:
                            code:
                              description: The status code, which should be an enum
                                value of [google.rpc.Code][google.rpc.Code].
                              format: int32
                              type: integer
                            message:
                              description: A developer-facing error message, which
                                should be in English. Any user-facing error message
                                should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details]
                                field, or localized by the client.
                              type: string
                          type: object
                        extraInfo:
                          description: Additional information about the error.
                          type: string
                        timestamps:
                          description: The times the error occurred. List includes
                            the oldest timestamp and the last 9 timestamps.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  lastRunTime:
                    description: Output only. The timestamp of the last time this
                      config was executed.
                    type: string
                  targets:
                    description: Target to match against for determining what to scan
                      and how frequently.
                    items:
                      properties:
                        bigQueryTarget:
                          description: BigQuery target for Discovery. The first target
                            to match a table will be the one applied.
                          properties:
                            cadence:
                              description: How often and when to update profiles.
                                New tables that match both the filter and conditions
                                are scanned as quickly as possible depending on system
                                capacity.
                              properties:
                                inspectTemplateModifiedCadence:
                                  description: Governs when to update data profiles
                                    when the inspection rules defined by the `InspectTemplate`
                                    change. If not set, changing the template will
                                    not cause a data profile to update.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when the template is modified.
                                        Defaults to never.
                                      type: string
                                  type: object
                                refreshFrequency:
                                  description: Frequency at which profiles should
                                    be updated, regardless of whether the underlying
                                    resource has changed. Defaults to never.
                                  type: string
                                schemaModifiedCadence:
                                  description: Governs when to update data profiles
                                    when a schema is modified.
                                  properties:
                                    frequency:
                                      description: How frequently profiles may be
                                        updated when schemas are modified. Defaults
                                        to monthly.
                                      type: string
                                    types:
                                      description: The type of events to consider
                                        when deciding if the table's schema has been
                                        modified and should have the profile updated.
                                        Defaults to NEW_COLUMNS.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                tableModifiedCadence:
                                  description: Governs when to update data profiles
                                    when a table is modified.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when tables are modified. Defaults
                                        to never.
                                      type: string
                                    types:
                                      description: The type of events to consider
                                        when deciding if the table has been modified
                                        and should have the profile updated. Defaults
                                        to MODIFIED_TIMESTAMP.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              type: object
                            conditions:
                              description: In addition to matching the filter, these
                                conditions must be true before a profile is generated.
                              properties:
                                createdAfter:
                                  description: BigQuery table must have been created
                                    after this date. Used to avoid backfilling.
                                  type: string
                                orConditions:
                                  description: At least one of the conditions must
                                    be true for a table to be scanned.
                                  properties:
                                    minAge:
                                      description: Minimum age a table must have before
                                        Cloud DLP can profile it. Value must be 1
                                        hour or greater.
                                      type: string
                                    minRowCount:
                                      description: Minimum number of rows that should
                                        be present before Cloud DLP profiles a table
                                      format: int32
                                      type: integer
                                  type: object
                                typeCollection:
                                  description: Restrict discovery to categories of
                                    table types.
                                  type: string
                                types:
                                  description: Restrict discovery to specific table
                                    types.
                                  properties:
                                    types:
                                      description: A set of BigQuery table types.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              type: object
                            disabled:
                              description: Tables that match this filter will not
                                have profiles created.
                              type: object
                            filter:
                              description: Required. The tables the discovery cadence
                                applies to. The first target with a matching filter
                                will be the one to apply to a table.
                              properties:
                                otherTables:
                                  description: Catch-all. This should always be the
                                    last filter in the list because anything above
                                    it will apply first. Should only appear once in
                                    a configuration. If none is specified, a default
                                    one will be added automatically.
                                  type: object
                                tableReference:
                                  description: The table to scan. Discovery configurations
                                    including this can only include one DiscoveryTarget
                                    (the DiscoveryTarget with this TableReference).
                                  properties:
                                    datasetID:
                                      description: Dataset ID of the table.
                                      type: string
                                    projectID:
                                      description: The Google Cloud project ID of
                                        the project containing the table. If omitted,
                                        the project ID is inferred from the parent
                                        project. This field is required if the parent
                                        resource is an organization.
                                      type: string
                                    tableID:
                                      description: Name of the table.
                                      type: string
                                  type: object
                                tables:
                                  description: A specific set of tables for this filter
                                    to apply to. A table collection must be specified
                                    in only one filter per config. If a table id or
                                    dataset is empty, Cloud DLP assumes all tables
                                    in that collection must be profiled. Must specify
                                    a project ID.
                                  properties:
                                    includeRegexes:
                                      description: A collection of regular expressions
                                        to match a BigQuery table against.
                                      properties:
                                        patterns:
                                          description: A single BigQuery regular expression
                                            pattern to match against one or more tables,
                                            datasets, or projects that contain BigQuery
                                            tables.
                                          items:
                                            properties:
                                              datasetIDRegex:
                                                description: If unset, this property
                                                  matches all datasets.
                                                type: string
                                              projectIDRegex:
                                                description: For organizations, if
                                                  unset, will match all projects.
                                                  Has no effect for data profile configurations
                                                  created within a project.
                                                type: string
                                              tableIDRegex:
                                                description: If unset, this property
                                                  matches all tables.
                                                type: string
                                            type: object
                                          type: array
                                      type: object
                                  type: object
                              type: object
                          type: object
                        cloudSQLTarget:
                          description: Cloud SQL target for Discovery. The first target
                            to match a table will be the one applied.
                          properties:
                            conditions:
                              description: In addition to matching the filter, these
                                conditions must be true before a profile is generated.
                              properties:
                                databaseEngines:
                                  description: Optional. Database engines that should
                                    be profiled. Optional. Defaults to ALL_SUPPORTED_DATABASE_ENGINES
                                    if unspecified.
                                  items:
                                    type: string
                                  type: array
                                types:
                                  description: Data profiles will only be generated
                                    for the database resource types specified in this
                                    field. If not specified, defaults to [DATABASE_RESOURCE_TYPE_ALL_SUPPORTED_TYPES].
                                  items:
                                    type: string
                                  type: array
                              type: object
                            disabled:
                              description: Disable profiling for database resources
                                that match this filter.
                              type: object
                            filter:
                              description: Required. The tables the discovery cadence
                                applies to. The first target with a matching filter
                                will be the one to apply to a table.
                              properties:
                                collection:
                                  description: A specific set of database resources
                                    for this filter to apply to.
                                  properties:
                                    includeRegexes:
                                      description: A collection of regular expressions
                                        to match a database resource against.
                                      properties:
                                        patterns:
                                          description: A group of regular expression
                                            patterns to match against one or more
                                            database resources. Maximum of 100 entries.
                                            The sum of all regular expression's length
                                            can't exceed 10 KiB.
                                          items:
                                            properties:
                                              databaseRegex:
                                                description: Regex to test the database
                                                  name against. If empty, all databases
                                                  match.
                                                type: string
                                              databaseResourceNameRegex:
                                                description: Regex to test the database
                                                  resource's name against. An example
                                                  of a database resource name is a
                                                  table's name. Other database resource
                                                  names like view names could be included
                                                  in the future. If empty, all database
                                                  resources match.
                                                type: string
                                              instanceRegex:
                                                description: Regex to test the instance
                                                  name against. If empty, all instances
                                                  match.
                                                type: string
                                              projectIDRegex:
                                                description: For organizations, if
                                                  unset, will match all projects.
                                                  Has no effect for configurations
                                                  created within a project.
                                                type: string
                                            type: object
                                          type: array
                                      type: object
                                  type: object
                                databaseResourceReference:
                                  description: The database resource to scan. Targets
                                    including this can only include one target (the
                                    target with this database resource reference).
                                  properties:
                                    database:
                                      description: Required. Name of a database within
                                        the instance.
                                      type: string
                                    databaseResource:
                                      description: Required. Name of a database resource,
                                        for example, a table within the database.
                                      type: string
                                    instance:
                                      description: 'Required. The instance where this
                                        resource is located. For example: Cloud SQL
                                        instance ID.'
                                      type: string
                                    projectID:
                                      description: Required. If within a project-level
                                        config, then this must match the config's
                                        project ID.
                                      type: string
                                  type: object
                                others:
                                  description: Catch-all. This should always be the
                                    last target in the list because anything above
                                    it will apply first. Should only appear once in
                                    a configuration. If none is specified, a default
                                    one will be added automatically.
                                  type: object
                              type: object
                            generationCadence:
                              description: How often and when to update profiles.
                                New tables that match both the filter and conditions
                                are scanned as quickly as possible depending on system
                                capacity.
                              properties:
                                inspectTemplateModifiedCadence:
                                  description: Governs when to update data profiles
                                    when the inspection rules defined by the `InspectTemplate`
                                    change. If not set, changing the template will
                                    not cause a data profile to update.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when the template is modified.
                                        Defaults to never.
                                      type: string
                                  type: object
                                refreshFrequency:
                                  description: Data changes (non-schema changes) in
                                    Cloud SQL tables can't trigger reprofiling. If
                                    you set this field, profiles are refreshed at
                                    this frequency regardless of whether the underlying
                                    tables have changed. Defaults to never.
                                  type: string
                                schemaModifiedCadence:
                                  description: When to reprofile if the schema has
                                    changed.
                                  properties:
                                    frequency:
                                      description: Frequency to regenerate data profiles
                                        when the schema is modified. Defaults to monthly.
                                      type: string
                                    types:
                                      description: The types of schema modifications
                                        to consider. Defaults to NEW_COLUMNS.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              type: object
                          type: object
                        cloudStorageTarget:
                          description: Cloud Storage target for Discovery. The first
                            target to match a table will be the one applied.
                          properties:
                            conditions:
                              description: Optional. In addition to matching the filter,
                                these conditions must be true before a profile is
                                generated.
                              properties:
                                cloudStorageConditions:
                                  description: Optional. Cloud Storage conditions.
                                  properties:
                                    includedBucketAttributes:
                                      description: Required. Only objects with the
                                        specified attributes will be scanned. Defaults
                                        to [ALL_SUPPORTED_BUCKETS] if unset.
                                      items:
                                        type: string
                                      type: array
                                    includedObjectAttributes:
                                      description: Required. Only objects with the
                                        specified attributes will be scanned. If an
                                        object has one of the specified attributes
                                        but is inside an excluded bucket, it will
                                        not be scanned. Defaults to [ALL_SUPPORTED_OBJECTS].
                                        A profile will be created even if no objects
                                        match the included_object_attributes.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                createdAfter:
                                  description: Optional. File store must have been
                                    created after this date. Used to avoid backfilling.
                                  type: string
                                minAge:
                                  description: Optional. Minimum age a file store
                                    must have. If set, the value must be 1 hour or
                                    greater.
                                  type: string
                              type: object
                            disabled:
                              description: Optional. Disable profiling for buckets
                                that match this filter.
                              type: object
                            filter:
                              description: Required. The buckets the generation_cadence
                                applies to. The first target with a matching filter
                                will be the one to apply to a bucket.
                              properties:
                                cloudStorageResourceReference:
                                  description: Optional. The bucket to scan. Targets
                                    including this can only include one target (the
                                    target with this bucket). This enables profiling
                                    the contents of a single bucket, while the other
                                    options allow for easy profiling of many bucets
                                    within a project or an organization.
                                  properties:
                                    bucketName:
                                      description: Required. The bucket to scan.
                                      type: string
                                    projectID:
                                      description: Required. If within a project-level
                                        config, then this must match the config's
                                        project id.
                                      type: string
                                  type: object
                                collection:
                                  description: Optional. A specific set of buckets
                                    for this filter to apply to.
                                  properties:
                                    includeRegexes:
                                      description: Optional. A collection of regular
                                        expressions to match a file store against.
                                      properties:
                                        patterns:
                                          description: Required. The group of regular
                                            expression patterns to match against one
                                            or more file stores. Maximum of 100 entries.
                                            The sum of all regular expression's length
                                            can't exceed 10 KiB.
                                          items:
                                            properties:
                                              cloudStorageRegex:
                                                description: Optional. Regex for Cloud
                                                  Storage.
                                                properties:
                                                  bucketNameRegex:
                                                    description: 'Optional. Regex
                                                      to test the bucket name against.
                                                      If empty, all buckets match.
                                                      Example: "marketing2021" or
                                                      "(marketing)\d{4}" will both
                                                      match the bucket gs://marketing2021'
                                                    type: string
                                                  projectIDRegex:
                                                    description: Optional. For organizations,
                                                      if unset, will match all projects.
                                                    type: string
                                                type: object
                                            type: object
                                          type: array
                                      type: object
                                    includeTags:
                                      description: |-
                                        Optional. To be included in the collection, a resource must meet all of the
                                         following requirements:

                                          - If tag filters are provided, match all provided tag filters.
                                          - If one or more patterns are specified, match at least one pattern.

                                         For a resource to match the tag filters, the resource must have all of the
                                         provided tags attached. Tags refer to Resource Manager tags bound to the
                                         resource or its ancestors. For more information, see [Manage
                                         schedules](https://cloud.google.com/sensitive-data-protection/docs/profile-project-cloud-storage#manage-schedules).
                                      properties:
                                        tagFilters:
                                          description: Required. A resource must match
                                            ALL of the specified tag filters to be
                                            included in the collection.
                                          items:
                                            properties:
                                              namespacedTagKey:
                                                description: The namespaced name for
                                                  the tag key. Must be in the format
                                                  `{parent_id}/{tag_key_short_name}`,
                                                  for example, "123456/sensitive"
                                                  for an organization parent, or "my-project/sensitive"
                                                  for a project parent.
                                                type: string
                                              namespacedTagValue:
                                                description: The namespaced name for
                                                  the tag value. Must be in the format
                                                  `{parent_id}/{tag_key_short_name}/{short_name}`,
                                                  for example, "123456/environment/prod"
                                                  for an organization parent, or "my-project/environment/prod"
                                                  for a project parent.
                                                type: string
                                            type: object
                                          type: array
                                      type: object
                                  type: object
                                others:
                                  description: Optional. Catch-all. This should always
                                    be the last target in the list because anything
                                    above it will apply first. Should only appear
                                    once in a configuration. If none is specified,
                                    a default one will be added automatically.
                                  type: object
                              type: object
                            generationCadence:
                              description: Optional. How often and when to update
                                profiles. New buckets that match both the filter and
                                conditions are scanned as quickly as possible depending
                                on system capacity.
                              properties:
                                inspectTemplateModifiedCadence:
                                  description: Optional. Governs when to update data
                                    profiles when the inspection rules defined by
                                    the `InspectTemplate` change. If not set, changing
                                    the template will not cause a data profile to
                                    update.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when the template is modified.
                                        Defaults to never.
                                      type: string
                                  type: object
                                refreshFrequency:
                                  description: Optional. Data changes in Cloud Storage
                                    can't trigger reprofiling. If you set this field,
                                    profiles are refreshed at this frequency regardless
                                    of whether the underlying buckets have changed.
                                    Defaults to never.
                                  type: string
                              type: object
                          type: object
                        otherCloudTarget:
                          description: Other clouds target for discovery. The first
                            target to match a resource will be the one applied.
                          properties:
                            conditions:
                              description: Optional. In addition to matching the filter,
                                these conditions must be true before a profile is
                                generated.
                              properties:
                                amazonS3BucketConditions:
                                  description: Amazon S3 bucket conditions.
                                  properties:
                                    bucketTypes:
                                      description: Optional. Bucket types that should
                                        be profiled. Optional. Defaults to TYPE_ALL_SUPPORTED
                                        if unspecified.
                                      items:
                                        type: string
                                      type: array
                                    objectStorageClasses:
                                      description: Optional. Object classes that should
                                        be profiled. Optional. Defaults to ALL_SUPPORTED_CLASSES
                                        if unspecified.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                minAge:
                                  description: Minimum age a resource must be before
                                    Cloud DLP can profile it. Value must be 1 hour
                                    or greater.
                                  type: string
                              type: object
                            dataSourceType:
                              description: 'Required. The type of data profiles generated
                                by this discovery target. Supported values are: *
                                aws/s3/bucket'
                              properties:
                                dataSource:
                                  description: |-
                                    A string that identifies the type of resource being profiled.
                                     Current values:

                                     * google/bigquery/table
                                     * google/project
                                     * google/sql/table
                                     * google/gcs/bucket
                                  type: string
                              type: object
                            disabled:
                              description: Disable profiling for resources that match
                                this filter.
                              type: object
                            filter:
                              description: Required. The resources that the discovery
                                cadence applies to. The first target with a matching
                                filter will be the one to apply to a resource.
                              properties:
                                collection:
                                  description: A collection of resources for this
                                    filter to apply to.
                                  properties:
                                    includeRegexes:
                                      description: A collection of regular expressions
                                        to match a resource against.
                                      properties:
                                        patterns:
                                          description: A group of regular expression
                                            patterns to match against one or more
                                            resources. Maximum of 100 entries. The
                                            sum of all regular expression's length
                                            can't exceed 10 KiB.
                                          items:
                                            properties:
                                              amazonS3BucketRegex:
                                                description: Regex for Amazon S3 buckets.
                                                properties:
                                                  awsAccountRegex:
                                                    description: The AWS account regex.
                                                    properties:
                                                      accountIDRegex:
                                                        description: Optional. Regex
                                                          to test the AWS account
                                                          ID against. If empty, all
                                                          accounts match.
                                                        type: string
                                                    type: object
                                                  bucketNameRegex:
                                                    description: Optional. Regex to
                                                      test the bucket name against.
                                                      If empty, all buckets match.
                                                    type: string
                                                type: object
                                            type: object
                                          type: array
                                      type: object
                                  type: object
                                others:
                                  description: Optional. Catch-all. This should always
                                    be the last target in the list because anything
                                    above it will apply first. Should only appear
                                    once in a configuration. If none is specified,
                                    a default one will be added automatically.
                                  type: object
                                singleResource:
                                  description: The resource to scan. Configs using
                                    this filter can only have one target (the target
                                    with this single resource reference).
                                  properties:
                                    amazonS3Bucket:
                                      description: Amazon S3 bucket.
                                      properties:
                                        awsAccount:
                                          description: The AWS account.
                                          properties:
                                            accountID:
                                              description: Required. AWS account ID.
                                              type: string
                                          type: object
                                        bucketName:
                                          description: Required. The bucket name.
                                          type: string
                                      type: object
                                  type: object
                              type: object
                            generationCadence:
                              description: How often and when to update data profiles.
                                New resources that match both the filter and conditions
                                are scanned as quickly as possible depending on system
                                capacity.
                              properties:
                                inspectTemplateModifiedCadence:
                                  description: Optional. Governs when to update data
                                    profiles when the inspection rules defined by
                                    the `InspectTemplate` change. If not set, changing
                                    the template will not cause a data profile to
                                    update.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when the template is modified.
                                        Defaults to never.
                                      type: string
                                  type: object
                                refreshFrequency:
                                  description: Optional. Frequency to update profiles
                                    regardless of whether the underlying resource
                                    has changes. Defaults to never.
                                  type: string
                              type: object
                          type: object
                        secretsTarget:
                          description: Discovery target that looks for credentials
                            and secrets stored in cloud resource metadata and reports
                            them as vulnerabilities to Security Command Center. Only
                            one target of this type is allowed.
                          type: object
                        vertexDatasetTarget:
                          description: Vertex AI dataset target for Discovery. The
                            first target to match a dataset will be the one applied.
                            Note that discovery for Vertex AI can incur Cloud Storage
                            Class B operation charges for storage.objects.get operations
                            and retrieval fees. For more information, see [Cloud Storage
                            pricing](https://cloud.google.com/storage/pricing#price-tables).
                            Note that discovery for Vertex AI dataset will not be
                            able to scan images unless DiscoveryConfig.processing_location.image_fallback_location
                            has multi_region_processing or global_processing configured.
                          properties:
                            conditions:
                              description: In addition to matching the filter, these
                                conditions must be true before a profile is generated.
                              properties:
                                createdAfter:
                                  description: Vertex AI dataset must have been created
                                    after this date. Used to avoid backfilling.
                                  type: string
                                minAge:
                                  description: Minimum age a Vertex AI dataset must
                                    have. If set, the value must be 1 hour or greater.
                                  type: string
                              type: object
                            disabled:
                              description: Disable profiling for datasets that match
                                this filter.
                              type: object
                            filter:
                              description: Required. The datasets the discovery cadence
                                applies to. The first target with a matching filter
                                will be the one to apply to a dataset.
                              properties:
                                collection:
                                  description: A specific set of Vertex AI datasets
                                    for this filter to apply to.
                                  properties:
                                    vertexDatasetRegexes:
                                      description: The regex used to filter dataset
                                        resources.
                                      properties:
                                        patterns:
                                          description: Required. The group of regular
                                            expression patterns to match against one
                                            or more datasets. Maximum of 100 entries.
                                            The sum of the lengths of all regular
                                            expressions can't exceed 10 KiB.
                                          items:
                                            properties:
                                              projectIDRegex:
                                                description: For organizations, if
                                                  unset, will match all projects.
                                                  Has no effect for configurations
                                                  created within a project.
                                                type: string
                                            type: object
                                          type: array
                                      type: object
                                  type: object
                                others:
                                  description: Catch-all. This should always be the
                                    last target in the list because anything above
                                    it will apply first. Should only appear once in
                                    a configuration. If none is specified, a default
                                    one will be added automatically.
                                  type: object
                                vertexDatasetResourceReference:
                                  description: The dataset resource to scan. Targets
                                    including this can only include one target (the
                                    target with this dataset resource reference).
                                  properties:
                                    datasetResourceName:
                                      description: |-
                                        Required. The name of the Vertex AI resource. If set within a project-level
                                         configuration, the specified resource must be within the project.
                                         Examples:

                                         * `projects/{project}/locations/{location}/datasets/{dataset}`
                                      type: string
                                  type: object
                              type: object
                            generationCadence:
                              description: How often and when to update profiles.
                                New datasets that match both the filter and conditions
                                are scanned as quickly as possible depending on system
                                capacity.
                              properties:
                                inspectTemplateModifiedCadence:
                                  description: Governs when to update data profiles
                                    when the inspection rules defined by the `InspectTemplate`
                                    change. If not set, changing the template will
                                    not cause a data profile to be updated.
                                  properties:
                                    frequency:
                                      description: How frequently data profiles can
                                        be updated when the template is modified.
                                        Defaults to never.
                                      type: string
                                  type: object
                                refreshFrequency:
                                  description: If you set this field, profiles are
                                    refreshed at this frequency regardless of whether
                                    the underlying datasets have changed. Defaults
                                    to never.
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  updateTime:
                    description: Output only. The last update timestamp of a DiscoveryConfig.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
