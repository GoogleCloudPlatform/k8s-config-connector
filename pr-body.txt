This PR addresses the mock drift for `orgpolicy` by:
1. Moving `orgpolicy/v2` protos from `third_party/googleapis` to `mockgcp/apis/mockgcp/cloud/orgpolicy/v2` to follow the standard pattern for managed mocks.
2. Implementing missing `ListPolicies`, `GetEffectivePolicy`, and `ListCustomConstraints` methods in `mockorgpolicy`.
3. Updating the mock normalizer to handle fields in the new `List` responses.
4. Ensuring `Policy` and `PolicySpec` etags are correctly handled.

The protos used are updated to the latest version from `googleapis` master.

This PR was generated by **Overseer** (powered by the gemini-3-flash-preview model).

Fixes #7486
