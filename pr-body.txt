This PR addresses the mock drift for datastream by moving the protos to `apis/mockgcp/cloud` and implementing missing CRUD and List methods in `mockdatastream`.

### Changes:
- Moved datastream v1 protos to `mockgcp/apis/mockgcp/cloud/datastream/v1` to align with project conventions for tracked protos.
- Updated `mockgcp/Makefile` to use the protos from the new location and added a cleanup step to avoid shadowing issues.
- Enhanced `UpdateConnectionProfile` to support updating all fields using `UpdateByFieldMask`.
- Implemented missing CRUD methods for `Stream` and `StreamObject`.
- Implemented missing `List` methods for `ConnectionProfile`, `PrivateConnection`, `Route`, and `Stream`.
- Implemented `FetchStaticIps`.

This PR was generated by **Overseer** (powered by the gemini-3-flash-preview model).

Fixes #7507
