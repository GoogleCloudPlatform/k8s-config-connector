- exec: gcloud deploy apply --file=mockdeploy/testdata/deliverypipeline/crud/pipeline.yaml --region=us-central1
- exec: gcloud deploy delivery-pipelines list --region=us-central1 --filter="name:test-delivery-pipeline-${uniqueId}"
- exec: gcloud deploy delivery-pipelines describe test-delivery-pipeline-${uniqueId} --region=us-central1
- exec: gcloud deploy delivery-pipelines delete test-delivery-pipeline-${uniqueId} --region=us-central1 --quiet
