- exec: gcloud edge-cloud networking subnets create test-${uniqueId} --region=${region} --network=projects/${projectID}/global/networks/test-${uniqueId}
- exec: gcloud edge-cloud networking subnets describe test-${uniqueId} --region=${region}
- exec: gcloud edge-cloud networking subnets delete test-${uniqueId} --region=${region} --quiet