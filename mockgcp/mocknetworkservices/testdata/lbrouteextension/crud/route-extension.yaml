name: projects/${projectId}/locations/global/lbRouteExtensions/test-${uniqueId}
loadBalancingScheme: EXTERNAL_MANAGED
forwardingRules:
- https://www.googleapis.com/compute/v1/projects/${projectId}/global/forwardingRules/test-fr-${uniqueId}
extensionChains:
- name: chain1
  matchCondition:
    celExpression: "request.host == 'example.com'"
  extensions:
  - name: ext1
    service: https://www.googleapis.com/compute/v1/projects/${projectId}/global/backendServices/test-bs-${uniqueId}
    timeout: 0.1s
