
- exec: gcloud essential-contacts create --email=test-${uniqueId}@example.com --notification-categories=all --language=en --project=${projectId}
- exec: gcloud essential-contacts describe ${uniqueId} --project=${projectId}
- exec: gcloud essential-contacts update ${uniqueId} --notification-categories=technical --project=${projectId}
- exec: gcloud essential-contacts delete ${uniqueId} --project=${projectId}
