
- exec: gcloud compute addresses create test-${uniqueId} --region us-central1
- exec: gcloud compute addresses describe test-${uniqueId} --region us-central1
- exec: gcloud compute addresses list --filter="name=('test-${uniqueId}')" --region us-central1
- exec: gcloud compute addresses delete test-${uniqueId} --region us-central1 --quiet
