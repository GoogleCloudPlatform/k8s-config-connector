- exec: gcloud beta quotas preferences create --service=compute.googleapis.com --quota-id=GLOBAL-INTERNAL-MANAGED-BACKEND-BUCKETS-per-project --preferred-value=29 --project=${projectId} --preference-id=testp-${uniqueId}
- exec: gcloud beta quotas preferences describe testp-${uniqueId} --project=${projectId}
- exec: gcloud beta quotas preferences update --service=compute.googleapis.com --quota-id=GLOBAL-INTERNAL-MANAGED-BACKEND-BUCKETS-per-project --preferred-value=30 --project=${projectId} testp-${uniqueId}
- exec: gcloud beta quotas preferences describe testp-${uniqueId} --project=${projectId}