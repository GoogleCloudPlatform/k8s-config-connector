apiVersion: networkservices.cnrm.cloud.google.com/v1alpha1
kind: NetworkServicesEdgeCacheOrigin
metadata:
  labels:
    label-one: two
    label-three: three
  name: networkservicesedgecacheorigin-${uniqueId}
spec:
  description: The default bucket for media edge test
  originAddress: gs://media-edge-default
  projectRef:
    external: ${projectId}
  resourceID: networkservicesedgecacheorigin${uniqueId}
