- caisURL: //compute.googleapis.com/projects/${projectId}/zones/us-central1-a/disks/computedisk-${uniqueId}
  group: compute.cnrm.cloud.google.com
  kind: ComputeDisk
  name: computedisk-${uniqueId}
  namespace: ${uniqueId}
  version: v1beta1
- caisURL: //compute.googleapis.com/projects/${projectId}/global/images/computeimage-test-${uniqueId}
  group: compute.cnrm.cloud.google.com
  kind: ComputeImage
  name: computeimage-test-${uniqueId}
  namespace: ${uniqueId}
  version: v1beta1
