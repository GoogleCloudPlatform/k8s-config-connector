apiVersion: kms.cnrm.cloud.google.com/v1beta1
kind: KMSCryptoKey
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: ${projectId}
  labels:
    cnrm-test: "true"
    label-one: two
    label-three: three
    managed-by-cnrm: "true"
  name: kmscryptokey-${uniqueId}
spec:
  destroyScheduledDuration: 720h0m0s
  keyRingRef:
    external: projects/${projectId}/locations/us-central1/keyRings/kmscryptokey-${uniqueId}
  purpose: ENCRYPT_DECRYPT
  resourceID: kmscryptokey-${uniqueId}
  rotationPeriod: 48h0m0s
  versionTemplate:
    algorithm: GOOGLE_SYMMETRIC_ENCRYPTION
    protectionLevel: SOFTWARE