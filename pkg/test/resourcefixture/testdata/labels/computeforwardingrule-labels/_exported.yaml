apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputeForwardingRule
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: ${projectId}
  labels:
    cnrm-test: "true"
    label-one: two
    label-three: three
    label-two: two
    managed-by-cnrm: "true"
  name: ${forwardingRuleID}
spec:
  description: A global forwarding rule
  ipAddress:
    ip: 0.0.0.0
  ipProtocol: TCP
  loadBalancingScheme: INTERNAL_SELF_MANAGED
  location: global
  networkRef:
    external: https://www.googleapis.com/compute/v1/projects/${projectId}/global/networks/default
  networkTier: PREMIUM
  portRange: 80-80
  resourceID: ${forwardingRuleID}
  target:
    targetHTTPProxyRef:
      external: https://www.googleapis.com/compute/v1/projects/${projectId}/global/targetHttpProxies/computetargethttpproxy-${uniqueId}