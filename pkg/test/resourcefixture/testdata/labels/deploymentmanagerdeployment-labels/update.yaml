apiVersion: deploymentmanager.cnrm.cloud.google.com/v1alpha1
kind: DeploymentManagerDeployment
metadata:
  labels:
    label-one: two
    label-three: three
  name: deploymentmanagerdeployment-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  resourceID: deploymentmanagerdeployment${uniqueId}
  target:
  - config:
    - content: ${file("path/to/config.yml")}
