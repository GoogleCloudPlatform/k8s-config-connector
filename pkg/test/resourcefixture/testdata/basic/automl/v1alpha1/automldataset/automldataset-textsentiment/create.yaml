apiVersion: automl.cnrm.cloud.google.com/v1alpha1
kind: AutoMLDataset
metadata:
  name: automldataset-ts-${uniqueId}
spec:
  displayName: "ds_ts_${uniqueId}"
  location: us-central1
  projectRef:
    external: ${projectId}
  textSentimentDatasetMetadata:
    sentimentMax: 5
