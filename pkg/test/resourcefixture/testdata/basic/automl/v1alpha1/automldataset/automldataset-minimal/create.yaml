apiVersion: automl.cnrm.cloud.google.com/v1alpha1
kind: AutoMLDataset
metadata:
  name: automldataset-min-${uniqueId}
spec:
  displayName: "ds_min_${uniqueId}"
  location: us-central1
  projectRef:
    external: ${projectId}
  textClassificationDatasetMetadata:
    classificationType: "MULTICLASS"
