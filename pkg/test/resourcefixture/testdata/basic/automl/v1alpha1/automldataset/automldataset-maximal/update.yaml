apiVersion: automl.cnrm.cloud.google.com/v1alpha1
kind: AutoMLDataset
metadata:
  name: automldataset-max-${uniqueId}
spec:
  displayName: "ds_max_upd_${uniqueId}"
  description: "Updated description"
  labels:
    env: "prod"
    test: "false"
    newlabel: "val"
  location: us-central1
  projectRef:
    external: ${projectId}
  textClassificationDatasetMetadata:
    classificationType: "MULTICLASS"
