apiVersion: automl.cnrm.cloud.google.com/v1alpha1
kind: AutoMLDataset
metadata:
  name: automldataset-te-${uniqueId}
spec:
  displayName: "ds_te_${uniqueId}"
  location: us-central1
  projectRef:
    external: ${projectId}
  textExtractionDatasetMetadata: {}
