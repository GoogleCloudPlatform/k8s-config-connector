apiVersion: automl.cnrm.cloud.google.com/v1alpha1
kind: AutoMLDataset
metadata:
  name: automldataset-ic-${uniqueId}
spec:
  displayName: "ds_ic_${uniqueId}"
  location: us-central1
  projectRef:
    external: ${projectId}
  imageClassificationDatasetMetadata:
    classificationType: "MULTICLASS"
