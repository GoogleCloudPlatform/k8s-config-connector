apiVersion: run.cnrm.cloud.google.com/v1alpha1
kind: CloudRunInstance
metadata:
  name: cloudruninstance-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  description: "Updated instance description"
  containers:
  - image: "us-docker.pkg.dev/cloudrun/container/hello"
    name: "main"
