apiVersion: tasks.cnrm.cloud.google.com/v1alpha1
kind: TasksQueue
metadata:
  name: tasksqueue-minimal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  locationID: us-west2
  description: "Updated description"
