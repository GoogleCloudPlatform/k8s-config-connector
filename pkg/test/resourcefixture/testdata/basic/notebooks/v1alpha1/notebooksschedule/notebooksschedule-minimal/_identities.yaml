- caisURL: //storage.googleapis.com/projects/${projectId}/buckets/${projectId}-bucket-${uniqueId}
  group: storage.cnrm.cloud.google.com
  kind: StorageBucket
  name: ${projectId}-bucket-${uniqueId}
  namespace: ${uniqueId}
  version: v1beta1
- caisURL: //notebooks.googleapis.com/projects/${projectId}/locations/us-central1/schedules/notebooksschedule-minimal-${uniqueId}
  group: notebooks.cnrm.cloud.google.com
  kind: NotebooksSchedule
  name: notebooksschedule-minimal-${uniqueId}
  namespace: ${uniqueId}
  version: v1alpha1
