apiVersion: modelarmor.cnrm.cloud.google.com/v1alpha1
kind: ModelArmorFloorSetting
metadata:
  name: modelarmorfloorsetting-maximal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: global
  enableFloorSettingEnforcement: true
  # integratedServices must be set to AI_PLATFORM when aiPlatformFloorSetting is configured.
  integratedServices:
  - AI_PLATFORM
  filterConfig:
    raiSettings:
      raiFilters:
      - filterType: SEXUALLY_EXPLICIT
        confidenceLevel: LOW_AND_ABOVE
      - filterType: HATE_SPEECH
        confidenceLevel: LOW_AND_ABOVE
    piAndJailbreakFilterSettings:
      filterEnforcement: ENABLED
      confidenceLevel: LOW_AND_ABOVE
    maliciousURIFilterSettings:
      filterEnforcement: ENABLED
    sdpSettings:
      basicConfig:
        filterEnforcement: ENABLED
  aiPlatformFloorSetting:
    inspectOnly: true
    enableCloudLogging: true
  floorSettingMetadata:
    multiLanguageDetection:
      enableMultiLanguageDetection: true
