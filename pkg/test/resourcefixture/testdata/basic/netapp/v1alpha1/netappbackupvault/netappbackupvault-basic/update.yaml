apiVersion: netapp.cnrm.cloud.google.com/v1alpha1
kind: NetAppBackupVault
metadata:
  name: netappbackupvault-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-west2
  description: "An updated backup vault"
