apiVersion: contentwarehouse.cnrm.cloud.google.com/v1alpha1
kind: ContentWarehouseSynonymSet
metadata:
  name: cws${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us
  synonyms:
    - words:
        - "invoice"
        - "bill"
