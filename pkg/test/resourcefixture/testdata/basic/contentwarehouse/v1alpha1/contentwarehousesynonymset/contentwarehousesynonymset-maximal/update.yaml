apiVersion: contentwarehouse.cnrm.cloud.google.com/v1alpha1
kind: ContentWarehouseSynonymSet
metadata:
  name: ${uniqueId}
spec:
  resourceID: "business"
  projectRef:
    external: ${projectId}
  location: us
  context: "business"
  synonyms:
    - words:
        - "sale"
        - "invoice"
        - "bill"
        - "receipt"
    - words:
        - "finance"
        - "money"
        - "cash"
