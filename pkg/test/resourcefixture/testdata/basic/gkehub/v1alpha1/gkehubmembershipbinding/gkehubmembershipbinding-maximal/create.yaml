apiVersion: gkehub.cnrm.cloud.google.com/v1alpha1
kind: GKEHubMembershipBinding
metadata:
  name: gkhmb-max-${uniqueId}
spec:
  projectRef:
    external: projects/${projectId}
  location: global
  membershipRef:
    name: gkhmb-max-membership-${uniqueId}
  scopeRef:
    name: gkhmb-max-scope-${uniqueId}
  resourceID: gkhmb-max-id-${uniqueId}
