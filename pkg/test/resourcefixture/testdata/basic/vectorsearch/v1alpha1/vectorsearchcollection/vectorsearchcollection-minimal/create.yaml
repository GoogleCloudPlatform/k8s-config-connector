apiVersion: vectorsearch.cnrm.cloud.google.com/v1alpha1
kind: VectorSearchCollection
metadata:
  name: collection-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  vectorSchema:
    text_vector:
      denseVector:
        dimensions: 128
