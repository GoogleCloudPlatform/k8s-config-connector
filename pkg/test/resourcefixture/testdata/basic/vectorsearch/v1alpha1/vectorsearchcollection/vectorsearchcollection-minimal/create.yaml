apiVersion: vectorsearch.cnrm.cloud.google.com/v1alpha1
kind: VectorSearchCollection
metadata:
  name: collection-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  displayName: "Minimal Vector Search Collection"
  vectorSchema:
    my-vector:
      denseVector:
        dimensions: 128
