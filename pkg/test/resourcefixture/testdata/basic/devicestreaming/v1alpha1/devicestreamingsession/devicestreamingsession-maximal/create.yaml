apiVersion: devicestreaming.cnrm.cloud.google.com/v1alpha1
kind: DeviceStreamingSession
metadata:
  name: session-max-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  ttl: 900s
  androidDevice:
    androidModelID: "shiba"
    androidVersionID: "34"
