apiVersion: contactcenterinsights.cnrm.cloud.google.com/v1alpha1
kind: CCInsightsQAScorecard
metadata:
  name: qascorecard-maximal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  displayName: "Scorecard Maximal Updated ${uniqueId}"
  description: "This is a scorecard created to test maximal configuration updated ${uniqueId}"
