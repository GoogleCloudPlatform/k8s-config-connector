apiVersion: contactcenterinsights.cnrm.cloud.google.com/v1alpha1
kind: CCInsightsView
metadata:
  name: ccinsightsview-maximal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  displayName: "My CCInsights View"
  value: "projects/${projectId}/locations/us-central1/conversations/*"
