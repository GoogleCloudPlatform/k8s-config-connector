apiVersion: securesourcemanager.cnrm.cloud.google.com/v1beta1
kind: SecureSourceManagerInstance
metadata:
  name: ssminstance-${uniqueId}
spec:
  location: us-central1
  privateConfig:
    caPoolRef:
      external: projects/${projectId}/locations/us-central1/caPools/privatecacapool-${uniqueId}
    isPrivate: true
  projectRef:
    external: ${projectId}
  resourceID: ssminstance-${uniqueId}