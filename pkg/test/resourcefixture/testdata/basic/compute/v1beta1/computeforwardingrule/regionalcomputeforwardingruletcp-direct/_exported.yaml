apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputeForwardingRule
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: ${projectId}
  labels:
    cnrm-test: "true"
    label-one: value-two
    managed-by-cnrm: "true"
  name: ${forwardingRuleID}
spec:
  description: A regional forwarding rule
  ipAddress:
    ip: 10.1.2.2
  ipProtocol: TCP
  loadBalancingScheme: INTERNAL_MANAGED
  location: us-central1
  networkRef:
    external: https://www.googleapis.com/compute/v1/projects/${projectId}/global/networks/${networkID}
  networkTier: PREMIUM
  portRange: 90-90
  resourceID: ${forwardingRuleID}
  subnetworkRef:
    external: https://www.googleapis.com/compute/v1/projects/${projectId}/regions/us-central1/subnetworks/${subnetworkID}
  target:
    targetTCPProxyRef:
      external: https://www.googleapis.com/compute/v1/projects/${projectId}/regions/us-central1/targetTcpProxies/${targetTcpProxyID}