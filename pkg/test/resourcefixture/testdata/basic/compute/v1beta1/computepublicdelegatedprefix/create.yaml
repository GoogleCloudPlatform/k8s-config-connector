apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputePublicDelegatedPrefix
metadata:
  name: computepublicdelegatedprefix-${uniqueId}
spec:
  location: us-central1
  ipCidrRange: 1.2.3.0/24
  description: "Test Public Delegated Prefix"
  projectRef:
    external: ${projectId}
  parentPrefixRef:
    external: "projects/${projectId}/global/publicAdvertisedPrefixes/my-parent-prefix"
