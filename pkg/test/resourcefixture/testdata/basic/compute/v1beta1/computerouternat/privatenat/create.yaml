apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputeRouterNAT
metadata:
  name: computerouternat-${uniqueId}
spec:
  region: us-central1
  routerRef:
    name: computerouternat-dep-${uniqueId}
  type: PRIVATE
  sourceSubnetworkIpRangesToNat: LIST_OF_SUBNETWORKS
  subnetwork:
  - subnetworkRef:
      name: computerouternat-dep-${uniqueId}
    sourceIpRangesToNat:
    - ALL_IP_RANGES
