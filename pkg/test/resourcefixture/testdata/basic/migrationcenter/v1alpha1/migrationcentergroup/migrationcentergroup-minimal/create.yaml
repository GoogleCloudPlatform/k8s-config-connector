apiVersion: migrationcenter.cnrm.cloud.google.com/v1alpha1
kind: MigrationCenterGroup
metadata:
  name: migrationcentergroup-minimal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
