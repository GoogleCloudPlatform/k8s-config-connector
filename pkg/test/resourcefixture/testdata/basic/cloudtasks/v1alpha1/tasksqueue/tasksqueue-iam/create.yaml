apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: tasksqueue-iam-member-${uniqueId}
spec:
  resourceRef:
    apiVersion: cloudtasks.cnrm.cloud.google.com/v1alpha1
    kind: TasksQueue
    name: tasksqueue-iam-${uniqueId}
  role: roles/cloudtasks.viewer
  member: "serviceAccount:${projectId}.svc.id.goog[default/default]"
