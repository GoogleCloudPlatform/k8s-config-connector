apiVersion: securitycentermanagement.cnrm.cloud.google.com/v1alpha1
kind: SecurityCenterManagementSecurityHealthAnalyticsCustomModule
metadata:
  name: securityhealthanalyticscustommodule-project
  labels:
    test-label: update
spec:
  projectRef:
    external: "projects/${projectId}"
  location: "global"
  displayName: "custom_module_test"
  enablementState: "DISABLED"
  customConfig:
    predicate:
      expression: "resource.name == 'test2'"
    severity: "MEDIUM"
    description: "test description updated"
    recommendation: "test recommendation updated"
    resourceSelector:
      resourceTypes:
      - "compute.googleapis.com/Network"
