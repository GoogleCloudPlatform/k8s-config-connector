apiVersion: mapmanagement.cnrm.cloud.google.com/v1alpha1
kind: MapManagementMapConfig
metadata:
  name: mapmanagementmapconfig-maximal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  displayName: "Maximal Map Config ${uniqueId}"
  description: "This is a maximal map config used for testing ${uniqueId}"
  mapFeatures:
    simpleFeatures:
      - FLATTEN_BUILDINGS
      - ICONIC_ICONS
    poiBoostLevel: 1
  mapType: VECTOR
