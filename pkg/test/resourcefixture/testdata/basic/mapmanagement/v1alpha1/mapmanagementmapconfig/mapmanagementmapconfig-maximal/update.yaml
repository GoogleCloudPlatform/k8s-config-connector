apiVersion: mapmanagement.cnrm.cloud.google.com/v1alpha1
kind: MapManagementMapConfig
metadata:
  name: mapmanagementmapconfig-maximal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  displayName: "Maximal Map Config Updated ${uniqueId}"
  description: "This is an updated maximal map config used for testing ${uniqueId}"
  mapFeatures:
    simpleFeatures:
      - ICONIC_ICONS
    poiBoostLevel: -1
  mapType: VECTOR
