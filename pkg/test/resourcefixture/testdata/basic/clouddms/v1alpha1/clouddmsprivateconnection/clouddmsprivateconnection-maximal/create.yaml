apiVersion: clouddms.cnrm.cloud.google.com/v1alpha1
kind: CloudDMSPrivateConnection
metadata:
  name: pc-maximal-${uniqueId}
spec:
  location: us-central1
  projectRef:
    external: ${projectId}
  displayName: My Private Connection
  labels:
    label-one: value-one
  vpcPeeringConfig:
    vpcNameRef:
      name: computenetwork-maximal-${uniqueId}
    subnet: 10.0.0.0/29
