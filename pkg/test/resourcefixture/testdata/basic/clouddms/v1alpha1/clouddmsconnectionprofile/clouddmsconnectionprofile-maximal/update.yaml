apiVersion: clouddms.cnrm.cloud.google.com/v1alpha1
kind: CloudDMSConnectionProfile
metadata:
  name: clouddmsconnectionprofile-max-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  displayName: "Updated Maximal MySQL connection profile"
  labels:
    foo: bar-updated
  mysql:
    host: "10.0.0.5"
    port: 3306
    username: "max-username"
    password: "max-password"
