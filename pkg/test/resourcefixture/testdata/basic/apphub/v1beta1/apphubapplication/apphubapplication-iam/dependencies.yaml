apiVersion: apphub.cnrm.cloud.google.com/v1beta1
kind: AppHubApplication
metadata:
  name: apphubapplication-iam-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-central1
  description: "AppHub Application for IAM test"
  scope:
    type: REGIONAL
