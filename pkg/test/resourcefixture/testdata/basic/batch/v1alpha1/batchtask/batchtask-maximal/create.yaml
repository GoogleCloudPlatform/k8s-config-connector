apiVersion: batch.cnrm.cloud.google.com/v1alpha1
kind: BatchTask
metadata:
  annotations:
    cnrm.cloud.google.com/external-id: "projects/${projectId}/locations/us-west2/jobs/batchjob-max-${uniqueId}/taskGroups/group0/tasks/0"
  name: batchtask-max-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: us-west2
  resourceID: "0"
