apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: iampolicymember-${uniqueId}
spec:
  memberFrom:
    serviceAccountRef:
      name: parameter-dep-${uniqueId}
  role: roles/parametermanager.admin
  resourceRef:
    apiVersion: parametermanager.cnrm.cloud.google.com/v1alpha1
    kind: ParameterManagerParameter
    name: parameter-${uniqueId}
