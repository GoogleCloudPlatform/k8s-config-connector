apiVersion: parametermanager.cnrm.cloud.google.com/v1alpha1
kind: ParameterManagerParameter
metadata:
  labels:
    label-one: value-two
    label-two: value-three
  name: parametermanagerparameter-full-${uniqueId}
spec:
  resourceID: parametermanagerparameter-full-${uniqueId}
  projectRef:
    external: projects/${projectId}
  location: global
  format: JSON
  kmsKeyRef:
    name: kmscryptokey-2-${uniqueId}