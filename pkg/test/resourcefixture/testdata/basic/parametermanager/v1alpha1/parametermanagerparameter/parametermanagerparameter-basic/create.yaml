apiVersion: parametermanager.cnrm.cloud.google.com/v1alpha1
kind: ParameterManagerParameter
metadata:
  labels:
    label-one: value-one
  name: parametermanagerparameter-${uniqueId}
spec:
  projectRef:
    external: projects/${projectId}
  location: global
  format: JSON
  kmsKeyRef:
    name: kmscryptokey-${uniqueId}