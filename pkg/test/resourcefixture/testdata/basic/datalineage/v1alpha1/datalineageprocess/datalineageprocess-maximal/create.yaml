apiVersion: datalineage.cnrm.cloud.google.com/v1alpha1
kind: DataLineageProcess
metadata:
  name: dlprocess-max-${uniqueId}
spec:
  location: us
  projectRef:
    external: ${projectId}
  displayName: Maximal Data Lineage Process
  origin:
    sourceType: CUSTOM
    name: myCustomIntegration
  attributes:
    engine: "spark"
    version: "3.1.2"
    debug: "true"
