apiVersion: firestore.cnrm.cloud.google.com/v1alpha1
kind: FirestoreBackupSchedule
metadata:
  name: firestorebackupschedule-minimal-${uniqueId}
spec:
  databaseRef:
    name: firestoredatabase-dep-${uniqueId}
  retention: "604800s"
  dailyRecurrence: {}
