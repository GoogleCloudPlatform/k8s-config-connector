apiVersion: storage.cnrm.cloud.google.com/v1alpha1
kind: StorageFolder
metadata:
  name: storagefolder-minimal-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  locationID: us-west2
  description: "Updated description"
