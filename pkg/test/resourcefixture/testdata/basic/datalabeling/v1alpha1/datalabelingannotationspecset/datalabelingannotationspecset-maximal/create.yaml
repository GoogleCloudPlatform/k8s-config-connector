apiVersion: datalabeling.cnrm.cloud.google.com/v1alpha1
kind: DataLabelingAnnotationSpecSet
metadata:
  name: datalabelingannotationspecset-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  displayName: "Maximal ${uniqueId}"
  description: "A maximal test annotation spec set for continuous evaluation"
  annotationSpecs:
    - displayName: "label_positive"
      description: "positive sentiment"
    - displayName: "label_negative"
      description: "negative sentiment"
