- caisURL: //pubsub.googleapis.com/projects/${projectId}/topics/pubsubtopic-${uniqueId}
  group: pubsub.cnrm.cloud.google.com
  kind: PubSubTopic
  name: pubsubtopic-${uniqueId}
  namespace: ${uniqueId}
  version: v1beta1
- caisURL: //pubsub.googleapis.com/projects/${projectId}/subscriptions/pubsubsubscription-${uniqueId}
  group: pubsub.cnrm.cloud.google.com
  kind: PubSubSubscription
  name: pubsubsubscription-${uniqueId}
  namespace: ${uniqueId}
  version: v1beta1
- caisURL: //pubsub.googleapis.com/projects/${projectId}/snapshots/pubsubsnapshot-minimal-${uniqueId}
  group: pubsub.cnrm.cloud.google.com
  kind: PubSubSnapshot
  name: pubsubsnapshot-minimal-${uniqueId}
  namespace: ${uniqueId}
  version: v1alpha1
