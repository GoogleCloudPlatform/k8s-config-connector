apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubSnapshot
metadata:
  labels:
    cnrm-test: "true"
    managed-by-cnrm: "true"
  name: pubsubsnapshot-minimal-${uniqueId}
spec:
  labels:
    cnrm-test: "true"
    managed-by-cnrm: "true"
  projectRef:
    external: ${projectId}
  pubSubSubscriptionRef: null
  resourceID: pubsubsnapshot-minimal-${uniqueId}
  topicRef:
    external: projects/${projectId}/topics/pubsubtopic-${uniqueId}