apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityUrlList
metadata:
  labels:
    test-label: "urllist-test"
  name: urllist-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: global
  description: "Test URL List"
  values:
    - "example.com"
    - "google.com"
