apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityURLList
metadata:
  name: urllist-max-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: us-central1
  description: "An updated URL list maximal test"
  values:
    - "google.com"
    - "github.com"
    - "gitlab.com"
