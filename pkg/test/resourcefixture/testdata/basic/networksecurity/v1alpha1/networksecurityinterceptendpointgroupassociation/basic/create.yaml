apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityInterceptEndpointGroupAssociation
metadata:
  labels:
    label-one: "value-one"
  name: networksecurityinterceptendpointgroupassociation-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  location: global
  networkRef:
    name: networksecurityinterceptendpointgroupassociation-${uniqueId}
  interceptEndpointGroupRef:
    external: projects/${projectId}/locations/global/interceptEndpointGroups/my-ieg-${uniqueId}
