apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityInterceptDeploymentGroup
metadata:
  name: networksecurityinterceptdeploymentgroup-sample
spec:
  projectRef:
    name: ${projectId}
  location: "global"
  description: "A sample intercept deployment group"
  networkRef:
    name: networksecurity-dep-${uniqueId}
