apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityFirewallEndpoint
metadata:
  name: fwe-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: us-central1
  resourceID: fwe-${uniqueId}
  description: "An updated maximal firewall endpoint"
  labels:
    foo: baz
    newkey: value
  billingProjectID: "${projectId}"
  endpointSettings:
    jumboFramesEnabled: true
