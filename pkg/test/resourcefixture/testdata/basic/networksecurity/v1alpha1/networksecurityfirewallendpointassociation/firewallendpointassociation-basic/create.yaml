apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityFirewallEndpointAssociation
metadata:
  name: fwea-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: us-central1-a
  networkRef:
    name: computenetwork-${uniqueId}
  firewallEndpointRef:
    name: fwe-${uniqueId}
  disabled: false
  labels:
    foo: bar
