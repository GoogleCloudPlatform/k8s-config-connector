apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecuritySecurityProfileGroup
metadata:
  name: spg-max-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: global
  description: "Maximal security profile group - updated"
  labels:
    foo: baz
  threatPreventionProfileRef:
    external: "projects/${projectId}/locations/global/securityProfiles/dummy-threat-${uniqueId}"
  customInterceptProfileRef:
    external: "projects/${projectId}/locations/global/securityProfiles/dummy-intercept-up-${uniqueId}"
  customMirroringProfileRef:
    external: "projects/${projectId}/locations/global/securityProfiles/dummy-mirroring-up-${uniqueId}"
  urlFilteringProfileRef:
    external: "projects/${projectId}/locations/global/securityProfiles/dummy-url-up-${uniqueId}"
