apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecuritySecurityProfileGroup
metadata:
  name: spg-max-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: global
  description: "security profile group"
  labels:
    foo: bar
  customInterceptProfileRef:
    name: sp-dep-${uniqueId}-ci

