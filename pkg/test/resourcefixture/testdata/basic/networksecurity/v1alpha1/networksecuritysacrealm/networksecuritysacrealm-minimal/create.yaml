apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecuritySACRealm
metadata:
  name: sacrealm-min-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: us-central1
