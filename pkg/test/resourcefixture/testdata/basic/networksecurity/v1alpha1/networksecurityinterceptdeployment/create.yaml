apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityInterceptDeployment
metadata:
  name: networksecurityinterceptdeployment-sample
spec:
  projectRef:
    name: ${projectId}
  location: "us-central1-a"
  description: "A sample intercept deployment"
  forwardingRuleRef:
    name: networksecurity-dep-${uniqueId}
  interceptDeploymentGroupRef:
    name: networksecurity-dep-${uniqueId}
