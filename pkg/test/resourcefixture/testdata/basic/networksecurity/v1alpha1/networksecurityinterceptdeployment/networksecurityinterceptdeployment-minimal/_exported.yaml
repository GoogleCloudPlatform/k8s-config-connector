apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityInterceptDeployment
metadata:
  name: interceptdep-${uniqueId}
spec:
  forwardingRuleRef:
    external: projects/${projectId}/regions/us-central1/forwardingRules/shared-fr
  interceptDeploymentGroupRef:
    external: projects/${projectId}/locations/global/interceptDeploymentGroups/shared-dg
  location: us-central1-a
  projectRef:
    external: ${projectId}
  resourceID: interceptdep-${uniqueId}