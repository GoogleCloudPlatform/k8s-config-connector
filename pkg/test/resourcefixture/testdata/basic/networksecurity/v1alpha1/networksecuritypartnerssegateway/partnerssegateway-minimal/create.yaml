apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityPartnerSSEGateway
metadata:
  name: partnerssegateway-min-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: global
  sseGatewayReferenceID: "ref-${uniqueId}"
