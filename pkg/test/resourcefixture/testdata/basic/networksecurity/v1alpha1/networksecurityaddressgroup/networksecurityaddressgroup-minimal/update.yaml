apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityAddressGroup
metadata:
  name: addressgroup-min-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: global
  type: IPV4
  capacity: 100
  description: "An updated description for minimal address group"
