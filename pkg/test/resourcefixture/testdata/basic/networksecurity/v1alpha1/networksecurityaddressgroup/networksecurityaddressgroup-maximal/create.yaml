apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityAddressGroup
metadata:
  name: addressgroup-max-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: global
  type: IPV4
  capacity: 150
  description: "A maximal address group test"
  labels:
    foo: bar
  items:
    - "10.0.0.0/24"
  purpose:
    - DEFAULT
