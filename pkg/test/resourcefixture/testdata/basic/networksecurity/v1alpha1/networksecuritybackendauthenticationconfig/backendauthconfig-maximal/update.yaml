apiVersion: networksecurity.cnrm.cloud.google.com/v1alpha1
kind: NetworkSecurityBackendAuthenticationConfig
metadata:
  name: backendauth-max-${uniqueId}
spec:
  projectRef:
    external: "projects/${projectId}"
  location: us-central1
  description: "Updated description of backend auth config"
  labels:
    foo: baz
    new-key: new-val
  wellKnownRoots: NONE
