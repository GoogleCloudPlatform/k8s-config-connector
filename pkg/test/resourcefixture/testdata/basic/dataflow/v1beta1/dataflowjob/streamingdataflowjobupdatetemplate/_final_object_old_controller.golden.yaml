apiVersion: dataflow.cnrm.cloud.google.com/v1beta1
kind: DataflowJob
metadata:
  annotations:
    cnrm.cloud.google.com/management-conflict-prevention-policy: none
    cnrm.cloud.google.com/mutable-but-unreadable-fields: '{"spec":{"ipConfiguration":"WORKER_IP_PUBLIC","parameters":{"inputTopic":"projects/${projectId}/topics/pubsubtopic-${uniqueId}","outputTableSpec":"${projectId}:bigquerydataset${uniqueId}.bigquerytable${uniqueId}"}}}'
    cnrm.cloud.google.com/on-delete: cancel
    cnrm.cloud.google.com/project-id: ${projectId}
    cnrm.cloud.google.com/state-into-spec: absent
  finalizers:
  - cnrm.cloud.google.com/finalizer
  - cnrm.cloud.google.com/deletion-defender
  generation: 3
  labels:
    cnrm-test: "true"
    label-one: value-one
  name: dataflowjob-${uniqueId}
  namespace: ${uniqueId}
  selfLink: /apis/dataflow.cnrm.cloud.google.com/v1beta1/namespaces/${uniqueId}/dataflowjobs/dataflowjob-${uniqueId}
spec:
  additionalExperiments:
  - enable_stackdriver_agent_metrics
  enableStreamingEngine: true
  ipConfiguration: WORKER_IP_PUBLIC
  machineType: n1-standard-1
  maxWorkers: 3
  networkRef:
    name: default
  parameters:
    inputTopic: projects/${projectId}/topics/pubsubtopic-${uniqueId}
    outputTableSpec: ${projectId}:bigquerydataset${uniqueId}.bigquerytable${uniqueId}
  resourceID: dataflowjob-${uniqueId}
  serviceAccountRef:
    name: gsa-${uniqueId}
  subnetworkRef:
    name: default
  tempGcsLocation: gs://storagebucket-${uniqueId}/tmp
  templateGcsPath: gs://dataflow-templates/2020-02-10-00_RC00/PubSub_to_BigQuery
  zone: us-central1-a
status:
  conditions:
  - lastTransitionTime: "1970-01-01T00:00:00Z"
    message: The resource is up to date
    reason: UpToDate
    status: "True"
    type: Ready
  jobId: ${jobID}
  observedGeneration: 3
  state: JOB_STATE_RUNNING
  type: JOB_TYPE_STREAMING
