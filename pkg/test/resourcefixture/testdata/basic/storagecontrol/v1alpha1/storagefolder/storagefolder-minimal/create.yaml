apiVersion: storage.cnrm.cloud.google.com/v1alpha1
kind: StorageFolder
metadata:
  name: folder-${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  storagebucketRef:
    name: bucket-${uniqueId}
