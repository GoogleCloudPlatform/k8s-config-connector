apiVersion: bigquery.cnrm.cloud.google.com/v1beta1
kind: BigQueryDataset
metadata:
  annotations:
    cnrm.cloud.google.com/reconcile-interval-in-seconds: "0"
  name: bqdataset-${uniqueId}
spec:
  resourceID: bqdataset_${uniqueId}
  location: us-central1
