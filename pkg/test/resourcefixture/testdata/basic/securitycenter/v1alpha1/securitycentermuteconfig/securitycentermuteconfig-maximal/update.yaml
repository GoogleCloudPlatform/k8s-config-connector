apiVersion: securitycenter.cnrm.cloud.google.com/v1alpha1
kind: SecurityCenterMuteConfig
metadata:
  name: scmuteconfig-max-${uniqueId}
spec:
  organizationRef:
    external: organizations/${TEST_ORG_ID}
  description: "Updated Maximal SecurityCenterMuteConfig description"
  filter: category = "VULNERABILITY" AND severity = "MEDIUM"
  type: STATIC
