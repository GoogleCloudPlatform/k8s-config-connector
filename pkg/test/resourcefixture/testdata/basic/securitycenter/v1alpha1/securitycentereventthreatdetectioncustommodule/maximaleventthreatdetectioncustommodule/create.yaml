apiVersion: securitycenter.cnrm.cloud.google.com/v1alpha1
kind: SecurityCenterEventThreatDetectionCustomModule
metadata:
  name: ${uniqueId}
spec:
  projectRef:
    external: ${projectId}
  displayName: "Maximal ETD Module"
  description: "Detailed description"
  type: CONFIGURABLE_SSH_BRUTE_FORCE
  config:
    metadata:
      module_enablement_state: ENABLED
  enablementState: ENABLED
