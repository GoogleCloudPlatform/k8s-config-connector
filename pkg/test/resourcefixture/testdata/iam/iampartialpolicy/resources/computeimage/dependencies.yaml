apiVersion: compute.cnrm.cloud.google.com/v1beta1
kind: ComputeImage
metadata:
  annotations:
    cnrm.cloud.google.com/reconcile-interval-in-seconds: "0"
  name: computeimage-${uniqueId}
spec:
  rawDisk:
    source: https://storage.googleapis.com/test-bucket/test-image.tar.gz
    containerType: TAR
