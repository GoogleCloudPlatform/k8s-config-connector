// Copyright 2020 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//    http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Config Connector and manual
//     changes will be clobbered when the file is regenerated.
//
// ----------------------------------------------------------------------------

// *** DISCLAIMER ***
// Config Connector's go-client for CRDs is currently in ALPHA, which means
// that future versions of the go-client may include breaking changes.
// Please try it out and give us feedback!

package v1alpha1

import (
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/clients/generated/apis/k8s/v1alpha1"
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

type JobAdBreaks struct {
	/* Start time in seconds for the ad break, relative to the output file timeline. The default is `0s`. */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`
}

type JobAes128 struct {
}

type JobAnimationEnd struct {
	/* The time to end overlay object, in seconds. Default: 0 */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`
}

type JobAnimationFade struct {
	/* The time to end the fade animation, in seconds. Default: `start_time_offset` + 1s */
	// +optional
	EndTimeOffset *string `json:"endTimeOffset,omitempty"`

	/* Required. Type of fade animation: `FADE_IN` or `FADE_OUT`. */
	// +optional
	FadeType *string `json:"fadeType,omitempty"`

	/* The time to start the fade animation, in seconds. Default: 0 */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`

	/* Normalized coordinates based on output video resolution. Valid values: `0.0`–`1.0`. `xy` is the upper-left coordinate of the overlay object. For example, use the x and y coordinates {0,0} to position the top-left corner of the overlay animation in the top-left corner of the output video. */
	// +optional
	Xy *JobXy `json:"xy,omitempty"`
}

type JobAnimationStatic struct {
	/* The time to start displaying the overlay object, in seconds. Default: 0 */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`

	/* Normalized coordinates based on output video resolution. Valid values: `0.0`–`1.0`. `xy` is the upper-left coordinate of the overlay object. For example, use the x and y coordinates {0,0} to position the top-left corner of the overlay animation in the top-left corner of the output video. */
	// +optional
	Xy *JobXy `json:"xy,omitempty"`
}

type JobAnimations struct {
	/* End previous animation. */
	// +optional
	AnimationEnd *JobAnimationEnd `json:"animationEnd,omitempty"`

	/* Display overlay object with fade animation. */
	// +optional
	AnimationFade *JobAnimationFade `json:"animationFade,omitempty"`

	/* Display static overlay object. */
	// +optional
	AnimationStatic *JobAnimationStatic `json:"animationStatic,omitempty"`
}

type JobAttributes struct {
	/* Optional. A list of track definitions for the input asset. */
	// +optional
	TrackDefinitions []JobTrackDefinitions `json:"trackDefinitions,omitempty"`
}

type JobAudio struct {
	/* Enable boosting high frequency components. The default is `false`.

	**Note:** This field is not supported. */
	// +optional
	HighBoost *bool `json:"highBoost,omitempty"`

	/* Enable boosting low frequency components. The default is `false`.

	**Note:** This field is not supported. */
	// +optional
	LowBoost *bool `json:"lowBoost,omitempty"`

	/* Specify audio loudness normalization in loudness units relative to full
	scale (LUFS). Enter a value between -24 and 0 (the default), where:

	*   -24 is the Advanced Television Systems Committee (ATSC A/85) standard
	*   -23 is the EU R128 broadcast standard
	*   -19 is the prior standard for online mono audio
	*   -18 is the ReplayGain standard
	*   -16 is the prior standard for stereo audio
	*   -14 is the new online audio standard recommended by Spotify, as well
	as Amazon Echo
	*   0 disables normalization */
	// +optional
	Lufs *float64 `json:"lufs,omitempty"`
}

type JobAudioStream struct {
	/* Required. Audio bitrate in bits per second. Must be between 1 and 10,000,000. */
	// +optional
	BitrateBps *int32 `json:"bitrateBps,omitempty"`

	/* Number of audio channels. Must be between 1 and 6. The default is 2. */
	// +optional
	ChannelCount *int32 `json:"channelCount,omitempty"`

	/* A list of channel names specifying layout of the audio channels.
	This only affects the metadata embedded in the container headers, if
	supported by the specified format. The default is `["fl", "fr"]`.

	Supported channel names:

	- `fl` - Front left channel
	- `fr` - Front right channel
	- `sl` - Side left channel
	- `sr` - Side right channel
	- `fc` - Front center channel
	- `lfe` - Low frequency */
	// +optional
	ChannelLayout []string `json:"channelLayout,omitempty"`

	/* The codec for this audio stream. The default is `aac`.

	Supported audio codecs:

	- `aac`
	- `aac-he`
	- `aac-he-v2`
	- `mp3`
	- `ac3`
	- `eac3`
	- `vorbis` */
	// +optional
	Codec *string `json:"codec,omitempty"`

	/* The name for this particular audio stream that will be added to the HLS/DASH manifest. Not supported in MP4 files. */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* The BCP-47 language code, such as `en-US` or `sr-Latn`. For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier. Not supported in MP4 files. */
	// +optional
	LanguageCode *string `json:"languageCode,omitempty"`

	/* The mapping for the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list] atoms with audio [EditAtom.inputs][google.cloud.video.transcoder.v1.EditAtom.inputs]. */
	// +optional
	Mapping []JobMapping `json:"mapping,omitempty"`

	/* The audio sample rate in Hertz. The default is 48000 Hertz. */
	// +optional
	SampleRateHertz *int32 `json:"sampleRateHertz,omitempty"`
}

type JobBwdif struct {
	/* Deinterlace all frames rather than just the frames identified as interlaced. The default is `false`. */
	// +optional
	DeinterlaceAllFrames *bool `json:"deinterlaceAllFrames,omitempty"`

	/* Specifies the deinterlacing mode to adopt.
	The default is `send_frame`.
	Supported values:

	- `send_frame`: Output one frame for each frame
	- `send_field`: Output one frame for each field */
	// +optional
	Mode *string `json:"mode,omitempty"`

	/* The picture field parity assumed for the input interlaced video.
	The default is `auto`.
	Supported values:

	- `tff`: Assume the top field is first
	- `bff`: Assume the bottom field is first
	- `auto`: Enable automatic detection of field parity */
	// +optional
	Parity *string `json:"parity,omitempty"`
}

type JobClearkey struct {
}

type JobColor struct {
	/* Control brightness of the video. Enter a value between -1 and 1, where -1 is minimum brightness and 1 is maximum brightness. 0 is no change. The default is 0. */
	// +optional
	Brightness *float64 `json:"brightness,omitempty"`

	/* Control black and white contrast of the video. Enter a value between -1 and 1, where -1 is minimum contrast and 1 is maximum contrast. 0 is no change. The default is 0. */
	// +optional
	Contrast *float64 `json:"contrast,omitempty"`

	/* Control color saturation of the video. Enter a value between -1 and 1, where -1 is fully desaturated and 1 is maximum saturation. 0 is no change. The default is 0. */
	// +optional
	Saturation *float64 `json:"saturation,omitempty"`
}

type JobConfig struct {
	/* List of ad breaks. Specifies where to insert ad break tags in the output manifests. */
	// +optional
	AdBreaks []JobAdBreaks `json:"adBreaks,omitempty"`

	/* List of edit atoms. Defines the ultimate timeline of the resulting file or manifest. */
	// +optional
	EditList []JobEditList `json:"editList,omitempty"`

	/* List of elementary streams. */
	// +optional
	ElementaryStreams []JobElementaryStreams `json:"elementaryStreams,omitempty"`

	/* List of encryption configurations for the content. Each configuration has an ID. Specify this ID in the [MuxStream.encryption_id][google.cloud.video.transcoder.v1.MuxStream.encryption_id] field to indicate the configuration to use for that `MuxStream` output. */
	// +optional
	Encryptions []JobEncryptions `json:"encryptions,omitempty"`

	/* List of input assets stored in Cloud Storage. */
	// +optional
	Inputs []JobInputs `json:"inputs,omitempty"`

	/* List of output manifests. */
	// +optional
	Manifests []JobManifests `json:"manifests,omitempty"`

	/* List of multiplexing settings for output streams. */
	// +optional
	MuxStreams []JobMuxStreams `json:"muxStreams,omitempty"`

	/* Output configuration. */
	// +optional
	Output *JobOutput `json:"output,omitempty"`

	/* List of overlays on the output video, in descending Z-order. */
	// +optional
	Overlays []JobOverlays `json:"overlays,omitempty"`

	/* Destination on Pub/Sub. */
	// +optional
	PubsubDestination *JobPubsubDestination `json:"pubsubDestination,omitempty"`

	/* List of output sprite sheets. Spritesheets require at least one VideoStream in the Jobconfig. */
	// +optional
	SpriteSheets []JobSpriteSheets `json:"spriteSheets,omitempty"`
}

type JobCrop struct {
	/* The number of pixels to crop from the bottom. The default is 0. */
	// +optional
	BottomPixels *int32 `json:"bottomPixels,omitempty"`

	/* The number of pixels to crop from the left. The default is 0. */
	// +optional
	LeftPixels *int32 `json:"leftPixels,omitempty"`

	/* The number of pixels to crop from the right. The default is 0. */
	// +optional
	RightPixels *int32 `json:"rightPixels,omitempty"`

	/* The number of pixels to crop from the top. The default is 0. */
	// +optional
	TopPixels *int32 `json:"topPixels,omitempty"`
}

type JobDash struct {
	/* The segment reference scheme for a `DASH` manifest. The default is `SEGMENT_LIST`. */
	// +optional
	SegmentReferenceScheme *string `json:"segmentReferenceScheme,omitempty"`
}

type JobDeblock struct {
	/* Enable deblocker. The default is `false`. */
	// +optional
	Enabled *bool `json:"enabled,omitempty"`

	/* Set strength of the deblocker. Enter a value between 0 and 1. The higher the value, the stronger the block removal. 0 is no deblocking. The default is 0. */
	// +optional
	Strength *float64 `json:"strength,omitempty"`
}

type JobDeinterlace struct {
	/* Specifies the Bob Weaver Deinterlacing Filter Configuration. */
	// +optional
	Bwdif *JobBwdif `json:"bwdif,omitempty"`

	/* Specifies the Yet Another Deinterlacing Filter Configuration. */
	// +optional
	Yadif *JobYadif `json:"yadif,omitempty"`
}

type JobDenoise struct {
	/* Set strength of the denoise. Enter a value between 0 and 1. The higher the value, the smoother the image. 0 is no denoising. The default is 0. */
	// +optional
	Strength *float64 `json:"strength,omitempty"`

	/* Set the denoiser mode. The default is `standard`.

	Supported denoiser modes:

	- `standard`
	- `grain` */
	// +optional
	Tune *string `json:"tune,omitempty"`
}

type JobDrmSystems struct {
	/* Clearkey configuration. */
	// +optional
	Clearkey *JobClearkey `json:"clearkey,omitempty"`

	/* Fairplay configuration. */
	// +optional
	Fairplay *JobFairplay `json:"fairplay,omitempty"`

	/* Playready configuration. */
	// +optional
	Playready *JobPlayready `json:"playready,omitempty"`

	/* Widevine configuration. */
	// +optional
	Widevine *JobWidevine `json:"widevine,omitempty"`
}

type JobEditList struct {
	/* End time in seconds for the atom, relative to the input file timeline. When `end_time_offset` is not specified, the `inputs` are used until the end of the atom. */
	// +optional
	EndTimeOffset *string `json:"endTimeOffset,omitempty"`

	/* List of [Input.key][google.cloud.video.transcoder.v1.Input.key] values identifying files that should be used in this atom. The listed `inputs` must have the same timeline. */
	// +optional
	Inputs []JobInputs `json:"inputs,omitempty"`

	/* A unique key for this atom. Must be specified when using advanced mapping. */
	// +optional
	Key *string `json:"key,omitempty"`

	/* Start time in seconds for the atom, relative to the input file timeline. The default is `0s`. */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`
}

type JobElementaryStreams struct {
	/* Encoding of an audio stream. */
	// +optional
	AudioStream *JobAudioStream `json:"audioStream,omitempty"`

	/* A unique key for this elementary stream. */
	// +optional
	Key *string `json:"key,omitempty"`

	/* Encoding of a text stream. For example, closed captions or subtitles. */
	// +optional
	TextStream *JobTextStream `json:"textStream,omitempty"`

	/* Encoding of a video stream. */
	// +optional
	VideoStream *JobVideoStream `json:"videoStream,omitempty"`
}

type JobEncryptions struct {
	/* Configuration for AES-128 encryption. */
	// +optional
	Aes128 *JobAes128 `json:"aes128,omitempty"`

	/* Required. DRM system(s) to use; at least one must be specified. If a DRM system is omitted, it is considered disabled. */
	// +optional
	DrmSystems *JobDrmSystems `json:"drmSystems,omitempty"`

	/* Required. Identifier for this set of encryption options. */
	// +optional
	Id *string `json:"id,omitempty"`

	/* Configuration for MPEG Common Encryption (MPEG-CENC). */
	// +optional
	MpegCenc *JobMpegCenc `json:"mpegCenc,omitempty"`

	/* Configuration for SAMPLE-AES encryption. */
	// +optional
	SampleAes *JobSampleAes `json:"sampleAes,omitempty"`

	/* Keys are stored in Google Secret Manager. */
	// +optional
	SecretManagerKeySource *JobSecretManagerKeySource `json:"secretManagerKeySource,omitempty"`
}

type JobFairplay struct {
}

type JobFmp4 struct {
	/* Optional. Specify the codec tag string that will be used in the media
	bitstream. When not specified, the codec appropriate value is used.

	Supported H265 codec tags:

	- `hvc1` (default)
	- `hev1` */
	// +optional
	CodecTag *string `json:"codecTag,omitempty"`
}

type JobH264 struct {
	/* Specifies whether an open Group of Pictures (GOP) structure should be allowed or not. The default is `false`. */
	// +optional
	AllowOpenGop *bool `json:"allowOpenGop,omitempty"`

	/* Specify the intensity of the adaptive quantizer (AQ). Must be between 0 and 1, where 0 disables the quantizer and 1 maximizes the quantizer. A higher value equals a lower bitrate but smoother image. The default is 0. */
	// +optional
	AqStrength *float64 `json:"aqStrength,omitempty"`

	/* The number of consecutive B-frames. Must be greater than or equal to zero. Must be less than [H264CodecSettings.gop_frame_count][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.gop_frame_count] if set. The default is 0. */
	// +optional
	BFrameCount *int32 `json:"bFrameCount,omitempty"`

	/* Allow B-pyramid for reference frame selection. This may not be supported on all decoders. The default is `false`. */
	// +optional
	BPyramid *bool `json:"bPyramid,omitempty"`

	/* Required. The video bitrate in bits per second. The minimum value is 1,000. The maximum value is 800,000,000. */
	// +optional
	BitrateBps *int32 `json:"bitrateBps,omitempty"`

	/* Target CRF level. Must be between 10 and 36, where 10 is the highest quality and 36 is the most efficient compression. The default is 21. */
	// +optional
	CrfLevel *int32 `json:"crfLevel,omitempty"`

	/* Use two-pass encoding strategy to achieve better video quality. [H264CodecSettings.rate_control_mode][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.rate_control_mode] must be `vbr`. The default is `false`. */
	// +optional
	EnableTwoPass *bool `json:"enableTwoPass,omitempty"`

	/* The entropy coder to use. The default is `cabac`.

	Supported entropy coders:

	- `cavlc`
	- `cabac` */
	// +optional
	EntropyCoder *string `json:"entropyCoder,omitempty"`

	/* Required. The target video frame rate in frames per second (FPS). Must be less than or equal to 120. */
	// +optional
	FrameRate *float64 `json:"frameRate,omitempty"`

	/* Optional. Frame rate conversion strategy for desired frame rate. The default is `DOWNSAMPLE`. */
	// +optional
	FrameRateConversionStrategy *string `json:"frameRateConversionStrategy,omitempty"`

	/* Select the GOP size based on the specified duration. The default is `3s`. Note that `gopDuration` must be less than or equal to [`segmentDuration`](#SegmentSettings), and [`segmentDuration`](#SegmentSettings) must be divisible by `gopDuration`. */
	// +optional
	GopDuration *string `json:"gopDuration,omitempty"`

	/* Select the GOP size based on the specified frame count. Must be greater than zero. */
	// +optional
	GopFrameCount *int32 `json:"gopFrameCount,omitempty"`

	/* The height of the video in pixels. Must be an even integer.
	When not specified, the height is adjusted to match the specified width
	and input aspect ratio. If both are omitted, the input height is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the height, in pixels, per the horizontal ASR. The API calculates
	the width per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	HeightPixels *int32 `json:"heightPixels,omitempty"`

	/* Optional. HLG color format setting for H264. */
	// +optional
	Hlg *JobHlg `json:"hlg,omitempty"`

	/* Pixel format to use. The default is `yuv420p`.

	Supported pixel formats:

	- `yuv420p` pixel format
	- `yuv422p` pixel format
	- `yuv444p` pixel format
	- `yuv420p10` 10-bit HDR pixel format
	- `yuv422p10` 10-bit HDR pixel format
	- `yuv444p10` 10-bit HDR pixel format
	- `yuv420p12` 12-bit HDR pixel format
	- `yuv422p12` 12-bit HDR pixel format
	- `yuv444p12` 12-bit HDR pixel format */
	// +optional
	PixelFormat *string `json:"pixelFormat,omitempty"`

	/* Enforces the specified codec preset. The default is `veryfast`. The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Preset). Note that certain values for this field may cause the transcoder to override other fields you set in the `H264CodecSettings` message. */
	// +optional
	Preset *string `json:"preset,omitempty"`

	/* Enforces the specified codec profile. The following profiles are
	supported:

	*   `baseline`
	*   `main`
	*   `high` (default)

	The available options are
	[FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Tune).
	Note that certain values for this field may cause the
	transcoder to override other fields you set in the `H264CodecSettings`
	message. */
	// +optional
	Profile *string `json:"profile,omitempty"`

	/* Specify the mode. The default is `vbr`.

	Supported rate control modes:

	- `vbr` - variable bitrate
	- `crf` - constant rate factor */
	// +optional
	RateControlMode *string `json:"rateControlMode,omitempty"`

	/* Optional. SDR color format setting for H264. */
	// +optional
	Sdr *JobSdr `json:"sdr,omitempty"`

	/* Enforces the specified codec tune. The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.264#Tune). Note that certain values for this field may cause the transcoder to override other fields you set in the `H264CodecSettings` message. */
	// +optional
	Tune *string `json:"tune,omitempty"`

	/* Initial fullness of the Video Buffering Verifier (VBV) buffer in bits. Must be greater than zero. The default is equal to 90% of [H264CodecSettings.vbv_size_bits][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.vbv_size_bits]. */
	// +optional
	VbvFullnessBits *int32 `json:"vbvFullnessBits,omitempty"`

	/* Size of the Video Buffering Verifier (VBV) buffer in bits. Must be greater than zero. The default is equal to [H264CodecSettings.bitrate_bps][google.cloud.video.transcoder.v1.VideoStream.H264CodecSettings.bitrate_bps]. */
	// +optional
	VbvSizeBits *int32 `json:"vbvSizeBits,omitempty"`

	/* The width of the video in pixels. Must be an even integer.
	When not specified, the width is adjusted to match the specified height
	and input aspect ratio. If both are omitted, the input width is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the width, in pixels, per the horizontal ASR. The API calculates
	the height per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	WidthPixels *int32 `json:"widthPixels,omitempty"`
}

type JobH265 struct {
	/* Specifies whether an open Group of Pictures (GOP) structure should be allowed or not. The default is `false`. */
	// +optional
	AllowOpenGop *bool `json:"allowOpenGop,omitempty"`

	/* Specify the intensity of the adaptive quantizer (AQ). Must be between 0 and 1, where 0 disables the quantizer and 1 maximizes the quantizer. A higher value equals a lower bitrate but smoother image. The default is 0. */
	// +optional
	AqStrength *float64 `json:"aqStrength,omitempty"`

	/* The number of consecutive B-frames. Must be greater than or equal to zero. Must be less than [H265CodecSettings.gop_frame_count][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.gop_frame_count] if set. The default is 0. */
	// +optional
	BFrameCount *int32 `json:"bFrameCount,omitempty"`

	/* Allow B-pyramid for reference frame selection. This may not be supported on all decoders. The default is `false`. */
	// +optional
	BPyramid *bool `json:"bPyramid,omitempty"`

	/* Required. The video bitrate in bits per second. The minimum value is 1,000. The maximum value is 800,000,000. */
	// +optional
	BitrateBps *int32 `json:"bitrateBps,omitempty"`

	/* Target CRF level. Must be between 10 and 36, where 10 is the highest quality and 36 is the most efficient compression. The default is 21. */
	// +optional
	CrfLevel *int32 `json:"crfLevel,omitempty"`

	/* Use two-pass encoding strategy to achieve better video quality. [H265CodecSettings.rate_control_mode][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.rate_control_mode] must be `vbr`. The default is `false`. */
	// +optional
	EnableTwoPass *bool `json:"enableTwoPass,omitempty"`

	/* Required. The target video frame rate in frames per second (FPS). Must be less than or equal to 120. */
	// +optional
	FrameRate *float64 `json:"frameRate,omitempty"`

	/* Optional. Frame rate conversion strategy for desired frame rate. The default is `DOWNSAMPLE`. */
	// +optional
	FrameRateConversionStrategy *string `json:"frameRateConversionStrategy,omitempty"`

	/* Select the GOP size based on the specified duration. The default is `3s`. Note that `gopDuration` must be less than or equal to [`segmentDuration`](#SegmentSettings), and [`segmentDuration`](#SegmentSettings) must be divisible by `gopDuration`. */
	// +optional
	GopDuration *string `json:"gopDuration,omitempty"`

	/* Select the GOP size based on the specified frame count. Must be greater than zero. */
	// +optional
	GopFrameCount *int32 `json:"gopFrameCount,omitempty"`

	/* Optional. HDR10 color format setting for H265. */
	// +optional
	Hdr10 *JobHdr10 `json:"hdr10,omitempty"`

	/* The height of the video in pixels. Must be an even integer.
	When not specified, the height is adjusted to match the specified width
	and input aspect ratio. If both are omitted, the input height is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the height, in pixels, per the horizontal ASR. The API calculates
	the width per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	HeightPixels *int32 `json:"heightPixels,omitempty"`

	/* Optional. HLG color format setting for H265. */
	// +optional
	Hlg *JobHlg `json:"hlg,omitempty"`

	/* Pixel format to use. The default is `yuv420p`.

	Supported pixel formats:

	- `yuv420p` pixel format
	- `yuv422p` pixel format
	- `yuv444p` pixel format
	- `yuv420p10` 10-bit HDR pixel format
	- `yuv422p10` 10-bit HDR pixel format
	- `yuv444p10` 10-bit HDR pixel format
	- `yuv420p12` 12-bit HDR pixel format
	- `yuv422p12` 12-bit HDR pixel format
	- `yuv444p12` 12-bit HDR pixel format */
	// +optional
	PixelFormat *string `json:"pixelFormat,omitempty"`

	/* Enforces the specified codec preset. The default is `veryfast`. The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.265). Note that certain values for this field may cause the transcoder to override other fields you set in the `H265CodecSettings` message. */
	// +optional
	Preset *string `json:"preset,omitempty"`

	/* Enforces the specified codec profile. The following profiles are
	supported:

	*   8-bit profiles
	*   `main` (default)
	*   `main-intra`
	*   `mainstillpicture`
	*   10-bit profiles
	*   `main10` (default)
	*   `main10-intra`
	*   `main422-10`
	*   `main422-10-intra`
	*   `main444-10`
	*   `main444-10-intra`
	*   12-bit profiles
	*   `main12` (default)
	*   `main12-intra`
	*   `main422-12`
	*   `main422-12-intra`
	*   `main444-12`
	*   `main444-12-intra`

	The available options are
	[FFmpeg-compatible](https://x265.readthedocs.io/).
	Note that certain values for this field may cause the
	transcoder to override other fields you set in the `H265CodecSettings`
	message. */
	// +optional
	Profile *string `json:"profile,omitempty"`

	/* Specify the mode. The default is `vbr`.

	Supported rate control modes:

	- `vbr` - variable bitrate
	- `crf` - constant rate factor */
	// +optional
	RateControlMode *string `json:"rateControlMode,omitempty"`

	/* Optional. SDR color format setting for H265. */
	// +optional
	Sdr *JobSdr `json:"sdr,omitempty"`

	/* Enforces the specified codec tune. The available options are [FFmpeg-compatible](https://trac.ffmpeg.org/wiki/Encode/H.265). Note that certain values for this field may cause the transcoder to override other fields you set in the `H265CodecSettings` message. */
	// +optional
	Tune *string `json:"tune,omitempty"`

	/* Initial fullness of the Video Buffering Verifier (VBV) buffer in bits. Must be greater than zero. The default is equal to 90% of [H265CodecSettings.vbv_size_bits][google.cloud.video.transcoder.v1.VideoStream.H265CodecSettings.vbv_size_bits]. */
	// +optional
	VbvFullnessBits *int32 `json:"vbvFullnessBits,omitempty"`

	/* Size of the Video Buffering Verifier (VBV) buffer in bits. Must be greater than zero. The default is equal to `VideoStream.bitrate_bps`. */
	// +optional
	VbvSizeBits *int32 `json:"vbvSizeBits,omitempty"`

	/* The width of the video in pixels. Must be an even integer.
	When not specified, the width is adjusted to match the specified height
	and input aspect ratio. If both are omitted, the input width is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the width, in pixels, per the horizontal ASR. The API calculates
	the height per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	WidthPixels *int32 `json:"widthPixels,omitempty"`
}

type JobHdr10 struct {
}

type JobHlg struct {
}

type JobImage struct {
	/* Target image opacity. Valid values are from  `1.0` (solid, default) to `0.0` (transparent), exclusive. Set this to a value greater than `0.0`. */
	// +optional
	Alpha *float64 `json:"alpha,omitempty"`

	/* Normalized image resolution, based on output video resolution. Valid values: `0.0`–`1.0`. To respect the original image aspect ratio, set either `x` or `y` to `0.0`. To use the original image resolution, set both `x` and `y` to `0.0`. */
	// +optional
	Resolution *JobResolution `json:"resolution,omitempty"`

	/* Required. URI of the image in Cloud Storage. For example, `gs://bucket/inputs/image.png`. Only PNG and JPEG images are supported. */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type JobInputs struct {
	/* Optional. Input Attributes. */
	// +optional
	Attributes *JobAttributes `json:"attributes,omitempty"`

	/* A unique key for this input. Must be specified when using advanced mapping and edit lists. */
	// +optional
	Key *string `json:"key,omitempty"`

	/* Preprocessing configurations. */
	// +optional
	PreprocessingConfig *JobPreprocessingConfig `json:"preprocessingConfig,omitempty"`

	/* URI of the media. Input files must be at least 5 seconds in duration and stored in Cloud Storage (for example, `gs://bucket/inputs/file.mp4`). If empty, the value is populated from [Job.input_uri][google.cloud.video.transcoder.v1.Job.input_uri]. See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats). */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type JobManifests struct {
	/* `DASH` manifest configuration. */
	// +optional
	Dash *JobDash `json:"dash,omitempty"`

	/* The name of the generated file. The default is `manifest` with the extension suffix corresponding to the [Manifest.type][google.cloud.video.transcoder.v1.Manifest.type]. */
	// +optional
	FileName *string `json:"fileName,omitempty"`

	/* Required. List of user supplied
	[MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] values that
	should appear in this manifest.

	When [Manifest.type][google.cloud.video.transcoder.v1.Manifest.type] is
	`HLS`, a media manifest with name
	[MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] and `.m3u8`
	extension is generated for each element in this list. */
	// +optional
	MuxStreams []JobMuxStreams `json:"muxStreams,omitempty"`

	/* Required. Type of the manifest. */
	// +optional
	Type *string `json:"type,omitempty"`
}

type JobMapping struct {
	/* Required. The [EditAtom.key][google.cloud.video.transcoder.v1.EditAtom.key] that references atom with text inputs in the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list]. */
	// +optional
	AtomKey *string `json:"atomKey,omitempty"`

	/* Required. The [Input.key][google.cloud.video.transcoder.v1.Input.key] that identifies the input file. */
	// +optional
	InputKey *string `json:"inputKey,omitempty"`

	/* Required. The zero-based index of the track in the input file. */
	// +optional
	InputTrack *int32 `json:"inputTrack,omitempty"`
}

type JobMpegCenc struct {
	/* Required. Specify the encryption scheme.

	Supported encryption schemes:

	- `cenc`
	- `cbcs` */
	// +optional
	Scheme *string `json:"scheme,omitempty"`
}

type JobMuxStreams struct {
	/* The container format. The default is `mp4`

	Supported streaming formats:

	- `ts`
	- `fmp4`- the corresponding file extension is `.m4s`

	Supported standalone file formats:

	- `mp4`
	- `mp3`
	- `ogg`
	- `vtt`

	See also:
	[Supported input and output
	formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats) */
	// +optional
	Container *string `json:"container,omitempty"`

	/* List of [ElementaryStream.key][google.cloud.video.transcoder.v1.ElementaryStream.key] values multiplexed in this stream. */
	// +optional
	ElementaryStreams []JobElementaryStreams `json:"elementaryStreams,omitempty"`

	/* Identifier of the encryption configuration to use. If omitted, output will be unencrypted. */
	// +optional
	EncryptionID *string `json:"encryptionID,omitempty"`

	/* The name of the generated file. The default is
	[MuxStream.key][google.cloud.video.transcoder.v1.MuxStream.key] with the
	extension suffix corresponding to the
	[MuxStream.container][google.cloud.video.transcoder.v1.MuxStream.container].

	Individual segments also have an incremental 10-digit zero-padded suffix
	starting from 0 before the extension, such as `mux_stream0000000123.ts`. */
	// +optional
	FileName *string `json:"fileName,omitempty"`

	/* Optional. `fmp4` container configuration. */
	// +optional
	Fmp4 *JobFmp4 `json:"fmp4,omitempty"`

	/* A unique key for this multiplexed stream. */
	// +optional
	Key *string `json:"key,omitempty"`

	/* Segment settings for `ts`, `fmp4` and `vtt`. */
	// +optional
	SegmentSettings *JobSegmentSettings `json:"segmentSettings,omitempty"`
}

type JobOutput struct {
	/* URI for the output file(s). For example, `gs://my-bucket/outputs/`. Must be a directory and not a top-level bucket. If empty, the value is populated from [Job.output_uri][google.cloud.video.transcoder.v1.Job.output_uri]. See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats). */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type JobOverlays struct {
	/* List of animations. The list should be chronological, without any time overlap. */
	// +optional
	Animations []JobAnimations `json:"animations,omitempty"`

	/* Image overlay. */
	// +optional
	Image *JobImage `json:"image,omitempty"`
}

type JobPad struct {
	/* The number of pixels to add to the bottom. The default is 0. */
	// +optional
	BottomPixels *int32 `json:"bottomPixels,omitempty"`

	/* The number of pixels to add to the left. The default is 0. */
	// +optional
	LeftPixels *int32 `json:"leftPixels,omitempty"`

	/* The number of pixels to add to the right. The default is 0. */
	// +optional
	RightPixels *int32 `json:"rightPixels,omitempty"`

	/* The number of pixels to add to the top. The default is 0. */
	// +optional
	TopPixels *int32 `json:"topPixels,omitempty"`
}

type JobPlayready struct {
}

type JobPreprocessingConfig struct {
	/* Audio preprocessing configuration. */
	// +optional
	Audio *JobAudio `json:"audio,omitempty"`

	/* Color preprocessing configuration. */
	// +optional
	Color *JobColor `json:"color,omitempty"`

	/* Specify the video cropping configuration. */
	// +optional
	Crop *JobCrop `json:"crop,omitempty"`

	/* Deblock preprocessing configuration. */
	// +optional
	Deblock *JobDeblock `json:"deblock,omitempty"`

	/* Specify the video deinterlace configuration. */
	// +optional
	Deinterlace *JobDeinterlace `json:"deinterlace,omitempty"`

	/* Denoise preprocessing configuration. */
	// +optional
	Denoise *JobDenoise `json:"denoise,omitempty"`

	/* Specify the video pad filter configuration. */
	// +optional
	Pad *JobPad `json:"pad,omitempty"`
}

type JobPubsubDestination struct {
	/* TopicRef is a reference to a PubSubTopic. */
	// +optional
	TopicRef *v1alpha1.ResourceRef `json:"topicRef,omitempty"`
}

type JobResolution struct {
	/* Normalized x coordinate. */
	// +optional
	X *float64 `json:"x,omitempty"`

	/* Normalized y coordinate. */
	// +optional
	Y *float64 `json:"y,omitempty"`
}

type JobSampleAes struct {
}

type JobSdr struct {
}

type JobSecretManagerKeySource struct {
	/* Required. The name of the Secret Version containing the encryption key in
	the following format:
	`projects/{project}/secrets/{secret_id}/versions/{version_number}`

	Note that only numbered versions are supported. Aliases like "latest" are
	not supported. */
	// +optional
	SecretVersion *string `json:"secretVersion,omitempty"`
}

type JobSegmentSettings struct {
	/* Required. Create an individual segment file. The default is `false`. */
	// +optional
	IndividualSegments *bool `json:"individualSegments,omitempty"`

	/* Duration of the segments in seconds. The default is `6.0s`. Note that `segmentDuration` must be greater than or equal to [`gopDuration`](#videostream), and `segmentDuration` must be divisible by [`gopDuration`](#videostream). */
	// +optional
	SegmentDuration *string `json:"segmentDuration,omitempty"`
}

type JobSpriteSheets struct {
	/* The maximum number of sprites per row in a sprite sheet. The default is 0, which indicates no maximum limit. */
	// +optional
	ColumnCount *int32 `json:"columnCount,omitempty"`

	/* End time in seconds, relative to the output file timeline. When `end_time_offset` is not specified, the sprites are generated until the end of the output file. */
	// +optional
	EndTimeOffset *string `json:"endTimeOffset,omitempty"`

	/* Required. File name prefix for the generated sprite sheets.

	Each sprite sheet has an incremental 10-digit zero-padded suffix starting
	from 0 before the extension, such as `sprite_sheet0000000123.jpeg`. */
	// +optional
	FilePrefix *string `json:"filePrefix,omitempty"`

	/* Format type. The default is `jpeg`.

	Supported formats:

	- `jpeg` */
	// +optional
	Format *string `json:"format,omitempty"`

	/* Starting from `0s`, create sprites at regular intervals. Specify the interval value in seconds. */
	// +optional
	Interval *string `json:"interval,omitempty"`

	/* The quality of the generated sprite sheet. Enter a value between 1 and 100, where 1 is the lowest quality and 100 is the highest quality. The default is 100. A high quality value corresponds to a low image data compression ratio. */
	// +optional
	Quality *int32 `json:"quality,omitempty"`

	/* The maximum number of rows per sprite sheet. When the sprite sheet is full, a new sprite sheet is created. The default is 0, which indicates no maximum limit. */
	// +optional
	RowCount *int32 `json:"rowCount,omitempty"`

	/* Required. The height of sprite in pixels. Must be an even integer. To
	preserve the source aspect ratio, set the
	[SpriteSheet.sprite_height_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_height_pixels]
	field or the
	[SpriteSheet.sprite_width_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_width_pixels]
	field, but not both (the API will automatically calculate the missing
	field).

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the height, in pixels, per the horizontal ASR. The API calculates
	the width per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	SpriteHeightPixels *int32 `json:"spriteHeightPixels,omitempty"`

	/* Required. The width of sprite in pixels. Must be an even integer. To
	preserve the source aspect ratio, set the
	[SpriteSheet.sprite_width_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_width_pixels]
	field or the
	[SpriteSheet.sprite_height_pixels][google.cloud.video.transcoder.v1.SpriteSheet.sprite_height_pixels]
	field, but not both (the API will automatically calculate the missing
	field).

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the width, in pixels, per the horizontal ASR. The API calculates
	the height per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	SpriteWidthPixels *int32 `json:"spriteWidthPixels,omitempty"`

	/* Start time in seconds, relative to the output file timeline. Determines the first sprite to pick. The default is `0s`. */
	// +optional
	StartTimeOffset *string `json:"startTimeOffset,omitempty"`

	/* Total number of sprites. Create the specified number of sprites distributed evenly across the timeline of the output media. The default is 100. */
	// +optional
	TotalCount *int32 `json:"totalCount,omitempty"`
}

type JobTextStream struct {
	/* The codec for this text stream. The default is `webvtt`.

	Supported text codecs:

	- `srt`
	- `ttml`
	- `cea608`
	- `cea708`
	- `webvtt` */
	// +optional
	Codec *string `json:"codec,omitempty"`

	/* The name for this particular text stream that will be added to the HLS/DASH manifest. Not supported in MP4 files. */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* The BCP-47 language code, such as `en-US` or `sr-Latn`. For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier. Not supported in MP4 files. */
	// +optional
	LanguageCode *string `json:"languageCode,omitempty"`

	/* The mapping for the [JobConfig.edit_list][google.cloud.video.transcoder.v1.JobConfig.edit_list] atoms with text [EditAtom.inputs][google.cloud.video.transcoder.v1.EditAtom.inputs]. */
	// +optional
	Mapping []JobMapping `json:"mapping,omitempty"`
}

type JobTrackDefinitions struct {
	/* Optional. Whether to automatically detect the languages present in the track. If true, the system will attempt to identify all the languages present in the track and populate the languages field. */
	// +optional
	DetectLanguages *bool `json:"detectLanguages,omitempty"`

	/* The input track. */
	// +optional
	InputTrack *int32 `json:"inputTrack,omitempty"`

	/* Optional. A list of languages spoken in the input asset, represented by a BCP 47 language code, such as "en-US" or "sr-Latn". For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier. */
	// +optional
	Languages []string `json:"languages,omitempty"`
}

type JobVideoStream struct {
	/* H264 codec settings. */
	// +optional
	H264 *JobH264 `json:"h264,omitempty"`

	/* H265 codec settings. */
	// +optional
	H265 *JobH265 `json:"h265,omitempty"`

	/* VP9 codec settings. */
	// +optional
	Vp9 *JobVp9 `json:"vp9,omitempty"`
}

type JobVp9 struct {
	/* Required. The video bitrate in bits per second. The minimum value is 1,000. The maximum value is 480,000,000. */
	// +optional
	BitrateBps *int32 `json:"bitrateBps,omitempty"`

	/* Target CRF level. Must be between 10 and 36, where 10 is the highest
	quality and 36 is the most efficient compression. The default is 21.

	**Note:** This field is not supported. */
	// +optional
	CrfLevel *int32 `json:"crfLevel,omitempty"`

	/* Required. The target video frame rate in frames per second (FPS). Must be less than or equal to 120. */
	// +optional
	FrameRate *float64 `json:"frameRate,omitempty"`

	/* Optional. Frame rate conversion strategy for desired frame rate. The default is `DOWNSAMPLE`. */
	// +optional
	FrameRateConversionStrategy *string `json:"frameRateConversionStrategy,omitempty"`

	/* Select the GOP size based on the specified duration. The default is `3s`. Note that `gopDuration` must be less than or equal to [`segmentDuration`](#SegmentSettings), and [`segmentDuration`](#SegmentSettings) must be divisible by `gopDuration`. */
	// +optional
	GopDuration *string `json:"gopDuration,omitempty"`

	/* Select the GOP size based on the specified frame count. Must be greater than zero. */
	// +optional
	GopFrameCount *int32 `json:"gopFrameCount,omitempty"`

	/* The height of the video in pixels. Must be an even integer.
	When not specified, the height is adjusted to match the specified width
	and input aspect ratio. If both are omitted, the input height is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the height, in pixels, per the horizontal ASR. The API calculates
	the width per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	HeightPixels *int32 `json:"heightPixels,omitempty"`

	/* Optional. HLG color format setting for VP9. */
	// +optional
	Hlg *JobHlg `json:"hlg,omitempty"`

	/* Pixel format to use. The default is `yuv420p`.

	Supported pixel formats:

	- `yuv420p` pixel format
	- `yuv422p` pixel format
	- `yuv444p` pixel format
	- `yuv420p10` 10-bit HDR pixel format
	- `yuv422p10` 10-bit HDR pixel format
	- `yuv444p10` 10-bit HDR pixel format
	- `yuv420p12` 12-bit HDR pixel format
	- `yuv422p12` 12-bit HDR pixel format
	- `yuv444p12` 12-bit HDR pixel format */
	// +optional
	PixelFormat *string `json:"pixelFormat,omitempty"`

	/* Enforces the specified codec profile. The following profiles are
	supported:

	*   `profile0` (default)
	*   `profile1`
	*   `profile2`
	*   `profile3`

	The available options are
	[WebM-compatible](https://www.webmproject.org/vp9/profiles/).
	Note that certain values for this field may cause the
	transcoder to override other fields you set in the `Vp9CodecSettings`
	message. */
	// +optional
	Profile *string `json:"profile,omitempty"`

	/* Specify the mode. The default is `vbr`.

	Supported rate control modes:

	- `vbr` - variable bitrate */
	// +optional
	RateControlMode *string `json:"rateControlMode,omitempty"`

	/* Optional. SDR color format setting for VP9. */
	// +optional
	Sdr *JobSdr `json:"sdr,omitempty"`

	/* The width of the video in pixels. Must be an even integer.
	When not specified, the width is adjusted to match the specified height
	and input aspect ratio. If both are omitted, the input width is used.

	For portrait videos that contain horizontal ASR and rotation metadata,
	provide the width, in pixels, per the horizontal ASR. The API calculates
	the height per the horizontal ASR. The API detects any rotation metadata
	and swaps the requested height and width for the output. */
	// +optional
	WidthPixels *int32 `json:"widthPixels,omitempty"`
}

type JobWidevine struct {
}

type JobXy struct {
	/* Normalized x coordinate. */
	// +optional
	X *float64 `json:"x,omitempty"`

	/* Normalized y coordinate. */
	// +optional
	Y *float64 `json:"y,omitempty"`
}

type JobYadif struct {
	/* Deinterlace all frames rather than just the frames identified as interlaced. The default is `false`. */
	// +optional
	DeinterlaceAllFrames *bool `json:"deinterlaceAllFrames,omitempty"`

	/* Disable spacial interlacing. The default is `false`. */
	// +optional
	DisableSpatialInterlacing *bool `json:"disableSpatialInterlacing,omitempty"`

	/* Specifies the deinterlacing mode to adopt.
	The default is `send_frame`.
	Supported values:

	- `send_frame`: Output one frame for each frame
	- `send_field`: Output one frame for each field */
	// +optional
	Mode *string `json:"mode,omitempty"`

	/* The picture field parity assumed for the input interlaced video.
	The default is `auto`.
	Supported values:

	- `tff`: Assume the top field is first
	- `bff`: Assume the bottom field is first
	- `auto`: Enable automatic detection of field parity */
	// +optional
	Parity *string `json:"parity,omitempty"`
}

type TranscoderJobSpec struct {
	/* The processing priority of a batch job. This field can only be set for batch mode jobs. The default value is 0. This value cannot be negative. Higher values correspond to higher priorities for the job. */
	// +optional
	BatchModePriority *int32 `json:"batchModePriority,omitempty"`

	/* The configuration for this job. */
	// +optional
	Config *JobConfig `json:"config,omitempty"`

	/* Optional. Insert silence and duplicate frames when timestamp gaps are detected in a given stream. */
	// +optional
	FillContentGaps *bool `json:"fillContentGaps,omitempty"`

	/* Input only. Specify the `input_uri` to populate empty `uri` fields in each element of `Job.config.inputs` or `JobTemplate.config.inputs` when using template. URI of the media. Input files must be at least 5 seconds in duration and stored in Cloud Storage (for example, `gs://bucket/inputs/file.mp4`). See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats). */
	// +optional
	InputURI *string `json:"inputURI,omitempty"`

	/* The location of this resource. */
	Location string `json:"location"`

	/* The processing mode of the job. The default is `PROCESSING_MODE_INTERACTIVE`. */
	// +optional
	Mode *string `json:"mode,omitempty"`

	/* Optional. The optimization strategy of the job. The default is `AUTODETECT`. */
	// +optional
	Optimization *string `json:"optimization,omitempty"`

	/* Input only. Specify the `output_uri` to populate an empty `Job.config.output.uri` or `JobTemplate.config.output.uri` when using template. URI for the output file(s). For example, `gs://my-bucket/outputs/`. See [Supported input and output formats](https://cloud.google.com/transcoder/docs/concepts/supported-input-and-output-formats). */
	// +optional
	OutputURI *string `json:"outputURI,omitempty"`

	/* The project that this resource belongs to. */
	ProjectRef v1alpha1.ResourceRef `json:"projectRef"`

	/* The TranscoderJob name. If not given, the metadata.name will be used. */
	// +optional
	ResourceID *string `json:"resourceID,omitempty"`

	/* Input only. Specify the `template_id` to use for populating `Job.config`.
	The default is `preset/web-hd`, which is the only supported preset.

	User defined JobTemplate: `{job_template_id}` */
	// +optional
	TemplateID *string `json:"templateID,omitempty"`

	/* Job time to live value in days, which will be effective after job completion. Job should be deleted automatically after the given TTL. Enter a value between 1 and 90. The default is 30. */
	// +optional
	TtlAfterCompletionDays *int32 `json:"ttlAfterCompletionDays,omitempty"`
}

type JobAttributesStatus struct {
	/* Optional. A list of track definitions for the input asset. */
	// +optional
	TrackDefinitions []JobTrackDefinitionsStatus `json:"trackDefinitions,omitempty"`
}

type JobConfigStatus struct {
	/* List of input assets stored in Cloud Storage. */
	// +optional
	Inputs []JobInputsStatus `json:"inputs,omitempty"`
}

type JobErrorStatus struct {
	/* The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code]. */
	// +optional
	Code *int32 `json:"code,omitempty"`

	/* A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client. */
	// +optional
	Message *string `json:"message,omitempty"`
}

type JobInputsStatus struct {
	/* Optional. Input Attributes. */
	// +optional
	Attributes *JobAttributesStatus `json:"attributes,omitempty"`
}

type JobObservedStateStatus struct {
	/* The configuration for this job. */
	// +optional
	Config *JobConfigStatus `json:"config,omitempty"`

	/* Output only. The time the job was created. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* Output only. The time the transcoding finished. */
	// +optional
	EndTime *string `json:"endTime,omitempty"`

	/* Output only. An error object that describes the reason for the failure. This property is always present when [ProcessingState][google.cloud.video.transcoder.v1.Job.ProcessingState] is `FAILED`. */
	// +optional
	Error *JobErrorStatus `json:"error,omitempty"`

	/* Output only. The time the transcoding started. */
	// +optional
	StartTime *string `json:"startTime,omitempty"`

	/* Output only. The current state of the job. */
	// +optional
	State *string `json:"state,omitempty"`
}

type JobTrackDefinitionsStatus struct {
	/* Output only. A list of languages detected in the input asset, represented by a BCP 47 language code, such as "en-US" or "sr-Latn". For more information, see https://www.unicode.org/reports/tr35/#Unicode_locale_identifier. This field is only populated if the detect_languages field is set to true. */
	// +optional
	DetectedLanguages []string `json:"detectedLanguages,omitempty"`
}

type TranscoderJobStatus struct {
	/* Conditions represent the latest available observations of the
	   TranscoderJob's current state. */
	Conditions []v1alpha1.Condition `json:"conditions,omitempty"`
	/* A unique specifier for the TranscoderJob resource in GCP. */
	// +optional
	ExternalRef *string `json:"externalRef,omitempty"`

	/* ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource. */
	// +optional
	ObservedGeneration *int64 `json:"observedGeneration,omitempty"`

	/* ObservedState is the state of the resource as most recently observed in GCP. */
	// +optional
	ObservedState *JobObservedStateStatus `json:"observedState,omitempty"`
}

// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +kubebuilder:resource:categories=gcp,shortName=gcptranscoderjob;gcptranscoderjobs
// +kubebuilder:subresource:status
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/managed-by-kcc=true"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/stability-level=alpha"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/system=true"
// +kubebuilder:printcolumn:name="Age",JSONPath=".metadata.creationTimestamp",type="date"
// +kubebuilder:printcolumn:name="Ready",JSONPath=".status.conditions[?(@.type=='Ready')].status",type="string",description="When 'True', the most recent reconcile of the resource succeeded"
// +kubebuilder:printcolumn:name="Status",JSONPath=".status.conditions[?(@.type=='Ready')].reason",type="string",description="The reason for the value in 'Ready'"
// +kubebuilder:printcolumn:name="Status Age",JSONPath=".status.conditions[?(@.type=='Ready')].lastTransitionTime",type="date",description="The last transition time for the value in 'Status'"

// TranscoderJob is the Schema for the transcoder API
// +k8s:openapi-gen=true
type TranscoderJob struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   TranscoderJobSpec   `json:"spec,omitempty"`
	Status TranscoderJobStatus `json:"status,omitempty"`
}

// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object

// TranscoderJobList contains a list of TranscoderJob
type TranscoderJobList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []TranscoderJob `json:"items"`
}

func init() {
	SchemeBuilder.Register(&TranscoderJob{}, &TranscoderJobList{})
}
