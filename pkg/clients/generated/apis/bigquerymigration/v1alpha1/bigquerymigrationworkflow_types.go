// Copyright 2020 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//    http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Config Connector and manual
//     changes will be clobbered when the file is regenerated.
//
// ----------------------------------------------------------------------------

// *** DISCLAIMER ***
// Config Connector's go-client for CRDs is currently in ALPHA, which means
// that future versions of the go-client may include breaking changes.
// Please try it out and give us feedback!

package v1alpha1

import (
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/clients/generated/apis/k8s/v1alpha1"
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

type WorkflowAttachments struct {
	/* A URL/resource name that uniquely identifies the type of the serialized
	protocol buffer message. This string must contain at least
	one "/" character. The last segment of the URL's path must represent
	the fully qualified name of the type (as in
	`path/google.protobuf.Duration`). The name should be in a canonical form
	(e.g., leading "." is not accepted).

	In practice, teams usually precompile into the binary all types that they
	expect it to use in the context of Any. However, for URLs which use the
	scheme `http`, `https`, or no scheme, one can optionally set up a type
	server that maps type URLs to message definitions as follows:

	* If no scheme is provided, `https` is assumed.
	* An HTTP GET on the URL must yield a [google.protobuf.Type][]
	value in binary format, or produce an error.
	* Applications are allowed to cache lookup results based on the
	URL, or have them precompiled into a binary to avoid any
	lookup. Therefore, binary compatibility needs to be preserved
	on changes to types. (Use versioned type names to manage
	breaking changes.)

	Note: this functionality is not currently available in the official
	protobuf release, and it is not used for type URLs beginning with
	type.googleapis.com.

	Schemes other than `http`, `https` (or the empty scheme) might be
	used with implementation specific semantics. */
	// +optional
	TypeURL *string `json:"typeURL,omitempty"`

	/* Must be a valid serialized protocol buffer of the above specified type. */
	// +optional
	Value *string `json:"value,omitempty"`
}

type WorkflowAzureSynapseDialect struct {
}

type WorkflowBigqueryDialect struct {
}

type WorkflowBucketOptions struct {
	/* The explicit buckets. */
	// +optional
	ExplicitBuckets *WorkflowExplicitBuckets `json:"explicitBuckets,omitempty"`

	/* The exponential buckets. */
	// +optional
	ExponentialBuckets *WorkflowExponentialBuckets `json:"exponentialBuckets,omitempty"`

	/* The linear bucket. */
	// +optional
	LinearBuckets *WorkflowLinearBuckets `json:"linearBuckets,omitempty"`
}

type WorkflowDb2Dialect struct {
}

type WorkflowDistributionValue struct {
	/* The number of values in each bucket of the histogram, as described in
	`bucket_options`. If the distribution does not have a histogram, then omit
	this field. If there is a histogram, then the sum of the values in
	`bucket_counts` must equal the value in the `count` field of the
	distribution.

	If present, `bucket_counts` should contain N values, where N is the number
	of buckets specified in `bucket_options`. If you supply fewer than N
	values, the remaining values are assumed to be 0.

	The order of the values in `bucket_counts` follows the bucket numbering
	schemes described for the three bucket types. The first value must be the
	count for the underflow bucket (number 0). The next N-2 values are the
	counts for the finite buckets (number 1 through N-2). The N'th value in
	`bucket_counts` is the count for the overflow bucket (number N-1). */
	// +optional
	BucketCounts []int64 `json:"bucketCounts,omitempty"`

	/* Defines the histogram bucket boundaries. If the distribution does not contain a histogram, then omit this field. */
	// +optional
	BucketOptions *WorkflowBucketOptions `json:"bucketOptions,omitempty"`

	/* The number of values in the population. Must be non-negative. This value must equal the sum of the values in `bucket_counts` if a histogram is provided. */
	// +optional
	Count *int64 `json:"count,omitempty"`

	/* Must be in increasing order of `value` field. */
	// +optional
	Exemplars []WorkflowExemplars `json:"exemplars,omitempty"`

	/* The arithmetic mean of the values in the population. If `count` is zero then this field must be zero. */
	// +optional
	Mean *float64 `json:"mean,omitempty"`

	/* If specified, contains the range of the population values. The field must not be present if the `count` is zero. */
	// +optional
	Range *WorkflowRange `json:"range,omitempty"`

	/* The sum of squared deviations from the mean of the values in the
	population. For values x_i this is:

	Sum[i=1..n]((x_i - mean)^2)

	Knuth, "The Art of Computer Programming", Vol. 2, page 232, 3rd edition
	describes Welford's method for accumulating this sum in one pass.

	If `count` is zero then this field must be zero. */
	// +optional
	SumOfSquaredDeviation *float64 `json:"sumOfSquaredDeviation,omitempty"`
}

type WorkflowExemplars struct {
	/* Contextual information about the example value. Examples are:

	Trace: type.googleapis.com/google.monitoring.v3.SpanContext

	Literal string: type.googleapis.com/google.protobuf.StringValue

	Labels dropped during aggregation:
	type.googleapis.com/google.monitoring.v3.DroppedLabels

	There may be only a single attachment of any given message type in a
	single exemplar, and this is enforced by the system. */
	// +optional
	Attachments []WorkflowAttachments `json:"attachments,omitempty"`

	/* The observation (sampling) time of the above value. */
	// +optional
	Timestamp *string `json:"timestamp,omitempty"`

	/* Value of the exemplar point. This value determines to which bucket the exemplar belongs. */
	// +optional
	Value *WorkflowValue `json:"value,omitempty"`
}

type WorkflowExplicitBuckets struct {
	/* The values must be monotonically increasing. */
	// +optional
	Bounds []float64 `json:"bounds,omitempty"`
}

type WorkflowExponentialBuckets struct {
	/* Must be greater than 1. */
	// +optional
	GrowthFactor *float64 `json:"growthFactor,omitempty"`

	/* Must be greater than 0. */
	// +optional
	NumFiniteBuckets *int32 `json:"numFiniteBuckets,omitempty"`

	/* Must be greater than 0. */
	// +optional
	Scale *float64 `json:"scale,omitempty"`
}

type WorkflowGreenplumDialect struct {
}

type WorkflowHiveqlDialect struct {
}

type WorkflowInterval struct {
	/* Required. The end of the time interval. */
	// +optional
	EndTime *string `json:"endTime,omitempty"`

	/* Optional. The beginning of the time interval.  The default value for the start time is the end time. The start time must not be later than the end time. */
	// +optional
	StartTime *string `json:"startTime,omitempty"`
}

type WorkflowLinearBuckets struct {
	/* Must be greater than 0. */
	// +optional
	NumFiniteBuckets *int32 `json:"numFiniteBuckets,omitempty"`

	/* Lower bound of the first bucket. */
	// +optional
	Offset *float64 `json:"offset,omitempty"`

	/* Must be greater than 0. */
	// +optional
	Width *float64 `json:"width,omitempty"`
}

type WorkflowLiteral struct {
	/* Literal byte data. */
	// +optional
	LiteralBytes *string `json:"literalBytes,omitempty"`

	/* Literal string data. */
	// +optional
	LiteralString *string `json:"literalString,omitempty"`

	/* Required. The identifier of the literal entry. */
	// +optional
	RelativePath *string `json:"relativePath,omitempty"`
}

type WorkflowMetrics struct {
	/* Required. The name of the metric.

	If the metric is not known by the service yet, it will be auto-created. */
	// +optional
	Metric *string `json:"metric,omitempty"`

	/* Optional. The metric kind of the time series.

	If present, it must be the same as the metric kind of the associated
	metric. If the associated metric's descriptor must be auto-created, then
	this field specifies the metric kind of the new descriptor and must be
	either `GAUGE` (the default) or `CUMULATIVE`. */
	// +optional
	MetricKind *string `json:"metricKind,omitempty"`

	/* Required. The data points of this time series. When listing time series,
	points are returned in reverse time order.

	When creating a time series, this field must contain exactly one point and
	the point's type must be the same as the value type of the associated
	metric. If the associated metric's descriptor must be auto-created, then
	the value type of the descriptor is determined by the point's type, which
	must be `BOOL`, `INT64`, `DOUBLE`, or `DISTRIBUTION`. */
	// +optional
	Points []WorkflowPoints `json:"points,omitempty"`

	/* Required. The value type of the time series. */
	// +optional
	ValueType *string `json:"valueType,omitempty"`
}

type WorkflowMysqlDialect struct {
}

type WorkflowNameMap struct {
	/* The name of the object in source that is being mapped. */
	// +optional
	Source *WorkflowSource `json:"source,omitempty"`

	/* The desired target name of the object that is being mapped. */
	// +optional
	Target *WorkflowTarget `json:"target,omitempty"`
}

type WorkflowNameMappingList struct {
	/* The elements of the object name map. */
	// +optional
	NameMap []WorkflowNameMap `json:"nameMap,omitempty"`
}

type WorkflowNetezzaDialect struct {
}

type WorkflowOracleDialect struct {
}

type WorkflowPoints struct {
	/* The time interval to which the data point applies.  For `GAUGE` metrics, the start time does not need to be supplied, but if it is supplied, it must equal the end time.  For `DELTA` metrics, the start and end time should specify a non-zero interval, with subsequent points specifying contiguous and non-overlapping intervals.  For `CUMULATIVE` metrics, the start and end time should specify a non-zero interval, with subsequent points specifying the same start time and increasing end times, until an event resets the cumulative value to zero and sets a new start time for the following points. */
	// +optional
	Interval *WorkflowInterval `json:"interval,omitempty"`

	/* The value of the data point. */
	// +optional
	Value *WorkflowValue `json:"value,omitempty"`
}

type WorkflowPostgresqlDialect struct {
}

type WorkflowPrestoDialect struct {
}

type WorkflowRange struct {
	/* The maximum of the population values. */
	// +optional
	Max *float64 `json:"max,omitempty"`

	/* The minimum of the population values. */
	// +optional
	Min *float64 `json:"min,omitempty"`
}

type WorkflowRedshiftDialect struct {
}

type WorkflowSnowflakeDialect struct {
}

type WorkflowSource struct {
	/* The attribute name (BigQuery column equivalent in the source data warehouse). */
	// +optional
	Attribute *string `json:"attribute,omitempty"`

	/* The database name (BigQuery project ID equivalent in the source data warehouse). */
	// +optional
	Database *string `json:"database,omitempty"`

	/* The relation name (BigQuery table or view equivalent in the source data warehouse). */
	// +optional
	Relation *string `json:"relation,omitempty"`

	/* The schema name (BigQuery dataset equivalent in the source data warehouse). */
	// +optional
	Schema *string `json:"schema,omitempty"`

	/* The type of object that is being mapped. */
	// +optional
	Type *string `json:"type,omitempty"`
}

type WorkflowSourceDialect struct {
	/* The Azure Synapse dialect */
	// +optional
	AzureSynapseDialect *WorkflowAzureSynapseDialect `json:"azureSynapseDialect,omitempty"`

	/* The BigQuery dialect */
	// +optional
	BigqueryDialect *WorkflowBigqueryDialect `json:"bigqueryDialect,omitempty"`

	/* DB2 dialect */
	// +optional
	Db2Dialect *WorkflowDb2Dialect `json:"db2Dialect,omitempty"`

	/* Greenplum dialect */
	// +optional
	GreenplumDialect *WorkflowGreenplumDialect `json:"greenplumDialect,omitempty"`

	/* The HiveQL dialect */
	// +optional
	HiveqlDialect *WorkflowHiveqlDialect `json:"hiveqlDialect,omitempty"`

	/* The MySQL dialect */
	// +optional
	MysqlDialect *WorkflowMysqlDialect `json:"mysqlDialect,omitempty"`

	/* The Netezza dialect */
	// +optional
	NetezzaDialect *WorkflowNetezzaDialect `json:"netezzaDialect,omitempty"`

	/* The Oracle dialect */
	// +optional
	OracleDialect *WorkflowOracleDialect `json:"oracleDialect,omitempty"`

	/* The Postgresql dialect */
	// +optional
	PostgresqlDialect *WorkflowPostgresqlDialect `json:"postgresqlDialect,omitempty"`

	/* The Presto dialect */
	// +optional
	PrestoDialect *WorkflowPrestoDialect `json:"prestoDialect,omitempty"`

	/* The Redshift dialect */
	// +optional
	RedshiftDialect *WorkflowRedshiftDialect `json:"redshiftDialect,omitempty"`

	/* The Snowflake dialect */
	// +optional
	SnowflakeDialect *WorkflowSnowflakeDialect `json:"snowflakeDialect,omitempty"`

	/* The SparkSQL dialect */
	// +optional
	SparksqlDialect *WorkflowSparksqlDialect `json:"sparksqlDialect,omitempty"`

	/* The SQL Server dialect */
	// +optional
	SqlServerDialect *WorkflowSqlServerDialect `json:"sqlServerDialect,omitempty"`

	/* SQLite dialect */
	// +optional
	SqliteDialect *WorkflowSqliteDialect `json:"sqliteDialect,omitempty"`

	/* The Teradata dialect */
	// +optional
	TeradataDialect *WorkflowTeradataDialect `json:"teradataDialect,omitempty"`

	/* The Vertica dialect */
	// +optional
	VerticaDialect *WorkflowVerticaDialect `json:"verticaDialect,omitempty"`
}

type WorkflowSourceEnv struct {
	/* The default database name to fully qualify SQL objects when their database name is missing. */
	// +optional
	DefaultDatabase *string `json:"defaultDatabase,omitempty"`

	/* Optional. Expects a valid BigQuery dataset ID that exists, e.g., project-123.metadata_store_123.  If specified, translation will search and read the required schema information from a metadata store in this dataset. If metadata store doesn't exist, translation will parse the metadata file and upload the schema info to a temp table in the dataset to speed up future translation jobs. */
	// +optional
	MetadataStoreDataset *string `json:"metadataStoreDataset,omitempty"`

	/* The schema search path. When SQL objects are missing schema name, translation engine will search through this list to find the value. */
	// +optional
	SchemaSearchPath []string `json:"schemaSearchPath,omitempty"`
}

type WorkflowSourceEnvironment struct {
	/* The default database name to fully qualify SQL objects when their database name is missing. */
	// +optional
	DefaultDatabase *string `json:"defaultDatabase,omitempty"`

	/* Optional. Expects a validQ BigQuery dataset ID that exists, e.g., project-123.metadata_store_123.  If specified, translation will search and read the required schema information from a metadata store in this dataset. If metadata store doesn't exist, translation will parse the metadata file and upload the schema info to a temp table in the dataset to speed up future translation jobs. */
	// +optional
	MetadataStoreDataset *string `json:"metadataStoreDataset,omitempty"`

	/* The schema search path. When SQL objects are missing schema name, translation engine will search through this list to find the value. */
	// +optional
	SchemaSearchPath []string `json:"schemaSearchPath,omitempty"`
}

type WorkflowSourceSpec struct {
	/* The base URI for all files to be read in as sources for translation. */
	// +optional
	BaseURI *string `json:"baseURI,omitempty"`

	/* Optional. The optional field to specify the encoding of the sql bytes. */
	// +optional
	Encoding *string `json:"encoding,omitempty"`

	/* Source literal. */
	// +optional
	Literal *WorkflowLiteral `json:"literal,omitempty"`
}

type WorkflowSourceTargetMapping struct {
	/* The source SQL or the path to it. */
	// +optional
	SourceSpec *WorkflowSourceSpec `json:"sourceSpec,omitempty"`

	/* The target SQL or the path for it. */
	// +optional
	TargetSpec *WorkflowTargetSpec `json:"targetSpec,omitempty"`
}

type WorkflowSparksqlDialect struct {
}

type WorkflowSqlServerDialect struct {
}

type WorkflowSqliteDialect struct {
}

type WorkflowTarget struct {
	/* The attribute name (BigQuery column equivalent in the target data warehouse). */
	// +optional
	Attribute *string `json:"attribute,omitempty"`

	/* The database name (BigQuery project ID equivalent in the target data warehouse). */
	// +optional
	Database *string `json:"database,omitempty"`

	/* The relation name (BigQuery table or view equivalent in the target data warehouse). */
	// +optional
	Relation *string `json:"relation,omitempty"`

	/* The schema name (BigQuery dataset equivalent in the target data warehouse). */
	// +optional
	Schema *string `json:"schema,omitempty"`
}

type WorkflowTargetDialect struct {
	/* The Azure Synapse dialect */
	// +optional
	AzureSynapseDialect *WorkflowAzureSynapseDialect `json:"azureSynapseDialect,omitempty"`

	/* The BigQuery dialect */
	// +optional
	BigqueryDialect *WorkflowBigqueryDialect `json:"bigqueryDialect,omitempty"`

	/* DB2 dialect */
	// +optional
	Db2Dialect *WorkflowDb2Dialect `json:"db2Dialect,omitempty"`

	/* Greenplum dialect */
	// +optional
	GreenplumDialect *WorkflowGreenplumDialect `json:"greenplumDialect,omitempty"`

	/* The HiveQL dialect */
	// +optional
	HiveqlDialect *WorkflowHiveqlDialect `json:"hiveqlDialect,omitempty"`

	/* The MySQL dialect */
	// +optional
	MysqlDialect *WorkflowMysqlDialect `json:"mysqlDialect,omitempty"`

	/* The Netezza dialect */
	// +optional
	NetezzaDialect *WorkflowNetezzaDialect `json:"netezzaDialect,omitempty"`

	/* The Oracle dialect */
	// +optional
	OracleDialect *WorkflowOracleDialect `json:"oracleDialect,omitempty"`

	/* The Postgresql dialect */
	// +optional
	PostgresqlDialect *WorkflowPostgresqlDialect `json:"postgresqlDialect,omitempty"`

	/* The Presto dialect */
	// +optional
	PrestoDialect *WorkflowPrestoDialect `json:"prestoDialect,omitempty"`

	/* The Redshift dialect */
	// +optional
	RedshiftDialect *WorkflowRedshiftDialect `json:"redshiftDialect,omitempty"`

	/* The Snowflake dialect */
	// +optional
	SnowflakeDialect *WorkflowSnowflakeDialect `json:"snowflakeDialect,omitempty"`

	/* The SparkSQL dialect */
	// +optional
	SparksqlDialect *WorkflowSparksqlDialect `json:"sparksqlDialect,omitempty"`

	/* The SQL Server dialect */
	// +optional
	SqlServerDialect *WorkflowSqlServerDialect `json:"sqlServerDialect,omitempty"`

	/* SQLite dialect */
	// +optional
	SqliteDialect *WorkflowSqliteDialect `json:"sqliteDialect,omitempty"`

	/* The Teradata dialect */
	// +optional
	TeradataDialect *WorkflowTeradataDialect `json:"teradataDialect,omitempty"`

	/* The Vertica dialect */
	// +optional
	VerticaDialect *WorkflowVerticaDialect `json:"verticaDialect,omitempty"`
}

type WorkflowTargetSpec struct {
	/* The relative path for the target data. Given source file `base_uri/input/sql`, the output would be `target_base_uri/sql/relative_path/input.sql`. */
	// +optional
	RelativePath *string `json:"relativePath,omitempty"`
}

type WorkflowTasks struct {
	/* Time when the task was created. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* Time when the task was last updated. */
	// +optional
	LastUpdateTime *string `json:"lastUpdateTime,omitempty"`

	/* The metrics for the task. */
	// +optional
	Metrics []WorkflowMetrics `json:"metrics,omitempty"`

	/* The number or resources with errors. Note: This is not the total number of errors as each resource can have more than one error. This is used to indicate truncation by having a `resource_error_count` that is higher than the size of `resource_error_details`. */
	// +optional
	ResourceErrorCount *int32 `json:"resourceErrorCount,omitempty"`

	/* Count of all the processing errors in this task and its subtasks. */
	// +optional
	TotalProcessingErrorCount *int32 `json:"totalProcessingErrorCount,omitempty"`

	/* Count of all the resource errors in this task and its subtasks. */
	// +optional
	TotalResourceErrorCount *int32 `json:"totalResourceErrorCount,omitempty"`

	/* Task configuration for CW Batch/Offline SQL Translation. */
	// +optional
	TranslationConfigDetails *WorkflowTranslationConfigDetails `json:"translationConfigDetails,omitempty"`

	/* Task details for unified SQL Translation. */
	// +optional
	TranslationDetails *WorkflowTranslationDetails `json:"translationDetails,omitempty"`

	/* The type of the task. This must be one of the supported task types: Translation_Teradata2BQ, Translation_Redshift2BQ, Translation_Bteq2BQ, Translation_Oracle2BQ, Translation_HiveQL2BQ, Translation_SparkSQL2BQ, Translation_Snowflake2BQ, Translation_Netezza2BQ, Translation_AzureSynapse2BQ, Translation_Vertica2BQ, Translation_SQLServer2BQ, Translation_Presto2BQ, Translation_MySQL2BQ, Translation_Postgresql2BQ, Translation_SQLite2BQ, Translation_Greenplum2BQ. */
	// +optional
	Type *string `json:"type,omitempty"`
}

type WorkflowTeradataDialect struct {
	/* Which Teradata sub-dialect mode the user specifies. */
	// +optional
	Mode *string `json:"mode,omitempty"`
}

type WorkflowTranslationConfigDetails struct {
	/* The Cloud Storage path for a directory of files to translate in a task. */
	// +optional
	GcsSourcePath *string `json:"gcsSourcePath,omitempty"`

	/* The Cloud Storage path to write back the corresponding input files to. */
	// +optional
	GcsTargetPath *string `json:"gcsTargetPath,omitempty"`

	/* The mapping of objects to their desired output names in list form. */
	// +optional
	NameMappingList *WorkflowNameMappingList `json:"nameMappingList,omitempty"`

	/* The indicator to show translation request initiator. */
	// +optional
	RequestSource *string `json:"requestSource,omitempty"`

	/* The dialect of the input files. */
	// +optional
	SourceDialect *WorkflowSourceDialect `json:"sourceDialect,omitempty"`

	/* The default source environment values for the translation. */
	// +optional
	SourceEnv *WorkflowSourceEnv `json:"sourceEnv,omitempty"`

	/* The target dialect for the engine to translate the input to. */
	// +optional
	TargetDialect *WorkflowTargetDialect `json:"targetDialect,omitempty"`

	/* The types of output to generate, e.g. sql, metadata etc. If not specified, a default set of targets will be generated. Some additional target types may be slower to generate. See the documentation for the set of available target types. */
	// +optional
	TargetTypes []string `json:"targetTypes,omitempty"`
}

type WorkflowTranslationDetails struct {
	/* The default source environment values for the translation. */
	// +optional
	SourceEnvironment *WorkflowSourceEnvironment `json:"sourceEnvironment,omitempty"`

	/* The mapping from source to target SQL. */
	// +optional
	SourceTargetMapping []WorkflowSourceTargetMapping `json:"sourceTargetMapping,omitempty"`

	/* The base URI for all writes to persistent storage. */
	// +optional
	TargetBaseURI *string `json:"targetBaseURI,omitempty"`

	/* The list of literal targets that will be directly returned to the response. Each entry consists of the constructed path, EXCLUDING the base path. Not providing a target_base_uri will prevent writing to persistent storage. */
	// +optional
	TargetReturnLiterals []string `json:"targetReturnLiterals,omitempty"`

	/* The types of output to generate, e.g. sql, metadata, lineage_from_sql_scripts, etc. If not specified, a default set of targets will be generated. Some additional target types may be slower to generate. See the documentation for the set of available target types. */
	// +optional
	TargetTypes []string `json:"targetTypes,omitempty"`
}

type WorkflowValue struct {
	/* A Boolean value: `true` or `false`. */
	// +optional
	BoolValue *bool `json:"boolValue,omitempty"`

	/* A distribution value. */
	// +optional
	DistributionValue *WorkflowDistributionValue `json:"distributionValue,omitempty"`

	/* A 64-bit double-precision floating-point number. Its magnitude is approximately `+/-10^(+/-300)` and it has 16 significant digits of precision. */
	// +optional
	DoubleValue *float64 `json:"doubleValue,omitempty"`

	/* A 64-bit integer. Its range is approximately `+/-9.2x10^18`. */
	// +optional
	Int64Value *int64 `json:"int64Value,omitempty"`

	/* A variable-length string value. */
	// +optional
	StringValue *string `json:"stringValue,omitempty"`
}

type WorkflowVerticaDialect struct {
}

type BigQueryMigrationWorkflowSpec struct {
	/* The display name of the workflow. This can be set to give a workflow a descriptive name. There is no guarantee or enforcement of uniqueness. */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* The location of this resource. */
	Location string `json:"location"`

	/* The project that this resource belongs to. */
	ProjectRef v1alpha1.ResourceRef `json:"projectRef"`

	/* The BigQueryMigrationWorkflow name. If not given, the metadata.name will be used. */
	// +optional
	ResourceID *string `json:"resourceID,omitempty"`

	/* The tasks in a workflow in a named map. The name (i.e. key) has no meaning and is merely a convenient way to address a specific task in a workflow. */
	// +optional
	Tasks map[string]WorkflowTasks `json:"tasks,omitempty"`
}

type WorkflowObservedStateStatus struct {
	/* Time when the workflow was created. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* Time when the workflow was last updated. */
	// +optional
	LastUpdateTime *string `json:"lastUpdateTime,omitempty"`

	/* Output only. Immutable. Identifier. The unique identifier for the migration
	workflow. The ID is server-generated.

	Example: `projects/123/locations/us/workflows/345` */
	// +optional
	Name *string `json:"name,omitempty"`

	/* Output only. That status of the workflow. */
	// +optional
	State *string `json:"state,omitempty"`
}

type BigQueryMigrationWorkflowStatus struct {
	/* Conditions represent the latest available observations of the
	   BigQueryMigrationWorkflow's current state. */
	Conditions []v1alpha1.Condition `json:"conditions,omitempty"`
	/* A unique specifier for the BigQueryMigrationWorkflow resource in GCP. */
	// +optional
	ExternalRef *string `json:"externalRef,omitempty"`

	/* ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource. */
	// +optional
	ObservedGeneration *int64 `json:"observedGeneration,omitempty"`

	/* ObservedState is the state of the resource as most recently observed in GCP. */
	// +optional
	ObservedState *WorkflowObservedStateStatus `json:"observedState,omitempty"`
}

// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +kubebuilder:resource:categories=gcp,shortName=gcpbigquerymigrationworkflow;gcpbigquerymigrationworkflows
// +kubebuilder:subresource:status
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/managed-by-kcc=true"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/stability-level=alpha"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/system=true"
// +kubebuilder:printcolumn:name="Age",JSONPath=".metadata.creationTimestamp",type="date"
// +kubebuilder:printcolumn:name="Ready",JSONPath=".status.conditions[?(@.type=='Ready')].status",type="string",description="When 'True', the most recent reconcile of the resource succeeded"
// +kubebuilder:printcolumn:name="Status",JSONPath=".status.conditions[?(@.type=='Ready')].reason",type="string",description="The reason for the value in 'Ready'"
// +kubebuilder:printcolumn:name="Status Age",JSONPath=".status.conditions[?(@.type=='Ready')].lastTransitionTime",type="date",description="The last transition time for the value in 'Status'"

// BigQueryMigrationWorkflow is the Schema for the bigquerymigration API
// +k8s:openapi-gen=true
type BigQueryMigrationWorkflow struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   BigQueryMigrationWorkflowSpec   `json:"spec,omitempty"`
	Status BigQueryMigrationWorkflowStatus `json:"status,omitempty"`
}

// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object

// BigQueryMigrationWorkflowList contains a list of BigQueryMigrationWorkflow
type BigQueryMigrationWorkflowList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []BigQueryMigrationWorkflow `json:"items"`
}

func init() {
	SchemeBuilder.Register(&BigQueryMigrationWorkflow{}, &BigQueryMigrationWorkflowList{})
}
