// Copyright 2020 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//    http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Config Connector and manual
//     changes will be clobbered when the file is regenerated.
//
// ----------------------------------------------------------------------------

// *** DISCLAIMER ***
// Config Connector's go-client for CRDs is currently in ALPHA, which means
// that future versions of the go-client may include breaking changes.
// Please try it out and give us feedback!

package v1alpha1

import (
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/clients/generated/apis/k8s/v1alpha1"
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

type DiscoveryconfigActions struct {
	/* Export data profiles into a provided location. */
	// +optional
	ExportData *DiscoveryconfigExportData `json:"exportData,omitempty"`

	/* Publish a message into the Pub/Sub topic. */
	// +optional
	PubSubNotification *DiscoveryconfigPubSubNotification `json:"pubSubNotification,omitempty"`

	/* Publishes generated data profiles to Google Security Operations. For more information, see [Use Sensitive Data Protection data in context-aware analytics](https://cloud.google.com/chronicle/docs/detection/usecase-dlp-high-risk-user-download). */
	// +optional
	PublishToChronicle *DiscoveryconfigPublishToChronicle `json:"publishToChronicle,omitempty"`

	/* Publishes a portion of each profile to Dataplex Catalog with the aspect type Sensitive Data Protection Profile. */
	// +optional
	PublishToDataplexCatalog *DiscoveryconfigPublishToDataplexCatalog `json:"publishToDataplexCatalog,omitempty"`

	/* Publishes findings to Security Command Center for each data profile. */
	// +optional
	PublishToScc *DiscoveryconfigPublishToScc `json:"publishToScc,omitempty"`

	/* Tags the profiled resources with the specified tag values. */
	// +optional
	TagResources *DiscoveryconfigTagResources `json:"tagResources,omitempty"`
}

type DiscoveryconfigAmazonS3Bucket struct {
	/* The AWS account. */
	// +optional
	AwsAccount *DiscoveryconfigAwsAccount `json:"awsAccount,omitempty"`

	/* Required. The bucket name. */
	// +optional
	BucketName *string `json:"bucketName,omitempty"`
}

type DiscoveryconfigAmazonS3BucketConditions struct {
	/* Optional. Bucket types that should be profiled. Optional. Defaults to TYPE_ALL_SUPPORTED if unspecified. */
	// +optional
	BucketTypes []string `json:"bucketTypes,omitempty"`

	/* Optional. Object classes that should be profiled. Optional. Defaults to ALL_SUPPORTED_CLASSES if unspecified. */
	// +optional
	ObjectStorageClasses []string `json:"objectStorageClasses,omitempty"`
}

type DiscoveryconfigAmazonS3BucketRegex struct {
	/* The AWS account regex. */
	// +optional
	AwsAccountRegex *DiscoveryconfigAwsAccountRegex `json:"awsAccountRegex,omitempty"`

	/* Optional. Regex to test the bucket name against. If empty, all buckets match. */
	// +optional
	BucketNameRegex *string `json:"bucketNameRegex,omitempty"`
}

type DiscoveryconfigAwsAccount struct {
	/* Required. AWS account ID. */
	// +optional
	AccountID *string `json:"accountID,omitempty"`
}

type DiscoveryconfigAwsAccountRegex struct {
	/* Optional. Regex to test the AWS account ID against. If empty, all accounts match. */
	// +optional
	AccountIDRegex *string `json:"accountIDRegex,omitempty"`
}

type DiscoveryconfigAwsLocation struct {
	/* The AWS account ID that this discovery config applies to. Within an AWS organization, you can find the AWS account ID inside an AWS account ARN. Example: arn:{partition}:organizations::{management_account_id}:account/{org_id}/{account_id} */
	// +optional
	AccountID *string `json:"accountID,omitempty"`

	/* All AWS assets stored in Asset Inventory that didn't match other AWS discovery configs. */
	// +optional
	AllAssetInventoryAssets *bool `json:"allAssetInventoryAssets,omitempty"`
}

type DiscoveryconfigBigQueryTarget struct {
	/* How often and when to update profiles. New tables that match both the filter and conditions are scanned as quickly as possible depending on system capacity. */
	// +optional
	Cadence *DiscoveryconfigCadence `json:"cadence,omitempty"`

	/* In addition to matching the filter, these conditions must be true before a profile is generated. */
	// +optional
	Conditions *DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* Tables that match this filter will not have profiles created. */
	// +optional
	Disabled *DiscoveryconfigDisabled `json:"disabled,omitempty"`

	/* Required. The tables the discovery cadence applies to. The first target with a matching filter will be the one to apply to a table. */
	// +optional
	Filter *DiscoveryconfigFilter `json:"filter,omitempty"`
}

type DiscoveryconfigCadence struct {
	/* Governs when to update data profiles when the inspection rules defined by the `InspectTemplate` change. If not set, changing the template will not cause a data profile to update. */
	// +optional
	InspectTemplateModifiedCadence *DiscoveryconfigInspectTemplateModifiedCadence `json:"inspectTemplateModifiedCadence,omitempty"`

	/* Frequency at which profiles should be updated, regardless of whether the underlying resource has changed. Defaults to never. */
	// +optional
	RefreshFrequency *string `json:"refreshFrequency,omitempty"`

	/* Governs when to update data profiles when a schema is modified. */
	// +optional
	SchemaModifiedCadence *DiscoveryconfigSchemaModifiedCadence `json:"schemaModifiedCadence,omitempty"`

	/* Governs when to update data profiles when a table is modified. */
	// +optional
	TableModifiedCadence *DiscoveryconfigTableModifiedCadence `json:"tableModifiedCadence,omitempty"`
}

type DiscoveryconfigCloudSQLTarget struct {
	/* In addition to matching the filter, these conditions must be true before a profile is generated. */
	// +optional
	Conditions *DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* Disable profiling for database resources that match this filter. */
	// +optional
	Disabled *DiscoveryconfigDisabled `json:"disabled,omitempty"`

	/* Required. The tables the discovery cadence applies to. The first target with a matching filter will be the one to apply to a table. */
	// +optional
	Filter *DiscoveryconfigFilter `json:"filter,omitempty"`

	/* How often and when to update profiles. New tables that match both the filter and conditions are scanned as quickly as possible depending on system capacity. */
	// +optional
	GenerationCadence *DiscoveryconfigGenerationCadence `json:"generationCadence,omitempty"`
}

type DiscoveryconfigCloudStorageConditions struct {
	/* Required. Only objects with the specified attributes will be scanned. Defaults to [ALL_SUPPORTED_BUCKETS] if unset. */
	// +optional
	IncludedBucketAttributes []string `json:"includedBucketAttributes,omitempty"`

	/* Required. Only objects with the specified attributes will be scanned. If an object has one of the specified attributes but is inside an excluded bucket, it will not be scanned. Defaults to [ALL_SUPPORTED_OBJECTS]. A profile will be created even if no objects match the included_object_attributes. */
	// +optional
	IncludedObjectAttributes []string `json:"includedObjectAttributes,omitempty"`
}

type DiscoveryconfigCloudStorageRegex struct {
	/* Optional. Regex to test the bucket name against. If empty, all buckets match. Example: "marketing2021" or "(marketing)\d{4}" will both match the bucket gs://marketing2021 */
	// +optional
	BucketNameRegex *string `json:"bucketNameRegex,omitempty"`

	/* Optional. For organizations, if unset, will match all projects. */
	// +optional
	ProjectIDRegex *string `json:"projectIDRegex,omitempty"`
}

type DiscoveryconfigCloudStorageResourceReference struct {
	/* Required. The bucket to scan. */
	// +optional
	BucketName *string `json:"bucketName,omitempty"`

	/* Required. If within a project-level config, then this must match the config's project id. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`
}

type DiscoveryconfigCloudStorageTarget struct {
	/* Optional. In addition to matching the filter, these conditions must be true before a profile is generated. */
	// +optional
	Conditions *DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* Optional. Disable profiling for buckets that match this filter. */
	// +optional
	Disabled *DiscoveryconfigDisabled `json:"disabled,omitempty"`

	/* Required. The buckets the generation_cadence applies to. The first target with a matching filter will be the one to apply to a bucket. */
	// +optional
	Filter *DiscoveryconfigFilter `json:"filter,omitempty"`

	/* Optional. How often and when to update profiles. New buckets that match both the filter and conditions are scanned as quickly as possible depending on system capacity. */
	// +optional
	GenerationCadence *DiscoveryconfigGenerationCadence `json:"generationCadence,omitempty"`
}

type DiscoveryconfigCollection struct {
	/* The regex used to filter dataset resources. */
	// +optional
	VertexDatasetRegexes *DiscoveryconfigVertexDatasetRegexes `json:"vertexDatasetRegexes,omitempty"`
}

type DiscoveryconfigConditions struct {
	/* Vertex AI dataset must have been created after this date. Used to avoid backfilling. */
	// +optional
	CreatedAfter *string `json:"createdAfter,omitempty"`

	/* Minimum age a Vertex AI dataset must have. If set, the value must be 1 hour or greater. */
	// +optional
	MinAge *string `json:"minAge,omitempty"`
}

type DiscoveryconfigDataSourceType struct {
}

type DiscoveryconfigDatabaseResourceReference struct {
	/* Required. Name of a database within the instance. */
	// +optional
	Database *string `json:"database,omitempty"`

	/* Required. Name of a database resource, for example, a table within the database. */
	// +optional
	DatabaseResource *string `json:"databaseResource,omitempty"`

	/* Required. The instance where this resource is located. For example: Cloud SQL instance ID. */
	// +optional
	Instance *string `json:"instance,omitempty"`

	/* Required. If within a project-level config, then this must match the config's project ID. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`
}

type DiscoveryconfigDisabled struct {
}

type DiscoveryconfigExportData struct {
	/* Store all profiles to BigQuery.

	* The system will create a new dataset and table for you if none are
	are provided. The dataset will be named
	`sensitive_data_protection_discovery` and table will be named
	`discovery_profiles`. This table will be placed in the same project as
	the container project running the scan. After the first profile is
	generated and the dataset and table are created, the discovery scan
	configuration will be updated with the dataset and table names.
	* See [Analyze data profiles stored in
	BigQuery](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles).
	* See [Sample queries for your BigQuery
	table](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles#sample_sql_queries).
	*  Data is inserted using [streaming
	insert](https://cloud.google.com/blog/products/bigquery/life-of-a-bigquery-streaming-insert)
	and so data may be in the buffer for a period of time after the
	profile has finished.
	* The Pub/Sub notification is sent before the streaming buffer is
	guaranteed to be written, so data may not be instantly
	visible to queries by the time your topic receives the Pub/Sub
	notification.
	* The best practice is to use the same table for an entire organization
	so that you can take advantage of the [provided Looker
	reports](https://cloud.google.com/sensitive-data-protection/docs/analyze-data-profiles#use_a_premade_report).
	If you use VPC Service Controls to define security perimeters, then
	you must use a separate table for each boundary. */
	// +optional
	ProfileTable *DiscoveryconfigProfileTable `json:"profileTable,omitempty"`

	/* Store sample [data profile findings][google.privacy.dlp.v2.DataProfileFinding] in an existing table or a new table in an existing dataset. Each regeneration will result in new rows in BigQuery. Data is inserted using [streaming insert](https://cloud.google.com/blog/products/bigquery/life-of-a-bigquery-streaming-insert) and so data may be in the buffer for a period of time after the profile has finished. */
	// +optional
	SampleFindingsTable *DiscoveryconfigSampleFindingsTable `json:"sampleFindingsTable,omitempty"`
}

type DiscoveryconfigExpressions struct {
	/* Conditions to apply to the expression. */
	// +optional
	Conditions []DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* The operator to apply to the collection of conditions. */
	// +optional
	LogicalOperator *string `json:"logicalOperator,omitempty"`
}

type DiscoveryconfigFilter struct {
	/* A specific set of Vertex AI datasets for this filter to apply to. */
	// +optional
	Collection *DiscoveryconfigCollection `json:"collection,omitempty"`

	/* Catch-all. This should always be the last target in the list because anything above it will apply first. Should only appear once in a configuration. If none is specified, a default one will be added automatically. */
	// +optional
	Others *DiscoveryconfigOthers `json:"others,omitempty"`

	/* The dataset resource to scan. Targets including this can only include one target (the target with this dataset resource reference). */
	// +optional
	VertexDatasetResourceReference *DiscoveryconfigVertexDatasetResourceReference `json:"vertexDatasetResourceReference,omitempty"`
}

type DiscoveryconfigGenerationCadence struct {
	/* Governs when to update data profiles when the inspection rules defined by the `InspectTemplate` change. If not set, changing the template will not cause a data profile to be updated. */
	// +optional
	InspectTemplateModifiedCadence *DiscoveryconfigInspectTemplateModifiedCadence `json:"inspectTemplateModifiedCadence,omitempty"`

	/* If you set this field, profiles are refreshed at this frequency regardless of whether the underlying datasets have changed. Defaults to never. */
	// +optional
	RefreshFrequency *string `json:"refreshFrequency,omitempty"`
}

type DiscoveryconfigGlobalProcessing struct {
}

type DiscoveryconfigImageFallbackLocation struct {
	/* Processing will happen in the global region. */
	// +optional
	GlobalProcessing *DiscoveryconfigGlobalProcessing `json:"globalProcessing,omitempty"`

	/* Processing will happen in a multi-region that contains the current region if available. */
	// +optional
	MultiRegionProcessing *DiscoveryconfigMultiRegionProcessing `json:"multiRegionProcessing,omitempty"`
}

type DiscoveryconfigIncludeRegexes struct {
	/* A group of regular expression patterns to match against one or more resources. Maximum of 100 entries. The sum of all regular expression's length can't exceed 10 KiB. */
	// +optional
	Patterns []DiscoveryconfigPatterns `json:"patterns,omitempty"`
}

type DiscoveryconfigInspectTemplateModifiedCadence struct {
	/* How frequently data profiles can be updated when the template is modified. Defaults to never. */
	// +optional
	Frequency *string `json:"frequency,omitempty"`
}

type DiscoveryconfigLocation struct {
	/* The ID of the folder within an organization to be scanned. */
	// +optional
	FolderID *int64 `json:"folderID,omitempty"`

	/* The ID of an organization to scan. */
	// +optional
	OrganizationID *int64 `json:"organizationID,omitempty"`
}

type DiscoveryconfigMultiRegionProcessing struct {
}

type DiscoveryconfigOrConditions struct {
	/* Minimum age a table must have before Cloud DLP can profile it. Value must be 1 hour or greater. */
	// +optional
	MinAge *string `json:"minAge,omitempty"`

	/* Minimum number of rows that should be present before Cloud DLP profiles a table */
	// +optional
	MinRowCount *int32 `json:"minRowCount,omitempty"`
}

type DiscoveryconfigOrgConfig struct {
	/* The data to scan: folder, org, or project */
	// +optional
	Location *DiscoveryconfigLocation `json:"location,omitempty"`

	/* The project that will run the scan. The DLP service account that exists within this project must have access to all resources that are profiled, and the DLP API must be enabled. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`
}

type DiscoveryconfigOtherCloudStartingLocation struct {
	/* The AWS starting location for discovery. */
	// +optional
	AwsLocation *DiscoveryconfigAwsLocation `json:"awsLocation,omitempty"`
}

type DiscoveryconfigOtherCloudTarget struct {
	/* Optional. In addition to matching the filter, these conditions must be true before a profile is generated. */
	// +optional
	Conditions *DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* Required. The type of data profiles generated by this discovery target. Supported values are: * aws/s3/bucket */
	// +optional
	DataSourceType *DiscoveryconfigDataSourceType `json:"dataSourceType,omitempty"`

	/* Disable profiling for resources that match this filter. */
	// +optional
	Disabled *DiscoveryconfigDisabled `json:"disabled,omitempty"`

	/* Required. The resources that the discovery cadence applies to. The first target with a matching filter will be the one to apply to a resource. */
	// +optional
	Filter *DiscoveryconfigFilter `json:"filter,omitempty"`

	/* How often and when to update data profiles. New resources that match both the filter and conditions are scanned as quickly as possible depending on system capacity. */
	// +optional
	GenerationCadence *DiscoveryconfigGenerationCadence `json:"generationCadence,omitempty"`
}

type DiscoveryconfigOtherTables struct {
}

type DiscoveryconfigOthers struct {
}

type DiscoveryconfigPatterns struct {
	/* For organizations, if unset, will match all projects. Has no effect for configurations created within a project. */
	// +optional
	ProjectIDRegex *string `json:"projectIDRegex,omitempty"`
}

type DiscoveryconfigProcessingLocation struct {
	/* Image processing will fall back using this configuration. */
	// +optional
	ImageFallbackLocation *DiscoveryconfigImageFallbackLocation `json:"imageFallbackLocation,omitempty"`
}

type DiscoveryconfigProfileTable struct {
	/* Dataset ID of the table. */
	// +optional
	DatasetID *string `json:"datasetID,omitempty"`

	/* The Google Cloud project ID of the project containing the table. If omitted, project ID is inferred from the API call. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`

	/* Name of the table. */
	// +optional
	TableID *string `json:"tableID,omitempty"`
}

type DiscoveryconfigPubSubNotification struct {
	/* How much data to include in the Pub/Sub message. If the user wishes to limit the size of the message, they can use resource_name and fetch the profile fields they wish to. Per table profile (not per column). */
	// +optional
	DetailOfMessage *string `json:"detailOfMessage,omitempty"`

	/* The type of event that triggers a Pub/Sub. At most one `PubSubNotification` per EventType is permitted. */
	// +optional
	Event *string `json:"event,omitempty"`

	/* Conditions (e.g., data risk or sensitivity level) for triggering a Pub/Sub. */
	// +optional
	PubsubCondition *DiscoveryconfigPubsubCondition `json:"pubsubCondition,omitempty"`

	/* Cloud Pub/Sub topic to send notifications to. */
	// +optional
	TopicRef *v1alpha1.ResourceRef `json:"topicRef,omitempty"`
}

type DiscoveryconfigPublishToChronicle struct {
}

type DiscoveryconfigPublishToDataplexCatalog struct {
	/* Whether creating a Dataplex Catalog aspect for a profiled resource should lower the risk of the profile for that resource. This also lowers the data risk of resources at the lower levels of the resource hierarchy. For example, reducing the data risk of a table data profile also reduces the data risk of the constituent column data profiles. */
	// +optional
	LowerDataRiskToLow *bool `json:"lowerDataRiskToLow,omitempty"`
}

type DiscoveryconfigPublishToScc struct {
}

type DiscoveryconfigPubsubCondition struct {
	/* An expression. */
	// +optional
	Expressions *DiscoveryconfigExpressions `json:"expressions,omitempty"`
}

type DiscoveryconfigSampleFindingsTable struct {
	/* Dataset ID of the table. */
	// +optional
	DatasetID *string `json:"datasetID,omitempty"`

	/* The Google Cloud project ID of the project containing the table. If omitted, project ID is inferred from the API call. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`

	/* Name of the table. */
	// +optional
	TableID *string `json:"tableID,omitempty"`
}

type DiscoveryconfigSchemaModifiedCadence struct {
	/* Frequency to regenerate data profiles when the schema is modified. Defaults to monthly. */
	// +optional
	Frequency *string `json:"frequency,omitempty"`

	/* The types of schema modifications to consider. Defaults to NEW_COLUMNS. */
	// +optional
	Types []DiscoveryconfigTypes `json:"types,omitempty"`
}

type DiscoveryconfigSecretsTarget struct {
}

type DiscoveryconfigSensitivityScore struct {
	/* The sensitivity score applied to the resource. */
	// +optional
	Score *string `json:"score,omitempty"`
}

type DiscoveryconfigSingleResource struct {
	/* Amazon S3 bucket. */
	// +optional
	AmazonS3Bucket *DiscoveryconfigAmazonS3Bucket `json:"amazonS3Bucket,omitempty"`
}

type DiscoveryconfigTableModifiedCadence struct {
	/* How frequently data profiles can be updated when tables are modified. Defaults to never. */
	// +optional
	Frequency *string `json:"frequency,omitempty"`

	/* The type of events to consider when deciding if the table has been modified and should have the profile updated. Defaults to MODIFIED_TIMESTAMP. */
	// +optional
	Types []DiscoveryconfigTypes `json:"types,omitempty"`
}

type DiscoveryconfigTableReference struct {
	/* Dataset ID of the table. */
	// +optional
	DatasetID *string `json:"datasetID,omitempty"`

	/* The Google Cloud project ID of the project containing the table. If omitted, the project ID is inferred from the parent project. This field is required if the parent resource is an organization. */
	// +optional
	ProjectID *string `json:"projectID,omitempty"`

	/* Name of the table. */
	// +optional
	TableID *string `json:"tableID,omitempty"`
}

type DiscoveryconfigTables struct {
	/* A collection of regular expressions to match a BigQuery table against. */
	// +optional
	IncludeRegexes *DiscoveryconfigIncludeRegexes `json:"includeRegexes,omitempty"`
}

type DiscoveryconfigTag struct {
	/* The namespaced name for the tag value to attach to resources. Must be in the format `{parent_id}/{tag_key_short_name}/{short_name}`, for example, "123456/environment/prod". */
	// +optional
	NamespacedValue *string `json:"namespacedValue,omitempty"`
}

type DiscoveryconfigTagConditions struct {
	/* Conditions attaching the tag to a resource on its profile having this sensitivity score. */
	// +optional
	SensitivityScore *DiscoveryconfigSensitivityScore `json:"sensitivityScore,omitempty"`

	/* The tag value to attach to resources. */
	// +optional
	Tag *DiscoveryconfigTag `json:"tag,omitempty"`
}

type DiscoveryconfigTagResources struct {
	/* Whether applying a tag to a resource should lower the risk of the profile for that resource. For example, in conjunction with an [IAM deny policy](https://cloud.google.com/iam/docs/deny-overview), you can deny all principals a permission if a tag value is present, mitigating the risk of the resource. This also lowers the data risk of resources at the lower levels of the resource hierarchy. For example, reducing the data risk of a table data profile also reduces the data risk of the constituent column data profiles. */
	// +optional
	LowerDataRiskToLow *bool `json:"lowerDataRiskToLow,omitempty"`

	/* The profile generations for which the tag should be attached to resources. If you attach a tag to only new profiles, then if the sensitivity score of a profile subsequently changes, its tag doesn't change. By default, this field includes only new profiles. To include both new and updated profiles for tagging, this field should explicitly include both `PROFILE_GENERATION_NEW` and `PROFILE_GENERATION_UPDATE`. */
	// +optional
	ProfileGenerationsToTag []string `json:"profileGenerationsToTag,omitempty"`

	/* The tags to associate with different conditions. */
	// +optional
	TagConditions []DiscoveryconfigTagConditions `json:"tagConditions,omitempty"`
}

type DiscoveryconfigTargets struct {
	/* BigQuery target for Discovery. The first target to match a table will be the one applied. */
	// +optional
	BigQueryTarget *DiscoveryconfigBigQueryTarget `json:"bigQueryTarget,omitempty"`

	/* Cloud SQL target for Discovery. The first target to match a table will be the one applied. */
	// +optional
	CloudSQLTarget *DiscoveryconfigCloudSQLTarget `json:"cloudSQLTarget,omitempty"`

	/* Cloud Storage target for Discovery. The first target to match a table will be the one applied. */
	// +optional
	CloudStorageTarget *DiscoveryconfigCloudStorageTarget `json:"cloudStorageTarget,omitempty"`

	/* Other clouds target for discovery. The first target to match a resource will be the one applied. */
	// +optional
	OtherCloudTarget *DiscoveryconfigOtherCloudTarget `json:"otherCloudTarget,omitempty"`

	/* Discovery target that looks for credentials and secrets stored in cloud resource metadata and reports them as vulnerabilities to Security Command Center. Only one target of this type is allowed. */
	// +optional
	SecretsTarget *DiscoveryconfigSecretsTarget `json:"secretsTarget,omitempty"`

	/* Vertex AI dataset target for Discovery. The first target to match a dataset will be the one applied. Note that discovery for Vertex AI can incur Cloud Storage Class B operation charges for storage.objects.get operations and retrieval fees. For more information, see [Cloud Storage pricing](https://cloud.google.com/storage/pricing#price-tables). Note that discovery for Vertex AI dataset will not be able to scan images unless DiscoveryConfig.processing_location.image_fallback_location has multi_region_processing or global_processing configured. */
	// +optional
	VertexDatasetTarget *DiscoveryconfigVertexDatasetTarget `json:"vertexDatasetTarget,omitempty"`
}

type DiscoveryconfigTypes struct {
	/* A set of BigQuery table types. */
	// +optional
	Types []DiscoveryconfigTypes `json:"types,omitempty"`
}

type DiscoveryconfigVertexDatasetRegexes struct {
	/* Required. The group of regular expression patterns to match against one or more datasets. Maximum of 100 entries. The sum of the lengths of all regular expressions can't exceed 10 KiB. */
	// +optional
	Patterns []DiscoveryconfigPatterns `json:"patterns,omitempty"`
}

type DiscoveryconfigVertexDatasetResourceReference struct {
	/* Required. The name of the dataset resource. If set within a project-level configuration, the specified resource must be within the project. */
	// +optional
	DatasetResourceName *string `json:"datasetResourceName,omitempty"`
}

type DiscoveryconfigVertexDatasetTarget struct {
	/* In addition to matching the filter, these conditions must be true before a profile is generated. */
	// +optional
	Conditions *DiscoveryconfigConditions `json:"conditions,omitempty"`

	/* Disable profiling for datasets that match this filter. */
	// +optional
	Disabled *DiscoveryconfigDisabled `json:"disabled,omitempty"`

	/* Required. The datasets the discovery cadence applies to. The first target with a matching filter will be the one to apply to a dataset. */
	// +optional
	Filter *DiscoveryconfigFilter `json:"filter,omitempty"`

	/* How often and when to update profiles. New datasets that match both the filter and conditions are scanned as quickly as possible depending on system capacity. */
	// +optional
	GenerationCadence *DiscoveryconfigGenerationCadence `json:"generationCadence,omitempty"`
}

type DLPDiscoveryConfigSpec struct {
	/* Actions to execute at the completion of scanning. */
	// +optional
	Actions []DiscoveryconfigActions `json:"actions,omitempty"`

	/* Display name (max 100 chars) */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* Detection logic for profile generation. */
	// +optional
	InspectTemplates []string `json:"inspectTemplates,omitempty"`

	/* The location of this resource. */
	Location string `json:"location"`

	/* Only set when the parent is an org. */
	// +optional
	OrgConfig *DiscoveryconfigOrgConfig `json:"orgConfig,omitempty"`

	/* Must be set only when scanning other clouds. */
	// +optional
	OtherCloudStartingLocation *DiscoveryconfigOtherCloudStartingLocation `json:"otherCloudStartingLocation,omitempty"`

	/* Optional. Processing location configuration. Vertex AI dataset scanning will set processing_location.image_fallback_type to MultiRegionProcessing by default. */
	// +optional
	ProcessingLocation *DiscoveryconfigProcessingLocation `json:"processingLocation,omitempty"`

	/* The project that this resource belongs to. */
	ProjectRef v1alpha1.ResourceRef `json:"projectRef"`

	/* The DLPDiscoveryConfig name. If not given, the metadata.name will be used. */
	// +optional
	ResourceID *string `json:"resourceID,omitempty"`

	/* Required. A status for this configuration. */
	Status string `json:"status"`

	/* Target to match against for determining what to scan and how frequently. */
	// +optional
	Targets []DiscoveryconfigTargets `json:"targets,omitempty"`
}

type DiscoveryconfigDataSourceTypeStatus struct {
	/* Output only. An identifying string to the type of resource being profiled.
	Current values:

	* google/bigquery/table
	* google/project
	* google/sql/table
	* google/gcs/bucket */
	// +optional
	DataSource *string `json:"dataSource,omitempty"`
}

type DiscoveryconfigDetailsStatus struct {
	/* The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code]. */
	// +optional
	Code *int32 `json:"code,omitempty"`

	/* A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client. */
	// +optional
	Message *string `json:"message,omitempty"`
}

type DiscoveryconfigErrorsStatus struct {
	/* Detailed error codes and messages. */
	// +optional
	Details *DiscoveryconfigDetailsStatus `json:"details,omitempty"`

	/* Additional information about the error. */
	// +optional
	ExtraInfo *string `json:"extraInfo,omitempty"`

	/* The times the error occurred. List includes the oldest timestamp and the last 9 timestamps. */
	// +optional
	Timestamps []string `json:"timestamps,omitempty"`
}

type DiscoveryconfigObservedStateStatus struct {
	/* Output only. The creation timestamp of a DiscoveryConfig. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* Output only. A stream of errors encountered when the config was activated. Repeated errors may result in the config automatically being paused. Output only field. Will return the last 100 errors. Whenever the config is modified this list will be cleared. */
	// +optional
	Errors []DiscoveryconfigErrorsStatus `json:"errors,omitempty"`

	/* Output only. The timestamp of the last time this config was executed. */
	// +optional
	LastRunTime *string `json:"lastRunTime,omitempty"`

	/* Target to match against for determining what to scan and how frequently. */
	// +optional
	Targets []DiscoveryconfigTargetsStatus `json:"targets,omitempty"`

	/* Output only. The last update timestamp of a DiscoveryConfig. */
	// +optional
	UpdateTime *string `json:"updateTime,omitempty"`
}

type DiscoveryconfigOtherCloudTargetStatus struct {
	/* Required. The type of data profiles generated by this discovery target. Supported values are: * aws/s3/bucket */
	// +optional
	DataSourceType *DiscoveryconfigDataSourceTypeStatus `json:"dataSourceType,omitempty"`
}

type DiscoveryconfigTargetsStatus struct {
	/* Other clouds target for discovery. The first target to match a resource will be the one applied. */
	// +optional
	OtherCloudTarget *DiscoveryconfigOtherCloudTargetStatus `json:"otherCloudTarget,omitempty"`
}

type DLPDiscoveryConfigStatus struct {
	/* Conditions represent the latest available observations of the
	   DLPDiscoveryConfig's current state. */
	Conditions []v1alpha1.Condition `json:"conditions,omitempty"`
	/* A unique specifier for the DLPDiscoveryConfig resource in Google Cloud. */
	// +optional
	ExternalRef *string `json:"externalRef,omitempty"`

	/* ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource. */
	// +optional
	ObservedGeneration *int64 `json:"observedGeneration,omitempty"`

	/* ObservedState is the state of the resource as most recently observed in Google Cloud. */
	// +optional
	ObservedState *DiscoveryconfigObservedStateStatus `json:"observedState,omitempty"`
}

// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +kubebuilder:resource:categories=gcp,shortName=gcpdlpdiscoveryconfig;gcpdlpdiscoveryconfigs
// +kubebuilder:subresource:status
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/managed-by-kcc=true"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/stability-level=alpha"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/system=true"
// +kubebuilder:printcolumn:name="Age",JSONPath=".metadata.creationTimestamp",type="date"
// +kubebuilder:printcolumn:name="Ready",JSONPath=".status.conditions[?(@.type=='Ready')].status",type="string",description="When 'True', the most recent reconcile of the resource succeeded"
// +kubebuilder:printcolumn:name="Status",JSONPath=".status.conditions[?(@.type=='Ready')].reason",type="string",description="The reason for the value in 'Ready'"
// +kubebuilder:printcolumn:name="Status Age",JSONPath=".status.conditions[?(@.type=='Ready')].lastTransitionTime",type="date",description="The last transition time for the value in 'Status'"

// DLPDiscoveryConfig is the Schema for the dlp API
// +k8s:openapi-gen=true
type DLPDiscoveryConfig struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   DLPDiscoveryConfigSpec   `json:"spec,omitempty"`
	Status DLPDiscoveryConfigStatus `json:"status,omitempty"`
}

// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object

// DLPDiscoveryConfigList contains a list of DLPDiscoveryConfig
type DLPDiscoveryConfigList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []DLPDiscoveryConfig `json:"items"`
}

func init() {
	SchemeBuilder.Register(&DLPDiscoveryConfig{}, &DLPDiscoveryConfigList{})
}
