// Copyright 2020 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//    http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Config Connector and manual
//     changes will be clobbered when the file is regenerated.
//
// ----------------------------------------------------------------------------

// *** DISCLAIMER ***
// Config Connector's go-client for CRDs is currently in ALPHA, which means
// that future versions of the go-client may include breaking changes.
// Please try it out and give us feedback!

package v1alpha1

import (
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/clients/generated/apis/k8s/v1alpha1"
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

type DatascanBigqueryExport struct {
	/* Optional. The BigQuery table to export DataQualityScan results to. Format: //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID or projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID */
	// +optional
	ResultsTableRef *v1alpha1.ResourceRef `json:"resultsTableRef,omitempty"`
}

type DatascanBigqueryPublishingConfig struct {
	/* Optional. The BigQuery connection used to create BigLake tables. Must be in the form `projects/{project_id}/locations/{location_id}/connections/{connection_id}` */
	// +optional
	ConnectionRef *v1alpha1.ResourceRef `json:"connectionRef,omitempty"`

	/* Optional. Determines whether to  publish discovered tables as BigLake external tables or non-BigLake external tables. */
	// +optional
	TableType *string `json:"tableType,omitempty"`
}

type DatascanCsvOptions struct {
	/* Optional. The delimiter that is used to separate values. The default is `,` (comma). */
	// +optional
	Delimiter *string `json:"delimiter,omitempty"`

	/* Optional. The character encoding of the data. The default is UTF-8. */
	// +optional
	Encoding *string `json:"encoding,omitempty"`

	/* Optional. The number of rows to interpret as header rows that should be skipped when reading data rows. */
	// +optional
	HeaderRows *int32 `json:"headerRows,omitempty"`

	/* Optional. The character used to quote column values. Accepts `"` (double quotation mark) or `'` (single quotation mark). If unspecified, defaults to `"` (double quotation mark). */
	// +optional
	Quote *string `json:"quote,omitempty"`

	/* Optional. Whether to disable the inference of data types for CSV data. If true, all columns are registered as strings. */
	// +optional
	TypeInferenceDisabled *bool `json:"typeInferenceDisabled,omitempty"`
}

type DatascanData struct {
	/* Immutable. The Dataplex entity that represents the data source (e.g. BigQuery table) for DataScan, of the form: `projects/{project_number}/locations/{location_id}/lakes/{lake_id}/zones/{zone_id}/entities/{entity_id}`. */
	// +optional
	EntityRef *v1alpha1.ResourceRef `json:"entityRef,omitempty"`

	/* Immutable. The service-qualified full resource name of the cloud resource for a DataScan job to scan against. The field could be: BigQuery table of type "TABLE" for DataProfileScan/DataQualityScan Format: //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID */
	// +optional
	Resource *string `json:"resource,omitempty"`
}

type DatascanDataDiscoverySpec struct {
	/* Optional. Configuration for metadata publishing. */
	// +optional
	BigqueryPublishingConfig *DatascanBigqueryPublishingConfig `json:"bigqueryPublishingConfig,omitempty"`

	/* Cloud Storage related configurations. */
	// +optional
	StorageConfig *DatascanStorageConfig `json:"storageConfig,omitempty"`
}

type DatascanDataProfileSpec struct {
	/* Optional. The fields to exclude from data profile.

	If specified, the fields will be excluded from data profile, regardless of
	`include_fields` value. */
	// +optional
	ExcludeFields *DatascanExcludeFields `json:"excludeFields,omitempty"`

	/* Optional. The fields to include in data profile.

	If not specified, all fields at the time of profile scan job execution are
	included, except for ones listed in `exclude_fields`. */
	// +optional
	IncludeFields *DatascanIncludeFields `json:"includeFields,omitempty"`

	/* Optional. Actions to take upon job completion.. */
	// +optional
	PostScanActions *DatascanPostScanActions `json:"postScanActions,omitempty"`

	/* Optional. A filter applied to all rows in a single DataScan job.
	The filter needs to be a valid SQL expression for a [WHERE clause in
	GoogleSQL
	syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#where_clause).

	Example: col1 >= 0 AND col2 < 10 */
	// +optional
	RowFilter *string `json:"rowFilter,omitempty"`

	/* Optional. The percentage of the records to be selected from the dataset for
	DataScan.

	* Value can range between 0.0 and 100.0 with up to 3 significant decimal
	digits.
	* Sampling is not applied if `sampling_percent` is not specified, 0 or
	100. */
	// +optional
	SamplingPercent *float64 `json:"samplingPercent,omitempty"`
}

type DatascanDataQualitySpec struct {
	/* Optional. Actions to take upon job completion. */
	// +optional
	PostScanActions *DatascanPostScanActions `json:"postScanActions,omitempty"`

	/* Optional. A filter applied to all rows in a single DataScan job.
	The filter needs to be a valid SQL expression for a [WHERE clause in
	GoogleSQL
	syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#where_clause).

	Example: col1 >= 0 AND col2 < 10 */
	// +optional
	RowFilter *string `json:"rowFilter,omitempty"`

	/* Required. The list of rules to evaluate against a data source. At least one rule is required. */
	// +optional
	Rules []DatascanRules `json:"rules,omitempty"`

	/* Optional. The percentage of the records to be selected from the dataset for
	DataScan.

	* Value can range between 0.0 and 100.0 with up to 3 significant decimal
	digits.
	* Sampling is not applied if `sampling_percent` is not specified, 0 or
	100. */
	// +optional
	SamplingPercent *float64 `json:"samplingPercent,omitempty"`
}

type DatascanExcludeFields struct {
	/* Optional. Expected input is a list of fully qualified names of fields as
	in the schema.

	Only top-level field names for nested fields are supported.
	For instance, if 'x' is of nested field type, listing 'x' is supported
	but 'x.y.z' is not supported. Here 'y' and 'y.z' are nested fields of
	'x'. */
	// +optional
	FieldNames []string `json:"fieldNames,omitempty"`
}

type DatascanExecutionSpec struct {
	/* Immutable. The unnested field (of type *Date* or *Timestamp*) that
	contains values which monotonically increase over time.

	If not specified, a data scan will run for all data in the table. */
	// +optional
	Field *string `json:"field,omitempty"`

	/* Optional. Spec related to how often and when a scan should be triggered.

	If not specified, the default is `OnDemand`, which means the scan will
	not run until the user calls `RunDataScan` API. */
	// +optional
	Trigger *DatascanTrigger `json:"trigger,omitempty"`
}

type DatascanIncludeFields struct {
	/* Optional. Expected input is a list of fully qualified names of fields as
	in the schema.

	Only top-level field names for nested fields are supported.
	For instance, if 'x' is of nested field type, listing 'x' is supported
	but 'x.y.z' is not supported. Here 'y' and 'y.z' are nested fields of
	'x'. */
	// +optional
	FieldNames []string `json:"fieldNames,omitempty"`
}

type DatascanJobEndTrigger struct {
}

type DatascanJobFailureTrigger struct {
}

type DatascanJsonOptions struct {
	/* Optional. The character encoding of the data. The default is UTF-8. */
	// +optional
	Encoding *string `json:"encoding,omitempty"`

	/* Optional. Whether to disable the inference of data types for JSON data. If true, all columns are registered as their primitive types (strings, number, or boolean). */
	// +optional
	TypeInferenceDisabled *bool `json:"typeInferenceDisabled,omitempty"`
}

type DatascanNonNullExpectation struct {
}

type DatascanNotificationReport struct {
	/* Optional. If set, report will be sent when a scan job ends. */
	// +optional
	JobEndTrigger *DatascanJobEndTrigger `json:"jobEndTrigger,omitempty"`

	/* Optional. If set, report will be sent when a scan job fails. */
	// +optional
	JobFailureTrigger *DatascanJobFailureTrigger `json:"jobFailureTrigger,omitempty"`

	/* Required. The recipients who will receive the notification report. */
	// +optional
	Recipients *DatascanRecipients `json:"recipients,omitempty"`

	/* Optional. If set, report will be sent when score threshold is met. */
	// +optional
	ScoreThresholdTrigger *DatascanScoreThresholdTrigger `json:"scoreThresholdTrigger,omitempty"`
}

type DatascanOnDemand struct {
}

type DatascanPostScanActions struct {
	/* Optional. If set, results will be exported to the provided BigQuery table. */
	// +optional
	BigqueryExport *DatascanBigqueryExport `json:"bigqueryExport,omitempty"`

	/* Optional. If set, results will be sent to the provided notification receipts upon triggers. */
	// +optional
	NotificationReport *DatascanNotificationReport `json:"notificationReport,omitempty"`
}

type DatascanRangeExpectation struct {
	/* Optional. The maximum column value allowed for a row to pass this validation. At least one of `min_value` and `max_value` need to be provided. */
	// +optional
	MaxValue *string `json:"maxValue,omitempty"`

	/* Optional. The minimum column value allowed for a row to pass this validation. At least one of `min_value` and `max_value` need to be provided. */
	// +optional
	MinValue *string `json:"minValue,omitempty"`

	/* Optional. Whether each value needs to be strictly lesser than ('<') the
	maximum, or if equality is allowed.

	Only relevant if a `max_value` has been defined. Default = false. */
	// +optional
	StrictMaxEnabled *bool `json:"strictMaxEnabled,omitempty"`

	/* Optional. Whether each value needs to be strictly greater than ('>') the
	minimum, or if equality is allowed.

	Only relevant if a `min_value` has been defined. Default = false. */
	// +optional
	StrictMinEnabled *bool `json:"strictMinEnabled,omitempty"`
}

type DatascanRecipients struct {
	/* Optional. The email recipients who will receive the DataQualityScan results report. */
	// +optional
	Emails []string `json:"emails,omitempty"`
}

type DatascanRegexExpectation struct {
	/* Optional. A regular expression the column value is expected to match. */
	// +optional
	Regex *string `json:"regex,omitempty"`
}

type DatascanRowConditionExpectation struct {
	/* Optional. The SQL expression. */
	// +optional
	SqlExpression *string `json:"sqlExpression,omitempty"`
}

type DatascanRules struct {
	/* Optional. The unnested column which this rule is evaluated against. */
	// +optional
	Column *string `json:"column,omitempty"`

	/* Optional. Description of the rule.

	* The maximum length is 1,024 characters. */
	// +optional
	Description *string `json:"description,omitempty"`

	/* Required. The dimension a rule belongs to. Results are also aggregated at the dimension level. Supported dimensions are **["COMPLETENESS", "ACCURACY", "CONSISTENCY", "VALIDITY", "UNIQUENESS", "FRESHNESS", "VOLUME"]** */
	// +optional
	Dimension *string `json:"dimension,omitempty"`

	/* Optional. Rows with `null` values will automatically fail a rule, unless
	`ignore_null` is `true`. In that case, such `null` rows are trivially
	considered passing.

	This field is only valid for the following type of rules:

	* RangeExpectation
	* RegexExpectation
	* SetExpectation
	* UniquenessExpectation */
	// +optional
	IgnoreNull *bool `json:"ignoreNull,omitempty"`

	/* Optional. A mutable name for the rule.

	* The name must contain only letters (a-z, A-Z), numbers (0-9), or
	hyphens (-).
	* The maximum length is 63 characters.
	* Must start with a letter.
	* Must end with a number or a letter. */
	// +optional
	Name *string `json:"name,omitempty"`

	/* Row-level rule which evaluates whether each column value is null. */
	// +optional
	NonNullExpectation *DatascanNonNullExpectation `json:"nonNullExpectation,omitempty"`

	/* Row-level rule which evaluates whether each column value lies between a specified range. */
	// +optional
	RangeExpectation *DatascanRangeExpectation `json:"rangeExpectation,omitempty"`

	/* Row-level rule which evaluates whether each column value matches a specified regex. */
	// +optional
	RegexExpectation *DatascanRegexExpectation `json:"regexExpectation,omitempty"`

	/* Row-level rule which evaluates whether each row in a table passes the specified condition. */
	// +optional
	RowConditionExpectation *DatascanRowConditionExpectation `json:"rowConditionExpectation,omitempty"`

	/* Row-level rule which evaluates whether each column value is contained by a specified set. */
	// +optional
	SetExpectation *DatascanSetExpectation `json:"setExpectation,omitempty"`

	/* Aggregate rule which evaluates the number of rows returned for the provided statement. If any rows are returned, this rule fails. */
	// +optional
	SqlAssertion *DatascanSqlAssertion `json:"sqlAssertion,omitempty"`

	/* Aggregate rule which evaluates whether the column aggregate statistic lies between a specified range. */
	// +optional
	StatisticRangeExpectation *DatascanStatisticRangeExpectation `json:"statisticRangeExpectation,omitempty"`

	/* Optional. Whether the Rule is active or suspended. Default is false. */
	// +optional
	Suspended *bool `json:"suspended,omitempty"`

	/* Aggregate rule which evaluates whether the provided expression is true for a table. */
	// +optional
	TableConditionExpectation *DatascanTableConditionExpectation `json:"tableConditionExpectation,omitempty"`

	/* Optional. The minimum ratio of **passing_rows / total_rows** required to
	pass this rule, with a range of [0.0, 1.0].

	0 indicates default value (i.e. 1.0).

	This field is only valid for row-level type rules. */
	// +optional
	Threshold *float64 `json:"threshold,omitempty"`

	/* Row-level rule which evaluates whether each column value is unique. */
	// +optional
	UniquenessExpectation *DatascanUniquenessExpectation `json:"uniquenessExpectation,omitempty"`
}

type DatascanSchedule struct {
	/* Required. [Cron](https://en.wikipedia.org/wiki/Cron) schedule for running
	scans periodically.

	To explicitly set a timezone in the cron tab, apply a prefix in the
	cron tab: **"CRON_TZ=${IANA_TIME_ZONE}"** or **"TZ=${IANA_TIME_ZONE}"**.
	The **${IANA_TIME_ZONE}** may only be a valid string from IANA time zone
	database
	([wikipedia](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List)).
	For example, `CRON_TZ=America/New_York 1 * * * *`, or
	`TZ=America/New_York 1 * * * *`.

	This field is required for Schedule scans. */
	// +optional
	Cron *string `json:"cron,omitempty"`
}

type DatascanScoreThresholdTrigger struct {
	/* Optional. The score range is in [0,100]. */
	// +optional
	ScoreThreshold *float64 `json:"scoreThreshold,omitempty"`
}

type DatascanSetExpectation struct {
	/* Optional. Expected values for the column value. */
	// +optional
	Values []string `json:"values,omitempty"`
}

type DatascanSqlAssertion struct {
	/* Optional. The SQL statement. */
	// +optional
	SqlStatement *string `json:"sqlStatement,omitempty"`
}

type DatascanStatisticRangeExpectation struct {
	/* Optional. The maximum column statistic value allowed for a row to pass
	this validation.

	At least one of `min_value` and `max_value` need to be provided. */
	// +optional
	MaxValue *string `json:"maxValue,omitempty"`

	/* Optional. The minimum column statistic value allowed for a row to pass
	this validation.

	At least one of `min_value` and `max_value` need to be provided. */
	// +optional
	MinValue *string `json:"minValue,omitempty"`

	/* Optional. The aggregate metric to evaluate. */
	// +optional
	Statistic *string `json:"statistic,omitempty"`

	/* Optional. Whether column statistic needs to be strictly lesser than ('<')
	the maximum, or if equality is allowed.

	Only relevant if a `max_value` has been defined. Default = false. */
	// +optional
	StrictMaxEnabled *bool `json:"strictMaxEnabled,omitempty"`

	/* Optional. Whether column statistic needs to be strictly greater than
	('>') the minimum, or if equality is allowed.

	Only relevant if a `min_value` has been defined. Default = false. */
	// +optional
	StrictMinEnabled *bool `json:"strictMinEnabled,omitempty"`
}

type DatascanStorageConfig struct {
	/* Optional. Configuration for CSV data. */
	// +optional
	CsvOptions *DatascanCsvOptions `json:"csvOptions,omitempty"`

	/* Optional. Defines the data to exclude during discovery. Provide a list of patterns that identify the data to exclude. For Cloud Storage bucket assets, these patterns are interpreted as glob patterns used to match object names. For BigQuery dataset assets, these patterns are interpreted as patterns to match table names. */
	// +optional
	ExcludePatterns []string `json:"excludePatterns,omitempty"`

	/* Optional. Defines the data to include during discovery when only a subset of the data should be considered. Provide a list of patterns that identify the data to include. For Cloud Storage bucket assets, these patterns are interpreted as glob patterns used to match object names. For BigQuery dataset assets, these patterns are interpreted as patterns to match table names. */
	// +optional
	IncludePatterns []string `json:"includePatterns,omitempty"`

	/* Optional. Configuration for JSON data. */
	// +optional
	JsonOptions *DatascanJsonOptions `json:"jsonOptions,omitempty"`
}

type DatascanTableConditionExpectation struct {
	/* Optional. The SQL expression. */
	// +optional
	SqlExpression *string `json:"sqlExpression,omitempty"`
}

type DatascanTrigger struct {
	/* The scan runs once via `RunDataScan` API. */
	// +optional
	OnDemand *DatascanOnDemand `json:"onDemand,omitempty"`

	/* The scan is scheduled to run periodically. */
	// +optional
	Schedule *DatascanSchedule `json:"schedule,omitempty"`
}

type DatascanUniquenessExpectation struct {
}

type DataplexDataScanSpec struct {
	/* Required. The data source for DataScan. */
	Data DatascanData `json:"data"`

	/* Settings for a data discovery scan. */
	// +optional
	DataDiscoverySpec *DatascanDataDiscoverySpec `json:"dataDiscoverySpec,omitempty"`

	/* Settings for a data profile scan. */
	// +optional
	DataProfileSpec *DatascanDataProfileSpec `json:"dataProfileSpec,omitempty"`

	/* Settings for a data quality scan. */
	// +optional
	DataQualitySpec *DatascanDataQualitySpec `json:"dataQualitySpec,omitempty"`

	/* Optional. Description of the scan.

	* Must be between 1-1024 characters. */
	// +optional
	Description *string `json:"description,omitempty"`

	/* Optional. User friendly display name.

	* Must be between 1-256 characters. */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* Optional. DataScan execution settings.

	If not specified, the fields in it will use their default values. */
	// +optional
	ExecutionSpec *DatascanExecutionSpec `json:"executionSpec,omitempty"`

	/* Optional. User-defined labels for the scan. */
	// +optional
	Labels map[string]string `json:"labels,omitempty"`

	/* The location of this resource. */
	Location string `json:"location"`

	/* The project that this resource belongs to. */
	ProjectRef v1alpha1.ResourceRef `json:"projectRef"`

	/* The DataplexDataScan name. If not given, the metadata.name will be used. */
	// +optional
	ResourceID *string `json:"resourceID,omitempty"`
}

type DatascanBigqueryPublishingStatus struct {
}

type DatascanColumnsStatus struct {
}

type DatascanDataDiscoveryResultStatus struct {
	/* Output only. Configuration for metadata publishing. */
	// +optional
	BigqueryPublishing *DatascanBigqueryPublishingStatus `json:"bigqueryPublishing,omitempty"`

	/* Output only. Describes result statistics of a data scan discovery job. */
	// +optional
	ScanStatistics *DatascanScanStatisticsStatus `json:"scanStatistics,omitempty"`
}

type DatascanDataProfileResultStatus struct {
	/* Output only. The result of post scan actions. */
	// +optional
	PostScanActionsResult *DatascanPostScanActionsResultStatus `json:"postScanActionsResult,omitempty"`

	/* The profile information per field. */
	// +optional
	Profile *DatascanProfileStatus `json:"profile,omitempty"`

	/* The count of rows scanned. */
	// +optional
	RowCount *int64 `json:"rowCount,omitempty"`

	/* The data scanned for this result. */
	// +optional
	ScannedData *DatascanScannedDataStatus `json:"scannedData,omitempty"`
}

type DatascanDataQualityResultStatus struct {
	/* Output only. A list of results at the column level.

	A column will have a corresponding `DataQualityColumnResult` if and only if
	there is at least one rule with the 'column' field set to it. */
	// +optional
	Columns []DatascanColumnsStatus `json:"columns,omitempty"`

	/* Output only. A list of results at the dimension level.

	A dimension will have a corresponding `DataQualityDimensionResult` if and
	only if there is at least one rule with the 'dimension' field set to it. */
	// +optional
	Dimensions []DatascanDimensionsStatus `json:"dimensions,omitempty"`

	/* Output only. Overall data quality result -- `true` if all rules passed. */
	// +optional
	Passed *bool `json:"passed,omitempty"`

	/* Output only. The result of post scan actions. */
	// +optional
	PostScanActionsResult *DatascanPostScanActionsResultStatus `json:"postScanActionsResult,omitempty"`

	/* Output only. The count of rows processed. */
	// +optional
	RowCount *int64 `json:"rowCount,omitempty"`

	/* Output only. A list of all the rules in a job, and their results. */
	// +optional
	Rules []DatascanRulesStatus `json:"rules,omitempty"`

	/* Output only. The data scanned for this result. */
	// +optional
	ScannedData *DatascanScannedDataStatus `json:"scannedData,omitempty"`

	/* Output only. The overall data quality score.

	The score ranges between [0, 100] (up to two decimal points). */
	// +optional
	Score *float64 `json:"score,omitempty"`
}

type DatascanDimensionsStatus struct {
}

type DatascanDoubleProfileStatus struct {
	/* Average of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Average *float64 `json:"average,omitempty"`

	/* Maximum of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Max *float64 `json:"max,omitempty"`

	/* Minimum of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Min *float64 `json:"min,omitempty"`

	/* A quartile divides the number of data points into four parts, or quarters, of more-or-less equal size. Three main quartiles used are: The first quartile (Q1) splits off the lowest 25% of data from the highest 75%. It is also known as the lower or 25th empirical quartile, as 25% of the data is below this point. The second quartile (Q2) is the median of a data set. So, 50% of the data lies below this point. The third quartile (Q3) splits off the highest 25% of data from the lowest 75%. It is known as the upper or 75th empirical quartile, as 75% of the data lies below this point. Here, the quartiles is provided as an ordered list of quartile values for the scanned data, occurring in order Q1, median, Q3. */
	// +optional
	Quartiles []float64 `json:"quartiles,omitempty"`

	/* Standard deviation of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	StandardDeviation *float64 `json:"standardDeviation,omitempty"`
}

type DatascanExecutionStatusStatus struct {
	/* Optional. The time when the DataScanJob execution was created. */
	// +optional
	LatestJobCreateTime *string `json:"latestJobCreateTime,omitempty"`

	/* Optional. The time when the latest DataScanJob ended. */
	// +optional
	LatestJobEndTime *string `json:"latestJobEndTime,omitempty"`

	/* Optional. The time when the latest DataScanJob started. */
	// +optional
	LatestJobStartTime *string `json:"latestJobStartTime,omitempty"`
}

type DatascanFieldsStatus struct {
	/* The mode of the field. Possible values include:

	* REQUIRED, if it is a required field.
	* NULLABLE, if it is an optional field.
	* REPEATED, if it is a repeated field. */
	// +optional
	Mode *string `json:"mode,omitempty"`

	/* The name of the field. */
	// +optional
	Name *string `json:"name,omitempty"`

	/* Profile information for the corresponding field. */
	// +optional
	Profile *DatascanProfileStatus `json:"profile,omitempty"`

	/* The data type retrieved from the schema of the data source. For instance, for a BigQuery native table, it is the [BigQuery Table Schema](https://cloud.google.com/bigquery/docs/reference/rest/v2/tables#tablefieldschema). For a Dataplex Entity, it is the [Entity Schema](https://cloud.google.com/dataplex/docs/reference/rpc/google.cloud.dataplex.v1#type_3). */
	// +optional
	Type *string `json:"type,omitempty"`
}

type DatascanIncrementalFieldStatus struct {
	/* Value that marks the end of the range. */
	// +optional
	End *string `json:"end,omitempty"`

	/* The field that contains values which monotonically increases over time (e.g. a timestamp column). */
	// +optional
	Field *string `json:"field,omitempty"`

	/* Value that marks the start of the range. */
	// +optional
	Start *string `json:"start,omitempty"`
}

type DatascanIntegerProfileStatus struct {
	/* Average of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Average *float64 `json:"average,omitempty"`

	/* Maximum of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Max *int64 `json:"max,omitempty"`

	/* Minimum of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	Min *int64 `json:"min,omitempty"`

	/* A quartile divides the number of data points into four parts, or quarters, of more-or-less equal size. Three main quartiles used are: The first quartile (Q1) splits off the lowest 25% of data from the highest 75%. It is also known as the lower or 25th empirical quartile, as 25% of the data is below this point. The second quartile (Q2) is the median of a data set. So, 50% of the data lies below this point. The third quartile (Q3) splits off the highest 25% of data from the lowest 75%. It is known as the upper or 75th empirical quartile, as 75% of the data lies below this point. Here, the quartiles is provided as an ordered list of approximate quartile values for the scanned data, occurring in order Q1, median, Q3. */
	// +optional
	Quartiles []int64 `json:"quartiles,omitempty"`

	/* Standard deviation of non-null values in the scanned data. NaN, if the field has a NaN. */
	// +optional
	StandardDeviation *float64 `json:"standardDeviation,omitempty"`
}

type DatascanObservedStateStatus struct {
	/* Output only. The time when the scan was created. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* Output only. The result of a data discovery scan. */
	// +optional
	DataDiscoveryResult *DatascanDataDiscoveryResultStatus `json:"dataDiscoveryResult,omitempty"`

	/* Output only. The result of a data profile scan. */
	// +optional
	DataProfileResult *DatascanDataProfileResultStatus `json:"dataProfileResult,omitempty"`

	/* Output only. The result of a data quality scan. */
	// +optional
	DataQualityResult *DatascanDataQualityResultStatus `json:"dataQualityResult,omitempty"`

	/* Output only. Status of the data scan execution. */
	// +optional
	ExecutionStatus *DatascanExecutionStatusStatus `json:"executionStatus,omitempty"`

	/* Output only. Current state of the DataScan. */
	// +optional
	State *string `json:"state,omitempty"`

	/* Output only. The type of DataScan. */
	// +optional
	Type *string `json:"type,omitempty"`

	/* Output only. System generated globally unique ID for the scan. This ID will be different if the scan is deleted and re-created with the same name. */
	// +optional
	Uid *string `json:"uid,omitempty"`

	/* Output only. The time when the scan was last updated. */
	// +optional
	UpdateTime *string `json:"updateTime,omitempty"`
}

type DatascanPostScanActionsResultStatus struct {
}

type DatascanProfileStatus struct {
	/* Ratio of rows with distinct values against total scanned rows. Not available for complex non-groupable field type, including RECORD, ARRAY, GEOGRAPHY, and JSON, as well as fields with REPEATABLE mode. */
	// +optional
	DistinctRatio *float64 `json:"distinctRatio,omitempty"`

	/* Double type field information. */
	// +optional
	DoubleProfile *DatascanDoubleProfileStatus `json:"doubleProfile,omitempty"`

	/* Integer type field information. */
	// +optional
	IntegerProfile *DatascanIntegerProfileStatus `json:"integerProfile,omitempty"`

	/* Ratio of rows with null value against total scanned rows. */
	// +optional
	NullRatio *float64 `json:"nullRatio,omitempty"`

	/* String type field information. */
	// +optional
	StringProfile *DatascanStringProfileStatus `json:"stringProfile,omitempty"`

	/* The list of top N non-null values, frequency and ratio with which they occur in the scanned data. N is 10 or equal to the number of distinct values in the field, whichever is smaller. Not available for complex non-groupable field type, including RECORD, ARRAY, GEOGRAPHY, and JSON, as well as fields with REPEATABLE mode. */
	// +optional
	TopNValues []DatascanTopNValuesStatus `json:"topNValues,omitempty"`
}

type DatascanRulesStatus struct {
}

type DatascanScanStatisticsStatus struct {
	/* The data processed in bytes. */
	// +optional
	DataProcessedBytes *int64 `json:"dataProcessedBytes,omitempty"`

	/* The number of files excluded. */
	// +optional
	FilesExcluded *int32 `json:"filesExcluded,omitempty"`

	/* The number of filesets created. */
	// +optional
	FilesetsCreated *int32 `json:"filesetsCreated,omitempty"`

	/* The number of filesets deleted. */
	// +optional
	FilesetsDeleted *int32 `json:"filesetsDeleted,omitempty"`

	/* The number of filesets updated. */
	// +optional
	FilesetsUpdated *int32 `json:"filesetsUpdated,omitempty"`

	/* The number of files scanned. */
	// +optional
	ScannedFileCount *int32 `json:"scannedFileCount,omitempty"`

	/* The number of tables created. */
	// +optional
	TablesCreated *int32 `json:"tablesCreated,omitempty"`

	/* The number of tables deleted. */
	// +optional
	TablesDeleted *int32 `json:"tablesDeleted,omitempty"`

	/* The number of tables updated. */
	// +optional
	TablesUpdated *int32 `json:"tablesUpdated,omitempty"`
}

type DatascanScannedDataStatus struct {
	/* The range denoted by values of an incremental field */
	// +optional
	IncrementalField *DatascanIncrementalFieldStatus `json:"incrementalField,omitempty"`
}

type DatascanStringProfileStatus struct {
	/* Average length of non-null values in the scanned data. */
	// +optional
	AverageLength *float64 `json:"averageLength,omitempty"`

	/* Maximum length of non-null values in the scanned data. */
	// +optional
	MaxLength *int64 `json:"maxLength,omitempty"`

	/* Minimum length of non-null values in the scanned data. */
	// +optional
	MinLength *int64 `json:"minLength,omitempty"`
}

type DatascanTopNValuesStatus struct {
	/* Count of the corresponding value in the scanned data. */
	// +optional
	Count *int64 `json:"count,omitempty"`

	/* Ratio of the corresponding value in the field against the total number of rows in the scanned data. */
	// +optional
	Ratio *float64 `json:"ratio,omitempty"`

	/* String value of a top N non-null value. */
	// +optional
	Value *string `json:"value,omitempty"`
}

type DataplexDataScanStatus struct {
	/* Conditions represent the latest available observations of the
	   DataplexDataScan's current state. */
	Conditions []v1alpha1.Condition `json:"conditions,omitempty"`
	/* A unique specifier for the DataplexDataScan resource in GCP. */
	// +optional
	ExternalRef *string `json:"externalRef,omitempty"`

	/* ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource. */
	// +optional
	ObservedGeneration *int64 `json:"observedGeneration,omitempty"`

	/* ObservedState is the state of the resource as most recently observed in GCP. */
	// +optional
	ObservedState *DatascanObservedStateStatus `json:"observedState,omitempty"`
}

// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +kubebuilder:resource:categories=gcp,shortName=gcpdataplexdatascan;gcpdataplexdatascans
// +kubebuilder:subresource:status
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/managed-by-kcc=true"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/system=true"
// +kubebuilder:printcolumn:name="Age",JSONPath=".metadata.creationTimestamp",type="date"
// +kubebuilder:printcolumn:name="Ready",JSONPath=".status.conditions[?(@.type=='Ready')].status",type="string",description="When 'True', the most recent reconcile of the resource succeeded"
// +kubebuilder:printcolumn:name="Status",JSONPath=".status.conditions[?(@.type=='Ready')].reason",type="string",description="The reason for the value in 'Ready'"
// +kubebuilder:printcolumn:name="Status Age",JSONPath=".status.conditions[?(@.type=='Ready')].lastTransitionTime",type="date",description="The last transition time for the value in 'Status'"

// DataplexDataScan is the Schema for the dataplex API
// +k8s:openapi-gen=true
type DataplexDataScan struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   DataplexDataScanSpec   `json:"spec,omitempty"`
	Status DataplexDataScanStatus `json:"status,omitempty"`
}

// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object

// DataplexDataScanList contains a list of DataplexDataScan
type DataplexDataScanList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []DataplexDataScan `json:"items"`
}

func init() {
	SchemeBuilder.Register(&DataplexDataScan{}, &DataplexDataScanList{})
}
