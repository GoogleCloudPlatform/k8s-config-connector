// Copyright 2020 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//    http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Config Connector and manual
//     changes will be clobbered when the file is regenerated.
//
// ----------------------------------------------------------------------------

// *** DISCLAIMER ***
// Config Connector's go-client for CRDs is currently in ALPHA, which means
// that future versions of the go-client may include breaking changes.
// Please try it out and give us feedback!

package v1alpha1

import (
	"github.com/GoogleCloudPlatform/k8s-config-connector/pkg/clients/generated/apis/k8s/v1alpha1"
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

type SessionQuery struct {
	/* Plain text. */
	// +optional
	Text *string `json:"text,omitempty"`
}

type SessionTurns struct {
	/* Optional. The resource name of the answer to the user query.

	Only set if the answer generation (/answer API call) happened in this
	turn. */
	// +optional
	Answer *string `json:"answer,omitempty"`

	/* Optional. The user query. May not be set if this turn is merely regenerating an answer to a different turn */
	// +optional
	Query *SessionQuery `json:"query,omitempty"`

	/* Optional. Represents metadata related to the query config, for example LLM model and version used, model parameters (temperature, grounding parameters, etc.). The prefix "google." is reserved for Google-developed functionality. */
	// +optional
	QueryConfig map[string]string `json:"queryConfig,omitempty"`
}

type DiscoveryEngineSessionSpec struct {
	/* The DataStore this session should be part of. */
	DataStoreRef v1alpha1.ResourceRef `json:"dataStoreRef"`

	/* Optional. The display name of the session. This field is used to identify the session in the UI. By default, the display name is the first turn query text in the session. */
	// +optional
	DisplayName *string `json:"displayName,omitempty"`

	/* Optional. Whether the session is pinned, pinned session will be displayed on the top of the session list. */
	// +optional
	IsPinned *bool `json:"isPinned,omitempty"`

	/* The DiscoveryEngineSession name. If not given, the metadata.name will be used. */
	// +optional
	ResourceID *string `json:"resourceID,omitempty"`

	/* The state of the session. */
	// +optional
	State *string `json:"state,omitempty"`

	/* Turns. */
	// +optional
	Turns []SessionTurns `json:"turns,omitempty"`

	/* A unique identifier for tracking users. */
	// +optional
	UserPseudoID *string `json:"userPseudoID,omitempty"`
}

type SessionActionsStatus struct {
	/* Observation. */
	// +optional
	Observation *SessionObservationStatus `json:"observation,omitempty"`

	/* Search action. */
	// +optional
	SearchAction *SessionSearchActionStatus `json:"searchAction,omitempty"`
}

type SessionChunkContentsStatus struct {
	/* Chunk textual content. */
	// +optional
	Content *string `json:"content,omitempty"`

	/* Page identifier. */
	// +optional
	PageIdentifier *string `json:"pageIdentifier,omitempty"`

	/* The relevance of the chunk for a given query. Values range from 0.0 (completely irrelevant) to 1.0 (completely relevant). This value is for informational purpose only. It may change for the same query and chunk at any time due to a model retraining or change in implementation. */
	// +optional
	RelevanceScore *float64 `json:"relevanceScore,omitempty"`
}

type SessionChunkInfoStatus struct {
	/* Chunk resource name. */
	// +optional
	Chunk *string `json:"chunk,omitempty"`

	/* Chunk textual content. */
	// +optional
	Content *string `json:"content,omitempty"`

	/* The relevance of the chunk for a given query. Values range from 0.0 (completely irrelevant) to 1.0 (completely relevant). This value is for informational purpose only. It may change for the same query and chunk at any time due to a model retraining or change in implementation. */
	// +optional
	RelevanceScore *float64 `json:"relevanceScore,omitempty"`
}

type SessionCitationsStatus struct {
	/* End of the attributed segment, exclusive. Measured in bytes (UTF-8 unicode). If there are multi-byte characters,such as non-ASCII characters, the index measurement is longer than the string length. */
	// +optional
	EndIndex *int64 `json:"endIndex,omitempty"`

	/* Citation sources for the attributed segment. */
	// +optional
	Sources []SessionSourcesStatus `json:"sources,omitempty"`

	/* Index indicates the start of the segment, measured in bytes (UTF-8 unicode). If there are multi-byte characters,such as non-ASCII characters, the index measurement is longer than the string length. */
	// +optional
	StartIndex *int64 `json:"startIndex,omitempty"`
}

type SessionDetailedAnswerStatus struct {
	/* Additional answer-skipped reasons. This provides the reason for ignored cases. If nothing is skipped, this field is not set. */
	// +optional
	AnswerSkippedReasons []string `json:"answerSkippedReasons,omitempty"`

	/* The textual answer. */
	// +optional
	AnswerText *string `json:"answerText,omitempty"`

	/* Citations. */
	// +optional
	Citations []SessionCitationsStatus `json:"citations,omitempty"`

	/* Output only. Answer completed timestamp. */
	// +optional
	CompleteTime *string `json:"completeTime,omitempty"`

	/* Output only. Answer creation timestamp. */
	// +optional
	CreateTime *string `json:"createTime,omitempty"`

	/* A score in the range of [0, 1] describing how grounded the answer is by the reference chunks. */
	// +optional
	GroundingScore *float64 `json:"groundingScore,omitempty"`

	/* Optional. Grounding supports. */
	// +optional
	GroundingSupports []SessionGroundingSupportsStatus `json:"groundingSupports,omitempty"`

	/* Immutable. Fully qualified name `projects/{project}/locations/global/collections/{collection}/engines/{engine}/sessions/* /answers/*` */
	// +optional
	Name *string `json:"name,omitempty"`

	/* Query understanding information. */
	// +optional
	QueryUnderstandingInfo *SessionQueryUnderstandingInfoStatus `json:"queryUnderstandingInfo,omitempty"`

	/* References. */
	// +optional
	References []SessionReferencesStatus `json:"references,omitempty"`

	/* Suggested related questions. */
	// +optional
	RelatedQuestions []string `json:"relatedQuestions,omitempty"`

	/* Optional. Safety ratings. */
	// +optional
	SafetyRatings []SessionSafetyRatingsStatus `json:"safetyRatings,omitempty"`

	/* The state of the answer generation. */
	// +optional
	State *string `json:"state,omitempty"`

	/* Answer generation steps. */
	// +optional
	Steps []SessionStepsStatus `json:"steps,omitempty"`
}

type SessionDocumentMetadataStatus struct {
	/* Document resource name. */
	// +optional
	Document *string `json:"document,omitempty"`

	/* Page identifier. */
	// +optional
	PageIdentifier *string `json:"pageIdentifier,omitempty"`

	/* The structured JSON metadata for the document. It is populated from the struct data from the Chunk in search result. */
	// +optional
	StructData apiextensionsv1.JSON `json:"structData,omitempty"`

	/* Title. */
	// +optional
	Title *string `json:"title,omitempty"`

	/* URI for the document. */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type SessionGroundingSupportsStatus struct {
	/* Required. End of the claim, exclusive. */
	// +optional
	EndIndex *int64 `json:"endIndex,omitempty"`

	/* Indicates that this claim required grounding check. When the system decided this claim didn't require attribution/grounding check, this field is set to false. In that case, no grounding check was done for the claim and therefore `grounding_score`, `sources` is not returned. */
	// +optional
	GroundingCheckRequired *bool `json:"groundingCheckRequired,omitempty"`

	/* A score in the range of [0, 1] describing how grounded is a specific claim by the references. Higher value means that the claim is better supported by the reference chunks. */
	// +optional
	GroundingScore *float64 `json:"groundingScore,omitempty"`

	/* Optional. Citation sources for the claim. */
	// +optional
	Sources []SessionSourcesStatus `json:"sources,omitempty"`

	/* Required. Index indicates the start of the claim, measured in bytes (UTF-8 unicode). */
	// +optional
	StartIndex *int64 `json:"startIndex,omitempty"`
}

type SessionObservationStatus struct {
	/* Search results observed by the search action, it can be snippets info or chunk info, depending on the citation type set by the user. */
	// +optional
	SearchResults []SessionSearchResultsStatus `json:"searchResults,omitempty"`
}

type SessionObservedStateStatus struct {
	/* Output only. The time the session finished. */
	// +optional
	EndTime *string `json:"endTime,omitempty"`

	/* Output only. The time the session started. */
	// +optional
	StartTime *string `json:"startTime,omitempty"`

	/* Turns. */
	// +optional
	Turns []SessionTurnsStatus `json:"turns,omitempty"`
}

type SessionQueryClassificationInfoStatus struct {
	/* Classification output. */
	// +optional
	Positive *bool `json:"positive,omitempty"`

	/* Query classification type. */
	// +optional
	Type *string `json:"type,omitempty"`
}

type SessionQueryStatus struct {
	/* Output only. Unique Id for the query. */
	// +optional
	QueryID *string `json:"queryID,omitempty"`
}

type SessionQueryUnderstandingInfoStatus struct {
	/* Query classification information. */
	// +optional
	QueryClassificationInfo []SessionQueryClassificationInfoStatus `json:"queryClassificationInfo,omitempty"`
}

type SessionReferencesStatus struct {
	/* Chunk information. */
	// +optional
	ChunkInfo *SessionChunkInfoStatus `json:"chunkInfo,omitempty"`

	/* Structured document information. */
	// +optional
	StructuredDocumentInfo *SessionStructuredDocumentInfoStatus `json:"structuredDocumentInfo,omitempty"`

	/* Unstructured document information. */
	// +optional
	UnstructuredDocumentInfo *SessionUnstructuredDocumentInfoStatus `json:"unstructuredDocumentInfo,omitempty"`
}

type SessionSafetyRatingsStatus struct {
	/* Output only. Indicates whether the content was filtered out because of this rating. */
	// +optional
	Blocked *bool `json:"blocked,omitempty"`

	/* Output only. Harm category. */
	// +optional
	Category *string `json:"category,omitempty"`

	/* Output only. Harm probability levels in the content. */
	// +optional
	Probability *string `json:"probability,omitempty"`

	/* Output only. Harm probability score. */
	// +optional
	ProbabilityScore *float64 `json:"probabilityScore,omitempty"`

	/* Output only. Harm severity levels in the content. */
	// +optional
	Severity *string `json:"severity,omitempty"`

	/* Output only. Harm severity score. */
	// +optional
	SeverityScore *float64 `json:"severityScore,omitempty"`
}

type SessionSearchActionStatus struct {
	/* The query to search. */
	// +optional
	Query *string `json:"query,omitempty"`
}

type SessionSearchResultsStatus struct {
	/* If citation_type is CHUNK_LEVEL_CITATION and chunk mode is on, populate chunk info. */
	// +optional
	ChunkInfo []SessionChunkInfoStatus `json:"chunkInfo,omitempty"`

	/* Document resource name. */
	// +optional
	Document *string `json:"document,omitempty"`

	/* If citation_type is DOCUMENT_LEVEL_CITATION, populate document level snippets. */
	// +optional
	SnippetInfo []SessionSnippetInfoStatus `json:"snippetInfo,omitempty"`

	/* Data representation. The structured JSON data for the document. It's populated from the struct data from the Document, or the Chunk in search result. */
	// +optional
	StructData apiextensionsv1.JSON `json:"structData,omitempty"`

	/* Title. */
	// +optional
	Title *string `json:"title,omitempty"`

	/* URI for the document. */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type SessionSnippetInfoStatus struct {
	/* Snippet content. */
	// +optional
	Snippet *string `json:"snippet,omitempty"`

	/* Status of the snippet defined by the search team. */
	// +optional
	SnippetStatus *string `json:"snippetStatus,omitempty"`
}

type SessionSourcesStatus struct {
	/* ID of the citation source. */
	// +optional
	ReferenceID *string `json:"referenceID,omitempty"`
}

type SessionStepsStatus struct {
	/* Actions. */
	// +optional
	Actions []SessionActionsStatus `json:"actions,omitempty"`

	/* The description of the step. */
	// +optional
	Description *string `json:"description,omitempty"`

	/* The state of the step. */
	// +optional
	State *string `json:"state,omitempty"`

	/* The thought of the step. */
	// +optional
	Thought *string `json:"thought,omitempty"`
}

type SessionStructuredDocumentInfoStatus struct {
	/* Document resource name. */
	// +optional
	Document *string `json:"document,omitempty"`

	/* Structured search data. */
	// +optional
	StructData apiextensionsv1.JSON `json:"structData,omitempty"`

	/* Output only. The title of the document. */
	// +optional
	Title *string `json:"title,omitempty"`

	/* Output only. The URI of the document. */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type SessionTurnsStatus struct {
	/* Output only. In [ConversationalSearchService.GetSession][google.cloud.discoveryengine.v1.ConversationalSearchService.GetSession] API, if [GetSessionRequest.include_answer_details][google.cloud.discoveryengine.v1.GetSessionRequest.include_answer_details] is set to true, this field will be populated when getting answer query session. */
	// +optional
	DetailedAnswer *SessionDetailedAnswerStatus `json:"detailedAnswer,omitempty"`

	/* Optional. The user query. May not be set if this turn is merely regenerating an answer to a different turn */
	// +optional
	Query *SessionQueryStatus `json:"query,omitempty"`
}

type SessionUnstructuredDocumentInfoStatus struct {
	/* List of cited chunk contents derived from document content. */
	// +optional
	ChunkContents []SessionChunkContentsStatus `json:"chunkContents,omitempty"`

	/* Document resource name. */
	// +optional
	Document *string `json:"document,omitempty"`

	/* The structured JSON metadata for the document. It is populated from the struct data from the Chunk in search result. */
	// +optional
	StructData apiextensionsv1.JSON `json:"structData,omitempty"`

	/* Title. */
	// +optional
	Title *string `json:"title,omitempty"`

	/* URI for the document. */
	// +optional
	Uri *string `json:"uri,omitempty"`
}

type DiscoveryEngineSessionStatus struct {
	/* Conditions represent the latest available observations of the
	   DiscoveryEngineSession's current state. */
	Conditions []v1alpha1.Condition `json:"conditions,omitempty"`
	/* A unique specifier for the DiscoveryEngineSession resource in GCP. */
	// +optional
	ExternalRef *string `json:"externalRef,omitempty"`

	/* ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource. */
	// +optional
	ObservedGeneration *int64 `json:"observedGeneration,omitempty"`

	/* ObservedState is the state of the resource as most recently observed in GCP. */
	// +optional
	ObservedState *SessionObservedStateStatus `json:"observedState,omitempty"`
}

// +genclient
// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object
// +kubebuilder:resource:categories=gcp,shortName=gcpdiscoveryenginesession;gcpdiscoveryenginesessions
// +kubebuilder:subresource:status
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/managed-by-kcc=true"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/stability-level=alpha"
// +kubebuilder:metadata:labels="cnrm.cloud.google.com/system=true"
// +kubebuilder:printcolumn:name="Age",JSONPath=".metadata.creationTimestamp",type="date"
// +kubebuilder:printcolumn:name="Ready",JSONPath=".status.conditions[?(@.type=='Ready')].status",type="string",description="When 'True', the most recent reconcile of the resource succeeded"
// +kubebuilder:printcolumn:name="Status",JSONPath=".status.conditions[?(@.type=='Ready')].reason",type="string",description="The reason for the value in 'Ready'"
// +kubebuilder:printcolumn:name="Status Age",JSONPath=".status.conditions[?(@.type=='Ready')].lastTransitionTime",type="date",description="The last transition time for the value in 'Status'"

// DiscoveryEngineSession is the Schema for the discoveryengine API
// +k8s:openapi-gen=true
type DiscoveryEngineSession struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   DiscoveryEngineSessionSpec   `json:"spec,omitempty"`
	Status DiscoveryEngineSessionStatus `json:"status,omitempty"`
}

// +k8s:deepcopy-gen:interfaces=k8s.io/apimachinery/pkg/runtime.Object

// DiscoveryEngineSessionList contains a list of DiscoveryEngineSession
type DiscoveryEngineSessionList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []DiscoveryEngineSession `json:"items"`
}

func init() {
	SchemeBuilder.Register(&DiscoveryEngineSession{}, &DiscoveryEngineSessionList{})
}
