diff --git a/scripts/generate-google3-docs/resource-reference/generated/resource-docs/backupdr/backupdrbackupplan.md b/scripts/generate-google3-docs/resource-reference/generated/resource-docs/backupdr/backupdrbackupplan.md
index af9c4ea352..2f241c6e64 100644
--- a/scripts/generate-google3-docs/resource-reference/generated/resource-docs/backupdr/backupdrbackupplan.md
+++ b/scripts/generate-google3-docs/resource-reference/generated/resource-docs/backupdr/backupdrbackupplan.md
@@ -147,7 +147,7 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">string</code></p>
-            <p>{% verbatim %}Required. Immutable. The unique id of this `BackupRule`. The `rule_id` is unique per `BackupPlan`.The `rule_id` must start with a lowercase letter followed by up to 62 lowercase letters, numbers, or hyphens. Pattern, /[a-z][a-z0-9-]{,62}/.{% endverbatim %}</p>
+            <p>{% verbatim %}Required. Immutable. The unique id of this `BackupRule`. The `rule_id` is unique per `BackupPlan`. The `rule_id` must start with a lowercase letter followed by up to 62 lowercase letters, numbers, or hyphens. Pattern, /[a-z][a-z0-9-]{,62}/.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
@@ -181,7 +181,7 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">integer</code></p>
-            <p>{% verbatim %}Required. The hour of day (1-24) when the window end for e.g. if value of
+            <p>{% verbatim %}Required. The hour of day (1-24) when the window end for example, if value of
  end hour of day is 10 that mean backup window end time is 10:00.
 
  End hour of day should be greater than start hour of day.
@@ -198,7 +198,7 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">integer</code></p>
-            <p>{% verbatim %}Required. The hour of day (0-23) when the window starts for e.g. if value of start hour of day is 6 that mean backup window start at 6:00.{% endverbatim %}</p>
+            <p>{% verbatim %}Required. The hour of day (0-23) when the window starts. For example, if the value is set to 6, the backup window starts at 6:00 a.m.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
@@ -257,8 +257,8 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">integer</code></p>
-            <p>{% verbatim %}Optional. Specifies frequency for hourly backups. A hourly frequency of 2
- means jobs will run every 2 hours from start time till end time defined.
+            <p>{% verbatim %}Optional. Specifies frequency for hourly backups. A hourly frequency of 4
+ means jobs will run every 4 hours from start time till end time defined.
 
  This is required for `recurrence_type`, `HOURLY` and is not applicable
  otherwise. A validation error will occur if a value is supplied and
@@ -266,10 +266,7 @@ resourceType: string
 
  Value of hourly frequency should be between 4 and 23.
 
- Reason for limit : We found that there is bandwidth limitation of 3GB/S for
- GMI while taking a backup and 5GB/S while doing a restore. Given the amount
- of parallel backups and restore we are targeting, this will potentially
- take the backup time to mins and hours (in worst case scenario).{% endverbatim %}</p>
+</p>
         </td>
     </tr>
     <tr>
@@ -313,7 +310,7 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">string</code></p>
-            <p>{% verbatim %}Required. The time zone to be used when interpreting the schedule. The value of this field must be a time zone name from the IANA tz database. See https://en.wikipedia.org/wiki/List_of_tz_database_time_zones for the list of valid timezone names. For e.g., Europe/Paris.{% endverbatim %}</p>
+            <p>{% verbatim %}Required. The time zone to be used when interpreting the schedule. The value of this field must be a time zone name from the IANA tz database. See https://en.wikipedia.org/wiki/List_of_tz_database_time_zones for the list of valid timezone names. For example, Europe/Paris.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
@@ -325,8 +322,8 @@ resourceType: string
             <p><code class="apitype">object</code></p>
             <p>{% verbatim %}Optional. Specifies a week day of the month like, FIRST SUNDAY or LAST
  MONDAY, on which jobs will run. This will be specified by two fields in
- `WeekDayOfMonth`, one for the day, e.g. `MONDAY`, and one for the week,
- e.g. `LAST`.
+ `WeekDayOfMonth`, one for the day, for example `MONDAY`, and one for the week,
+ for example `LAST`.
 
  This field is only applicable for `recurrence_type`, `MONTHLY` and
  `YEARLY`. A validation error will occur if other values are supplied.{% endverbatim %}</p>
@@ -484,7 +481,7 @@ resourceType: string
         </td>
         <td>
             <p><code class="apitype">string</code></p>
-            <p>{% verbatim %}Required. The resource type to which the `BackupPlan` will be applied. Examples include, "compute.googleapis.com/Instance", "sqladmin.googleapis.com/Instance", or "alloydb.googleapis.com/Cluster".{% endverbatim %}</p>
+            <p>{% verbatim %}Required. The resource type to which the `BackupPlan` will be applied. Examples include "compute.googleapis.com/Instance", "sqladmin.googleapis.com/Instance", or "alloydb.googleapis.com/Cluster".{% endverbatim %}</p>
         </td>
     </tr>
 </tbody>
@@ -570,7 +567,7 @@ observedState:
         <td><code>externalRef</code></td>
         <td>
             <p><code class="apitype">string</code></p>
-            <p>{% verbatim %}A unique specifier for the BackupDRBackupPlan resource in GCP.{% endverbatim %}</p>
+            <p>{% verbatim %}A unique specifier for the BackupDRBackupPlan resource in Google Cloud.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
@@ -584,14 +581,14 @@ observedState:
         <td><code>observedState</code></td>
         <td>
             <p><code class="apitype">object</code></p>
-            <p>{% verbatim %}ObservedState is the state of the resource as most recently observed in GCP.{% endverbatim %}</p>
+            <p>{% verbatim %}ObservedState is the state of the resource as most recently observed in Google Cloud.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
         <td><code>observedState.backupVaultServiceAccount</code></td>
         <td>
             <p><code class="apitype">string</code></p>
-            <p>{% verbatim %}Output only. The Google Cloud Platform Service Account to be used by the BackupVault for taking backups. Specify the email address of the Backup Vault Service Account.{% endverbatim %}</p>
+            <p>{% verbatim %}Output only. The Google Cloud Service Account to be used by the BackupVault for taking backups. Specify the email address of the Backup Vault Service Account.{% endverbatim %}</p>
         </td>
     </tr>
     <tr>
@@ -618,9 +615,7 @@ observedState:
 </tbody>
 </table>
 
-## Sample YAML(s)
-
 
 Note: If you have any trouble with instantiating the resource, refer to <a href="/config-connector/docs/troubleshooting">Troubleshoot Config Connector</a>.
 
-{% endblock %}
+{% endblock %}
\ No newline at end of file
