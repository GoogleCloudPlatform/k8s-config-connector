I have addressed the review feedback:
1. Created proper reference types for `NetworkServicesEdgeCacheKeyset` and `NetworkServicesEdgeCacheOrigin`.
2. Updated `NetworkServicesEdgeCacheService` types to use these references for `SignedRequestKeysetRef` and `OriginRef` fields.
3. Fixed the `fuzz-roundtrippers` failures for `NetworkManagementConnectivityTest` by marking the newly added fields as untriaged in the fuzzer.

*(This comment was generated by Overseer)*