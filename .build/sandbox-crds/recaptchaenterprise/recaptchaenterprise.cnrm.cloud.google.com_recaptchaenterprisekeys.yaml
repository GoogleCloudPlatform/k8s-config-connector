apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: recaptchaenterprisekeys.recaptchaenterprise.cnrm.cloud.google.com
spec:
  group: recaptchaenterprise.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: RecaptchaEnterpriseKey
    listKind: RecaptchaEnterpriseKeyList
    plural: recaptchaenterprisekeys
    shortNames:
    - gcprecaptchaenterprisekey
    - gcprecaptchaenterprisekeys
    singular: recaptchaenterprisekey
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RecaptchaEnterpriseKey is the Schema for the RecaptchaEnterpriseKey
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RecaptchaEnterpriseKeySpec defines the desired state of RecaptchaEnterpriseKey
            properties:
              androidSettings:
                description: Settings for keys that can be used by Android apps.
                properties:
                  allowAllPackageNames:
                    description: Optional. If set to true, allowed_package_names are
                      not enforced.
                    type: boolean
                  allowedPackageNames:
                    description: 'Optional. Android package names of apps allowed
                      to use the key. Example: ''com.companyname.appname'''
                    items:
                      type: string
                    type: array
                  supportNonGoogleAppStoreDistribution:
                    description: Optional. Set to true for keys that are used in an
                      Android application that is available for download in app stores
                      in addition to the Google Play Store.
                    type: boolean
                type: object
              displayName:
                description: Required. Human-readable display name of this key. Modifiable
                  by user.
                type: string
              expressSettings:
                description: Settings for keys that can be used by reCAPTCHA Express.
                type: object
              iosSettings:
                description: Settings for keys that can be used by iOS apps.
                properties:
                  allowAllBundleIds:
                    description: Optional. If set to true, allowed_bundle_ids are
                      not enforced.
                    type: boolean
                  allowedBundleIds:
                    description: 'Optional. iOS bundle ids of apps allowed to use
                      the key. Example: ''com.companyname.productname.appname'''
                    items:
                      type: string
                    type: array
                  appleDeveloperID:
                    description: Optional. Apple Developer account details for the
                      app that is protected by the reCAPTCHA Key. reCAPTCHA leverages
                      platform-specific checks like Apple App Attest and Apple DeviceCheck
                      to protect your app from abuse. Providing these fields allows
                      reCAPTCHA to get a better assessment of the integrity of your
                      app.
                    properties:
                      keyID:
                        description: Required. The Apple developer key ID (10-character
                          string).
                        type: string
                      privateKey:
                        description: Required. Input only. A private key (downloaded
                          as a text file with a .p8 file extension) generated for
                          your Apple Developer account. Ensure that Apple DeviceCheck
                          is enabled for the private key.
                        type: string
                      teamID:
                        description: Required. The Apple team ID (10-character string)
                          owning the provisioning profile used to build your application.
                        type: string
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Optional. See [Creating and managing labels] (https://cloud.google.com/recaptcha/docs/labels).
                type: object
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The RecaptchaEnterpriseKey name. If not given, the metadata.name
                  will be used.
                type: string
              testingOptions:
                description: Optional. Options for user acceptance testing.
                properties:
                  testingChallenge:
                    description: Optional. For challenge-based keys only (CHECKBOX,
                      INVISIBLE), all challenge requests for this site return nocaptcha
                      if NOCAPTCHA, or an unsolvable challenge if CHALLENGE.
                    type: string
                  testingScore:
                    description: Optional. All assessments for this Key return this
                      score. Must be between 0 (likely not legitimate) and 1 (likely
                      legitimate) inclusive.
                    type: number
                type: object
              wafSettings:
                description: Optional. Settings for WAF
                properties:
                  wafFeature:
                    description: Required. The WAF feature for which this key is enabled.
                    type: string
                  wafService:
                    description: Required. The WAF service that uses this key.
                    type: string
                type: object
              webSettings:
                description: Settings for keys that can be used by websites.
                properties:
                  allowAllDomains:
                    description: Optional. If set to true, it means allowed_domains
                      are not enforced.
                    type: boolean
                  allowAmpTraffic:
                    description: Optional. If set to true, the key can be used on
                      AMP (Accelerated Mobile Pages) websites. This is supported only
                      for the SCORE integration type.
                    type: boolean
                  allowedDomains:
                    description: 'Optional. Domains or subdomains of websites allowed
                      to use the key. All subdomains of an allowed domain are automatically
                      allowed. A valid domain requires a host and must not include
                      any path, port, query or fragment. Examples: ''example.com''
                      or ''subdomain.example.com'''
                    items:
                      type: string
                    type: array
                  challengeSecurityPreference:
                    description: Optional. Settings for the frequency and difficulty
                      at which this key triggers captcha challenges. This should only
                      be specified for IntegrationTypes CHECKBOX and INVISIBLE and
                      SCORE_AND_CHALLENGE.
                    type: string
                  integrationType:
                    description: Required. Describes how this key is integrated with
                      the website.
                    type: string
                type: object
            type: object
          status:
            description: RecaptchaEnterpriseKeyStatus defines the config connector
              machine state of RecaptchaEnterpriseKey
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the RecaptchaEnterpriseKey resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  createTime:
                    description: Output only. The timestamp corresponding to the creation
                      of this key.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
