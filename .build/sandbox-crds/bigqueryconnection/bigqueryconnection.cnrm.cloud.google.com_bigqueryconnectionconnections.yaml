apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: bigqueryconnectionconnections.bigqueryconnection.cnrm.cloud.google.com
spec:
  group: bigqueryconnection.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: BigQueryConnectionConnection
    listKind: BigQueryConnectionConnectionList
    plural: bigqueryconnectionconnections
    shortNames:
    - gcpbigqueryconnectionconnection
    - gcpbigqueryconnectionconnections
    singular: bigqueryconnectionconnection
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BigQueryConnectionConnection is the Schema for the BigQueryConnectionConnection
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BigQueryConnectionConnectionSpec defines the desired state
              of BigQueryConnectionConnection
            properties:
              aws:
                description: Amazon Web Services (AWS) properties.
                properties:
                  accessRole:
                    description: Authentication using Google owned service account
                      to assume into customer's AWS IAM Role.
                    properties:
                      iamRoleID:
                        description: The user’s AWS IAM Role that trusts the Google-owned
                          AWS IAM user Connection.
                        type: string
                      identity:
                        description: A unique Google-owned and Google-generated identity
                          for the Connection. This identity will be used to access
                          the user's AWS IAM Role.
                        type: string
                    type: object
                  crossAccountRole:
                    description: Authentication using Google owned AWS IAM user's
                      access key to assume into customer's AWS IAM Role. Deprecated,
                      do not use.
                    properties:
                      iamRoleID:
                        description: The user’s AWS IAM Role that trusts the Google-owned
                          AWS IAM user Connection.
                        type: string
                    type: object
                type: object
              azure:
                description: Azure properties.
                properties:
                  customerTenantID:
                    description: The id of customer's directory that host the data.
                    type: string
                  federatedApplicationClientID:
                    description: The client ID of the user's Azure Active Directory
                      Application used for a federated connection.
                    type: string
                  redirectURI:
                    description: The URL user will be redirected to after granting
                      consent during connection setup.
                    type: string
                type: object
              cloudResource:
                description: Cloud Resource properties.
                type: object
              cloudSQL:
                description: Cloud SQL properties.
                properties:
                  credential:
                    description: Input only. Cloud SQL credential.
                    properties:
                      password:
                        description: The password for the credential.
                        type: string
                      username:
                        description: The username for the credential.
                        type: string
                    type: object
                  database:
                    description: Database name.
                    type: string
                  instanceID:
                    description: Cloud SQL instance ID in the form `project:location:instance`.
                    type: string
                  type:
                    description: Type of the Cloud SQL database.
                    type: string
                type: object
              cloudSpanner:
                description: Cloud Spanner properties.
                properties:
                  database:
                    description: Cloud Spanner database in the form `project/instance/database'
                    type: string
                  databaseRole:
                    description: |-
                      Optional. Cloud Spanner database role for fine-grained access control.
                       The Cloud Spanner admin should have provisioned the database role with
                       appropriate permissions, such as `SELECT` and `INSERT`. Other users should
                       only use roles provided by their Cloud Spanner admins.

                       For more details, see [About fine-grained access control]
                       (https://cloud.google.com/spanner/docs/fgac-about).

                       REQUIRES: The database role name must start with a letter, and can only
                       contain letters, numbers, and underscores.
                    type: string
                  maxParallelism:
                    description: |-
                      Allows setting max parallelism per query when executing on Spanner
                       independent compute resources. If unspecified, default values of
                       parallelism are chosen that are dependent on the Cloud Spanner instance
                       configuration.

                       REQUIRES: `use_parallelism` must be set.
                       REQUIRES: Either `use_data_boost` or `use_serverless_analytics` must be
                       set.
                    format: int32
                    type: integer
                  useDataBoost:
                    description: |-
                      If set, the request will be executed via Spanner independent compute
                       resources.
                       REQUIRES: `use_parallelism` must be set.

                       NOTE: `use_serverless_analytics` will be deprecated. Prefer
                       `use_data_boost` over `use_serverless_analytics`.
                    type: boolean
                  useParallelism:
                    description: If parallelism should be used when reading from Cloud
                      Spanner
                    type: boolean
                  useServerlessAnalytics:
                    description: 'If the serverless analytics service should be used
                      to read data from Cloud Spanner. Note: `use_parallelism` must
                      be set when using serverless analytics.'
                    type: boolean
                type: object
              description:
                description: User provided description.
                type: string
              friendlyName:
                description: User provided display name for the connection.
                type: string
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The BigQueryConnectionConnection name. If not given,
                  the metadata.name will be used.
                type: string
              salesforceDataCloud:
                description: Optional. Salesforce DataCloud properties. This field
                  is intended for use only by Salesforce partner projects. This field
                  contains properties for your Salesforce DataCloud connection.
                properties:
                  instanceURI:
                    description: The URL to the user's Salesforce DataCloud instance.
                    type: string
                  tenantID:
                    description: The ID of the user's Salesforce tenant.
                    type: string
                type: object
              spark:
                description: Spark properties.
                properties:
                  metastoreServiceConfig:
                    description: Optional. Dataproc Metastore Service configuration
                      for the connection.
                    properties:
                      metastoreService:
                        description: |-
                          Optional. Resource name of an existing Dataproc Metastore service.

                           Example:

                           * `projects/[project_id]/locations/[region]/services/[service_id]`
                        type: string
                    type: object
                  sparkHistoryServerConfig:
                    description: Optional. Spark History Server configuration for
                      the connection.
                    properties:
                      dataprocCluster:
                        description: |-
                          Optional. Resource name of an existing Dataproc Cluster to act as a Spark
                           History Server for the connection.

                           Example:

                           * `projects/[project_id]/regions/[region]/clusters/[cluster_name]`
                        type: string
                    type: object
                type: object
            required:
            - location
            type: object
          status:
            description: BigQueryConnectionConnectionStatus defines the config connector
              machine state of BigQueryConnectionConnection
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the BigQueryConnectionConnection
                  resource in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                properties:
                  creationTime:
                    description: Output only. The creation timestamp of the connection.
                    format: int64
                    type: integer
                  hasCredential:
                    description: Output only. True, if credential is configured for
                      this connection.
                    type: boolean
                  lastModifiedTime:
                    description: Output only. The last update timestamp of the connection.
                    format: int64
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
