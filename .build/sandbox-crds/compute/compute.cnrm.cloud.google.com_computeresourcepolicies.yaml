---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/system: "true"
  name: computeresourcepolicies.compute.cnrm.cloud.google.com
spec:
  group: compute.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: ComputeResourcePolicy
    listKind: ComputeResourcePolicyList
    plural: computeresourcepolicies
    shortNames:
    - gcpcomputeresourcepolicy
    - gcpcomputeresourcepolicys
    singular: computeresourcepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ComputeResourcePolicy is the Schema for the ComputeResourcePolicy
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComputeResourcePolicySpec defines the desired state of ComputeResourcePolicy
            properties:
              creationTimestamp:
                description: '[Output Only] Creation timestamp in RFC3339 text format.'
                type: string
              description:
                type: string
              diskConsistencyGroupPolicy:
                description: Resource policy for disk consistency groups.
                type: object
              groupPlacementPolicy:
                description: Resource policy for instances for placement configuration.
                properties:
                  availabilityDomainCount:
                    description: The number of availability domains to spread instances
                      across. If two instances are in different availability domain,
                      they are not in the same low latency network.
                    format: int32
                    type: integer
                  collocation:
                    description: |-
                      Specifies network collocation
                       Check the Collocation enum for the list of possible values.
                    type: string
                  gpuTopology:
                    description: Specifies the shape of the GPU slice, in slice based
                      GPU families eg. A4X.
                    type: string
                  vmCount:
                    description: Number of VMs in this placement group. Google does
                      not recommend that you use this field unless you use a compact
                      policy and you want your policy to work only if it contains
                      this exact number of VMs.
                    format: int32
                    type: integer
                type: object
              id:
                description: '[Output Only] The unique identifier for the resource.
                  This identifier is defined by the server.'
                format: int64
                type: integer
              instanceSchedulePolicy:
                description: Resource policy for scheduling instance operations.
                properties:
                  expirationTime:
                    description: The expiration time of the schedule. The timestamp
                      is an RFC3339 string.
                    type: string
                  startTime:
                    description: The start time of the schedule. The timestamp is
                      an RFC3339 string.
                    type: string
                  timeZone:
                    description: 'Specifies the time zone to be used in interpreting
                      Schedule.schedule. The value of this field must be a time zone
                      name from the tz database: https://wikipedia.org/wiki/Tz_database.'
                    type: string
                  vmStartSchedule:
                    description: Specifies the schedule for starting instances.
                    properties:
                      schedule:
                        description: Specifies the frequency for the operation, using
                          the unix-cron format.
                        type: string
                    type: object
                  vmStopSchedule:
                    description: Specifies the schedule for stopping instances.
                    properties:
                      schedule:
                        description: Specifies the frequency for the operation, using
                          the unix-cron format.
                        type: string
                    type: object
                type: object
              kind:
                description: '[Output Only] Type of the resource. Always compute#resource_policies
                  for resource policies.'
                type: string
              location:
                description: The location of this resource.
                type: string
              projectRef:
                description: The project that this resource belongs to.
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              region:
                type: string
              resourceID:
                description: The ComputeResourcePolicy name. If not given, the metadata.name
                  will be used.
                type: string
              resourceStatus:
                description: '[Output Only] The system status of the resource policy.'
                properties:
                  instanceSchedulePolicy:
                    description: '[Output Only] Specifies a set of output values reffering
                      to the instance_schedule_policy system status. This field should
                      have the same name as corresponding policy field.'
                    properties:
                      lastRunStartTime:
                        description: '[Output Only] The last time the schedule successfully
                          ran. The timestamp is an RFC3339 string.'
                        type: string
                      nextRunStartTime:
                        description: '[Output Only] The next time the schedule is
                          planned to run. The actual time might be slightly different.
                          The timestamp is an RFC3339 string.'
                        type: string
                    type: object
                type: object
              selfLink:
                description: '[Output Only] Server-defined fully-qualified URL for
                  this resource.'
                type: string
              snapshotSchedulePolicy:
                description: Resource policy for persistent disks for creating snapshots.
                properties:
                  retentionPolicy:
                    description: Retention policy applied to snapshots created by
                      this resource policy.
                    properties:
                      maxRetentionDays:
                        description: Maximum age of the snapshot that is allowed to
                          be kept.
                        format: int32
                        type: integer
                      onSourceDiskDelete:
                        description: |-
                          Specifies the behavior to apply to scheduled snapshots when the source disk is deleted.
                           Check the OnSourceDiskDelete enum for the list of possible values.
                        type: string
                    type: object
                  schedule:
                    description: A Vm Maintenance Policy specifies what kind of infrastructure
                      maintenance we are allowed to perform on this VM and when. Schedule
                      that is applied to disks covered by this policy.
                    properties:
                      dailySchedule:
                        properties:
                          daysInCycle:
                            description: Defines a schedule with units measured in
                              days. The value determines how many days pass between
                              the start of each cycle.
                            format: int32
                            type: integer
                          duration:
                            description: '[Output only] A predetermined duration for
                              the window, automatically chosen to be the smallest
                              possible in the given scenario.'
                            type: string
                          startTime:
                            description: Start time of the window. This must be in
                              UTC format that resolves to one of 00:00, 04:00, 08:00,
                              12:00, 16:00, or 20:00. For example, both 13:00-5 and
                              08:00 are valid.
                            type: string
                        type: object
                      hourlySchedule:
                        properties:
                          duration:
                            description: '[Output only] Duration of the time window,
                              automatically chosen to be smallest possible in the
                              given scenario.'
                            type: string
                          hoursInCycle:
                            description: Defines a schedule with units measured in
                              hours. The value determines how many hours pass between
                              the start of each cycle.
                            format: int32
                            type: integer
                          startTime:
                            description: 'Time within the window to start the operations.
                              It must be in format "HH:MM", where HH : [00-23] and
                              MM : [00-00] GMT.'
                            type: string
                        type: object
                      weeklySchedule:
                        properties:
                          dayOfWeeks:
                            description: Up to 7 intervals/windows, one for each day
                              of the week.
                            items:
                              properties:
                                day:
                                  description: |-
                                    Defines a schedule that runs on specific days of the week. Specify one or more days. The following options are available: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
                                     Check the Day enum for the list of possible values.
                                  type: string
                                duration:
                                  description: '[Output only] Duration of the time
                                    window, automatically chosen to be smallest possible
                                    in the given scenario.'
                                  type: string
                                startTime:
                                  description: 'Time within the window to start the
                                    operations. It must be in format "HH:MM", where
                                    HH : [00-23] and MM : [00-00] GMT.'
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  snapshotProperties:
                    description: Properties with which snapshots are created such
                      as labels, encryption keys.
                    properties:
                      chainName:
                        description: Chain name that the snapshot is created in.
                        type: string
                      guestFlush:
                        description: Indication to perform a 'guest aware' snapshot.
                        type: boolean
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to apply to scheduled snapshots. These
                          can be later modified by the setLabels method. Label values
                          may be empty.
                        type: object
                      storageLocations:
                        description: Cloud Storage bucket storage location of the
                          auto snapshot (regional or multi-regional).
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              status:
                description: |-
                  [Output Only] The status of resource policy creation.
                   Check the Status enum for the list of possible values.
                type: string
              workloadPolicy:
                description: Resource policy for defining instance placement for MIGs.
                properties:
                  acceleratorTopology:
                    description: Specifies the topology required to create a partition
                      for VMs that have interconnected GPUs.
                    type: string
                  maxTopologyDistance:
                    description: |-
                      Specifies the maximum distance between instances.
                       Check the MaxTopologyDistance enum for the list of possible values.
                    type: string
                  type:
                    description: |-
                      Specifies the intent of the instance placement in the MIG.
                       Check the Type enum for the list of possible values.
                    type: string
                type: object
            required:
            - location
            type: object
          status:
            description: ComputeResourcePolicyStatus defines the config connector
              machine state of ComputeResourcePolicy
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              externalRef:
                description: A unique specifier for the ComputeResourcePolicy resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
